[pytest]
testpaths = tests
           content/video_engine/tests
python_files = test_*.py
markers =
    render_smoke: requires local Manim and ffmpeg rendering dependencies
    assembly: requires local ffmpeg and ffprobe binaries
    integration: exercises multiple video-engine stages together
