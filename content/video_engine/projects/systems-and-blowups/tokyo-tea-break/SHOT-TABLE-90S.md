# Tokyo Tea Break — shot table, 9:16 short

Authored 2026-09-04 against `SCRIPT-90S-VO.txt` (282 words, ~89 s after the tempo
edit). Stage 7 is authored, never generated — density rules are a checklist, not a
generator. Renders on `scene-evidence-player.template.html` with `"aspect": "9:16"`;
`player.html` is retired with it.

**Timings below are estimates at 173 wpm.** The ElevenLabs take is the clock (doc 37):
every span re-derives from the word timeline before the motion gate is trusted.

---

## The shots

| # | beat | world | evidence dock | why this pairing |
|---|---|---|---|---|
| 1 | "Your costs jumped." → the Fed didn't move | `plate-02-bill-shock-desk` | — | **irony, not illustration**: the line is about a policy rate, the image is a household bill. The gap is the point (J06). |
| 2 | `[archetype]` finance news, Jackson Hole | `plate-03-three-blue-ties` **parallax** | — | the cultural reference the line no longer has to make — operator's call: it reads on screen, not in the mouth |
| 3 | `[rehook]` "our biggest customer is selling" | `plate-05-tokyo-dawn-desk` **parallax dolly** | — | first sight of Tokyo, calm, before the number lands |
| 4 | `[payoff]` over a trillion, selling since February | `plate-05` (held) | **`ev-japan-holdings-v1`** ← spotlit entry | the chart IS the sentence. Peak Feb → latest, one line down. |
| 5 | `[promise]` the two numbers | `plate-09-unwind-trading-floor-mike` | — | Mike present, board blank — the promise is about what's coming |
| 6 | `[head-fake]` "where everyone goes wrong: the hedge" | `plate-08-currency-vault` **parallax** | — | currency, literally. The steelman gets its own world. |
| 7 | `[debate]` half what it did, pays two points | `plate-08` (held) | **`ev-hedge-then-now-v1`** ← spotlit | two bars, then vs now. Proves the demolition in one look. |
| 8 | "Tokyo took a tea break" / `[ring]` bar tab | `plate-05-tokyo-dawn-desk` | — | **ring plant returns**: same desk as shot 3, one register later |
| 9 | `[opponent]` "what they found that pays better" | **GAP — see below** | — | the open loop; needs a world that isn't Washington |
| 10 | `[rehook]` the tell — variable / threshold / where we sit / flip | `plate-09` (held) | **`ev-japan-holdings-v1`** ← **re-enters spotlit** on the flip | E25: a chart that returns re-enters on its new datum, it does not survive a plate change |
| 11 | "never a bond story" → pull up Meta, find its P/E | `plate-09` (held) | **`ev-discount-rate` — NOT BUILT** | the payoff the audience came for |
| 12 | `[tricolon]` Not a panic. Not a plot. Mechanics. | `plate-10-outro-signoff-mike` **parallax** | — | channel line, clean frame |
| 13 | `[ring]` Fed hasn't moved / tea break / the tab is ours | `plate-10` (held) | — | ring closes last; nothing new after it |

## Density check (doc 29 / E21 / E25)

- **13 shots in ~89 s** — mean 6.8 s, no still over 12 s (M01).
- **Evidence every ~25 s**: shots 4, 7, 10, 11 (M03 wants ≤45 s).
- **Six of thirteen worlds are parallax clips**, so the screen is never static under a
  held plate — which is the E21 failure ep1 died of.
- **First chart at ~0:21** (shot 4), inside E24's 8–20 s window at the tempo-edited
  clock, spotlit on entry with a sound hit (M11).
- **No chart survives a plate change** (M12): `ev-japan-holdings-v1` exits at shot 5
  and **re-enters** at shot 10 on the flip datum.
- **Captions carry the motion between docks** — STAGE mode where no dock is up.

## Two gaps, both real

**Shot 9 has no world.** "What they found that pays better" points at Japan's own
semiconductor build-out, and there is no plate for it. The nearest existing asset is a
Steel-and-Paper fab plate on the money-physics channel — **usable, same channel wall** —
or shot 9 borrows `plate-05` and the open loop rides on the voice. Recommend the fab
plate: the line's whole job is to point somewhere else, and reusing Tokyo undercuts it.

**Shot 11 needs the discount-rate chart**, which the evidence audit called for and the
builder does not yet emit: one company's value at 3 % versus 5 %, or P/E derating
against yields, using Meta's real trailing P/E (23.0, fetched 2026-09-03). Without it
the payoff beat has no proof and shot 11 is voice over a held plate.

**And `plate-01-*` is unusable** — all three Fed-briefing rolls are the photoreal
stranger from before the character binding was fixed. Not in this table; a re-roll on
the bound `@Mike` would give shot 1 or 5 a stronger world than `plate-02`.

## What this retires

`player.html` — the GSAP fork with the three wrong TIC figures (Japan 14.5 %,
China $770.2 B, UK $745.8 B ranked #3) and the strategy dock that contradicts the
script. None of it survives the move to the template.
