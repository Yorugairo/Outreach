# Tokyo tea break — the remake on the 09-09 engine (Claude, 2026-09-09)

The operator: *"how does the tokyo tea remake look?"* — after the tariff short's second cut shipped. The short was last built
2026-09-07 20:25 and rendered 2026-09-06; everything the tariff short taught landed in the shared engine after that. This is
the rebuild on today's engine plus the rulings that apply to it, watchable on :8738 (`tokyo-short-player-b`; :8733 is
serving `build-morph-demo` from another session).

**Recall:** E50–E53 (chart form; Tokyo's own watches), E54 (Chirp on both lanes), E55 (a dock that cannot show its subject is
useless; a short's bed at −20; the dead-space kill), E56 (a ring circles a number or a point on a chart only; a picture's
focus is a LIGHT; the life check on every addition), doc 37 §14 (the caps), the bed blueprint rule 3 (the bed breathes).

## What the engine changed by itself (a plain rebuild)

- The dip instead of the wipe at every world change (E47's `hardCut` fix), holds resolved by sentence (E25), the portrait
  fixes (the under species layer fitted, the camera centre), `idle` on docks/pages, the E56 callout gate — Tokyo's two rings
  are DATUM targets (the Fed-funds low, the Meta multiple) and pass. Gates: 0 FAIL / 2 WARN / 13 PASS (the two WARNs are
  pre-existing: 12-s plates, badge-less deck cards).
- The take is already tight: 236 words, 81.2 s, 174 WPM, no gap over doc 37's caps (it was recorded "raw on purpose: the gaps
  are the cut points") — nothing to kill, nothing to speed.

## Applied today (E55 / E56 / the bed)

1. **The pledge dock.** Tokyo's `dock-f-toll-gate-to-fab` clip under "pledged ten trillion yen to chips" is the asset the
   operator called useless without the plant. The fab plate (`omni-video/stills/sig-i-fab-wafer.png`, the operator's own Flow
   image, copied from the tariff short with its meta) takes the clip's own MEASURED place (x 78–882, y 552–1028 — the
   design pass's side-r slot) as a centred landscape card (`FAB_CROP` (0.22, 0.33), the band with the clipboard and the
   wafer chamber). It proves its sentence and leaves at the turn — "And here's what nobody says" (E50/E25).
2. **The light, twice.** On "chips" the light lands on the wafer (`FAB_WAFER`, the warm disc's centroid, `idle: live`); on
   "works" a second light starts on the wafer and GLIDES out to the whole fab — "if that works" is the plant — held until
   the card leaves. Two species starts, so M16's pulse holds (longest gap 1.7 s); the life check on the card region:
   0 identical pairs of 15 (0.25 s samples). No ring (E56).
3. **The bed.** `BED_LU` −28/−26 → **−20/−20** (E55: a short sits at −20), and it BREATHES +4 dB over the two thrown cards
   (the blue-ties panel at 9.09 s, the Fed card at 75.79 s through its snap) — `env` keys on the hook and turn beds, applied
   by the player and the render alike.

## Open — the operator's call on the watch

- **The Fed card vs the caption strip** (76.2–76.8 s): the thrown chart card is a 9:16 page (1458 px tall at reading width)
  and overlaps the caption for ~0.6 s before it snaps; the tariff short has the same overlap on both of its cards and the
  operator accepted it. A shorter landed card (a lower `CENTRE_MAX_H` for thrown pages) is the fix if wanted.
- **The empty plot under the docks** (37–39 s, 55–60 s): the balance-sheet page holds with its line un-drawn while the
  docks play — by the design pass; the fab card now covers it. Worth a second look on the watch.
- The design pass's own open items stand (the 10-year's inline name, the Meta ring through `$665`, R26-22, RU-2).
- The 09-06 render is behind this build; a new render after the word (`RENDER_ASPECT=9:16`, against its own port).

## 2026-09-10 — :8738 and :8740 closed out; the pledge's evidence; the camera beside

The operator read :8740 over :8738 ("8740>8738, thats good work") and asked for the two to be closed out: the DEFAULT build
(`build-short`, :8738) now carries the P48 cut - the rescale on "The Treasury's table", the park on "Two numbers" with
the fingers beside the chart, the recast into the signed monthly bars on "The Treasury prints", the June print as a
note - so `build-short-p48/` (:8740) is redundant and its launch entry stays only until the operator deletes the dir.

1. **The pledge's evidence (the operator: "we don't actually have any evidence for 'japan has pledged 10 trillion yen to
   chips.'").** A RECORD dock - the Nikkei lede (12 Nov 2024, Mari Ishibashi) typed on paper in the band under the parked
   bars on "pledged", the highlighter landing on "at least 10 trillion yen" as the narrator says "ten trillion yen"
   (per-word: "10" on "ten", "trillion" on "trillion", "yen" on "yen"); the tail of the lede types through "semiconductor
   and artificial intelligence industries" and the band goes to the fab card on "works" (the lights follow the plant:
   the wafer once the card has landed, the glide on "beats"). Source on disk:
   `evidence/sources/nikkei-2024-11-12-japan-chips-ai-support.txt` (+ the Reuters wire via SCMP, same figure). The
   record has 2.5 s (55.31-57.84); the phrase is what the viewer reads, the tail is context - if the watch says the
   tail is too fast, the cut is the tail after "fiscal 2030", never the phrase.
2. **The camera variant beside (:8742, `build-short-cam`).** `TOKYO_CAMERA=1`: the eye pulls toward the panel and the
   fingers as they land (row 2), toward the fab card (row 4), and goes TO the Fed card on "moved" (the arrival, in place
   of the snap). Nothing moves on a chart beat. Same take, same rows, same gates (PASS) - watch it against :8738.
3. **Not in this cut: the breakthrough bars** (bonds 1.52 % a year vs chips 36.59 % a year, ten years, the scale stated
   to 8 %) - built as a form in two mechanics and proven on `:8739/breakthrough-proof.html` (burst: the chips bar builds
   to the bonds' level, holds, then shoots to its number while the scale rewrites to 40 % and the bonds bar shrinks to a
   sliver - Bravos's own move, measured) and `:8739/breakthrough-stack-proof.html` (stack: the scale holds, the bar grows
   one bond-year per step off the top of the page). Where it would go in Tokyo is "it beats our bonds" (row 4's last
   sentence), which today is the fab card's beat. The operator picks the mechanic and the place.
