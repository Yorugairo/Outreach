# Steel and Paper H - the rebuild treatment (P68 T4, 2026-09-18)

Script H on its own scratch take (`vo-h-scratch/timeline.json`, Kokoro am_michael at rate 1.075,
**13:43**; the gate runner PASS, `SCRIPT-H-GATES.md` hash `1db00dc3…`). The reference is `build-f/`
(Script G's take, 806.5 s, 75 windows, 73 worlds, `ledger_pages: 0`). This treatment is authored NEW
under today's doctrine (E99 s72: a rebuild is judged on a new plan, never the old table dressed up);
build-f's rows are cited per beat only so the parent can read H's frames beside them at the same
sentence.

The operator's brief (E99 s81): the long form returns; Kokoro/Chirp tonight, the voice auditioned on
the 1:30 unit; Facebook first, YouTube probably; **Mike in the studio and newsroom scenes** through
Flow; 1440p. And the standing goal: *"Recall our assets/props also, which can add some motion as
stamps, but only use them for direct matches."*

## Recall

- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:1991 "A long form is authored on the ledger page by default: the chart-to-chart family (E58) carries the scenes across the phases, the park and the un-park are its breath, the burst (E60) its payoff, the camera (E59) locked unless a reason names itself. A plate appears for ONE of three uses" (E61 - the world is the chart; landing surface / bridge / reset)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:2342 "The hook opens on its axes" (E73 - row 1 is the page on its axes from the first frame)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:1494 "Nothing ever goes truly still: every held thing carries a named subtle idle" (E49 - `;idle=live` on every page, `;idle=drift;drift=20` on every plate)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:3248 "Ken Burns + the 20 px drift is the long-form plate life; parallax planes under a free drift read as random motion" (E99 s65 - every plate row carries the ken tuple and the 20 px drift; the plates on disk are FLAT, pre-E98 back catalogue, so the alive-water route is not available tonight and is not claimed)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:3266 "a cut or dip is the last resort and its why names the transform refused" (E99 s74 - the flow count at the foot)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:3270 "a light with nothing specific to point at is not applied" (E99 s76 - the camera and the light only where the sentence names the thing)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:3278 "a second card takes the outgoing card's slot; the chart never stands aside" (E99 s80 - the three quotes of P13 and the two docs of P18 take one slot each in turn)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:761 "the chart proves one sentence and leaves" (E25 - no page holds past its sentence; the 6 s ceiling on a held dock)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:1824 "a ring circles a point or a value on a chart" (E56 - the ring on the certificate card at 6:55 is a ring on a NUMBER on a card, the 1845 loss; the ring on the hynix tip is a datum)
- Recall(rulings): docs/portable/OPERATOR-RULINGS.md:3160 "a named thing ARRIVES (badge/pill + callout, stamp, docked screenshot, flight), never a spotlight" (E99 s71 - a prop is a STAMP that arrives, and it arrives only on the sentence that names the object)
- Recall(world): content/video_engine/sources/PLATE-LIBRARY.json (330 plates; the reference's worlds resolve under `review/claims/steel-and-paper-plates-wave-4/objects/` in the main checkout, `"flat": true, "flat_reason": "pre-E98 back catalogue"`, `render_eligible: true`)
- Recall(evidence): content/video_engine/projects/systems-and-blowups/steel-and-paper/evidence/objects/ (35 `ev-*` objects, 24 with `.series.json` sidecars; the HTML records `ev-doc-karp`, `ev-doc-leases`, `ev-doc-macdonald`, `ev-three-manias`, `ev-mechanism-ladder`, `ev-instrument-memory`)
- Recall(props): content/video_engine/assets/props/CATALOGUE.md:7 "Total Cutouts: 24" (`finance_props_catalog.v1`; no prop has ever been docked in a cut - `rg "prop-.*-v1" projects/*/build*.py` 0 hits)
- Recall(motion): docs/content-video-engine/CAPABILITIES.md:43 "THE NUMBERED AGENDA (P52 T8) - two to four numbered rows revealed one per word" (the three questions at 0:43 and 7:52)
- Recall(motion): content/video_engine/scripts/species/checklist.mjs "The test card - chart_dock:checklist - A procedure table lands row by row on the narration: each question types on" (the scorecard rows ticking scarce / cash / used at 9:42)
- Recall(motion): docs/content-video-engine/CAPABILITIES.md:86 "The BREAKTHROUGH bars - a bars page STATES its scale" (rail's half of everything against 28 cents, 3:05; twenty years against five, 7:20)
- Recall(motion): docs/content-video-engine/CAPABILITIES.md:27 "A CHART BECOMES ANOTHER CHART BY RE-WRITING (E64): the data-keyed recast" (every chart-to-chart move below)
- Recall(motion): docs/content-video-engine/CAPABILITIES.md:19 "Record-document species - LIVE - typewriter + per-word highlighter, stroke synced to the NARRATOR's word timings" (the Karp / Uber / leases / PIMCO records)
- Recall(motion): docs/content-video-engine/CAPABILITIES.md:63 "the Remotion kit outro, dissolved in as the last word ends, the recorded brand line stitched 0.7 s after it" (the close; `content/video_engine/channel-assets/money-physics/outro/`)
- Recall(host): C:/Users/Snipe/.claude/projects/C--Users-Snipe-Downloads-Outreach-Program/memory/mp-host-identity.md "@Mike is a BOUND Flow character and works for motion; colour cartoon, indigo suit" (the three host windows are Flow orders: character + image prompt, the studio / the newsroom / the desk)
- Recall(script): content/video_engine/projects/systems-and-blowups/steel-and-paper/REWRITE-ORDER-H.md:179 "The host: an open EXPERIMENT" - RULED IN by E99 s81
- Recall(publish): docs/portable/OPERATOR-RULINGS.md (E99 s81) "the post goes to Facebook first and probably YouTube again" - the render is one 2560x1440 / 24 fps file; the Facebook voice question is HG3's
- Recall(sound): content/video_engine/scripts/authoring/audio.py:429 "the cues the frame plays, the cues dropped" (cues bound to what fires, then the gate report - R26-198)
- Recall(package): content/video_engine/projects/systems-and-blowups/steel-and-paper/packaging/TITLE-CANDIDATES.md:54 "TITLE: The AI Bubble Is Real. What Survives Is Steel." (locked 2026-09-01; J12 PASS on the thumbnail today)

## The shape, in one paragraph

Twenty-six paragraphs on the take; **the ledger page is the world for twenty of them**. The page opens
on its axes at 0:00 (E73) and is re-written chart to chart through the phases (E58/E64): the
divergence → the layer it never drew → the railway index → the three manias → the divergence again
under the sell ticket → the records → the yardstick → the two eras → the borrowing → the bond index →
the consensus → the leases → the funding share → the concentration → the drawdown arithmetic → the
gear (20 vs 5) → the test → hynix → the wafer ratio → the contract price → the tripwire board → the
customs monitor → the fab arithmetic → the weight check. **Six plates**, each with its E61 use on the
row: three RESETS at the three world changes (1849 the steel; the turn; the ring close) and three
LANDING SURFACES for Mike's host windows (the studio at 0:54, the desk at 7:52, the newsroom at
12:30). **Six props** as stamps, each on the sentence that names the object (the rule at the foot).
Every page `;idle=live`; every plate the ken tuple plus `;idle=drift;drift=20`. Four camera moves,
each on a datum the sentence names. The outro dissolves in on the last word.

## The rows (H window on the take's clock → build-f's row → the H row → the record)

| # | H window | build-f | the H row | record |
|---|---|---|---|---|
| 1 | 0:00–0:09 P01–P02 *The AI bubble is real. Just not in the steel.* | #1 `world-spike-desk-v1` bare 11.8 s | **THE OPEN ON THE AXES**: `ledger:ev-divergence-v1:line:…:axes:cut;idle=live` - the Bravos divergence (the giants vs the chip index) draws from 0.00 on the axes clock through "just not in the steel"; the captions on the charcoal in STAGE mode; the title stroke in the page's hand | E73; E49; build-f's opening minute was its THINNEST (E21 §9.25) |
| 2 | 0:09–0:21 P03 *This certificate is what the last bubble was* | #2 broadcast set | the 1845 certificate ARRIVES as a thrown still card in the plot's low room (`dock_png` off `world-certificate-wall-v1`, crop the single certificate), `arrive: throw, mass: paper`; on "still carrying trains" the card holds and the page's line keeps building | E99 s71 (a named thing arrives); E65 the room |
| 3 | 0:21–0:29 P04 *the same chart: two lines, one warning* | #3 two-rooms + `ev-mega-vs-spy-v3` 10.4 s | the certificate card LEAVES (its slot handed on, s80) and Bravos' original chart takes the slot as a docked screenshot `ev-bravos-original-v1` with `arrive: throw`; a callout on its two lines on "two lines"; "AI is 1845 again" a retitle stroke | s80 the slot; E99 s71 the docked screenshot |
| 4 | 0:29–0:39 P05 *Here's the layer it never drew* | #4 dawn factory 21.3 s held dock (DEFECT D4) | `chart_to` the page to `ev-divergence-v1` with the MEMORY series added: three lines build to their ends on "twenty-one", "a hundred and five", "six hundred and thirteen" with end figures `+21%` / `+105%` / `+613%`; **camera 1**: `focus_zoom` on the +613 datum on "six hundred and thirteen" (the sentence names it) | E58/E64 recast; s76 the camera on the named datum; E25 |
| 5 | 0:39–0:42 P06 *The warning is right. The address is wrong.* | #5 kitchen 4.2 s | on the page: the callout moves from the chip line to the memory line ("the address") - one move, no world change | E25 one sentence, one proof |
| 6 | 0:42–0:54 P07–P08 *One test, three questions* | #6 three-notch slate, #7 nib | the page PARKS left (the un-park is its breath) and the **numbered agenda** lands in the right room: `1 scarce? 2 cash or paper? 3 used tomorrow?` one row per word ("One test" / "three questions" / "thirty seconds"); "your own top five" a pill under it | CAPABILITIES:43 agenda; E61 the park and un-park |
| 7 | 0:54–1:09 P09 *That's what the questions read for. Not Bravos… Capital arriving faster* | #8 hype machine, #9 assay | **HOST WINDOW 1 - the studio** (`world-broadcast-set-v2;use=landing;idle=drift;drift=20` + ken push): Mike's studio still (Flow order H-1) docked as the landing card; on "Not Bravos Research" the Bravos chart card is thrown onto the desk beside him; on "Capital arriving faster than the value" a chip flow: `capital → value` (CAPABILITIES:99 the flow diagram) drawn on the plate's quiet zone | E61 landing surface; E99 s81 the host; s65 the plate life |
| 8 | 1:04–1:09 P10 *paper trail, and paper you can read* | – | the DIP back to the page: the world changes from the studio to the ledger (E47) - **dip 1**, refused: a recast cannot take a photograph plate to a chart | E47; s74 |
| 9 | 1:09–1:30 P11 the catalyst | #10 steelman 11.5 s bare, #11 navvy + `ev-railway-index-v1` | the page on `ev-railway-index-v1:line` builds on "a quarter-billion pounds"; a figure `£250m` then `$1T+ today` (the thumbnail's own figure); on "crashed by nearly two-thirds" `build_to` the trough with a red figure `−66%`; on "In two thousand the internet crossed seven percent" `chart_to` `ev-equip-ipp-gdp-v1` (the share-of-GDP line, the 7% tick ringed); on "AI spending just crossed eight" the AI series draws past 8 and a **ring** lands on the 8% datum (E56: a number on a chart) | E58; E56; the record's `ev-three-manias` is the P14 page, not this one |
| 10 | 1:30–1:44 P12 the head-fake | #17 sell ticket 14 s | the SELL TICKET arrives as a thrown card over the page (`dock_png` off `world-sell-ticket-v1`), `mass: paper`; the page recasts under it to the divergence (chips doubling, customers flat) - the ticket reads "SELL" over the chip line's tip; "any adviser would sign it" a signature stroke on the ticket | s71 the arrival; E64 the recast under a card |
| 11 | 1:44–2:10 P13 the adjuster's walk | #19 memo, #20 burndown, #21 racks (three worlds) | ONE SLOT, THREE RECORDS (s80): the ticket leaves; `ev-doc-karp` lands in its slot as a **record document** typing on Karp's words; on "Uber's CTO" the Karp record hands the slot to `ev-uber-adoption-v1` (the burndown chart card); on "And their COO" the card's caption strip re-types the COO line - three quotes, one slot, no world change (the G31 listing risk closed on screen) | CAPABILITIES:19 record species; s80 the slot; SCRIPT-H-JUDGE G31 |
| 12 | 2:10–2:30 P14 *the trough already doing its job* | #22 exhibition + `ev-three-manias`, #23 viaduct | `chart_to` `ev-three-manias` (the hype-cycle HTML record): the peak marker and the trough marker; on "the trough already doing its job" the marker slides peak → trough; "what survives it" `[pre-key]` - the page holds still for the beat, its idle live | E58; the HTML record species |
| 13 | 2:30–2:45 P15 the dip to 1849 | #24 molten pour + #25 workbench (bare 3.2 s) | **RESET 1** (`world-viaduct-train-rain-v1;use=reset;idle=drift;drift=20` + ken push): the world changes to the steel - **dip 2**, refused: the page cannot recast into a train; on "The steel kept working. The paper stopped pretending." the anaphora in caption STAGE mode on the plate's quiet zone | E61 reset; E47 the dip at a world change; E21 captions are the motion |
| 14 | 2:45–3:20 P16 the yardstick | #12 server room, #13 ledger page 21.9 s held | the dip back to the page (**dip 3**, the plate cannot become a chart) on `ev-capital-formation-v1:line:axes`: the line builds; figures `23¢` at the dot-com peak and `28¢` today; **camera 2**: `focus_zoom` on the 28 datum on "twenty-eight, the most it has ever been"; on "railways took roughly half" `chart_to` the **breakthrough bars**: rail `50¢` beside `28¢`, the bars page stating its scale | CAPABILITIES:86; s76; E28 (drops down, rises green) |
| 15 | 3:20–4:06 P17 the trigger + the concession | #14 gauge + `ev-tnx-two-eras-v3`, #15 viaduct, #16 broadcast 30 s (DEFECT: bare 190–204) | `chart_to` `ev-tnx-two-eras-v3:line`: the rate line; on "Bank of England above six percent" a ring at the 6% crossing; on "The Fed at six and a half" a ring at 6.5% and **prop stamp 1** `prop-federal-reserve-building-v1` beside the tick (the sentence names the Fed); on "five and a half" a dashed tripwire line with the pill `Bravos 5.5%`; the concession block on the SAME page with the pill `agreed` - no bare plate, the page holds the concession (the 2:46 sputter closed: the evidence never goes dark) | SCRIPT-NOTES-retention-2-46; E56; the prop rule |
| 16 | 4:06–5:25 P18 who is paying | #26–#32 seven worlds, five docks | `chart_to` `ev-debt-issuance-v2:bars`: 28 → 121 → 150 build on their words; "bend the bond market" `chart_to` `ev-ig-credit-weighting-v1:line` 9 → 10 → 12 with the tech share ringed; "four hundred and eighty… six hundred and ninety" `chart_to` `ev-capex-consensus-v1:bars` (the thumbnail's 690); "another eight hundred and twenty-two billion in lease commitments" the **record** `ev-doc-leases` lands in the room with **prop stamp 2** `prop-hyperscale-datacenter-v1` on "Data centers they've already agreed to pay for" (named); "right there in the filing" a callout on the filing's line | E58 ×3; s80 the slot; the prop rule |
| 17 | 5:25–6:04 P19 the arithmetic | #33 cash count, #34 ledger, #35 orderbook, #36 signature | `chart_to` `ev-capex-funding-v1` (the 94% share): a bars page stating its scale, the 94 bar shooting on "Ninety-four" with the figure; the PIMCO record `ev-doc-macdonald` in the slot on "PIMCO has this"; on "The steel kept building. The paper just got heavier." the anaphora in STAGE mode over the page's quiet zone; `[post-key]` the page holds, idle live | CAPABILITIES:86; E21 |
| 18 | 6:04–7:13 P20 the turn | #37–#44 eight worlds | **RESET 2** on "It was never the AI stocks" (`world-paper-and-steel-press-v1;use=reset;idle=drift;drift=20` + ken) - **dip 4**, refused: the turn is a world change; on "railway certificates" the certificate card is thrown onto the plate (landing on the reset); the dip back to the page (**dip 5**) on "AI builders are now twenty percent of the S&P five hundred": `ev-index-concentration-v1:bars` - the 20 bar against the 2–4 band, **camera 3** on the 20 datum ("twenty percent" is named), **prop stamp 3** `prop-tech-sp500-concentration-v1` (the S&P 500 is named); "target-date fund" a thrown card off `world-target-date-envelope-v1`; "if the AI names fall by half, that erases ten percent" `chart_to compare` (E76): the 20 bar halves and the 10 figure lands; "So look at that certificate again" - the **ring** on the certificate card's `−66%` figure (a number on a card, E56); "1845 is the proof" the ring holds | E58; E76; E56; s76; the prop rule |
| 19 | 7:13–7:52 P21 skips a gear | #45 circuit terrain, #46 gpu crate, #47 fiber trench | the page `chart_to` the **breakthrough bars** `20 years` beside `5 years` on "twenty years" / "about five years"; "sold out into next year - the orders are in" a pill `sold out`; "Both are true at once" two callouts, one on each bar | CAPABILITIES:86 |
| 20 | 7:52–9:04 P22 the test | #48 scales, #49 assay, #50 workbench, #51 two-rooms, #52 broadcast | **HOST WINDOW 2 - the desk** (`world-spike-desk-v1;use=landing;idle=drift;drift=20` + ken): Mike at the desk (Flow order H-2) on "Now the test - the one from the top"; the **checklist** species lands row by row on the desk's quiet zone: `1 scarce?` `2 cash or paper?` `3 used tomorrow?` each typing on its question; "check all three from your phone" a phone card; **dip 6** back to the page on "Run it on Bravos' divergence chart" (`ev-divergence-v1` returns - the same page as row 4, un-parked from its point, never redrawn) with the checklist parked beside it; "administered in public" a retitle | checklist.mjs; E61 landing; E40 §4 a returning page unwinds from its point |
| 21 | 9:04–10:18 P23 SK hynix | #53 skyline (3.5 s bare), #54 fab floor, #55 die stack, #56 dram terrain, #57 allocation, #58 laptop, #59 mill | `chart_to` `ev-hynix-steel-v1:line:axes`: the +500% line builds on "five hundred percent", **camera 4** on the tip ("Five hundred" is named twice); "the stacked memory the AI racks need" **prop stamp 4** `prop-hbm-stacked-die-v1` (named); "three times the wafer capacity" `chart_to compare` `ev-hbm-wafer-ratio-v1` (1 vs 3) with **prop stamp 5** `prop-silicon-wafer-semiconductor-v1` on "wafer"; "the memory in a new laptop costs" `chart_to` `ev-dram-contract-v1:line`; "So ask it the three questions" the checklist un-parks and its rows TICK on "Scarce?" / "Cash or paper?" / "Used tomorrow morning?"; "The most vertical line on the board is steel" the hynix line returns with a **ring** on its tip | E58; E76; E56; the prop rule (three props, three named objects) |
| 22 | 10:18–11:33 P24 the tripwires | #60 spike desk, #61 gauge, #62 korea port, #63 wafer, #64 unwind desk 16 s | `chart_to` `ev-tripwire-board-v1` (Bravos 5.5 / mine); "The variable is memory - the RAM inside every one of these data centers" **prop stamp 6** `prop-dram-memory-module-v1` (RAM is named); "what memory costs leaving Korea, by the kilo" `chart_to` `ev-memory-monitor-v1:line:axes` (the customs series; the page cites its release in the caption strip, E52); "DRAM up seventeen percent, and the stacked memory… up fourteen" two end figures `+17%` `+14%`; "one soft month in June" `chart_to` `ev-june-print-v1` with the June datum ringed; "I trimmed some" the trim proof `ev-trim-proof-v1` as a card in the room; "The flip" a retitle in red; "paper is paper" the certificate card returns to the slot | E58; E52 the page cites; E56 |
| 23 | 11:33–13:00 P25 the ring | #65–#71 seven worlds | **RESET 3** on "So - the certificate, one more time" (`world-spike-certificate-ring-v2;use=reset;idle=drift;drift=20` + ken) - **dip 7**; the certificate card thrown onto it, the ring on its figure; the dip back to the page (**dip 8**) on "The order books are booked solid through twenty twenty-six": `ev-memory-arithmetic-v1:bars` (a fab: 5 years; a chip generation: 2×) - the breakthrough bars; on "the index they sold you as the safe version" `chart_to` `ev-weight-check-v1` (a fifth of the index ringed); **HOST WINDOW 3 - the newsroom** on "Decide for yourself which of those you believe" (`world-broadcast-set-v2;use=landing` + Mike, Flow order H-3) - **dip 9**; "accidental concentration, sold as safety" the weight-check card thrown onto the newsroom desk | E61; s80; E56 |
| 24 | 13:00–13:43 P26 the close | #72 two-rooms, #73 workbench 14 s, #74 kitchen, #75 spike rest | the dip back to the page (**dip 10**) on "The steel gets used": the divergence page one last time, un-parked, the anaphora in STAGE mode; "three questions, thirty seconds" the agenda returns in its slot; "Subscribe for the watch" the page parks and the **outro clip** dissolves in as "desk" ends, `recipe:outro-clip-life`, the recorded brand line 0.7 s after | CAPABILITIES:63; the outro recipe |

## The host windows (E99 s81 - Mike in the studio and newsroom scenes)

Three Flow orders, character + image prompt, the bound character (MP host identity; no paid generator):

| order | window | plate | the prompt's scene |
|---|---|---|---|
| H-1 | 0:54–1:09 | `world-broadcast-set-v2` (the studio) | Mike at the anchor desk, three-quarter, one hand on a printed chart |
| H-2 | 7:52–8:20 | `world-spike-desk-v1` (the desk) | Mike leaning over the desk with the spike and a slate, counting three on his fingers |
| H-3 | 12:30–12:50 | `world-broadcast-set-v2` (the newsroom) | Mike standing, the newsroom behind, a certificate in hand |

Each window is a plate row with `;use=landing`, the ken push and the 20 px drift, the caption in STAGE
mode on the plate's quiet zone. **Amended 2026-09-18 after the orders landed** (`host/HOST-NOTES-H.md`):
Flow returned full 16:9 scenes WITH Mike in them (`host/H-1-studio.png`, `H-2-desk.png`,
`H-3-newsroom.png`), so the plate IS the host window - one world, the cards land on it - rather than a
cutout docked on a bare set. The plates are quarantined until the operator approves the frames; H-3
carries printed text on the certificate and is a re-roll candidate before HG4.

## The props (the goal: stamps for DIRECT matches only)

| # | prop | the sentence that names the object | at |
|---|---|---|---|
| 1 | `prop-federal-reserve-building-v1` | *The Fed at six and a half* | 3:40 |
| 2 | `prop-hyperscale-datacenter-v1` | *Data centers they've already agreed to pay for* | 5:10 |
| 3 | `prop-tech-sp500-concentration-v1` | *twenty percent of the S&P five hundred* | 6:35 |
| 4 | `prop-hbm-stacked-die-v1` | *the stacked memory the AI racks need* | 9:12 |
| 5 | `prop-silicon-wafer-semiconductor-v1` | *three times the wafer capacity* | 9:35 |
| 6 | `prop-dram-memory-module-v1` | *the RAM inside every one of these data centers* | 10:30 |

Refused as indirect: `prop-ai-hype-bubble-v1` on "The AI bubble is real" (the bubble is the thesis,
not an object on screen - the page is the open); `prop-bigtech-capital-tower-v1` on "the biggest
builders borrowed" (no tower is named); `prop-gpu-accelerator-card-v1` / `prop-ai-server-rack-cabinet-v1`
on "the orders are in, the slots are gone" (neither is named); `prop-treasury-yield-gauge-5pct-v1` on
the tripwire (the gauge reads 5%, the sentence says five and a half - a wrong number on screen).
**CORRECTED 2026-09-22, before the body is built: "stamp" above is MY word and it collides with the
engine's.** `stamp` in the compiler is one of the VECTOR MAP's three species (`build_scene_timeline_f.py:319-320`:
"the three species of the VECTOR MAP world, and of no other world") and it writes a NUMBER or a name at a
country's centroid. It cannot carry a picture and it cannot sit on a ledger page; authoring one on a page row is
refused. The buildable form of what these six rows mean is a **docked prop card**: the cutout is a transparent
PNG (measured: `prop-federal-reserve-building-v1` 282x259 RGBA, 98 % clear at its corners; the others alike), so
it docks through `docks.dock_png` like any still, lands in the page's OWN room (E65) beside the datum the sentence
names, arrives on that word (`arrive: throw, mass: paper`) and holds - which is exactly E99 s71's "a named thing
ARRIVES", and is what the treatment meant. **Corrected again the same day by E99 s87, and this is the version the body builds:** a docked card
that throws and lands is our single-spring landing, which the remotion-ui RU-2 triage names as the thing that
*lacks* weight - so it is the carrier, not the motion. These six rows are props **STAMPED ON**, unframed: a prop is never
put in a card because the point of a prop is to join the WORLD - its own alpha is its shape, no frame, no border,
no room, and on a page it is page content that parks, rescales and leaves WITH the page (R26-219). `stamp` is the
ARRIVAL and carries whatever the author names (a card may be stamped too); a prop's other way in is simply thrown
on, also unframed. Stamped, it lands by RU-2's mechanism - a clamped scale spring settling while a free
trailing ROTATION spring is still unwinding under it, an impact ring on split shock curves out to about twice its
radius, and an exit owed on the landed mark (E50). Our look, never the reference's: charcoal on cream, drawn in
our hand, no gold ring. The engine's own `stamp` SPECIES is the vector map's number-at-a-centroid and is a
different thing entirely; these are arrivals, so the two never meet. The six direct matches and the five refusals
below stand unchanged. No prop has ever landed in a shipped cut, so the first one comes to the operator on a card.

## The camera (E99 s76 - only where the sentence names the thing)

| # | at | the datum | the words |
|---|---|---|---|
| 1 | 0:36 | the +613 line end | *six hundred and thirteen* |
| 2 | 3:05 | the 28¢ datum | *twenty-eight, the most it has ever been* |
| 3 | 6:35 | the 20% bar | *twenty percent of the S&P five hundred* |
| 4 | 9:12 | the hynix tip | *Five hundred* |

At 16:9 the caption strip sits under the plot, so a `focus_zoom` at 1.08 must be MEASURED on the
frame before it stays (R26-201 was a 9:16 finding; the 16:9 geometry is the audit's row). Rings and
callouts point without the camera everywhere else.

## The flow count (E99 s74)

- **Cuts: 0.** Every page-to-page move is a recast (E64), a park / un-park, or a card taking a slot.
- **Dips: 10**, each at a world change (E47) between the ledger and a plate, each refusing the same
  transform: *a chart cannot recast into a photograph plate and a plate cannot recast into a chart*.
  Three resets (1849 the steel 2:30; the turn 6:04; the ring 11:33) and three host windows (0:54,
  7:52, 12:30), each with its return - build-f made **75 windows across 73 worlds** with a plate
  change at nearly every one.
- **Idle tokens: 30** - `;idle=live` on every one of the 24 page rows (the counting is by page row in
  the table, not by paragraph), `;idle=drift;drift=20` on every one of the 6 plate rows. The count
  is written here so the build script's table can be checked against it (the s79 lesson).

## The fault ledger (build-f's measured defects → the H row that closes each)

| build-f fault (measured) | H |
|---|---|
| the opening minute the thinnest: 35.0 events/min vs a 46.5 median; three still stretches over 6 s in the first 60 s | rows 1–6: the page on its axes from frame one, four world-free moves in the first 45 s, the agenda by 0:43 |
| 30 of 43 docks held past the E25 ceiling; #4 21.3 s, #13 21.9 s, #14 17.7 s, #29 16.7 s | no held dock: every card takes a slot and leaves on its sentence; pages recast on the next figure |
| 23% of runtime visually still (E21) | `idle=live` on every page, the ken + drift on every plate, the captions in STAGE mode on every plate |
| six stretches over 12 s with no page, dock or plate life (94 s) | none authored; `measure_frozen_frames.py` proves it |
| the evidence dark 3:10–3:46 (three bare plates); the 2:46–3:23 sputter | row 15: the concession sits ON the two-eras page; no bare plate between 2:45 and 6:04 |
| the promise at 1:16 | 0:43 (G09 PASS) |
| `ledger_pages: 0` | 24 page rows |
| zero host plates (the decision recorded 09-01, never executed) | three host windows |

## What this treatment does NOT claim

- **The alive plate.** The six plates are flat, pre-E98 back catalogue. Their life is Ken Burns +
  the 20 px drift (s65's long-form route) - the alive-water region is not generated tonight.
- **The 16:9 caption strip** has no capability row (docs_find 0 hits) - T3's audit names what the
  strip IS today; the four camera moves are measured on the frame, not assumed.
- **The voice** is the scratch take. Every timing above moves when the 1:30 unit's voice is chosen
  (HG3) and the whole cut is re-taken - the rows anchor on WORDS (`at(ws, phrase)`), not seconds.
- **The one-shot floor's reference** is a short; its rows on this cut are read as INFO (T3 (b)).

## Amended 2026-09-18 after the operator's read of the 1:30 unit (E99 s82)

The operator on frozen copy d: the docked card of the same chart is nonsense; the chart's text must be sized for the
phone; the take sounds slow (it measured 167 wpm, not 170-180); the crawl draw - a line moving then stopping for no
reason - is wrong: *"draw the first line completely, label it, badge it, draw the 2nd line completely, badge it..."*

| row | was | now |
|---|---|---|
| 3 (0:21-0:29 "his fourth copy of the same chart") | Bravos' chart as a docked still taking the certificate's slot | NO CARD - the page IS the chart; the retitle "AI is 1845 again" carries the beat; the certificate leaves on its sentence and the slot stays empty |
| 1 + 4 (the lines) | each series staged one `build_to` per spoken phrase; the memory line staged on "Here's the layer" | LINE BY LINE (R26-226): mega-cap draws whole -> its label and badge land -> the S&P draws whole -> label, badge -> the semis draw whole -> label, badge; the memory line draws whole on "Here's the layer it never drew" -> its +613 % label and "our layer" badge; no stop inside a line unless the sentence names a datum |
| every page row | the page parks to 0.64 to keep its tags out of the STAGE caption's room (labels 5.5 css) | FULL STAGE: the caption on a page row in the anchored bottom strip (R26-205 - the first engine door, before the body); the page parks only as a breath for a card or the agenda (E61), never as a resting size; labels sized for the phone (doc 50: the chart reads at a glance on a 6-inch screen) |
| the take | Kokoro at rate 1.075 = 167 wpm measured | re-taken at rate 1.14 (~177 wpm measured and written on the card - R26-227); every row anchors on words, so the rows stand |

The unit rebuilds on these once R26-205 and R26-226 are in the engine; the treatment's other rows are unchanged.
