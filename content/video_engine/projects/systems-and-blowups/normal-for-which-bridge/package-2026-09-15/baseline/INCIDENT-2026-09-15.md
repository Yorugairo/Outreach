# Baseline audit incident ledger

The first discovery pass mistakenly invoked `build_short.py --help`. That entry point ignores the help flag and ran the original build. It wrote the original project `build-short`, `SHOT-TABLE-SHORT.py`, and `sound/SOUND-PLAN.json`. Tracked generated paths were restored explicitly from HEAD. The original untracked `build-short/SELF-WATCH.html` was overwritten; its prior bytes cannot be recovered. No original build was intentionally run after recovery.

A second discovery pass mistakenly invoked `build_timeline_f.py --help`. It ignored the help flag and wrote the unrelated tracked steel-and-paper `build-f/timeline.json`; that exact path was restored explicitly from HEAD. `build_render_f.py --help` and `align_take_whisper.py --help` exited before any known write. No further unsafe help invocations were made.

The copied baseline and all audit outputs are confined to this directory. See RUN-SUMMARY.tsv and each `logs/*.command.txt` for exact commands and exit codes.
