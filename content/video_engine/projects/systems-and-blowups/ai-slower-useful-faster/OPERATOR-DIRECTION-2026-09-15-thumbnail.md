# Operator direction - the thumbnail and the hook (2026-09-15, late)

Written by the P61 parent (Claude/Fable) from the operator's chat message, verbatim, for the package lane that owns this
project. Not a completion claim; not a script edit. E27: the package (thumbnail + title + hook) is decided first and
sentence 1 answers the thumbnail.

The operator, verbatim:

> The thumbnail can be trump as uncle sam "AI still needs you! on screen" "Trump Said that America needs to be an AI
> leader so it can't slow down. But what if the AI bosses know that slowing down is exactly what AI needs to speed up?"
> or something similar

What that sets:

- **Thumbnail:** Trump as Uncle Sam (the recruiting-poster pose, the pointing finger), the on-screen words
  **"AI still needs you!"** - the B direction's "STILL NEEDS YOU" checkpoint (`thumbnail-b/`) made literal with the
  poster and the figure. A public figure's likeness on the thumbnail is satire of a public act; the cutout goes through
  the head pipeline (SAM, contact sheet, the operator's approval) like every head (E68), never a straight generated
  portrait passed off as a photo.
- **Hook (sentence 1 answers the thumbnail):** *"Trump said that America needs to be an AI leader so it can't slow
  down. But what if the AI bosses know that slowing down is exactly what AI needs to speed up?"* - or something
  similar; the operator left the wording open.
- **`[verify]` before it is spoken:** the Trump line is a paraphrase. Find the primary statement (the speech or
  executive order on AI leadership / not slowing down), quote it with its date in `EVIDENCE-DOSSIER.md`, and keep the
  hook's wording faithful to it. Figures and quotes are never fabricated; a hook, a promise or a tell may not carry an
  unverified claim (SOURCES-TO-VERIFY).
- **Gates that apply:** G45 (title-word proxy) + J12 (the thumbnail file opened and sentence 1 read against it);
  `run_script_gates.py --short --title "<locked title>" --thumb "AI still needs you" --thumb-file <png>`.

Nothing else in this project was touched by the P61 parent.
