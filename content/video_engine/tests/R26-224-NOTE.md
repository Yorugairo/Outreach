# R26-224 - the suite read test by test, and made green (2026-09-18)

The row (`docs/content-video-engine/BACKLOG.md:655`) says **fifteen**. On the tree this lane
measured - `26c406a`, five engine commits after the row was written - it was **thirty-three**:
the fifteen of `R26-218-219-NOTE.md`, plus eighteen that today's own lanes broke (R26-226 /
R26-228 / R26-233 / R26-234 authored six new golden sources carrying the idle switch; `26c406a`
inserted rows into `CAPABILITIES.md` and moved twenty cited line numbers under the choreography
vocabulary). That is the row's own point: a suite red by default hides the next regression, and
the next regression arrived inside one working day.

```
Recall: docs/content-video-engine/BACKLOG.md:655        (R26-224, the row and its list)
Recall: docs/content-video-engine/BACKLOG.md:656        (R26-225 - "the re-stamp and the re-pin are their own wave")
Recall: docs/content-video-engine/BACKLOG.md:457        (R26-51 - the page-box fixture's sha pin, "re-measure ONCE ... in a commit that says why")
Recall: docs/GATES-REGISTRY.md:154                      (M44 sub-6s-plate - ep1's ninth FAIL)
Recall: content/video_engine/scripts/kinetics/stopaction.mjs:25      (ON1_PX_S: 154, the reference that replaced 250)
Recall: docs/content-video-engine/CAPABILITIES.md:276   (the portrait parity gate - no landscape literal in player code)
Recall: content/video_engine/scripts/docs_find.py:183   (the effects Layer - the one recall layer with no rank_by_field)
Recall: content/video_engine/scripts/build_scene_timeline_f.py:481   (CHART_TO_WHEN["remake"], 278 chars)
Recall: content/video_engine/scripts/measure_page_boxes.py:320       (template_sha - "the fixture is stale when EITHER changes")
Recall: content/video_engine/tests/R26-225-NOTE.md      (the write half, fixed; the re-stamp still open)
Recall: docs_find "cadence on 1s threshold" -> 0 hits
Recall: docs_find "Ken Burns" -> [effects] plate_option:alive BEFORE plate_option:ken (the defect itself, R26-240)
```

Every `path:line` above was re-read against the final file after the last edit of this lane.

## The classification

Four classes, as the order names them: **(a)** a stale expectation against a since-ruled
behaviour, re-pinned with the ruling cited in the test; **(b)** a stale golden or fixture,
re-rendered only where the new artifact can be shown to be the ruled one; **(c)** a real defect,
NOT fixed here - filed as a row and marked `xfail(strict=True)` so the suite is green and the
defect is named; **(d)** a test that writes where it should not (R26-225's class).

| # | test | class | what was done |
|---|---|---|---|
| 1 | `test_breakthrough.py::test_the_stepped_shoot_is_piecewise_constant_between_frames_and_monotone_across_them` | **a** | `CADENCE.ON1_PX_S` is **154**, not 250, since `e087ba3` (E99 s30; R26-64 / R26-85, P53 T5) - the cinema-parity reference, CONFIRMED in `docs/research/runs/strobe_stop_motion/VERIFICATION-2026-09-13.md`. The measured speed (359 px/s) and the verdict (hold 1, on 1s) did not move with it, which is that commit's own claim. 250 -> 154 in the assertion, the ruling cited in the docstring. |
| 2 | `test_effects_catalog_drift.py::test_the_committed_catalogue_passes_every_check` | **c** | Four compiler tokens have no card: `room` (R26-221), `domain` (R26-223), `build` (R26-226) - all three built today - and `labelfit` (R26-191). **R26-239** + `xfail(strict=True)`. |
| 3 | `test_effects_catalog_drift.py::test_coverage_accepts_a_token_listed_as_a_parent_cards_option` | **c** | The same four, last assertion only. **R26-239** + `xfail(strict=True)`. |
| 4 | `test_effects_catalog_drift.py::test_the_ken_burns_card_is_implicit_and_docs_find_answers_with_it_first` | **c** | `docs_find`'s effects layer is the only recall layer without `rank_by_field=True`, so `plate_option:alive` (matched in `does`, field index 3) outranks `plate_option:ken` (matched in `title`, index 1). **R26-240** + `xfail(strict=True)`; the card's own three assertions still pass. |
| 5-7 | `test_effects_gallery.py::test_each_pinned_page_frame_is_unchanged` - the three page pins | **b** | Re-pinned. The page is built from the committed cards and the delta is ONE card: **143 -> 144 cards, live 46 -> 47, plate_option 13 -> 14, with golden 63 -> 67, with test 129 -> 130** - `plate_option:alive` (81210d8 / 17fc475, E99 s55 / s63; CAPABILITIES.md:153 LIVE). Judged on the frames: gallery-foot changes only the axis chip's count, gallery-top the counts line, the chip and one new alias line. |
| 8 | `test_effects_gallery.py::test_the_three_motion_cards_are_the_ones_the_rule_picks` | **a** | The SET is the same three; the ORDER moved because `exit:melt`'s card grew to 8 phases (the gather `3fb4783` / E99 s2, the one closing `310c4e3` / E99 s53, the throw `7d07ff0` / E99 s51). Re-derived by the rule itself: compare (10 phases, 3 pinned instants), melt (8, 5), remake (7, 2), stack (6, 1). |
| 9-10 | `test_idle_e49.py` - the two golden-source idle pins | **a** | Six PAGE goldens carry `kinetics.idle` because a page's own life is behind E49's switch too (R26-226 / R26-228 / R26-233 / R26-234, E99 s82-s83): page-life-still, page-life-live, page-build-lines, page-build-lines-4th, page-rescale-follow, page-rescale-follow-yield. Pinned as their own set; the PLATE pair keeps its two assertions (`plate_idle_paints`, 20 px) and the page six assert what they are (a ledger world, no drift amplitude). |
| 11 | `test_lint_species_choice.py::test_every_kind_and_verb_carries_a_when` | **c** | The `remake` verb's `when` is 278 chars against this lint's 260 (the other six run 118-185). **R26-238** + `xfail(strict=True)`; the SET half runs live in a new companion test, so a kind that ships with no `when` still fails today. |
| 12 | `test_lint_species_choice.py::test_map_carries_the_compiler_when_and_the_ten_acts` | **b** | SPECIES-BY-SENTENCE.md s4 was one row stale (the `chart_to remake` row, added by `d4294e2` without the regeneration). Regenerated with the command the test's own failure message names: `lint_species_choice.py --write-doc`. Diff: **+1 line**. |
| 13 | `test_metric_comparator.py::test_compare_is_the_sixth_chart_to_verb_and_carries_its_when` | **a** | `CHART_TO_KINDS` is a closed tuple and `remake` joined it as the SEVENTH (`d4294e2`, P61 T2, E99 s1 / s34). Tuple re-pinned; the name stays (a test is never renamed) and the docstring says why. |
| 14 | `test_motion_gate_wiring.py::test_ep1_report_is_the_four_fail_baseline` | **a** | R26-225's second half. Measured on the private bed: **9 FAIL / 1 WARN / 5 PASS / 1 JUDGE / 9 INFO**. The ninth is **M44** (`3fb4783`; GATES-REGISTRY.md:154 - 6 of ep1's 75 world plates are under 6 s AND carry a dock); the other eight are ep1's long-ruled reds (M01, M03, M05, M07, M08, M10, M11, M12). Re-pinned to 9. The committed build-f report was **not** re-stamped - it is under `projects/`, outside this lane's write set, and stays R26-225's open half. |
| 15 | `test_page_boxes.py::test_the_fixture_names_every_builder_at_both_aspects_and_the_player_it_was_read_from` | **b** | Re-measured with `measure_page_boxes.py --write` (R26-51's own remedy). **The whole diff is one line - `player_sha256`.** Not one measured box moved across five engine commits, which is both the justification and **R26-241**. |
| 16 | `test_portrait_parity.py::test_no_landscape_literal_in_player_code` | **a** | The four offenders are the PORTRAIT reference of the verdict stack's 9:16 form (`5eb4fa3`, P61 T7 / E99 s21): `REF_W: 1080`, `REF_H: 1920` and two ORIGIN lines dividing the portrait safe box by 1080 / 1920 inside `VERDICT_9X16`. That is this lint's intent, not its offence; allowlisted with the reason, the landscape pair untouched. |
| 17 | `test_race_path.py::test_the_golden_pair_is_unchanged` - the eased half | **c** | A render flake of R26-140's class, NOT a stale golden: the same tree passes it alone (`14 passed in 13.28s`) and the diff is only the title / sub / cite losing their italic face and shifting ~20 px, every bar, tick and the year identical. **R26-243**; the golden was not re-rendered (the golden is right, the run was wrong) and no marker was added, because it passes. |
| 18-32 | `test_shape_skeletons.py::test_every_word_cites_the_record_and_a_table_and_both_still_say_it` - 15 words | **b** | The vocabulary cites CAPABILITIES.md **by line**, and `26c406a` moved every row past 76 by three. **20 cites** re-pointed against the text each was written against (`git show cc16776:<file>`) - the 16 that were failing and **4 that were silently passing on the wrong row** (cut, card, hold, object-becomes-chart). The fragility itself is **R26-242**. |
| 33 | `test_video_dock.py::test_the_world_clip_and_the_video_dock_share_one_seek` | **a** | A third caller joined `seekVideo`: the ALIVE PLANE (`81210d8`, P61 T14 / E99 s55) seeks its background clip on the scene's own clock, looped, and joins `clipSeeks`. Count 2 -> 3; the invariant the test exists for (one definition, one `seeked` listener, one `__clipsSeeked`) is unchanged. |

**Class (d) - none left.** R26-225 fixed the one offender; the projects tree is clean after both
runs. No test was deleted or renamed; test 11 keeps its name and a new companion test carries the
live half of it.

## What was re-rendered, and why the new artifact is the ruled one

Scratch root for every before/after copy below (this session's own, outside the repo):
`C:/Users/Snipe/AppData/Local/Temp/claude/C--Users-Snipe-Downloads-Outreach-Program--claude-worktrees-sweet-villani-1c3a16/45114c3b-258a-4ca8-9aaf-b674a804cc7e/scratchpad/r26224/`

| artifact | before | after | the proof |
|---|---|---|---|
| the three gallery page pins under `tests/golden/frames/` | `gallery-frames-before/` | `gallery-frames-after/` (byte-identical to what was pinned) | The page lists exactly the committed catalogue, and the catalogue is in sync with the committed cards (`build_effects_catalog.check()` names no stale artifact - only R26-239's coverage). The delta is `plate_option:alive`, a committed LIVE card. Captured twice, byte-identical both times. |
| `content/video_engine/assets/page-boxes.v1.json` | `page-boxes.committed.json` | `page-boxes.after.json` | One line changed - `player_sha256`. A dry measurement into scratch BEFORE the write proved every box identical first (`page-boxes.dry.json`, diff = 1 line). |
| `docs/content-video-engine/SPECIES-BY-SENTENCE.md` s4 | `SPECIES-BY-SENTENCE.before.md` | the file | Written by the generator the test names; the added row is the compiler's own `remake` sentence, verbatim. |
| `content/video_engine/configs/shape_skeleton.schema.json` | `shape_skeleton.schema.before.json` | the file | Only line numbers moved (19 lines each way); each new number is where the originally cited TEXT now lives, and the test re-opens and re-greps every one of them. |

Nothing under `content/video_engine/projects/` was touched, and no engine file was edited: the two
engine-owned defects (R26-238, R26-240) and the two gaps (R26-239, R26-243) are named in rows and
in the markers, not patched here.

## The two runs

The whole suite, unpiped, written to a file, `-p no:cacheprovider`, on the same tree apart from
this lane's own changes.

**BEFORE** (`26c406a`, before the first edit):

```
33 failed, 4712 passed, 14 skipped, 1 warning in 1467.55s (0:24:27)
```

**AFTER** (the final tree; the same command, run twice - once when the last edit landed and once
more after the row renumber, and both read the same):

```
FAILED content/video_engine/tests/test_lab_log.py::test_the_operators_six_answers_derive_to_six_live_judgements_that_validate
FAILED content/video_engine/tests/test_lab_log.py::test_a_later_answer_supersedes_the_earlier_and_the_earlier_is_kept
2 failed, 4740 passed, 14 skipped, 4 xfailed, 1 warning in 1587.59s (0:26:27)
```

**All thirty-three are gone**; the four `xfailed` are R26-238 (1), R26-239 (2) and R26-240 (1),
each strict and each named in a row. The two failures left are R26-244's - `bd2a000`'s, landed
mid-run, on a file this lane never touched (see the section above). `git status --porcelain
--untracked-files=no -- content/video_engine/projects/` is EMPTY after both runs.

## What arrived DURING the after-run, and is not this lane's

Two tests that PASSED in the before-run FAILED in the after-run, and neither is in this lane's
write set: `test_lab_log.py::test_the_operators_six_answers_derive_to_six_live_judgements_that_validate`
and `::test_a_later_answer_supersedes_the_earlier_and_the_earlier_is_kept`. Cause, measured:
`bd2a000` (E99 s84) landed at **15:51**, in the middle of the run, and wrote the queue's
15:39-15:43 answers into `docs/content-video-engine/review-answers.jsonl` - a file this lane
never touched (`git log -1` on it names that commit; it is absent from this lane's diff). The
two pins are written against the six answers of 2026-09-17: one asserts every live record's
`answer_at` starts with `2026-09-17T` (the seventh reads `2026-09-18T15:41:46-07:00`), the other
that `recipe:plate-dock-wipe` has exactly two records (it now has three). Reproduced on their own
in 13.79 s, so it is the DATA, not the run.

Filed as **R26-244**, owned by the lane that recorded the answers, and deliberately not fixed
here: another agent is editing that area of this checkout right now, and the ruling is theirs to
read. It is also the cleanest possible restatement of R26-224 - the suite went from green to red
again inside the same 25 minutes it took to prove it green.

Row numbers: this lane first wrote its six rows as R26-236..241; `bd2a000` took R26-236 and
R26-237 for E99 s84 while the run was in flight, so they were renumbered to **R26-238..R26-243**
(and R26-244 added above). Every reference in the tests and in this note was re-audited against
the BACKLOG's own titles afterwards.
