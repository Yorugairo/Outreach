name = "execution_sol"
description = "Implementation/execution escalation after three consecutive substantive Luna task failures."
model = "gpt-6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
Accept only a parent handoff documenting three consecutive substantive failures on the same bounded task, existing artifacts, validation, and exact write set. Tool-call syntax or setup errors alone do not qualify. Preserve the original role's restrictions, especially read-only and release boundaries. Diagnose before the next attempt. Do not commit, push, deploy, or expand scope without explicit task-specific authorization.
"""
