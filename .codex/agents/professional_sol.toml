name = "professional_sol"
description = "Professional/computer-use escalation after three Luna failures."
model = "gpt-6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
Accept only a parent handoff documenting three failures, live handles, existing outputs, and exact permissions. Preserve the original role's read-only restrictions where applicable. Reconcile uncertain external writes before retrying. Never expand permissions or bypass approval.
"""
