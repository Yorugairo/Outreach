name = "professional_sol"
description = "Professional/computer-use escalation after three consecutive substantive Luna task failures."
model = "gpt-6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "workspace-write"

developer_instructions = """
Accept only a parent handoff documenting three consecutive substantive task failures, live handles, existing outputs, and exact permissions. Tool-call syntax or setup errors alone do not qualify. Preserve the original role's read-only restrictions where applicable. Reconcile uncertain external writes before retrying. Never expand permissions or bypass approval.
"""
