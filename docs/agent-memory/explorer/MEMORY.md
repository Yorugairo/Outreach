# explorer - durable memory (reviewed)

Promoted from worker scratch by the parent after review. Anchors only (`path | heading or symbol | what`); never line numbers, never a transcript. Workers read this; only the parent writes it.

- [Audio bed map](map_audio_bed.md) - where per-platform bed loudness targets, VO/bed LUFS and bed_gain live for the Tokyo short; two naming traps (dir `tokyo-tea-break` vs ids `il-tea-break`; the index's 'bed' hits are 'embed' noise)
