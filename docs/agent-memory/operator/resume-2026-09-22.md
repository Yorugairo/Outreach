---
name: resume-2026-09-22
description: "State 2026-09-22 - the FABLE WORKTREE exists at last (.claude/worktrees/fable-p68, branch claude/fable-p68, from main at bd2a000) and builds; rulings s87 (the stamp is the reference's, and a prop is never framed) and s84 (Ken Burns alone) applied; the main checkout is still on codex/Astra with 65 dirty files from three lanes and the reference cut recompiled, awaiting the operator's word; next = the stamp port lands, then T6 the body"
metadata:
  node_type: memory
  type: project
  originSessionId: 45114c3b-258a-4ca8-9aaf-b674a804cc7e
  modified: 2026-09-22T00:00:00.000Z
---

**THE LANES.** The Fable lane finally has a worktree: `.claude/worktrees/fable-p68`, branch `claude/fable-p68`,
branched from `main` at bd2a000, head **c7e6065**. It BUILDS - proven, not assumed: `build_episode_h.py` runs there
end to end (cues 3 of 3, motion gate 1 FAIL M11 / 1 WARN / 16 PASS) once the gitignored inputs are copied in (the
scratch take, evidence objects, host plates, props, icons, sound bed, and the ~900 MB of plate claim dirs under
`review/claims/`). Plate ids resolve by ABSOLUTE path into the main checkout, so reading plates works anywhere;
anything ffmpeg opens by repo-relative path does not. **The main checkout is still on `codex/Astra`** at bd2a000
with 65 dirty tracked files from three lanes (the parent's R26-224 hygiene wave, a Codex tooling lane, and Astra's
three nights - Astra has committed NOTHING since the hand-off), and `build-f`, the reference cut, was recompiled
Friday 20:58. The ownership list is `scratchpad/assembly/DIRTY-OWNERSHIP-2026-09-22.md`. **Waiting on the operator:**
whether Astra's 44 files commit where they sit, and whether to restore the reference cut. Nothing there has been
touched. `main` is 72 ahead of origin, unpushed; no checkout sits on `main`.

**E99 s87 - THE LESSON THAT COST THREE PASSES.** The operator: *"Stamp was supposed to be built off of the reference
remotion or hyperframes, i think we didn't take the right lesson."* `REMOTION-UI-INTAKE-2026-09-07.md` triaged RU-2
badge-stamp as PRIORITY and named the mechanism - the two-spring offset, a clamped scale spring landing while a free
rotation spring still unwinds, *"the weight cue our single-spring landings lack"* - and R26-20 stood HALF since 09-11
with the rotation spring deferred *"port it when a stamp asks for it."* Meanwhile `stamp` in the compiler became the
vector map's number-at-a-centroid, and the parent "proved" a prop as a docked card, which is that weightless landing.
Same fortnight, `REMOTION-UI-HARVEST.md` said the line's tip is *"One gesture instead of overlapping animations"* and
R26-228 built five at once, which the operator read as *"2 different pointers"* and *"chaos"* (s83). **Read the
harvest note and the intake triage BEFORE building anything they name; port the mechanism, not the look.**

**THE MODEL THE OPERATOR SETTLED (s87, two axes - the parent kept collapsing them).** (1) The ARRIVAL is the motion:
`stamp` sits beside `land` and `throw` - the clamped scale spring settling while the free rotation spring still
unwinds so the thing rests slightly OFF-SQUARE, the impact ring from the contact point on split shock curves to ~2x
radius, an exit owed on what landed (E50), our look (charcoal on cream, never a gold ring). It carries whatever the
author names; a card may be stamped. (2) What arrives is FRAMED or BARE: a card is framed evidence with a box, edge,
shadow and room; a PROP is never that - its alpha is its shape and it joins the WORLD, so on a page it is page
content that parks, rescales and leaves with the page (R26-219). **A prop arrives one of two ways, both unframed:
stamped on, or thrown on.** Open for the operator's eye: whether a stamped prop keeps its colour or is laid in the
page's ink; the lane renders the golden pair both ways.

**BUILT TODAY in the Fable worktree:** R26-236 (E99 s84 - Ken Burns ALONE, `plate_idle_paints` off, the drift
withdrawn for long form; committed c7e6065) and R26-235 (a full-stage 16:9 page is MEASURED not estimated - the
fixture grows a `full_stage` key; the card's room on the bed's page goes from `corner [982,214,100,80]` at the
legibility floor to `empty [167,224,382,239]`; the reachable zoom is UNCHANGED at 1.08 and the 1.3 px slack was never
the box model, it is the page's own Ken Burns which nothing models; a test had PINNED the defect and was flipped).
R26-20's stamp port was in flight at the compact. R26-245/246 filed.

**NEXT:** the stamp port lands and is reviewed; then R26-230 (the full-stage page's top and bottom bands, for the
9:16 re-stage and captions - after the operator answers the caption-size question on HG3), R26-222, R26-231; then
**T6 the body** the bed's way, with the six prop rows as STAMPS and the room the fixture now gives the cards.
The HG1 and HG3 cards are still open on the queue. See [steal-now-harvest-not-replace](steal-now-harvest-not-replace.md), [worktree-sprawl](worktree-sprawl.md),
[resume-2026-09-18](resume-2026-09-18.md).
