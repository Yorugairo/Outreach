---
name: one-shot-lessons-2026-09-16
description: "One-shot #3 (the Fable parent's own, E99 s66): eight build passes read on contact sheets taught what the gates and the frames want on a 9:16 short - and where the floors contradict each other; the loop the next one-shots (Opus, Astra) should start from"
metadata: 
  node_type: memory
  type: project
  originSessionId: 45114c3b-258a-4ca8-9aaf-b674a804cc7e
  modified: 2026-09-17T04:33:37.212Z
---

**The cut:** `content/video_engine/projects/systems-and-blowups/memory-trades-the-calendar/` (the print calendar is the
metronome; 72 s; Flow stills on HollowStickMike; the Chirp scratch as the clock), build `build-oneshot-3/`, the card
`one-shot-3-fable-memory-calendar` on the queue; the ledger's FINDINGS list is the record.

**What the gates want on a 9:16 short (learned by failing them):** a world plate holds >= 6 s (M44) and something moves on it
every 2.5 s - captions never count (M16): lights that follow the sentence, a ticker on a screen region, a card landing; the
first page is the world by 0:10 and lit at once (M11); a chart dock leaves inside 6 s counted from its THROW (M12); plates + clips
must match pages + chart-dock INSTANCES (M39: a card re-used counts twice); docks on >= 0.36 of the SENTENCE beats (M37);
the beat plan is per sentence, t0 = the sentence start (M41). No badge rail on any card at 9:16 (13 stage px); no `park`
(the pills fall under 11 px); a host card reads in the page's own empty room; the verdict wall clips portrait cards; a
bars page with negative values collides its labels (builder).

**The contradiction to fix before Opus/Astra are measured:** M38 (proven-recipe coverage 0.60) is unreachable under
M44/M11 with today's proven set (R26-168) - the recipes were proven on 3-5 s plates and building pages.

**Process that worked:** author the rows from the words, build, read the contact sheets as a viewer BEFORE reading the
gate, fix what the frame shows first; run the motion gate on a fresh probe after the self-watch (the compile-time gate
can read a stale layout probe); the blind viewer catches laundered [rehook] tags - rewrite as a question or untag.

Related: [proof-is-a-scene](proof-is-a-scene.md), [judge-the-frame-not-the-diff](judge-the-frame-not-the-diff.md), [one-shot-floor-recipes-not-events](one-shot-floor-recipes-not-events.md), [first-pass-additive-capability-led](first-pass-additive-capability-led.md).

**THE OPERATOR'S FOUR CORRECTIONS (E99 s67, the same night) - the first pass was refused:** lights are not animation; a chart never lands fully built - it BUILDS with the effects (the axes entry with the line drawing from frame 0, the mount's own build, bars growing, the slices arriving) and then holds built for its number, and the timing is solved by starting the page on the beat's first sentence; the open STARTS ON THE CHART (Tokyo, Japan) and that is one of the few places a chart may stand fully drawn; a dock is an evidence still, never a full chart card thrown; no borrowed footage (the two-fingers clip was Japan's gesture); the signature transitions (the mount, the spiral return, the dip, the suck) are the shape a one-shot blends new capabilities INTO; the label crashes have fixes on record (name_clear; a stated domain); and recall the motion doctrine + the approved shorts' tables BEFORE authoring (the runbook's step 0 now; R26-177). The parity table must compare MECHANISMS, not counts (Apply 7). Better defaults come from enumerating the combination space and proving it on the test bed (R26-176, the recipe lab).
