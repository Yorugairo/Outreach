---
name: animators-loop-p51
description: "The grill of 2026-09-11 settled the animator's loop: P51 (draft) - the agent's loop first, the editor a client of a pure function of t, a one-shot bar before the operator watches, the chart as the world; AnimatorOS is the horizon"
metadata:
  type: project
---

The operator's grill (2026-09-11) settled: the time goes into watch-then-cut rounds because the first pass is not close
enough; the agent's loop gets faster FIRST (probe + layout gate M25 + hot reload + self-watch), the operator's editor second;
the player stays a pure function of t (a ghost outline may follow a drag; the real frame on release); a human's or a flash
agent's edit is a SIDECAR of overrides keyed by row id + field (Motion Canvas's pattern) that the agent reads as a diff + the
frames at the changed instant + the gate delta; hot reload proves determinism (warm vs cold hashes at the changed instants);
goldens are consistency, not quality (regenerated on purpose); the one-shot bar = gates + lint + viewer + the opening's sheet
(3:00 long / 0:60 short at 2 s); the ledger/chart page is the WORLD (the main character), plates are narration plates/B-roll;
no reproductions (new content at each level); the "AI slop" comment was about the package + generated art volume, not hands.

**Why:** ep1's curve holds 42% flat from 2:00 to 9:00; the loss is 0:30-1:13 (the promise window); 39% APV is a hair under
the published "strong" band; 60% at 0:30 IS the hook target. Records: `docs/content-video-engine/GRILL-ANIMATOR-ITERATION-2026-09-11.md`,
`.claude/PRPs/plans/P51-THE-ANIMATORS-LOOP.plan.md` (draft), `docs/research/runs/grill_animator-iteration/findings_r1.md`.

**How to apply:** order after P50 T1: T0 kit -> T2 eyes -> T3 bar -> T1 split -> T4 reload -> T5 sidecar -> T6 change report
-> T7 editor; T8 doctrine (E61) in parallel. Never a hand-count gate; never runtime state in the engine; eyes = numbers first.
AnimatorOS (component library + canvas + Gemini/GPT/Claude agents, vidrush-style asset pulls) waits until P51 stands. See
[recall-before-propose](recall-before-propose.md), [judge-the-frame-not-the-diff](judge-the-frame-not-the-diff.md), [bravos-reference](bravos-reference.md).
