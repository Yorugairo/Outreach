---
name: resume-2026-09-06
description: "Resume state through 2026-09-07 night - what is on main (head 2a30c4f: P48 T1 + T4 landed), what runs, what waits on the operator (HG2 the recast, the v3 watch, the cut ledger column, the render)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 45114c3b-258a-4ca8-9aaf-b674a804cc7e
  modified: 2026-09-10T22:42:24.565Z
---

**On main (all pushed, head ~bfc853f):** P45 complete; P46 bridge LIVE (four orders round-tripped, tier 0 + one tier-1 pattern proven, ~10 parser gaps fixed the same day); Tokyo v1 rendered + approved (1440x2560, in the MAIN checkout, backed up on G:); Tokyo v2 built as PLAYER only (E44 page on the hook, mount 1.99 s, video docks springing in and parking, dips at the two world changes; gate PASS; served on :8731 by `serve_player.py 8731 build-short` from the main checkout - NOT rendered, the operator watches first). Rulings E42-E49 written today (poses first; DERIVED on our thresholds; chart-carried beats not viewer misses; the chart on the hook; docks by the springs, the mount is the roll-out + savor, spirals for returns; E44/E45 the shorts standard; dip + blur-zoom in, wipe an effect; E48 amended: hero = spiral, primaries NOT prescribed, mount is a build, cut minimally / one thread; E49 nothing ever goes truly still - the named idle). Transitions review + TR-1 (36/35/28 measured) + TR-2 (cut 3 frames before the next word's onset; dips centred) done by our own tools. HyperFrames intake gated and triaged (HF-1..18, R1..R8). Gemini research profiles: model flash + loop discipline.

**P47 RUNNING (operator: "continue with p47", 2026-09-06 late):** T5 the idle COMPLETE + pushed (d39a08e, 4e3678b, 53723ff): kinetics/idle.mjs behind `kinetics.idle` (compiler turns it ON for every new timeline; `;idle=<kind>` per plate; goldens untouched), M18 frozen frames + measure_frozen_frames.py, Tokyo v2 rebuilt with the idle (pre-E49 copy in build-short.v2-pre-e49/). Finding: whole-frame hashes pass even pre-E49 because phrase captions boil - per-layer hash is backlog R26-13. **T2 build-on COMPLETE + pushed (67ac1ca):** build_to / bracket / retitle / relight as PAGE species (compiler grammar, gate credits, M19 holds, M11 takes a build_to landing as the annotation, M18 refuses stale hashes; the empty-caption-page PG[-1] throw fixed). **Tokyo v3 first pass authored** (build_short.py rows 2/4/6; v2 preserved as build-short.v2/; gate PASS, M18 no identical run) - it is the PLAYER on :8731, unrendered; stills sent. **T1 stop-action COMPLETE + pushed (75620ea):** stopaction.mjs (cadence rule on the frame index, MASS presets, throwXf/landXf/lag) behind kinetics.stop_action; dock tuple 5th element {arrive, mass}; `;arrive=`/`;mass=` plate options; M20. Tokyo v3 second pass authored (panel THROWN on 'Three men', two fingers LANDED metal on 'Two numbers'); stills sent for HG1/HG2 - the operator watches v3 on :8731 (unrendered). **T3 the morph COMPLETE + pushed (bfc853f, 2026-09-07 ~01:00):** arap.mjs (polar decomposition, SO(2)xSym+(2), factored Laplacian, the STRIP mesh - a fan flips on a jagged series; inertia-based axis; oriented bounding area), the `morph` page enter (`ledger:...:morph[=s]`, `;morph=tab|plate|card`, behind arap_morph, falls back to a mount), M17 + measure_morph.py. HG3 material = tokyo-tea-break/build-morph-demo/player.html (M17 PASS); v3 keeps its mount. THE WATCH HAPPENED (2026-09-07 ~01:30): HG1 'nothing reads as drift' (E49 confirmed by eye), the tea dock at 0:16 removed; HG2 'doesn't land with weight, no shadow coming in' -> four mechanisms built the same morning (contactShadow, impactSquash envelope about the base, groundShake on the world, MASS.impact) - dials for the ear, research brief docs/research/motion/BRIEF-WEIGHT-DENSITY-MASS-2026-09-07.md written for the operator to run; HG3 'reads but not effectively; must be the red tie; too long on a mount' -> R26-16 amended (source planted per scene, a seam's length, never on a mount). HyperFrames harvest: 19 components in content/video_engine/hyperframes (HARVEST-2026-09-07.md). R26-17 TOP: the engine apart from the editor (P48 candidate). Still waiting: the second watch of v3 on :8731 with the weight cues, then the render. P47 is now WAITING ON THE OPERATOR'S WATCH: HG1 (v3 on :8731 - the cut ledger column is still empty), HG2 (the thrown panel / the landed fingers), HG3 (the morph demo). T4 stays `running` until the watch; render only after. Deviations on record: ink.mjs stepClock / plate_life not moved to the frame-index form (goldens); no object-page renderer (the morph's source is a named outline); no sound cue for the throw's landing. **Waits on the operator:** (1) HG1 after T2 (watch v3 in the player); (2) the v2 cut ledger's operator column (`tokyo-tea-break/SHOT-TABLE-V3-PROPOSAL.md` part A); (3) a watch of v2 in the player, then the render (never before the word); (4) P46 HG1 (does Astra's runner read our queue?) and the Task Scheduler entry for the daemon; (5) the `/prompting/vocabulary` page if they have it.

**2026-09-07 afternoon/evening (all pushed, head 9706ba7):** P46 T7/T8 bridge hardening DONE (da6aa84, e40ab90, dc32f03): failure classes form-vs-substance, `order_roots`, `check_verify`, TEMPLATES + `bridge_check.py` CLI (Gemini runs tier 0 itself before replying), the repair round in the daemon (form failures repaired at zero Claude tokens; substance -> tier 1 with a reason), new shapes fetch/measure/watch/intake-triage (`docs/runbooks/BRIDGE-SHAPES.md`); watcher placeholder fix b561419 ("Waiting for task-N" is pending, not a reply), supersede fix 6d114a8 (daemon no longer crashes when a packet lands twice). Two Gemini profile orders closed at tier 0 (bridge shapes block + `video-watcher` profile; /watch must be CITED in every watch order - operator rule). The weight research landed and was REPAIRED for provenance (24 proof lines, 8 sources on disk under docs/research/motion/sources/). **9706ba7: the stop-action dials moved onto the sourced report** - squash ONE frame (rigid: none, contact frame uncompressed), shadow depth = BLUR 16->0.8 px, mass = the ground DIPPING (groundDip; the shake is violence, opt-in `violent`), ballistic chord (no deceleration), the thing never sinks, restitution `e` [UNVERIFIED], the landing SOUND cue on contact-1 frame written by build_short.py from the module's own dials. v3 rebuilt on :8731 (gate PASS, M18 fresh); stills sent. Full engine suite: 1708 passed / 57 pre-existing failures outside the kinetics (finance-whiteboard proofs on missing deck assets, production console, audio_synth) - not ours to fix this lane. **Later the same evening (E50 + P47 T6, pushed):** the operator's second watch ruled E50 - a chart's deployed life is 6-8 s from its LAST data mark, 12 s at most, then un-draw or become the next thing (docs/portable/OPERATOR-RULINGS.md E50). Built the same night: `undraw` + `figure` page species, M21 (per ledger page, from the last data mark to the exit or the undraw; > 12 s WARN, > 8 s INFO), an undraw with no series takes the brackets; v3 row 2 un-draws into the two treasury figures, row 4 into the June print (the first number), row 6 carries the HOOK'S OWN PROOF as a CHART CARD (scripts/chart_card.py renders a ledger page once at its landing to a dock still; the Fed-vs-yields object from FRED with proof lines, fetch_fed_vs_yields.py) thrown on "The Fed still". M21 PASS on every page. The card's labels are tight at 720 px (a portrait-typed card is a dial). R26-18/R26-19 built; open: JGB-vs-UST fetch, portrait card. **The third watch (same night, 1647bf9 pushed):** operator reads -> E51 (a push only tied to a landing; M22; v3's two punches cut), lingering dots gone (an un-drawn path is hidden; the figure has no pin), `note` species (side handwriting), `snap` page enter (a landed card grows to be the world) + `centre` dock option; the ring restructured: the Fed card thrown on the Meta page, SNAPS on 'moved' to be the last world, notes from the object's facts, the host centred. remotion-ui: 7 components installed as references (0.9.0) + REMOTION-UI-INTAKE (RU-2 badge-stamp = priority port, R26-20). Deviation: a cold seek into the snap window is one frame impure (R26-21). **The design pass (138203b, /design-engine):** E52 (a page cites in one short line - `src_style: compact` per object; a chart reads with no caption; the math drawn). The Fed page = Fed funds vs the 10-year on one scale + the +80 bp bracket; the third perspective ev-japan-selling-v1 (signed monthly bars, from the holdings object) thrown on 'here's what nobody says'; centred docks now real (dock entry `centre`, sized by card_aspect, in the band above the caption); the placement study tokyo-tea-break/DESIGN-PASS-2026-09-07.md. Open: R26-22 default centring by E50's clock; the 10-year's corner name. **Watches 3-5 (2026-09-07 night, all pushed - head 2fb85f9):** E50 (a chart's deployed life, gate M21), E51 (a push is tied to a landing, M22), E52 (a page cites, it does not footnote), E53 (chart form is law) - see [chart-form-rulings-e50-e53](chart-form-rulings-e50-e53.md). Engine gained: `undraw` + `figure` + `note` + `spread` page species, `snap` page enter (a landed card grows to be the world), `tiers`/overlay + `axes.hlines` reference rules on a ledger line page, `centre` docks placed in the page's free bands, chart cards (`chart_card.py`), `callout` `pad`, `spread` `from_index`, keyed `paths` selector so a page can un-draw and REDRAW. The ring page is now the Fed-vs-10-year page in ONE unit with the low circled and the gap bled red; the bar combo is behind `FED_WITH_BARS=1`. Macro-chart research gated + triaged: `docs/content-video-engine/MACRO-CHART-INTAKE-2026-09-07.md` (MC-1..MC-11); remotion-ui 7 components harvested as references (`REMOTION-UI-INTAKE-2026-09-07.md`, RU-2 badge-stamp = next port). **P48 DRAFT written and awaiting approval**: chart-to-chart transitions as a first-rate feature (keyed marks, up to 3 states per page, `chart_to` rescale/extend/recast/morph_to, gate M23) - T1 is the risk and lands alone with the goldens as its acceptance. R26-17 (engine apart from editor) renumbered to P49. **Watch 6 (the tea cup, head 2681c4e):** the host dock is GONE from the ring page (it was parking over the title). In its place a small live VIDEO card of the steaming cup, zoomed out of the opening scene's own footage - `zoom_clip()` / `dock_zoom()` in build_short.py crop a region of another clip and scale it back up, keyframed, so the steam keeps moving (E49). The crop is centred on the cup's MEASURED centre (ink-row scan: cup+saucer x 440-570, steam 492-535, so centre x 505 - centring on the bbox put the cup off-frame because the little tab card pulled it right). The card sits in the plot's empty upper-left. New dock options: `centre_w`, `centre_band`, `centre_x`, `centre_y`. **R26-27 (found here, real):** `page_boxes` and the player's own layout DISAGREE on a portrait page - `centred_place` was handed y 536 for a band the player draws at 1250; x is exact, only y is wrong. Until reconciled a row names the centre outright. **P48 APPROVED 2026-09-07 - THIS IS THE NEXT WORK.** Order: **T1** (the mark model: every builder emits KEYED marks, zero behaviour change, the golden suite is its acceptance - if one byte moves, stop) → **T4 recast** with a NEW pie/share builder, acceptance = Tokyo row 2's beat: the holdings line un-draws, the title typewriter-backspaces and rewrites (`retitle`'s eraser already does this), a PIE of the top-5 foreign holders of US Treasuries draws on, and Japan's -$122.6B peels out as the sold wedge in blood red → HG2 → T2/T3 (rescale, extend) → T5 morph_to → T6 gate M23 → T7 doctrine+Tokyo. **E53 s1 AMENDED and approved**: a pie/donut is allowed ONLY when (a) the claim is about one named slice, (b) that slice is highlighted and the rest muted, (c) the figure is written on the page, (d) five slices or fewer. Outside that the donut refusal in `ledger_page.py` stands. **Data is in hand**: `evidence/build_tokyo_evidence.py:121` already receives the whole TIC Major Foreign Holders table (it reads Japan / United Kingdom / China, Mainland / Grand Total from `rows`), so the top five need no fetch. **Why row 2 is currently just two figures**: E50 says un-draw OR BECOME THE NEXT THING; I redrew the same line's tail and relabelled it, which is neither, and it could not read anyway (5 of 316 points = 1.27 % of the axis; a focus_zoom is a ~1.1x push and does not rescue it). Removed, with the reason in build_short.py. **Waits on the operator now:** the next watch of v3 on :8731 (the ring as the Fed page, the snap, the notes, the centred host; the un-draws and figures; no pushes) (weight by ear: the panel's throw, the fingers' land), the cut ledger's operator column (SHOT-TABLE-V3-PROPOSAL.md part A), the render only on the word; a documented hit sound file for the metal landing (fs-riserhit-754771.mp3 has no provenance line). Open backlog: R26-16 morph from the tie (planted per scene, a seam's length), R26-17/P48 the engine apart from the editor, R26-13 per-layer hash.

**P48 T1 + T4 LANDED 2026-09-07 (head 2a30c4f, pushed).** **T1 the mark model** (50eb4bb, alone, goldens byte-identical):
every ledger builder registers what it draws as `{key, role, el, geom}` on `st.marks` / `st.markBy` via `lpMark`, keyed
`b:<i>` a bar datum, `s<si>` a series' stroke (its `geom.pts` carrying datum `s<si>:<k>`), `r:<j>` a race row, plus role
keys (axis, tick:<n>, ylab:<n>, rule:<n>, xtick:<n>, xlab:<i>, val:b:<i>, name:s<si>, title, sub, src); `lpMarkDatum`
resolves a datum through whichever mark owns it; `__lpProbe` reports them. **Acceptance row (2) was wrong and is recorded
as such**: no `paintLedger*` ever read a builder closure - every paint step already read a record on `st` - so T1 keys
those records and each mark carries `rec`. Only dense-line is goldened, so a parametrized browser test covers story /
combo / decline / race by asserting each builder's LAST registrations.
**T4** (f4a5f60 + 2a30c4f): the `share` page (a pie) with E53 s1's four bounds enforced BY NAME in `ledger_page.py`
(`SHARE_BOUNDS`, `SHARE_MAX_SLICES` 5) - the donut refusal still stands for every other variant; the `peel` species
(the named slice is TWO adjacent wedges in one colour, and the piece slides out along its own bisector and goes blood
red); `;then=<series>:<variant>` -> `world.page_states` (a second FULL spec, `STATE_MAX` 3); `chart_to {at, dur, to:
"recast", state}`; `lpPaintChart` (paintLedger's build beat, lifted out unchanged) + `lpPaintStates`.
**The recast is a HAND-OVER, not the keyed tween the plan drafted** - deliberately, because the plan's own Not Building
refuses a tween with no honest 1:1 correspondence and this beat is 316 points -> 5 wedges. The standing state runs its
own build law BACKWARDS (that IS an un-draw, for every builder, free) and the named state draws on by its own law, with
the title, sub and source rewritten by the hand that rewrites a title. **The keyed tween for legal pairs is T4b, after
HG2.** Two things only the FRAMES showed: the sub and source went on describing the chart that had left, and a line
un-drawn through the pen's two-thirds law backwards stands still then vanishes (it leaves by LENGTH, in the reverse
order it was drawn).
**Data**: `fetch_top_holders.py` + `objects/ev-top-holders-v1.series.json` - TIC Table 5 parsed for EVERY country row
(the holdings build named only four; the plan's claim that the whole table was already in hand was half right). The pie
is drawn at Japan's PEAK month, not the latest, so the sold piece is geometrically inside the wedge: 1,239.3 - 122.6 =
1,116.7, exactly today's holding. Top five at Feb 2026: Japan 1,239.3 / UK 897.3 / China 694.2 / Belgium 454.7 /
Canada 446.4. `.gitattributes` now pins `evidence/sources/**` as `-text` so a proof file's sha256 survives a checkout.
**HG2 stills sent** (`tokyo-tea-break/build-f/hg2/`). Next after the watch: T2 rescale / T3 extend, then T5, T6, T7 -
and whether T4b (the keyed tween) is worth building at all is part of what HG2 decides.

**2026-09-08 — the tariff short's design pass, and what it taught the engine (head b9084d9, all pushed).**
Order of the day: ep1 analytics (see [ep1-lifetime-curve](ep1-lifetime-curve.md), [search-intent-cohort](search-intent-cohort.md)) -> SCRIPT-H for Steel and Paper
-> the Japan tariff short's charts.

**Steel and Paper:** `REWRITE-ORDER-H.md` (scope corrected to a FULL pass, not opening-only - 14 of 23 gate failures were
outside P1) and `SCRIPT-H-VO.txt` at **42 PASS / 2 FAIL**, lint and audit clean, all twelve JUDGE rows verdicted by hand
in `SCRIPT-H-JUDGE.md`. The iron spike is retired: the ring object is the **certificate** (the thing we WARN about, drawn
in the thumbnail, and 'certificate' already appeared six times in the back half). G02 is a record-time lead-in, not a
script property. G15b remains an unresolved disagreement: a faithful replication of its own block scores the close group
at 4 shared stems where the gate scores 1 - **I called it a defect prematurely and retracted that**; it responds to the
script (0 -> 1 when the token changed), so it needs its own look, not a prose workaround. The blind viewer has NOT run
(a Codex batch = a cost call) and it is the row that caught G's opening. SHOT-TABLE-H does not exist.

**Engine capabilities added (all opt-in, goldens byte-identical throughout):** `enter=built` (a page mounts with its
chart already drawn - the mechanism existed for spiral/snap; what was missing was an enter with no RETURN meaning);
`page.build_s` (a page draws over its own seconds instead of LP.BUILD 3.0); `axes.hlines` on a BARS page (the comparator
rule - the receipt page now draws the $1,740 gap instead of asking the viewer to subtract); `axes.name_clear` (lift an
inline series name clear of the data it would be written across); declared colour now outranks the sign default in
`buildLedgerBars` (E53 s7 - a page of COSTS was coming out green because the values were positive); the emphasised bar's
callout no longer buries its own x-label; six tick divisions on a bars page (lpNiceStep's 1-2-5 ladder was giving two
labels).

**E50 amended (see the ruling):** the 6 s is a FLOOR and binds ONLY on a chart that arrives built. **If you watched it
draw you do not need long to read it; if it arrived complete you do.** M21 carries it and reports `arrival + build +
deployed`, because only the last is E50's clock. My "four rushed pages" framing was wrong and is retracted - counting the
build as dead time is exactly backwards.

**Two traps to remember.** (1) `evidence/objects/` in the tariff project is a **generated copy** - `build_short.py:307`
copies `evidence/<s>.series.json` over it every run. Edit the SOURCE, one directory up; I lost a whole pass of object
edits to this and only the rendered frame caught it. (2) **`flow_enqueue_batch` / `capability_snapshot` is the RETIRED bridge lane
(2026-09-03) - never cite it as a blocker again.** Plates roll through `video-engine`'s `create_flow_image` over CDP, and
the MCP need not be attached: `mcp/server.mjs` answers JSON-RPC over stdio (precedent `mp-host-transitions-v1/
dispatch_batch.py`; the tariff one is `japan-tariff-trick/omni-video/dispatch_crossings_map.py`). The only real
precondition is the OPERATOR's automation Chrome: `--user-data-dir=C:\Users\Snipe\.flow-chrome-profile
--remote-debugging-port=9223`, signed in - a script never enters credentials. Dispatcher ABORTs (exit 2) without it.

**Waiting on the operator:** dispatch the crossings-map plate (`PLATE-ORDER-MAP-BEAT.md`, prompt final, beat grown from
4.57 s to 9.5 s by moving `t_gates` to `cut("Auto parts")` - which also fixes "Auto parts taxes compounded against
Detroit" being spoken over the Japan HOLDINGS chart). **The boundary move and the plate must land together** or M16's
pulse trips on a 9.5 s still. Also open: SCRIPT-H's viewer run, SHOT-TABLE-H, and Steel and Paper's publication route
(unlist + re-upload recommended, not confirmed).

**Running / fragile:** the bridge daemon loop is a detached background python (lock in `docs/research/runs/bridge/daemon.lock`); it dies with the machine, not the session - re-check with `tasklist` and restart `bridge_daemon.py --loop` if the lock's pid is gone. The player server on :8731 likewise.

**How to apply:** start from BACKLOG rows R26-0..R26-12 and the P47 plan; do not re-derive the rulings; a shell heredoc mangles `
` in python strings - write patch scripts with the Write tool.

## 2026-09-08 evening - the crossings beat is BUILT

The tariff short's six-gates plate is replaced by the crossings map: the operator's own Flow edit of the v2-HollowStickMike
ground (`omni-video/stills/sig-b-crossings-map-v9-operator-edit.png`, candidate until "approved"). Boundary = `cut("But look")`
11.78 s - `cut("Auto parts")` has NO M13 gap and was never legal. `crossings_species()` in `build_short.py`: six `trace` hops
(new opt-in `hop {from,to,bow,draw_s,width}`, held to the cut, ONE bow sign so returns bow the other way) + six `callout` "25%"
stamps (new opt-in `label_scale`), stacked passport-style so six are visible = the count. Goldens byte-identical; motion gate
0 FAIL. Judged on rendered frames (scratchpad beat_frames.py pattern: RB.serve + frame_png on build-short/player.html).
Style atom amended in E39: "A light application of 2.5D woodblock print and vox newspaper meets rich anime colors."
Driver fixes ON DISK, UNCOMMITTED (other lane's file `cdp-driver.mjs`): Agent-mode toggle, Characters view listing, grid
refill wait, pill-settle retry, persistent `runtime/seen-image-ids.json`. Still open: SCRIPT-H viewer run, SHOT-TABLE-H,
the multi-agent MCP (one long-lived driver + mutex + shared seen-set) as its own PRP.
TRANSITIONS, same evening: a BUILT page NEVER mounts (mount = cream mounting onto the world, then ink); a `cut` row used to
fall through to the WIPE in the player - now a cut (E47: wipe retired as default, by name only). The tariff short's declared
vocabulary (E48): primary DIP (plate to plate), ACCENT `enter=throw` (a built chart page THROWN onto the world on throwXf,
lands with the chart standing - operator: "the transition is literally the plate entering the world"), hero spiral.
Recall order for any transition question: TRANSITIONS-REVIEW-2026-09-06.md -> E46/E47/E48 -> the player. Never reach for the wipe.
THE THROW, final form (2026-09-08 late): enter=throw = a whole dock-sized card (0.62) thrown UP and OUT of the top of the frame
(arc 1.5x chord), growing into the world only on the descent (1/z, min-jerk), pitch + paper flex, camera follow 0.25 (wA scales
about its bottom edge so no black band), the paper dip + contact shadow on landing. Operator's three corrections in order: (1) not
a scaled screenshot - needs the dock's stop-motion weight; (2) not huge-and-clipped ON the frame - it must LEAVE the frame;
(3) the snap happens on the landing. Dials [DERIVED], tune by eye. throw=<grow>,<from>,<s> on the plate id.
Throw corrections 4-5 (same night): flight 1.1 s ("way too fast" at 0.7); THROW_SQUASH 0.15 - the motion squash at page speed
stretched the page to 120% of the stage and cut the title ("it is cut off" - MEASURE the element rects, don't read instants);
growth to the stage ONLY AFTER CONTACT (any growth while above rest pushes the top out of frame). Lesson: when the operator
says cut off, inspect page/field/title rects in the pane at that instant before tuning anything.
FINAL FORM of the chart arrival (2026-09-08, late): NOT enter=throw/drop (kept as opt-ins). The Tokyo third-watch pattern:
chart_dock_card() renders the page as a portrait card -> thrown as a dock on the PREVIOUS scene ({"arrive":"throw","mass":"paper"},
CARD_LEAD_S 1.0 before the boundary) -> the page enters `snap=<dock>` (grows from the landed card's rect, SNAP_S 0.45, min-jerk,
SNAP_BLUR velocity-envelope whoosh, whoosh cue in SOUND-PLAN). The CARD PAGE: a snapped page (or ;card=yes) keeps rounded corners
at full size, CARD_SCALE 0.955, doc 29 s1.2 hard-edge ink shadow, no border, world visible around it. M11 ignores a snap card.
Operator: "use the same throw as we did for the docks in Tokyo, then snap to the full plate size, not grow it".

## CLOSING CHECKPOINT 2026-09-08 (the last session in the sweet-villani worktree)

State on main (head 4653723): the tariff short's chart arrival = the THIRD WATCH end to end (chart_dock_card thrown on the
previous scene with arrive:throw, centred; the page enters snap=<card> with the velocity-envelope whoosh blur + whoosh cue;
the CARD PAGE: rounded, no black frame, the cream is the edge, fills the frame at rest). enter=throw / enter=drop stay in the
kit as opt-ins. The crossings map beat (six hops, six stamps) at 11.78-18.30. Player: `preview_start japan-short-player`
(:8734, main's launch.json has it). Motion gate 0 FAIL; goldens byte-identical throughout.
P49-THE-CAMERA.plan.md is DRAFT (validates); the operator settled: attention drives the camera, authored keys override;
the camera arrival stays opt-in until watched; captions do not ride. NOT approved yet - do not implement until "approved".
The in-flight size change is PERSPECTIVE (4-6 %), the fill is the CAMERA - the object never grows.
Hooks: .git/hooks/commit-msg -> scripts/hooks/recall_receipt.py INSTALLED (shared by all worktrees). Global instructions
reduced to identity + process ownership + failures-written-down; the rest lives in CLAUDE.md fast routes, AGENTS-VIDEO-ENGINE
rule 4, docs/runbooks/RECALL-RECEIPT.md. RECALL BEFORE YOU BUILD: "let's build / try / iterate / review the docs" open with
Recall: lines from docs_find.
Flattening: sweet-villani is preserved (artifacts copied to main, branch content all on main) - remove from a session on main:
`git worktree remove .claude/worktrees/sweet-villani-1c3a16 && git branch -D claude/content-generation-system-52f077`. The
other Claude worktree (content-generation-system-52f077, 14 GB untracked) is NOT preserved yet.
Still open: the plate's word "approved" (the map + the two chart cards are candidates); SCRIPT-H blind viewer run (Codex
batch, a cost call) and SHOT-TABLE-H; ElevenLabs only on the operator's word; the render after the word (REVIEW-CLAUDE.md);
Steel and Paper's publication route; the multi-agent Flow MCP (one long-lived driver + mutex + shared seen-set) as its own
PRP; the five driver fixes sit in the other lane's uncommitted cdp-driver.mjs; G15b gate-vs-replication.
First-watch fixes (2026-09-08, late): idle=live on ledger pages (breath has a fixed point at the centre - the chart WAS still);
spotlight dur:"hold" -> until the next event / a card arriving / the cut / the NEXT SENTENCE's first word (E25: the light follows
its sentence, read off the take's punctuation - the hook light lifts on "Auto parts" 8.88); two species layers (datum -> under
the docks, point/region -> above); the harness callout cut (the two-lanes card is the story). Voice: keep ElevenLabs on the
body so retention deltas are attributable - the spend still needs the operator's word.
The glitch (2026-09-08, last): #species-under was left on the landscape viewBox -> a portrait spotlight at the axis corner; a
0.61 s light flashed. Fixed + gated: test_portrait_parity.py (no landscape literal in player code; every layer fitted at 9:16;
worlds are 1.10x centred BY DESIGN), HOLD_MIN_S 1.0 (a light with no room is dropped, printed at build). The lint also caught
camXf/camCss zooming about 960/540 in portrait (fixed) and the stack burst offsets (fixed). When the operator says "glitch":
measure the layer's viewBox/rect in the pane first. Ink bloom = intake doc + TR-14, not built.
Beds: a SHORT runs at -26 LU under the VO on both platforms (operator 2026-09-08); -28 is long-form. The plan is written AFTER the compile in build_short, so a gain change needs two builds to reach the player.

**Closing checkpoint 2026-09-08 (late):** main at c7c3da6, everything pushed. The tariff short is approved and rendered (see [japan-tariff-trick-v1](japan-tariff-trick-v1.md)); E54 rules the voice; the eight mechanisms of the day are in CAPABILITIES.md; the recall receipt hook is installed. Next session on main: remove the sweet-villani worktree (`git worktree remove .claude/worktrees/sweet-villani-1c3a16 && git branch -D claude/content-generation-system-52f077`), inventory the other Claude worktree before deleting it, then P49-THE-CAMERA (draft; needs "approved").

**2026-09-10 checkpoint:** P48 running - T2 rescale (235832a) and T3 extend (7e77f0c, 6de864f) built and pushed; HG1 (the
operator's watch of both on a real page) is open on :8739 `chart-transitions-proof`. Next in P48: T2b park, T4b the keyed
tween (n lines <-> n bars), T5 morph_to, T6 the M23 gate, T7 doctrine + Tokyo (row 2 = the holdings line rescaled to its
Feb-Jun window, then extended). P50 (the Bravos grammar) is a validated draft awaiting "approved" (all four gates answered;
E53's census exception written). Tokyo's remake is on :8738 awaiting the watch + the render word. The Bravos dossier and
doc 46 s46.7 are the reference for all of it.

**2026-09-10 late checkpoint - P48 built through T7:** T2b park (340c317), the vortex drains the active state (25e4918),
the plot-box pin (2d6c805), T6 M23 (435d5ea), T4b the keyed recast (7fd2b02: n lines -> n bars by series, the name gives
way to the value, the line leaves by its history; golden `ledger-keyed`), T5 morph_to (6c9a07e: the area under the line
becomes another line's area by ARAP; M17 per morph; flag off = the recast hand-over byte for byte), T7 (E58 the ruling +
doc 29 s9.28(e); Tokyo row 2 rescaled on :8740 beside :8738; the perform layer on its own svg so figures follow the
active state). Proof page :8739 now runs 62 s: rescale 8, extend 13, park 17.5, keyed 35, morph 50. Open in P48: HG2/HG3/
HG4 are the operator's watches; T5b the planted-element morph (R26-16, the page-enter path); the second Tokyo verb is the
operator's beat to pick; R26-28 brackets/spreads follow the active state. P50 still awaits "approved". The Fable parent did
T2-T7 itself (the mechanics were too coupled to lpPaintStates to delegate safely); every slice: goldens byte-identical,
frames read as a viewer, the life check, a `Recall:` line in the commit.

**P49 THE CAMERA running (the operator's `/prp-implement P49`, 2026-09-10 late):** Bravos's camera MEASURED first
(37/45 held compositions still; every chart and diagram) -> P49 amended: LOCKED is the default. T1-T3 (9ef10c0):
`kinetics/camera.mjs` - one state {zoom, look, at} per frame, the three species pixel-identical through it, authored keys
per shot row (the 8th tuple element), `window.__camera(t, target)` = the frustum + in-frame. T6 (5363605): M09/M14 read key
segments, M24 in-frame from the track; the animation registry's latent unpack bug fixed. T4 (ea09eed): `attention:
"landings"` opt-in pulls 1.06 toward a dock that arrives (HG1 open). T5: `enter=camera=<dock>` - the eye goes to the card,
the world switches at the match; the tariff short built BESIDE (`TARIFF_BUILD_DIR=build-short-p49
TARIFF_CHART_ARRIVAL=camera`, :8741 `japan-short-player-p49`) for HG2; three cuts from the frames (a thrown card's
transform branch, the bottom-edge origin, the card leaving the dock list 5 ms before the match). Traps: two sync Playwright
sessions in one thread refuse each other; a table cell with `|` inside breaks the animation registry's row parser; the
Write tool refuses main-checkout paths (stage in the scratchpad, copy in); the scrub steps 10 ms. T7 done (258f56a: E59).
R26-28 BUILT on the operator's word ("fold in the proper bracket/spread handling before review"): brackets and spreads
follow the active chart state per frame, lerped across a rescale/extend, hidden off-window; the Tokyo P48 cut rebuilt on
:8740. Open: HG1/HG2 (P49), HG4 (Tokyo :8740), docks still in screen space (a ruling to take), the cold-seek first frame
of an arrival (R26-21's class), P50 awaits "approved".


**2026-09-10 late (P48 complete through T7 + R26-28; P49 built through T7, HG1/HG2 open):** the Tokyo default build carries the
P48 cut + the pledge RECORD (Nikkei lede, typewriter + highlighter, sourced); the camera variant on :8742 (`TOKYO_CAMERA=1`);
the BREAKTHROUGH bars form (`domain` + `overflow: "break"` on a bars page; `proof_breakthrough.py`; `test_breakthrough.py`;
R26-29 the printed as-of date before any render). Fixes on the way: the callout pill fits its number (was fixed 160 px), the
record keeps the space after its phrase. Waits on the operator: the watch of :8738 vs :8742 (HG1 dials, HG2 arrival vs snap),
the render word, whether the breakthrough goes into Tokyo's "it beats our bonds". See [tokyo-short-render](tokyo-short-render.md).

**2026-09-10 night: P49 COMPLETE** - the operator's watch ("8742>8738") closed HG1/HG2; the camera cut is Tokyo's default.
Same night: the breakthrough in the cut on "beats our bonds" (E60), the pledge record's read->park beside the chart with the
plant beneath (the dock read->park mechanism: `read`/`read_s`/`park_s`, a placed slot 1, the record scales in cqw). Waits on
the operator: the render word for Tokyo; whether the tariff short is re-cut with the camera.

**2026-09-10, the exploration review** (`docs/content-video-engine/EXPLORATION-REVIEW-2026-09-10.md`, BACKLOG R26-31): Gemini's
Bravos dossier is pacing arithmetic only (carried / re-measured / rejected; the phase windows now in 46 §46.7); the gaps are
SPECIES (press card, TV-embed world, vector map, flow diagram, the burst's furniture, treemap ruling, count array) plus R26-30
the stop-motion burst, R26-24/25, TR-7's ARAP invariants as gates, Euler spirals (42 §42.4), morph Method A (43 §43.5), the
viability matrix (45 §45.2). The operator picks the next.

**2026-09-10 late: P50 amended with the operator's burn-down (still DRAFT, awaits "approved").** T1 the species-by-sentence map
FIRST (the only "when to use" doc beyond doc 29 §9.28's surface grammar; widened to E58/E59/E60 + the dock laws); the TV
becomes the ART-embed world (our narrative plates, a press card projected onto a painted surface by a homography; parallax
dropped by the operator); T10 the burst's furniture, T11 tags-to-bars, T12 morph Method A + TR-7 refusals, T13 the stop-motion
burst, T14 the clothoid fitter (doc 42's last law; customers: T4 arrows, T5 arcs), T15 HF-15/16/17, T16 R26-22/27. MC-8 closed
(spread). Order: T1, T2, T3, T7, T4+T14, T5, T11, T10, T13, T12, T9, T15, T16; T6 when a beat asks.
