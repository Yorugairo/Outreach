# The thin one-shot

**The operator**, three messages typed while the agent was mid-turn (2026-09-13):
- ledger `4ca256e095ca` (03:14): *"looking at your frame by frame doesnt look like ther'es going to be anywher enear
  enough motion/animation on the screen ... Im also concerned that the charts don't actually show much comparison or
  tel much story, because we show them in isolation, there's no comparator or reference points. They're all basic one
  line charts. I don't see any interesting sequence."*
- ledger `f96cbd9489f2` (03:16): *"i feel we need more chart variety, better comparators, and more on-screen action.
  transforms should actually lead to new charts. We don't use our stop motion capabilities, or really most any of our
  capabilities."*
- ledger `9b975256b6cb` (03:20): *"a one-shot is never a test of the page with no library, a one-shot is always your
  best attempt to produce an complete episode in a pass without my input"*
- ledger `6651399cae4a` (03:21): *"You should always make use of your resources to empower yourself."*

**Where:** `normal-for-which-bridge` review-v1, the whole cut; `before.png` is 0:43, typical of its middle.

**Before** (`before.png`): one line on one page, no comparator, no reference rule, a note parked below the caption.
The motion gate read 0 FAIL / 1 WARN and the layout probe passed. There is no `after.png` yet - the after is pass 2.

**Why it happened:** the agent (1) treated the one-shot as a test of the page with no library and never widened that
scope; (2) fixed every defect the frames showed by DELETING (a figure, a span, a note, a callout), so the frames got
clean and empty; (3) chose charts from the series already on disk instead of from the comparison each claim needs;
(4) never inventoried the capabilities beat by beat; (5) never set the cut beside an approved short.

**The rule:** `docs/agent-memory/operator/first-pass-additive-capability-led.md` - the comparator before the series, a
capability inventory per beat, parity with the approved shorts, a fix never lowers motion, the resources (FRED, Yahoo,
SCML, bridge research) used without asking.

**The gate:** none - JUDGE. Gate-clean is not ready. A reference-parity reading (events/min and compositions/min
against the approved shorts) would catch part of it.

**The lesson for any author:** a cut that passes every gate can still have nothing to watch. Judge it against the best
approved short, not against the gates.
