---
name: composite-effects-keep-every-phase
description: Never describe a composite effect by its first phase or a doc one-liner; read the painter code for the whole arc and name the references it blends (the verdict stack = 3 remotion-bits + the hyperframes pilot)
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 45114c3b-258a-4ca8-9aaf-b674a804cc7e
  modified: 2026-09-13T19:37:22.050Z
---

The verdict stack ("evidence wall") is a five-phase choreography, not "N proofs fly in from depth": each card enters from
depth ONE AT A TIME on its verbatim phrase, holds LARGE near centre while spoken (drifting), recedes to one of nine
asymmetric rail spots when the next beat lands (the page re-composes as a mosaic), rails idle, and on the pivot line every
card bursts radially along its own bearing, spinning, 60 ms staggered (`drawStack`, doc 29 s9.24-9.24b,
`docs/portable/MOTION-GRAMMAR.md`). It blends the operator's references of 2026-08-31 (ledger `ddc90e1656f9`):
remotion-bits transform3d-showcase, mosaic-reframe, fracture-reassemble, and the hyperframes-opening-v1 pilot's focus
hand-off. On 2026-09-13 I proposed its card from CAPABILITIES' one-liner; the operator: *"it soudns like your verdict stack
is very limited, are you ignoring the other effects we blended in? we didn't just fly them in - we flew them in, 1 at a
time, dancing with choreography, spread assigned them around the page, then burst them out"* and *"thats why i pointed out
to you that what we did was based off ~3 different remotion-bits effects"*.

**Why:** a catalogue read by less-capable agents copies its one-liner; a collapsed description ships a dead fly-in-and-sit
grid, which MOTION-GRAMMAR says reads dead within seconds.

**How to apply:** for any effect card, recall line, or proposal: read the painter's code for every phase and its trigger,
quote the operator's references and which phase each became, and make `does` summarise the whole arc. P55 cards carry
`phases` + `blends`. See [recall-before-propose](recall-before-propose.md), [animators-loop-p51](animators-loop-p51.md), [judge-the-frame-not-the-diff](judge-the-frame-not-the-diff.md).
