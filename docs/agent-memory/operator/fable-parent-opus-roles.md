---
name: fable-parent-opus-roles
description: "Operator 2026-09-05: Fable is the clearly stronger model with a 50% weekly cap - spend it on judgement only; deploy / review / recall / bounded implementation go to the eight PRP roles as Opus 5 agents (.claude/agents/<role>.md; speedster Sonnet), CLAUDE_CODE_SUBAGENT_MODEL=opus catches the built-ins"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 45114c3b-258a-4ca8-9aaf-b674a804cc7e
  modified: 2026-09-05T23:30:43.188Z
---

**What the operator said (2026-09-05):** "with Fable being the clearly superior model and only allowing 50% weekly usage, we should probably optimize our harness so that we off-load more work to Opus 5 ... so that we don't end up using Fable tokens to deploy, review, recall, and drive the agent. Probably this means using the ECC named agents as Opus 5.0 agents."

**What shipped:** `.claude/agents/{speedster,junior_developer,implementation_luna,architect_sol,explorer,docs_researcher,reviewer,release_steward}.md` (project scope, committed 9824c1d) ported from `.codex/agents/*.toml`, `model: opus` (speedster `sonnet` since 2026-09-05 evening - not Haiku: overhead dominates, a wrong edit is dearer); `CLAUDE_CODE_SUBAGENT_MODEL=opus` in `~/.claude/settings.json` so `Explore` / `Plan` / `general-purpose` stop inheriting Fable; the policy in `docs/runbooks/PRP_EXECUTION.md` "Dispatch mapping" + AGENTS.md section 9 + CLAUDE.md.

**Why:** the parent session is where judgement compounds (architecture, integration, protected actions, the operator's conversation, completion truth). Everything else was being done inline by Fable - file reads for recall, diff reviews, git mechanics - because the roles existed as doctrine only and every delegation inherited the parent model.

**How to apply:** in a Fable session, delegate by role name (`subagent_type: "explorer"` for recall, `"reviewer"` before a commit, `"release_steward"` for staged commits, `"junior_developer"` / `"implementation_luna"` for bounded slices) and keep the parent on the brief and the diff review. Never launch a delegated role on Fable. `effort` is a supported frontmatter key but is not set on the roles yet (values unverified) - a knob to tune per role when cost data arrives. The runbook's fallback mapping still applies in a harness without the roles. Related: [worktree-sprawl](worktree-sprawl.md) (write sets), [judging-is-codex-cli-batch](judging-is-codex-cli-batch.md) (LLM judges go through Codex, not Claude subagents).

**Measured (2026-09-05, evals/RETRIEVAL-BENCHMARK-2026-09-05.md):** five held-truth lookups, Explore-on-Fable vs the Opus explorer role: 5/5 correct both; Opus 156k vs Fable 243k tokens (36% fewer), none in the parent; ~20-25k fixed overhead per dispatch, so delegate hunts of 10+ tool calls, do one-grep lookups inline. The parent's own hunts ran 12-50 tool calls and up to 2 MB of tool output. SigMap does not index the docs (ranked the retired Manim scene for 'animation math'); ECC's agents are generic sonnet/haiku prompts and were not loaded - keep ECC for skills/hooks only. docs/research/runs is gitignored (disk-as-bus scratch) - durable records go under evals/.

**Round 2 (hard retrievals, same file):** Opus role 4.5/5 vs Fable 5/5; Opus 26% fewer tokens, half the wall time; the Opus miss was a FALSE NEGATIVE ('07 s5.3 does not exist' - it does, in sources/reference_analyses/.../07_academic_literature_...md). Rule: a delegated agent reports 'not found where I looked', never 'does not exist'; the parent verifies negatives with one grep. Persistence (SendMessage) is not exposed in the desktop build; the `memory` frontmatter key is the next experiment.

**Operator corrections (2026-09-05, later):** neither agent type's context returns to the parent - only the ~200-word result; the difference is whose budget the 50-70k tokens hit. A persistent agent's growing context never touches the Fable cap, so persistence is the right design; `/resume` is CLI-only (not the desktop Code tab), so `memory: project` is set on explorer / docs_researcher / reviewer (`.claude/agent-memory/<role>/MEMORY.md`, committed) and the roles carry a retrieval discipline: rg `docs/DOCS-INDEX.jsonl` first (build_docs_index.py), ast-grep/sed windows over whole-file reads, 'not found where I looked' never 'does not exist'.

**Round 3 + policy (same day):** Sonnet as searcher NOT adopted (3.5/5, 238k tokens vs Opus 156k for the same five - the fixed overhead dominates; confidently wrong labels). memory: project landed in the WORKTREE -> roles use memory: user (~/.claude/agent-memory/<role>/). Cost model for inline vs delegate: 1 KB of tool output read inline ~ 300 tokens that persist and are re-sent every later turn; a dispatch returns ~300 tokens total. Delegate at >5 tool calls / unknown file / 'does X exist'; inline for one rg + one <=40-line window. Policy text: docs/runbooks/PRP_EXECUTION.md 'Hand-off policy'.

**Effort + ledger (2026-09-05 evening):** operator's rule - effort high on Fable and every Opus role (the efficient frontier; depth of research, attention over long integrations), low on the Sonnet speedster (execute the exact command, do not improve). Set in the role frontmatter and verified to load. `~/.claude/hooks/dispatch_ledger.py` on SubagentStop appends usage to evals/DISPATCH-LOG.jsonl (gitignored) - a command hook, zero tokens; it fires from the NEXT session (hooks load at session start). ECC rules kept (TDD, review, security, git, style are quality rules); only hooks.md and performance.md were trimmed to their guidance lines.

**The eight suggestions, status 2026-09-05 evening:** 1 backup - main bundled to G:/My Drive/Backups/outreach-program/ (Google Drive; the push is still the operator's); 2 overhead - AGENTS.md 2-8 moved to docs/AGENTS-SEO-PLATFORM.md (15.4 -> 9.9 KB), ECC meta rules trimmed, quality rules kept; 3 context_guard.py (PreToolUse Read|Bash: no whole-file reads over 400 lines, no bare cat, no commit on a stale docs index); 4 dispatch_ledger.py (SubagentStop); 5 agent memories use anchors `path | heading | what`; 6 docs-index body-vocabulary slice delegated to implementation_luna; 7 one-checkout-per-agent DEFERRED - agents coordinate across projects (operator); 8 reviewer-before-commit used on the split: it caught 3 dangling citations + 1 overstatement. Hooks load at session start - both guards are live from the next session.

**The dispatch floor, measured (grill 2026-09-05, benchmark round 6):** a bare dispatch is 27.6k tokens = ~16.3k harness (system prompt + tools + account connectors - the operator disables AOY/Vercel/Supabase/Drive/chrome/computer-use in the app) + ~10.8k instructions attachment SNAPSHOTTED AT SESSION START (AGENTS.md from the session's project dir - a worktree's old branch copy!, ~/.claude/rules which DO load into subagents, the parent's MEMORY.md index, CLAUDE.md). Mid-session file changes never reach a running session. Cuts made: AGENTS.md 3.9 KB, rules = security + git only (the rest = quality-rules skill preloaded by implementing roles), MEMORY.md hooks capped, roles 1.3 KB + retrieval-layers skill, maxTurns. Verify next session (round 7). Start sessions on main or a worktree fast-forwarded to main. Keep MEMORY.md hooks short - every line costs every dispatch.
