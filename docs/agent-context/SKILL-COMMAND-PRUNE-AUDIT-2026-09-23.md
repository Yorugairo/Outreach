# Skill and command prune audit — 2026-09-23

Status: usage-based prune at the operator's direction. Unused skills do not need to be duplicates to leave the active catalog. Owned unused files were deleted without backups; managed plugin files were not edited. Scope: project `.agents/skills`, shared `C:/Users/Snipe/.agents/skills`, personal `C:/Users/Snipe/.codex/skills`, project/global Codex config, router, and command directories. Worktree copies, backups, archives, and generated indexes were excluded from the primary inventory.

| Candidate | Evidence | Disposition |
| --- | --- | --- |
| Shared `grilling` | Its sole 5,758-byte `SKILL.md` was byte-identical to shared `grill-me/SKILL.md` (SHA-256 `EF85A1F6E8A8314BC688BA866565E0E9F7BC8CC76E4FB6F8F9110683F2A1EE85`); both declared `name: grill-me`. The router and `GEMINI.md` call `/grill-me`, with no separate `/grilling` consumer found. | Removed the discoverable duplicate, config/profile entries, and later deleted its archived file at the operator's request. Canonical `grill-me` is unchanged. |
| Project vs shared `brand-voice` | Their other two files match. The project `SKILL.md` adds quantity-claim guidance and an E54 sound-level update. | Keep the richer project overlay. Do not silently promote project-specific rules into the shared skill or delete the overlay. |
| Project vs shared `script-writer` | Project adds narrative-map, review-receipt, and recording-clearance gates. Shared retains the full Humanizer rules, upstream snapshot, and license; the project reference points to shared material. | Synced the shared entrypoint to the current project gate contract. Both now contain Humanizer and production clearance; the project reference still resolves to the shared, licensed Humanizer source. Retain the project entrypoint until allowlist/router reconciliation establishes one canonical discoverable path. |
| Personal `video-script-architect` | Its three reference files hash-match `script-writer` references. Its six-stage skill omits Humanizer, narrative-map, semantic receipt, and recording clearance and uses obsolete viewer-score invocation. | Removed its allowlist entry, routed the harness profile to `script-writer`, and deleted its archived files at the operator's request. |
| Product Design | The plugin's ten 0.1.55 skill entrypoints are not part of the operator's used workflow. Distinct UX functions do not justify active discovery when unused. | Disabled all ten current entrypoints in project and global Codex config, removed dashboard routing to Product Design, and blocked the family in the project allowlist generator. Left plugin-managed cache files intact. |
| `design-engine` vs project `modern-design-frameworks` | Shared `design-engine` prescribes Tailwind v4/shadcn for implementation; the project skill requires vanilla CSS by default and Tailwind only on explicit user request. This is a real implementation-policy conflict, not a stronger/weaker duplicate. | Keep the project CSS rule authoritative in Outreach. Review the shared design-engine default separately before applying it in other projects; do not silently combine the rules. |
| `artifact-template-business-review`, `-design-report`, `-experiment-analysis`, `-system-design` | Four installed template packages have no project callers; the current 0.1.1 paths were already disabled in project and global config. | Keep them inactive; do not modify managed plugin-cache files. |
| `clean-code-guard` | Project config already disabled the shared skill, but the router and three harness profiles still named it. | Deleted its eight owned files without backup and removed the router/profile mentions. Existing stale disabled config entries are inert until the separate config-generation cleanup. |
| `excel-live-control` | Old disabled paths target absent runtime versions; current runtime is `26.915.20218`. | Added explicit disabled entries for the current path in project and global Codex config. Leave managed plugin files untouched. |
| Common creative skills | The project generated block had disabled shared `content-engine`, `council`, and `design-engine` even though the operator identified them as regularly used. | Added all three to the allowlist source and removed those three current project disable entries. Their distinct functions remain available. |
| Project vs shared `google-flow-production` | Project carries live-UI reconciliation and worker quarantine; shared carries compact submission limits, meter check, zero paid credits, candidate/extraction caps, dimensions, and registration. | Genuine tradeoff. Keep both pending operator review of a combined route; neither is safe to remove by size alone. |
| Router / allowlist / generated config | `configure_codex_skill_allowlist.py --check` reports `ok: false` (104 discovered, 40 active, 64 disabled after duplicate retirement). The router recommends some disabled skills; `.codex/config.toml` also contains older generated plugin paths. | Reconcile as a separate preview-and-review task. A disabled entry is not prune proof; do not run `--write` merely to silence the check. |

No `.claude/commands`, `.agents/commands`, or `.codex/commands` directory exists in this checkout. `scripts/harness-stamp.mjs` is a command-like legacy profile generator; only its confirmed redundant `grilling` entries were changed here. `scripts/sigmap_context.py` supports both `ask` and `query`, so those two documented invocations are not duplicates or broken commands.
