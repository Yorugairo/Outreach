# MP-FED-LIQUIDITY pilot assets — isolated Flow order

Repository: `C:/Users/Snipe/Downloads/Outreach Program`.
Execute the frozen service-generated order at:
`C:/Users/Snipe/Downloads/Outreach Program/content/video_engine/projects/systems-and-blowups/fed-liquidity-pressure/review/claims/mp-fed-liquidity-pilot-worlds-v1/WORK-ORDER.md`.

Use `/google-flow-production` and the `flow-asset-producer` profile, including its required video-engine references. The parent does not drive the Flow session. Read project AGENTS/rulings; current layer generation and slot-specific full-canvas overrides govern.

## Boundary

Seven still-image slots only: W2 workshop background/mid/owner, W4 funding counter background/mid/two representatives, P2 blank loan folio. Maximum paid credits ZERO. Verify the intended active account and zero-credit image mode before generating. Stop for login, account ambiguity, unavailable isolated tools, payment or installation. Do not switch accounts. No video, voice, uploads/publication or approval/promotion.

Only write inside the claim delivery directory above: raw/source/objects, per-attempt prompts and receipts, manifests, contact sheet, validation helper/report, approvals.json and bridge reply.md. Read the machine claim via `generation_claim.load_claim`; do not edit it or the frozen order. No shared code, docs, registry or other claim writes.

Generate each depth layer independently using the chosen background as registration reference. No segmentation of a newly generated composite. The full-canvas layer override means matte in place, no independent trim/centering; world boards stay opaque. At most three generated candidates and two extractions per slot. Save progress after each slot, preserve failed-attempt sources, deliver unresolved items rather than hiding failures. Never generate labels or evidence figures.

## Validation and return

The profile's `content/video_engine/scripts/prepare_props.py` path is absent in this checkout; do not invent it or install a substitute. Existing deterministic read-only gate is `content.video_engine.src.services.delivery_scan.scan_claim_delivery(load_claim("mp-fed-liquidity-pilot-worlds-v1"))`. Run it via a small claim-local Python helper from repo root, save the full JSON report, and retain all flags/failures. Verify image dimensions, true alpha (except opaque boards), hashes, registration and absence of generated text. If incompatible with a registered full-canvas layer, report the actual check and stop short of PASS; do not relax the gate.

Deliver the manifest shape supplied by the frozen order, a contact sheet, source hashes and exact prompts. Worker-selected `approved` in approvals.json is only a self-judgment; all assets remain `review_only`, `render_eligible:false` until operator HG2. Write approvals.json last, only when the batch ends. If blocked before generation, write readiness/reply evidence instead and explicitly report zero attempts.

Run `python content/video_engine/scripts/bridge_check.py --shape review --reply <absolute claim directory>/reply.md` before replying. Return the standard bridge grammar and compact receipt (<=250 words), actual paths and attempts/credit use. No raw DOM or secrets in the reply.
