# Fed pilot Flow v3 — operator-corrected style and character

Repository: `C:/Users/Snipe/Downloads/Outreach Program`.
Use `/google-flow-production` through one isolated `flow-asset-producer` worker. Execute only the frozen service order:
`C:/Users/Snipe/Downloads/Outreach Program/content/video_engine/projects/systems-and-blowups/fed-liquidity-pressure/review/claims/mp-fed-liquidity-pilot-worlds-v3/WORK-ORDER.md`.

The operator's EXACT art direction is:
> A light application of wood block print meets vox newspaper with rich anime colors

This overrides the older style-spine block for THIS episode. Do not append the old heavy carving, muted/strict palette or universal orthographic constraints. Light print treatment, rich anime colors; retain the authored scene composition and registered layers. Existing v1/v2 orders and candidates stay quarantined and unchanged. V2 has been stopped; do not resume it or inherit its candidates as v3 passes.

The operator also permits `@mike2` in scenes. Bind the existing `@mike2` character for the W2 workshop-owner slot using the picker and confirm an actual entity mention chip. Do not re-describe his appearance or substitute a newly invented identity. Other layers remain separate; W4 keeps its two anonymous transaction representatives. If the character cannot be found/bound, report that slot unresolved; other independent slots may proceed. No new character-creation job.

Use the already-signed-in `magolliet@gmail.com` Flow account. Verify the live image-mode meter is zero credits before each submission. Still images only, maximum paid credits ZERO, at most3 candidates and2 extraction attempts per slot. Seven slots: W2 background/mid/@mike2, W4 background/counters/two representatives, P2 blank folio. Generate layers separately, not by segmenting a new composite. Keep world layers full registered1920x1080; folio1024x1024. All numbers, chart marks and document text are rendered in code, not generated.

Write ONLY under v3 delivery: raw/sources/objects, exact prompts, per-slot checkpoints, hashes/manifests, real-image contact sheet, readiness, validation report and reply.md. Do not edit registry, frozen order, scripts, gates, doctrine, other claims or account settings. No installs, resets, driver edits, paid voice/video, publication or promotion. Reuse the supported working browser route from v2; no blind retries of stale9222/9223 endpoints.

The profile's prepare_props.py path is absent. Use the existing service `delivery_scan.scan_claim_delivery(load_claim('mp-fed-liquidity-pilot-worlds-v3'))` via a claim-local helper. Nonzero exit when fail counts or conflicts exist; preserve scanner failures without relaxing them. Preserve raw images even when extraction fails. Visually inspect registration and alpha too. All selected outputs remain review_only/render_eligible=false: worker selection never means HG2 approval. Write approvals.json last.

Do not finish merely after dispatching a child. Record its handle, collect its receipt and actual artifacts; return <=250words plus standard bridge grammar with attempts, credits, paths and blockers. A missing/blocked batch is not complete. Run existing bridge_check on the final reply and preserve its verdict.
