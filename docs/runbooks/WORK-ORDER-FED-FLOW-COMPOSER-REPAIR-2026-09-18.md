# Flow composer recovery — NO GENERATION

Root C:/Users/Snipe/Downloads/Outreach Program. The operator authorizes fixes/iteration toward video; previous V6 failed and retained a stale workshop thumbnail despite two correct inline chips. V6 coordinator and producer are terminal. This is a NEW bounded diagnostic/reset order, not a retry or amendment of V6.

Use one isolated Flow browser worker, reuse current signed-in magolliet@gmail.com session. Read google-flow-production skill and doc54 reconciliation. Write ONLY `docs/research/runs/fed-flow-composer-recovery/`, including ALL helpers. No shared driver edits, no installs, no account changes. Do not alter/delete existing images or failed cards. No generation, no submit click, no paid action, no regeneration/upscale. Stop at prepared composer.

1. Inspect actual current composer DOM and its dedicated attachment tray, not the whole gallery. Persist a compact sanitized fixture: composer-root selector and bounding box; actual inline chip text/type/provider IDs when exposed; thumbnail/title/alt/provider IDs plus enclosing elements and remove controls; explicit edit/start-frame context. No auth headers, cookies, tokens, browser storage or giant page dump. Record observable fields; unknown IDs are unknown, never guessed.
2. Reset using supported UI controls, scoped strictly to composer content/attachments (NOT gallery delete). Clear text, ALL attachments, inherited source/edit context and pending picker selections. Save read-only proof of zero text/chips/tray thumbnails/context. If cannot establish zero state, STOP and report exact remaining node, no more writes.
3. Prepare a short diagnostic finance prompt with the existing Mike2 inserted at its in-sentence target via picker; use operator's delete/retype/reselect rule. Attach ONLY existing hero-fab-constraint-v1.png media from project assets; do not upload duplicates. No other images. Avoid long generation prompt while testing composition state. Save post-bind fixture/screenshot. Required exact set: Mike2 entity + hero-fab media, no stale W2, no additional pending selections. Record provider IDs if present and how tray thumbnails map to the two selected entities; character internal sheet tiles must be distinguished from extra media.
4. Leave the composer PREPARED BUT UNSUBMITTED. Return compact receipt with live DOM facts, proven reset/bind sequence, scoped selectors, fixture paths and mismatches. This is preparation only; do not use existing V6 run_generation script or any helper that may click submit. Parent implements/tests a reusable guard from these actual fixtures before any generation.

Coordinator must collect actual worker receipt, not finish on dispatch. Record child handle immediately. No full transcript/DOM dump; <=200word receipt plus paths-written grammar.
