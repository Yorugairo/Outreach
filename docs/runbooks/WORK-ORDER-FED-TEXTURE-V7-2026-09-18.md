# Fed V7 — single guarded texture proof

Root C:/Users/Snipe/Downloads/Outreach Program. One isolated Flow producer, already signed-in magolliet@gmail.com. V6 failed/no charge; reset diagnostic terminal, prepared but UNSUBMITTED. Do not resume old generation helpers or submit the short diagnostic prompt.

Coordinator AND executing producer read `.agents/skills/google-flow-production/SKILL.md`, doc54 including reconciliation, episode STYLE-DIRECTION.md, and frozen `content/video_engine/projects/systems-and-blowups/fed-liquidity-pressure/review/claims/mp-fed-liquidity-texture-v7/WORK-ORDER.md`. Leading overrides win. ONE candidate, ONE submit only, zero paid credits. Writes only this delivery including helpers. Shared driver/source/guards read-only.

Use the service order's full art prompt, verbatim exact style sentence. Primary ONLY media reference is existing hero-fab-constraint-v1.png (source SHA2560212175acd5b8b868577e69b6eda403b3220440ece0d6522ac552aebab16dfea); transfer visible paper fibers, broken ink, fine carved hatching and rich printed color planes into finance, not factory objects. Mike2 is finance representative, not labor. Do not add desk/V4/workshop references.

Mandatory preflight: import `captureComposerState` and `assertLiveComposerState` from `tools/google-flow-driver/src/composer-state.mjs` plus `assertComposerState` from `composer-guard.mjs`. Do not recreate/weaken them. First reset unique visible `flow-prompt-box` / `.ProseMirror` using the proven scoped range-select/backspace and tray removal sequence from recovery; capture and require assertComposerState(snapshot,[],{mode:'zero'}). A failed reset STOPs, never generating to test it.

Then type full art prose and bind existing Mike2 at intended sentence location through actual picker. Later literal @mike2 does not bind; remove/retype/select that exact target and verify after final edits. Attach existing fab from picker, no uploads or bulk selection. Capture actual picker-to-tray thumbnail identity equality (queryless URL) and entity ID. Preserve proof.

Required expected allowlist passed to guard:
`[{type:'entity',id:'dab5d902-9559-44bb-9fea-4823c52de93f',name:'Mike2'},{type:'media',id:'d9daed6d-e54f-4fec-b060-dcd41e308a3e',name:'hero-fab-constraint-v1.png'}]`.
Pass separately observed `entityTrayBindings:[{entityId,thumbnailKey}]` to `assertLiveComposerState(page,expected,bindings)`; thumbnailKey is actual queryless picker image URL matching tray, never invented or copied from count. Save returned sanitized snapshot. Any exception or unknown identity STOPs without submit.

Verify actual Image/Nano Banana Pro/16:9/x1/0credit meter, save screenshot. Close settings/pickers, run imported live guard immediately before clicking unique scoped submit. Save full submitted prose and reference-binding proof. Exactly one submission; on error stop, retain no-charge/failure state, no retry. Preserve native original/hash/dimensions; free same-generation Flow 2K export if available and label Upscaled honestly. No local upscaling, segmentation, or promotion. No paid/video/audio/install/account actions. Never store signed URL query parameters or secrets.

Return raw/export, metadata, attempts, readiness, manifest, contact sheet, scan and reply paths. Generic extraction/dimension flags remain visible, no waivers. Coordinator records child handle immediately and collects actual terminal result. <=200word receipt, no browser dump. Direction proof only, not HG2 or pilot approval.
