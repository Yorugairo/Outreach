# Work order - the living metallic drop: surface modes, damping, the highlight, and the stop-motion craft of a heavy ball (2026-09-14)

**To:** the Gemini research lane (profile `video-researcher`). **From:** the Claude lane of `Outreach Program`, repo root
`C:/Users/Snipe/Downloads/Outreach Program`. Reply shape: `report-landed`. **Skills: use the `/research` skill** - run it as
`/research --deep the physics and the craft of a heavy liquid-metal drop on screen: Rayleigh surface modes, viscous damping, the specular highlight, and how stop-motion sells a heavy ball at 12 fps` (the deep two-pass form: discovery, then full-page primary extraction). Use
`python content/video_engine/scripts/docs_find.py "<term>"` for every claim you check against our docs. A QUESTION, never a design
(RECALL-RECEIPT s3 step 5): the answer is quarantined until the operator approves it, and lands only through a BACKLOG row (R26-119).


## Why

The operator ruled (E88 s5-s7, `docs/portable/OPERATOR-RULINGS.md`) that every melt goes THROUGH a ball, that the ball has real,
metallic mass and density, and that it is *"a more living ball, that is wriggling to contain itself"*. The engine will paint the
drop's boundary as a ring of damped Rayleigh surface modes (l = 2, 3, 4) with a floor amplitude, the area held constant, metal by
default, with a specular highlight sliding on the surface; the roll and the landing are P47's stop-action machinery
(`content/video_engine/scripts/kinetics/stopaction.mjs`). The LAW's shape we hold; the NUMBERS are `[DERIVED]` until you source them.
`docs_find`: 0 hits on the record for surface tension, Rayleigh, specular. Do not re-research what `stopaction.mjs`'s header and
`docs/research/motion/WEIGHT_DENSITY_MASS_RESEARCH_BLUEPRINT.md` already hold (the hit frame, the cast shadow, the receiver, the settle).

## The questions

1. Rayleigh's drop-oscillation modes: the frequency law omega_l^2 = l (l - 1)(l + 2) sigma / (rho R^3) and Lamb's viscous damping
   tau_l = R^2 / ((l - 1)(2l + 1) nu) - quote the primary statements (Rayleigh 1879, Lamb 1932 s355, or a modern review), and give
   sigma, rho, nu for: mercury, gallium/galinstan (a liquid metal at room temperature), water, and an ink or paint - one table.
2. What a REAL liquid-metal drop looks like when it lands, rolls and settles - high-speed footage or papers: which modes dominate
   after an impact, how many oscillations are visible before it rests, whether it ever rests (thermal/ambient excitation) - the
   floor amplitude we keep by ruling wants a physical reason or an honest "stylised".
3. The specular highlight on a liquid-metal drop: where it sits relative to the light, how it moves with the surface modes and
   with rolling, and the one rendering rule (Phong/Blinn or a measured reflectance) that reads as METAL at a glance on a dark board
   - a number for the highlight's size as a share of the radius, sourced.
4. THE CRAFT: how stop-motion and claymation animators sell a heavy metal ball or a mercury-like blob at 12 fps on 2s - the frame
   counts of the landing wobble, the roll's rotation cue (a mark, a highlight, both), the anticipation before a nudge, the settle
   - Williams, Aardman/Laika practice, Priebe, any HyperFrames-style reference with numbers. Rank the cues: which one sells mass
   FIRST in a flat drawing of a ball.
5. A ball that rolls with no slip in drawn animation: the rule animators use for the turn per frame at a given speed, and the
   failure modes (a disc sliding, strobing spokes) with the fix.

## Form (the intake's proof discipline - `docs/content-video-engine/HYPERFRAMES-INTAKE-2026-09-06.md` s1)

Every statement tagged `[source on file | practitioner doctrine | DERIVED | UNVERIFIED]`; every figure with a proof line
`[Metric | value | authority | URL | Verified 2026-09-14]` quoting the page it is on; the numbers in ONE table at the top (question, cue,
number, source, tag); a NOT FOUND block naming the roots you searched; no design proposals - questions answered, sources on disk under
`docs/research/runs/living_drop/`. Tiers: CONFIRMED (primary fetched and quoted) / PLAUSIBLE (secondary) / UNSOURCED-editorial / REJECTED.
