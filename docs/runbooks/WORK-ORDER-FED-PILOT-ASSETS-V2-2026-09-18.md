# Fed pilot Flow v2 — operator account confirmed

Repository: `C:/Users/Snipe/Downloads/Outreach Program`.
Use `/google-flow-production` and the isolated `flow-asset-producer` profile, including its required references. Execute the service-generated frozen order:
`C:/Users/Snipe/Downloads/Outreach Program/content/video_engine/projects/systems-and-blowups/fed-liquidity-pressure/review/claims/mp-fed-liquidity-pilot-worlds-v2/WORK-ORDER.md`.

The operator explicitly said: "flow uses magolliet@gmail.com and is already signed in. you can send gemini the workorder for Flow." This overrides the old account note that stopped v1. Use the existing signed-in magolliet@gmail.com Flow session. Do NOT switch accounts or ask for that same clarification again. Still stop for an actual different account/login/payment requirement. V1 ended with zero generation attempts and is closed; do not resume or modify v1.

Seven still slots only: W2 workshop background/mid/owner, W4 funding room background/counters/two representatives, P2 blank folio. Nano Banana Pro image mode,16:9 worlds, maximum PAID credits ZERO; verify the live meter. At most3 candidates and2 extraction attempts per slot. Each world layer is independently generated, full-canvas registered; do not segment a new composite. Canonical style block is now verbatim, landscape/layer overrides explicit. All evidence text/numbers are added in code later.

Only write under the v2 claim delivery directory: source/raw/objects, exact prompts, per-attempt checkpoints, manifests, contact sheet, validation helper/report, readiness and reply.md. Read the machine claim; do not modify its registry, the frozen order, shared scripts, gates or other claims. No installs, browser/profile resets, driver modifications, paid voice/video, publication or asset promotion.

V1's MCP connection failed on9222/9223. Reconnect through available supported isolated-worker browser/MCP/CDP tools using the actual live endpoint; existing alternative tools are allowed, but do not repeat stale endpoints blindly. Do not expose DOM, secrets or credentials to the parent. If all supported routes fail, retain concise endpoint/error evidence and report the specific dependency; no software repair is authorized here.

The profile's prepare_props.py path is absent. Use existing `delivery_scan.scan_claim_delivery(load_claim('mp-fed-liquidity-pilot-worlds-v2'))` from `content.video_engine.src.services` for independent hash/alpha/dimension checks, via a claim-local helper. It must exit nonzero when counts.fail>0 or conflicts exist; save the raw report. Inspect geometry and alpha yourself too. Do not relax the scanner if full-canvas layers trigger flags; report them. Save originals even when extraction fails.

All outputs stay review_only/render_eligible=false. Worker selection is not HG2 operator approval. Generate a contact sheet with actual images, not a claimed successful sheet of missing slots. Write approvals.json last. Each slot gets a durable checkpoint; do not stop at merely dispatching a child. If a child is asynchronous, return its handle, then collect its final receipt through the supported wait/status mechanism before final completion.

Return standard bridge grammar plus <=250word receipt, actual images/attempts/credit use, child handles if any, and remaining blockers. Write reply.md in the claim directory; run `python content/video_engine/scripts/bridge_check.py --shape review --reply <absolute reply.md>` and preserve any FAIL. Zero files generated is BLOCKED, not done.
