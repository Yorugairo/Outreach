# TRIAGE 2026-09-05 — the corpus against what is built

P45 T2 (`.claude/PRPs/plans/P45-CORPUS-RECONCILIATION.plan.md:121-128`). A judgment pass over
the generated registries. Nothing here is implemented; every row is a recommendation for HG1.

**The rule (operator, 2026-09-05 — plan:27-29).** Everything in the docs came from research
filtered at least twice by models and partly by the operator, never fully checked. If it is not
implemented: a gap we stumbled on recently → **TOP**; otherwise → **BACKLOG** or **EXPLORE**. A
derived number (a computation over sources, like the 0.22) is tested as a starting reference,
never cited as research. "Superseded by" labels came from compression, not from bad docs — lift
the differences, dedupe, never kill.

**The four verdicts, as used below.** **TOP** = next list, addresses a stumble of the last two
weeks. **BACKLOG** = real work, owned by a plan or a row, not now. **EXPLORE** = we cannot answer
it yet; the first step is a measurement or a read, not a build. **RETIRE** = strike from the
triage list only — the item is already built under another name, superseded by a measurement, or
is not a buildable claim. **RETIRE never deletes a doc line** (ruling 7).

**Rulings already made (plan:195), cited per row as R1–R8.** R1 motion-authoring order restored
into 29, gated later · R2 cadence module TOP · R3 the 0.22 measured at three granularities (T8) ·
R4 stick lane in flight → FK/IK + zero-slip TOP · R5 multi-plane inpainting BACKLOG unless it
beats parallax · R6 `[DERIVED]` adopted · R7 compressed docs lifted and deduped · R8 M13 gate TOP.

**Inputs.** `docs/ANIMATION-REGISTRY.md` / `.jsonl` (348 formula records: 191 implemented, 96
tracked, 7 retired, 145 orphaned — `docs/ANIMATION-REGISTRY.md:6`) · `docs/CRAFT-MAP.md` /
`.jsonl` (81 devices, 32 judge-only — `docs/CRAFT-MAP.md:24`) · `docs/GATES-REGISTRY.md` (128
records / 116 ids across 5 checkers — `docs/GATES-REGISTRY.md:19`) · `docs/DOCS-MANIFEST.jsonl` ·
`docs/DOCS-INDEX.jsonl` · `docs/DOCS-TOPICS.jsonl` · `docs/DOCS-CITATIONS.jsonl` ·
`docs/content-video-engine/BACKLOG.md` · `docs/content-video-engine/47-FINDINGS-TO-CHECKS.md` ·
`docs/portable/OPERATOR-RULINGS.md` E35–E41 · `evals/RETRIEVAL-BENCHMARK-2026-09-05.md`.

**Path shorthand used in the cells.** A bare `NN-NAME.md` is under `docs/content-video-engine/`;
`patterns/…` and `briefs/…` likewise. Otherwise:

| shorthand | full path |
|---|---|
| `briefs/…animation-craft.md` | `docs/content-video-engine/briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md` |
| `15-…md` · `16-…md` · `48…` | `docs/content-video-engine/15-LIVING-SCENE-COMMUNICATION-LANGUAGE.md` · `16-EDITORIAL-MOTION-SYSTEM.md` · `48-THE-FIGURE-AND-THE-GROUND.md` |
| `…template.html` | `docs/content-video-engine/samples/scene-evidence-player.template.html` |
| `…/ACADEMIC_LITERATURE…md` | `content/video_engine/sources/reference_analyses/ACADEMIC_LITERATURE_DRAWING_AND_2_5D_ANIMATION_ENGINE.md` (byte-identical copy: `…/complete_research_evidence_bundle/07_academic_literature_drawing_and_2_5d_animation_engine.md`, same line numbers) |
| `…/DRAWING_ENGINE…RESEARCH.md` | `content/video_engine/sources/reference_analyses/DRAWING_ENGINE_ANIMATION_TRANSFORM_RESEARCH.md` (copy: `…/02_drawing_engine_and_transforms_research.md`) |
| `…/UNIFIED_LEDGER…md` | `content/video_engine/sources/reference_analyses/UNIFIED_LEDGER_DRAWING_ENGINE_AND_COMFY_SPEC.md` (copy: `…/06_unified_ledger_drawing_engine_and_comfy_spec.md`) |
| `…/09_…grounding.md` | `content/video_engine/sources/reference_analyses/complete_research_evidence_bundle/09_2d_and_2_5d_body_animation_object_handling_and_grounding.md` (bundle only) |
| `…/08_answers_animation_craft_brief.md` | same bundle directory |

Every `path:line` in this file was resolved against the tree on 2026-09-05: 184 full-path cites
checked for existence and line count, and every shorthand cite read back for content.

**Not this pass.** The full compressed-doc delta list is T4's generated `docs/DOC-OVERLAP.md`
(plan:139-146); the 0.22 measurement is T8 (plan:175-182). §1g lists only the compressed-doc
rules verified by hand today.

---

## 1. The triage table

### 1a. Orphaned in the animation registry — doc-side (10 names / 17 records)

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **O1 on-1s / on-2s / on-3s cadence** | frame cadence chosen by translation speed: >250 px/s on-1s, ≤250 px/s on-2s, background boil on-3s | `docs/content-video-engine/briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md:185-193` \| The On-1s/On-2s/On-3s Decision Rule | orphaned — `docs/ANIMATION-REGISTRY.md:582` | `BACKLOG.md:218` (six ink rounds in motion incl. step motion, 2026-09-05); stepped clocks already ship: `ANIMATION-REGISTRY.md:84` (`SOAK_STEP.FPS 8`), `:231` (`SP.LIFE_FPS 10`) | **TOP** | R2: a kinetics cadence module; we hand-tuned two stepped clocks this week with no rule behind them |
| **O2 dynamic nib pooling** | stroke width from speed, `w(s)=w₀(v/v_mean)^-0.25`, plus a 75° corner-dwell rule | brief:295-298 \| 2. Dynamic Nib Pooling [RECLASSIFIED] | orphaned — `ANIMATION-REGISTRY.md:488` | none | **RETIRE** | width/ink coupling ships with our own dials: `content/video_engine/scripts/kinetics/stroke.mjs:16,39` (`LAMBDA_W 0.12`, `ALPHA_W 1`); the −0.25 exponent is a competing derived number (§3) |
| **O3 the five overlapping synergies** | a synthesis blurb pairing conceptual-metaphor theory with ARAP morphs and charts | brief:120 \| 2.1 The 5 Overlapping Synergies | orphaned — `ANIMATION-REGISTRY.md:492` | none | **RETIRE** | prose, not a formula; the registry matched a `$`-bearing line inside it — nothing to build |
| **O4 analytic mass-spring-damper** | `mẍ+cẋ+kx=0`, ω₀, ζ and the underdamped step response | brief:222-224 \| A3 Spring vs Curve | orphaned — `ANIMATION-REGISTRY.md:553` | none | **RETIRE** | shipped: `content/video_engine/scripts/kinetics/spring.mjs:6-8`, all three regimes (P43 T5, `47-FINDINGS-TO-CHECKS.md:199`) |
| **O5 B5 rhythm as a distribution** | shot lengths as a lognormal, `P(T)=1/(Tσ√2π)·exp(−(lnT−µ)²/2σ²)` | brief:396 \| B5 Rhythm as a Distribution | orphaned — `ANIMATION-REGISTRY.md:564` | none | **RETIRE** | the tail item it implies is deprioritised by ruling — `BACKLOG.md:438-440`, `47-FINDINGS-TO-CHECKS.md:150` |
| **O6 C3 nested coordinate spaces** | `M_world = M_viewport × M_camera × M_ledger × M_chart`, the transform chain | brief:431 \| C3 Nested Coordinate Spaces | orphaned — `ANIMATION-REGISTRY.md:570` | none | **BACKLOG** | A6 already owns it — "expose the chart's coordinate mapper", `BACKLOG.md:289`; 43 §43.2's anchor work is its general form |
| **O7 C6 what Rive / Lottie / Flash got right** | `State(f) = SceneGraph(t = f/fps)` — the frame-addressed, stateless render | brief:456 \| C6 [Tier 1 Architecture] | orphaned — `ANIMATION-REGISTRY.md:572` | none | **RETIRE** | this is what P39 proved and locked: determinism + golden frames, `BACKLOG.md:229` |
| **O8 43.5 the morph — two methods** | cotangent-weight Laplacian; when method A (vertex) vs method B (triangle ARAP) applies | `docs/content-video-engine/43-SCENE-GRAPH-AND-TRANSFORM.md:91` \| 43.5 | orphaned — `ANIMATION-REGISTRY.md:528` | none | **BACKLOG** | same item as tracked T1 below (ARAP); the heading reads orphaned only because the name differs — dedupe into P38 T4 (`BACKLOG.md:247`) |
| **O9 48.2 balance — the gesturing figure's hips** | `XCOM = r_COM + v_COM/ω₀`, `ω₀=√(g/l)` (Hof 2005): static COM is not the balance condition | `docs/content-video-engine/48-THE-FIGURE-AND-THE-GROUND.md:39` \| 48.2 | orphaned — `ANIMATION-REGISTRY.md:529` | stick lane in flight — `OPERATOR-RULINGS.md:1210-1230` (E39) | **BACKLOG** | R4 named FK/IK and zero-slip, not balance; a stick figure that gestures may need it — decision D3 |
| **O10 48.8 harmonisation in our tokens** | Koschmieder contrast drift to the cream, 4–12 px light wrap, washi grain `C=C_vec[1+κ(T−0.5)]`, κ≈0.12–0.16 | `48-THE-FIGURE-AND-THE-GROUND.md:163` \| 48.8 | orphaned — `ANIMATION-REGISTRY.md:530` | none | **BACKLOG** | P42's content, and what gives G-i/G-j a path to green — `BACKLOG.md:235`, `:316` |

### 1b. Orphaned in the animation registry — research-side (39 names / 128 records)

One row per decision; the per-theme detail and counts are §2. All are in
`content/video_engine/sources/reference_analyses/**`, registry section
`docs/ANIMATION-REGISTRY.md:458-605` ("Orphaned (145)", `:606-612` explains the duplicate copies).

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **S1 stroke kinematics** (arc-length reparameterisation, κ₀ regulariser, the physiological mandates) | `v(s)=γ(abs κ + κ₀)^(−1/3)`, `t(s)=∫ds/v`, Newton inversion; centripetal saturation, motor noise, isochrony | `…/ACADEMIC_LITERATURE_DRAWING_AND_2_5D_ANIMATION_ENGINE.md:96` \| 1.2; `:89` \| Physical & Neurological Mandates | orphaned — `ANIMATION-REGISTRY.md:466`, `:594` (20 records) | none | **RETIRE** | implemented verbatim: `kinetics/stroke.mjs:5-7,16,38-39` (P43 T2, `47-FINDINGS-TO-CHECKS.md:198`) — a citation orphan, not a gap |
| **S2 figure kinematics / biomechanics** (2-bone trig IK, FK-vs-IK boundaries, ankle/hip strategies, COM/BOS, respiration) | the source math behind doc 48: law of cosines IK, postural equilibrium, breath | `…/09_2d_and_2_5d_body_animation_object_handling_and_grounding.md:149,182,120,92,252` | orphaned — `ANIMATION-REGISTRY.md:533,504,494,543,549` (19 records) | E39 stick lane — `OPERATOR-RULINGS.md:1210` | **EXPLORE** | the buildable half is already TOP as T4/T5 below; these are its source derivations — read when the rig is built, do not plan them separately |
| **S3 skinning and deformation** (BBW, DQS, LBS volume loss, path morphing, Riemannian geodesic, zero-area proof) | why linear blend skinning collapses, and the three replacements | `…/09_…grounding.md:192,200,208`; `…/ACADEMIC_LITERATURE…md:205,175,179`; `…/DRAWING_ENGINE_ANIMATION_TRANSFORM_RESEARCH.md:155` | orphaned — `ANIMATION-REGISTRY.md:540,559,568,496,596,580,502` (19 records) | none | **BACKLOG** | the live items are P38 T4 (ARAP) and T7 (DQS), `BACKLOG.md:247`, `:249`; the rest is their bibliography |
| **S4 damping regimes** (underdamped / critical / overdamped, step-response derivation) | the closed-form second-order step response in all three regimes | `…/ACADEMIC_LITERATURE…md:242,248,250,239` | orphaned — `ANIMATION-REGISTRY.md:460,486,510,574` (14 records) | none | **RETIRE** | shipped: `kinetics/spring.mjs:6-8` with the seek test (P43 T5) — citation orphan |
| **S5 secondary motion and placement dynamics** | appendages as driven damped oscillators; phase lag φ; follow-through, overlapping action, drag | `…/09_…grounding.md:277` \| 2.8; `:383` \| 3.6 | orphaned — `ANIMATION-REGISTRY.md:507`, `:520` (4 records) | R3 measurement in flight — plan:175-182 | **EXPLORE** | **this is the buried motion-cohesion research T8 was told to look for** (`:283-285` names follow-through / overlapping action / drag); doc 48 §48.6 lifted only the phase lag |
| **S6 paper and ink physics** (anisotropic Darcy flow in washi, elastic paper / spacetime constraints, Koschmieder harmonisation) | wicking anisotropy, sheet mechanics, atmospheric contrast drift | `…/ACADEMIC_LITERATURE…md:146,272`; `…/09_…grounding.md:500` | orphaned — `ANIMATION-REGISTRY.md:557,518,526` (6 records) | K-M ruled off after six rounds — `BACKLOG.md:218`; E22 refusal list | **BACKLOG** | 44.3 wicking is a JUDGE row bounded by E22 (`47-FINDINGS-TO-CHECKS.md:131-133`) and deprioritised (`BACKLOG.md:441-442`); harmonisation rides with O10/P42 |
| **S7 geometry and projection** (transform pipeline, affine matrix, anchor point, matrix concat/dirty flags, Z-stacking, ground projection, Blinn, homography, horizon, disocclusion, re-parenting, virtual parents, contact shadow) | the 2.5D stack: what a card is, where it sits, how it is parented and shadowed | `…/DRAWING_ENGINE_ANIMATION_TRANSFORM_RESEARCH.md:94,101,110,193,200`; `…/09_…grounding.md:453,448,415,472,349,357,441`; `…/UNIFIED_LEDGER…md:68` | orphaned — `ANIMATION-REGISTRY.md:482,600,603,514,516,505,566,576,602,550,561,525,500` (24 records) | none | **BACKLOG** | P42 (grounding) and 43 §43.3 (the Z-stack) own the buildable parts, `BACKLOG.md:235`, `:321`; nothing here is unclaimed as capability |
| **S8 LTX mechanics under the hood** | the 3D VAE compression, `num_frames = 8n+1`, latent mask pinning | `…/UNIFIED_LEDGER_DRAWING_ENGINE_AND_COMFY_SPEC.md:111` \| 4.1 | orphaned — `ANIMATION-REGISTRY.md:521` (4 records) | none | **RETIRE** | the frame law is a shipped gate: `content/video_engine/scripts/gate_comfy_config.py:81-82` (G-m, `47-FINDINGS-TO-CHECKS.md:191`) |
| **S9 word-gap boundary snapping** | snap cuts to Whisper silences of `Δt ≥ 0.45 s`, land inside `[start+0.05, end−0.05]` | `…/DRAWING_ENGINE_ANIMATION_TRANSFORM_RESEARCH.md:400-403` \| Improvement 5 | orphaned — `ANIMATION-REGISTRY.md:578` (2 records) | E38 — `OPERATOR-RULINGS.md:1196-1208`; X3 closed — `BACKLOG.md:392` | **RETIRE** | superseded by our own measurement on the reference: 0.30 s, cut at 0.8 of the gap (46 §46.3) — keeping 0.45 would reinstate the error E38 corrected |

### 1c. Tracked in the animation registry (18 names / 96 records), grouped by the row that tracks them

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **T1 ARAP / polar decomposition** | morph with the rotation factored out: `J=RS`, `det(J(t))>0` at every t; the object→chart transform | `47-FINDINGS-TO-CHECKS.md:55`; `43-SCENE-GRAPH-AND-TRANSFORM.md:91` | tracked (40 records) — `ANIMATION-REGISTRY.md:268,373-412`; rows `BACKLOG.md:247`, `:222`, `:415` | none — no object page in the short (`BACKLOG.md:222`) | **BACKLOG** | P38 T4 owns it; its flag is declared and unread (F1 below) |
| **T2 Euler spiral / clothoid** | `dκ/ds = const` curves for generated geometry; minimise `E_MVS=∫(dκ/ds)²ds` | `42-DRAWING-KINETICS.md:112` \| 42.4 | tracked (16) — `ANIMATION-REGISTRY.md:422-437`; rows `47:144`, `BACKLOG.md:321`, `:401` | none | **BACKLOG** | D1 says build, do not gate; the quality threshold is X7 and has no basis yet (`BACKLOG.md:401`) |
| **T3 zero-slip anchoring (+ the floor-shear paradox)** | a grounded sprite anchors `50% 100%` and binds to floor velocity; `v_contact − v_surface = 0` | `48-THE-FIGURE-AND-THE-GROUND.md:16`; `47-FINDINGS-TO-CHECKS.md:76` \| G-j | tracked (8) — `ANIMATION-REGISTRY.md:440-446`; row `BACKLOG.md:274` | E39 stick lane — `OPERATOR-RULINGS.md:1210-1230`; the gate checks what nothing builds — `BACKLOG.md:304` | **TOP** | R4; G-j exists and can only ever INFO until something binds the ground |
| **T4 FK/IK boundary** | contact resolves IK, free gesture resolves FK; handover blended on the min-jerk quintic | `48-THE-FIGURE-AND-THE-GROUND.md:16,26` \| 48.1 | **registry says retired — corrected here to tracked** (7 records) — `ANIMATION-REGISTRY.md:453-456`, evidence `BACKLOG.md:402` | E39 — `OPERATOR-RULINGS.md:1210`; G-k has no owner — `BACKLOG.md:305` | **TOP** | R4. The "retired" read is a misread: `BACKLOG.md:402` is X10 **CLOSED by doc 48** — a graduation from question to written standard, not a withdrawal |
| **T5 48.3 the rig / DQS / LBS collapse** | joints blend on SE(2) geodesics; LBS returns the zero matrix at 180°, w=0.5 — the candy-wrapper elbow | `48-THE-FIGURE-AND-THE-GROUND.md` \| 48.3 | tracked (2) — `ANIMATION-REGISTRY.md:366-367`; rows `BACKLOG.md:237`, `:420` | E39 stick lane | **BACKLOG** | P38 T7 owns it (`BACKLOG.md:249`); TOP only if the stick lane rigs joints rather than swapping poses — decision D3 |
| **T6 48.5 reach and grasp / prop attachment** | `v_max=1.875·v̄` at τ=0.50, grip aperture peaks at τ≈0.68; attach by cached offset matrix | `48-THE-FIGURE-AND-THE-GROUND.md` \| 48.5 | tracked (2) — `ANIMATION-REGISTRY.md:368-369`; row `BACKLOG.md:237` | none | **BACKLOG** | P38 T8 (`BACKLOG.md:250`); its flag is declared and unread (F3) |
| **T7 48.6 mass before the object moves** | phase lag `φ = arctan(2ζ(ω_d/ω₀)/(1−(ω_d/ω₀)²))` on the `M_p` we already adopted | `48-THE-FIGURE-AND-THE-GROUND.md` \| 48.6 | tracked (2) — `ANIMATION-REGISTRY.md:370-371`; row `BACKLOG.md:420` | R3 in flight — plan:175-182 | **EXPLORE** | this is what replaced the 0.22 (`BACKLOG.md:428`); T8 measures whether the phase lag alone accounts for what we see |
| **T8 48.7 grounding (eye-line half)** | a composited figure's eyes sit on the plate's horizon; shadow softens with distance from the base | `47-FINDINGS-TO-CHECKS.md:75` \| G-i; `48…:163` | tracked (2) — `ANIMATION-REGISTRY.md:372`, `:438`; rows `BACKLOG.md:273`, `:296` | gate with no owner — `BACKLOG.md:303` | **BACKLOG** | P42; the zero-slip half is T3 (TOP), the harmonisation half is O10 |
| **T9 multi-plane geometry / LDIs / inpainting** | planar homography `H=K₂(R+tnᵀ/d)K₁⁻¹`, disparity sampling, SAM 2 → LaMa → Z-stack cards | `45-PARALLAX-AND-PLATE-MOTION.md` \| 45.5; `…/09_…grounding.md` \| 2.4, 4.4 | tracked (8) — `ANIMATION-REGISTRY.md:352-357,362,365`; rows `BACKLOG.md:319`, `:354-360` | E40 — `OPERATOR-RULINGS.md:1245-1253` (stills are the asset; ask first whether our own setup does the motion) | **BACKLOG** | R5: only if it buys capability beyond parallax; B6's ban currently leaves those plates still (`BACKLOG.md:356-360`) |
| **T10 Deegan dark rim (coffee-ring)** | evaporative flux diverges at a pinned perimeter — a 1–2 px darker edge density at the ink boundary | `44-INK-AND-SURFACE.md:27` \| 44.2 | tracked (9) — `ANIMATION-REGISTRY.md:269,413-421`; rows `BACKLOG.md:413`, `:407` | R1, found by the retrieval benchmark 2026-09-05 — `BACKLOG.md:413` | **BACKLOG** | the operator deprioritised the coffee-ring edge on 2026-09-04 (`BACKLOG.md:441-442`) and ruled K-M off the soak (`:218`); R1 is a retrieval finding, not a production stumble — decision D2 |
| **T11 2.6 pseudo-3D head turns** | cylindrical projection of feature points; sprite swap over 2–3 frames at peak angular velocity | `…/09_…grounding.md` \| 2.6 | tracked (3) — `ANIMATION-REGISTRY.md:358-360`; rows `BACKLOG.md:420`, `:430` | none | **BACKLOG** | D8 is deferred with a trigger — the first shot that turns a head (`BACKLOG.md:430`) |
| **T12 3.7 two-handed closed kinematic chains** | both wrists lock to a prop's sockets; the prop is the master | `…/09_…grounding.md:394` \| 3.7 | **registry says retired — corrected here to tracked/deferred** — `ANIMATION-REGISTRY.md:450`, evidence `BACKLOG.md:431` | none | **BACKLOG** | misread: the retire rule fires on the word *closed* inside the title "**Closed** Kinematic Chains"; `BACKLOG.md:431` actually says D9 is *deferred with a trigger* |
| **T13 A6 the secondary-motion budget (0.22)** | `E_secondary = ∫ v_sec² dt ≤ 0.22·E_primary`, lag 2–4 frames, settle within 1.5× primary | brief:302-307 \| A6 [RECLASSIFIED] | retired-as-invented — `ANIMATION-REGISTRY.md:451-452`; `47-FINDINGS-TO-CHECKS.md:146`, `BACKLOG.md:428` | R3, E38 — `OPERATOR-RULINGS.md:1196-1208` | **EXPLORE** | R3: measure per motion piece, per scene, per screen (T8) and print the 0.22 beside each; it is a starting reference and carries `[DERIVED]` (R6) |
| **T14 43.4 dirty flags** | mark a subtree dirty instead of recomputing `M_world` every frame | `43-SCENE-GRAPH-AND-TRANSFORM.md` \| 43.4 | tracked (1) — `ANIMATION-REGISTRY.md:364`; row `47-FINDINGS-TO-CHECKS.md:148` | none | **BACKLOG** | D4: build, low priority, triggered when node counts grow (`BACKLOG.md:429`) |
| **T15 the doc-47 section headings** ("1. DESIGNED OUT", "4. DEMOTED") | two table rows inside doc 47 captured as if they were laws | `47-FINDINGS-TO-CHECKS.md:46,137` | tracked (2) — `ANIMATION-REGISTRY.md:351`, `:361` | none | **RETIRE** | registry artifact: the captured text is the squash row (shipped, P43 T4) and the A1-timing-chart row (superseded by `M_p`, `47:147`) — both already decided |

### 1d. Judge-only craft devices (32), grouped by the CHECK-RESPONSIBILITIES section that owns the by-hand verdict

`docs/CRAFT-MAP.md:24` — 53 devices carry a gate id, 32 carry `judge`. "Owned" means
`docs/content-video-engine/patterns/CHECK-RESPONSIBILITIES.md` names it in §3 (R4, `:62-66`: what
the tools do not reach is the agent's, **by name**).

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **C1 the opening gate's JUDGE rows** — terminal stress (J03), context-dump ban (J04), phonetic anchor (J08) | judgments the gate prints and the agent verdicts with the quoted line | `CHECK-RESPONSIBILITIES.md:126-128`, roster at `:107` | judge-only — `CRAFT-MAP.md:59,81,104`; gate ids `GATES-REGISTRY.md:75`, `:80` | none | **BACKLOG** | already owned and already emitted; no gap — the verdict kind is correct (`CHECK-RESPONSIBILITIES.md:17-41`) |
| **C2 the classical nodes outside the gate** — the grand payoff, the chiastic center, recontextualization, the final triad, best evidence late, the pattern unit | the P3–P6 structure no tool reaches: reversal, self-revelation, the paid promise, the triad | `CHECK-RESPONSIBILITIES.md:130-145` | judge-only — `CRAFT-MAP.md:41,40,39,46,37,91` | none | **BACKLOG** | §3c owns all six by name; T6's craft map now carries them with `gates: judge`, which is the enumeration they lacked |
| **C3 the roster rows the audit does not count** — the callback ledger, the duty roster | tokens planted/touched/recontextualized/closed; the counted roster (rehooks, foreshadows, loops) | `CHECK-RESPONSIBILITIES.md:147-150` | judge-only — `CRAFT-MAP.md:43,55` | none | **BACKLOG** | owned by name; the audit counts what it can and §3d says the rest is the agent's |
| **C4 antecedent integrity** | every sentence opening on a connective or bare pronoun resolves to its predecessor | `patterns/STRENGTH-LOOP.md:84` \| 5. X — cross-scale | judge-only — `CRAFT-MAP.md:109`; enumerated by X1 — `CHECK-RESPONSIBILITIES.md:155-156` | none | **BACKLOG** | the screens file enumerates the candidates (`enumerate_strength_screens.py:19`); only the verdict is human |
| **C5 the L0 reader's five** — cashed concreteness (S4), the ear test (S6), the delete test (S8), the never-list (S10) | four of the ten sentence gates that need a reader, every sentence, in order | `patterns/SENTENCE-STRENGTH-CHECK.md:22` \| The ten gates; `CHECK-RESPONSIBILITIES.md:158-161` | judge-only — `CRAFT-MAP.md:105,107,106,113` | none | **BACKLOG** | owned by name; the log is the deliverable |
| **C6 the thesis lens** | the script argues one of the standing theses so the worldview compounds | `patterns/STRENGTH-LOOP.md:96` \| 6. L6 | judge-only — `CRAFT-MAP.md:65`; owned at `CHECK-RESPONSIBILITIES.md:180` | none | **BACKLOG** | owned (T1–T12 named in §3h) |
| **C7 the fixpoint loop** | a round that fires no gate but made an edit runs again; convergence is a fixpoint | `patterns/STRENGTH-LOOP.md:108` \| 7. The loop protocol | judge-only — `CRAFT-MAP.md:62`; owned at `CHECK-RESPONSIBILITIES.md:196` | none | **BACKLOG** | owned by the runtime sequence, §4 |
| **C8 fourteen judge-only devices with no §3 owner row** — the answer format, peak-end, the dopamine ladder, the assignment, the never-repeat rule, the camera test, the cadence wave, the aside voice, the named-subject rule, the referent test, instrument continuity, mechanisms not prescriptions, the rewrite budget, the enumeration mandate | doctrine devices with no gate and no named verdict row — nobody is told to check them on a run | `CRAFT-MAP.md:35,49,53,48,57,97,103,111,108,110,66,67,114,63` | judge-only — same lines; absent from `CHECK-RESPONSIBILITIES.md:117-182` | R4 is the rule they break — `CHECK-RESPONSIBILITIES.md:62-66` | **BACKLOG** | by the doc's own rule an unreached device must be named; the fix is one additive edit to §3, not fourteen items — decision D1 |

### 1e. M13

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **M13 the cut-gap gate** | cuts land in an acoustic gap ≥ 0.30 s, at 0.8 of the gap, mid-word share ≤ 25 % | `47-FINDINGS-TO-CHECKS.md:202`; `46-REFERENCE-RHYTHM.md` \| 46.3 | **not a gate**: absent from `docs/GATES-REGISTRY.md` (128 records / 116 ids, 5 checkers — `:19`); what exists is `content/video_engine/scripts/measure_cut_gaps.py` and the per-project constant `content/video_engine/projects/systems-and-blowups/tokyo-tea-break/build_short.py:33` (`CUT_AT = 0.8`) | E38 settled the thresholds 2026-09-04 — `OPERATOR-RULINGS.md:1196-1208`; E41 §3 writes M13 into the caption/cut rule 2026-09-05 — `OPERATOR-RULINGS.md:1283-1284` | **TOP** | R8. Caveat for honesty: the gates registry scans 5 script-side checkers only (`GATES-REGISTRY.md:19`), so its silence is evidence M13 is not in the script/motion set — the measurement and one hard-coded constant are the whole implementation |

### 1f. The three declared-never-read flags

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **F1 `arap_morph`** | flag for the 43.5B polar-decomposition morph | `docs/content-video-engine/samples/scene-evidence-player.template.html:446` | declared, no read site — `BACKLOG.md:415` (R1–R6 read, 2026-09-05) | R3 of the benchmark read, 2026-09-05 — `BACKLOG.md:415` | **BACKLOG** | folds into P38 T4 (T1 above); the flag is a promise the code does not keep |
| **F2 `dqs_skinning`** | flag for the 48.3 SE(2) geodesic joint blend | `…template.html:447` | declared, no read site — same shape as F1 | none | **BACKLOG** | folds into P38 T7 (T5 above) |
| **F3 `prop_attach`** | flag for the 48.5 cached-offset prop attachment | `…template.html:448` | declared, no read site — same shape as F1 | none | **BACKLOG** | folds into P38 T8 (T6 above) |
| **F4 the flag-roster test** | make the roster test refuse a declared flag that no site reads | `content/video_engine/tests/test_kinetics_flags.py` | asked for by R3 — `BACKLOG.md:415` | R3, 2026-09-05 | **BACKLOG** | cheap and mechanical, but it gates nothing we shipped wrong — the three flags above are already known |

### 1g. Compressed-doc rules verified by hand (T4 owns the generated full list)

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **X1 the motion-authoring order** | character/prop action → localized environmental action → information reveal → camera action | `15-LIVING-SCENE-COMMUNICATION-LANGUAGE.md:139-149` \| 5. Motion discipline; `16-EDITORIAL-MOTION-SYSTEM.md:48-60` \| 3. Motion ownership | present in both compressed docs, absent from 29 — `rg "authored in this order" docs/` returns only those two files and the index copies (`docs/DOCS-INDEX.jsonl:890,905`) | R1 — plan:195, :198 | **TOP** | R1: restore into 29 now, gate after one measured episode |
| **X2 "a camera move alone is not a visual event"** | the sub-clause of X1: a meaningful beat changes one of the first three layers, not the camera | `15-…md:148-149`; `16-…md:57` | contradicts the shipped gate: `content/video_engine/scripts/gate_motion_density.py:108-111` counts `punch`/`push`/`focus_zoom` as visual events (M01/M10/M16) | R1 restores the order — the clause rides with it | **EXPLORE** | restoring the prose is free; changing `SPECIES_EVENTS` changes every motion verdict we have — measure one episode both ways first (decision D4) |
| **X3 the plate-hold ceiling, 2–6 s** | each primary plate holds two to six seconds, six seconds a hard ceiling | `16-EDITORIAL-MOTION-SYSTEM.md:42` \| 2. Timing authority | conflicts with 29 §9.13 as gated: `gate_motion_density.py:64` `PLATE_HOLD_MAX_S = 20.0` | E21/E24's first-minute rule already carries the 6 s figure — `GATES-REGISTRY.md:121` (M10) | **EXPLORE** | the compressed doc's number survives *inside the first minute* and nowhere else; whether it generalises is a measurement, not a merge — decision D5 |
| **X4 no facts, dates, labels or quotations inside generated pixels** | generated art carries gesture and negative space; the information layer is composited afterward | `15-…md:135-137` \| Fact surfaces | duplicated by doctrine already live: E33 `OPERATOR-RULINGS.md:1102`; partially in `29-EVIDENCE-MOTION-STANDARDS.md:1070`; enforced for parallax by G-b (`47-FINDINGS-TO-CHECKS.md:70`) | none | **RETIRE** | a DUPLICATE in T4's terms — strike in doc 15 with a pointer to E33, never delete (R7) |

### 1h. What else the registries surfaced

| item | what it is (≤120 chars) | where (path \| heading) | status + evidence (registry line) | recent-stumble evidence | rec | reason |
|---|---|---|---|---|---|---|
| **W1 four shipped gate scripts have no registry row** | `gate_comfy_config.py`, `gate_grounding.py`, `gate_vertical_safe_box.py`, `judge_muted_caption.py` are outside the scan | `docs/GATES-REGISTRY.md:19` (5 checkers); the four are named shipped at `47-FINDINGS-TO-CHECKS.md:191-196` | not scanned — `GATES-REGISTRY.jsonl` tools are `gate_opening_structure`, `gate_motion_density`, `audit_script_doctrine`, `lint_script_pattern`, `viewer_score` | the M13 read (1e) needed the caveat spelled out | **BACKLOG** | extend the registry's tool list, or say in its header that it is the script-side set — a reader will otherwise take 116 ids for the whole gate surface |
| **W2 the research bundle is counted twice** | three scanned research files exist byte-identically in `complete_research_evidence_bundle/`, so most research orphans appear twice | `ANIMATION-REGISTRY.md:606-612` (the registry says so itself) | ~half of the 128 research-side records are duplicate paths (§2 counts include both) | none | **BACKLOG** | dedupe by content hash in `build_animation_registry.py` so the orphan count means what it says |
| **W3 89 dial records carry no status** | the `dial` kind is emitted without a status field, so "191/96/7/145" describes formulas + code only | `ANIMATION-REGISTRY.md:6`, `:138-243`; e.g. `CAP_LAST_HOLD_S` in `docs/ANIMATION-REGISTRY.jsonl` has no `status` key | by construction — no status field is emitted for `kind: dial` | none | **RETIRE** | correct as designed — a dial is a value, not a claim; noted so the header's counts are not misread |

---

## 2. The research-side orphans by theme (128 records / 39 names, `ANIMATION-REGISTRY.md:458-605`)

| theme | records | names | rec | the one derivation that matters most, and why |
|---|---|---|---|---|
| **kinematics — stroke** | 20 | 2 | **RETIRE** (row S1) | `…/ACADEMIC_LITERATURE…md:96` §1.2 arc-length reparameterisation with the κ₀ cutoff — it is `kinetics/stroke.mjs:5-7` line for line; the theme is a citation gap, not a capability gap |
| **kinematics — figure / IK** | 19 | 7 | **EXPLORE** (row S2) | `…/09_…grounding.md:149` A. 2-Bone Analytical Trigonometric IK — the law of cosines is the whole solver for a two-bone arm, so the TOP FK/IK item (T4) is an afternoon once the rig exists |
| **skinning and deformation** | 19 | 7 | **BACKLOG** (row S3) | `…/09_…grounding.md:192` A. LBS volume loss — it states the failure our own T7 test asserts (`det(T_blend)=0` at 180°, w=0.5); DQS and BBW are the two candidate fixes |
| **damping — the regimes** | 14 | 4 | **RETIRE** (row S4) | `…/ACADEMIC_LITERATURE…md:239` the step-response derivation — shipped as `kinetics/spring.mjs:6-8`; keeping it open would re-open a closed item |
| **damping — secondary motion** | 4 | 2 | **EXPLORE** (row S5) | `…/09_…grounding.md:277` §2.8 driven damped oscillators — the equation `ÿ+2ζω₀ẏ+ω₀²y = −ẍ_base` **drives secondary motion from the primary's acceleration**, and `:283-285` names follow-through, overlapping action and drag. This is the motion-cohesion research T8 was sent to find; doc 48 §48.6 took only the phase lag out of it |
| **paper and ink physics** | 6 | 3 | **BACKLOG** (row S6) | `…/ACADEMIC_LITERATURE…md:146` anisotropic Darcy flow in washi (Chu & Tai 2005) — the only sourced model of directional wicking we hold; bounded by E22 and by 44.4 (a wick you can name by eye is wrong) |
| **geometry and projection** | 28 | 14 | **BACKLOG** (rows S7, S8) | `…/09_…grounding.md:415` the ground-plane homography invariant (Hartley & Zisserman 2004) — it is the formal statement of what G-i and G-j check, so P42 gets its acceptance test for free |
| **rhythm** | 9 | 3 | **RETIRE** (rows S9, O5; the cadence half is TOP as O1) | `…/DRAWING_ENGINE…RESEARCH.md:400-403` word-gap snapping at 0.45 s — the derivation is right, the number is the one E38's reference measurement overturned (0.30 s). Keep the method, drop the constant |
| **engine architecture** | 7 | 4 | **RETIRE** (rows S8, O3, O6, O7) | `…/UNIFIED_LEDGER…md:111` §4.1 the `8n+1` frame law — already a shipped gate (`gate_comfy_config.py:81`), which is the pattern for the whole theme: architecture claims we either built or refused |

Totals: 20+19+19+14+4+6+28+9+7 = **126**; the remaining 2 records are the source copy of A6's 0.22
(`ANIMATION-REGISTRY.md:451`), triaged as T13. Duplicate-path inflation is real here — see W2.

---

## 3. Derived numbers — computations over sources, not sourced findings

`[DERIVED: from <sources>, <how>]` is adopted (R6). "Sources on file" means a path in
`content/video_engine/sources/**` carries the material the number was computed from.

| number | where (path:line) | what it is derived from | sources on file? | note |
|---|---|---|---|---|
| **0.22 secondary-motion energy ratio** | `briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md:305`; source copy `…/08_answers_animation_craft_brief.md:291` | an extrapolation over Lasseter 1987 / Williams 2001 craft doctrine; the doc itself is headed `[RECLASSIFIED: Design Proposal]` | **not on file** — both are books; the bundle holds no excerpt | already called "an invented number" at `47-FINDINGS-TO-CHECKS.md:146`; R3 measures it (T8) |
| **lag 2–4 frames, settle ≤ 1.5× primary** | `briefs/…animation-craft.md:306` | same paragraph, same sources | **not on file** | the *shape* is craft (47:146); the constants are derived |
| **the A1 timing chart** (2–14 frame anticipations, 1.01–1.18× overshoot, 3–12 frame settles) | `briefs/…animation-craft.md:165-183` | Williams 2001 pp. 35–42 and Lasseter 1987, tabulated by mass class | **not on file** | superseded by `M_p = exp(−πζ/√(1−ζ²))` — `47-FINDINGS-TO-CHECKS.md:147` |
| **on-1s / on-2s / on-3s thresholds (250 px/s, 300 px/s, 100 px/s)** | `briefs/…animation-craft.md:186-193`, and `:545` | Williams/Lasseter cadence practice plus Watson et al. 1986 on judder | **not on file** (Watson 1986 not in the bundle) | **the doc contradicts itself**: `:187` switches at 250 px/s, `:545` says force on-1s above 100 px/s. O1 must test, not adopt |
| **nib-pooling exponent −0.25 and the 75° corner rule** | `briefs/…animation-craft.md:297-298` | an engineering heuristic on top of Viviani's velocity law; the doc says so | **partly** — Viviani & Terzuolo 1982 is the sourced part (`:293`), the exponent is not | our shipped coupling uses different dials (`kinetics/stroke.mjs:16`), declared ours at 42 §42.5 |
| **material spring presets (m, k, c, ζ, ω₀ per material)** | `briefs/…animation-craft.md:226-232` | a table computed to hit chosen overshoot/settle targets | **not on file** | X11 / D2 say these are ours to derive (`BACKLOG.md:403`, `:427`) |
| **ease-equivalence bezier parameters** | `briefs/…animation-craft.md:196-215` | frame-count spacing charts converted to cubic-bezier control points | **not on file** | min-jerk (Flash & Hogan 1985) is the sourced row and is implemented (`kinetics/ease.mjs:5-8`) |
| **J-cut 4–8 frames / L-cut 6–10 frames** | `briefs/…animation-craft.md:364-380` | Pincus & Ascher 2013, converted to frames at 24 fps | **not on file** | never measured on a reference; E38's method applies before use |
| **word-gap threshold 0.45 s** | `…/DRAWING_ENGINE_ANIMATION_TRANSFORM_RESEARCH.md:402` | a proposed silence floor | **not on file** | overturned by measurement: 0.30 s (46 §46.3, `BACKLOG.md:392`) |
| **≥ 59 px stage type / the 0.203 portrait scale** | `50-THE-PHONE-IS-THE-SCREEN.md:36,47` | arithmetic over declared template font sizes and 390/1920 | **n/a — computed from our own template** | 47 §2 says so outright ("arithmetic from declared font sizes, not observation" — `47-FINDINGS-TO-CHECKS.md:68`); X0 is the observation |
| **the 12 px legibility floor** it rests on | `50-THE-PHONE-IS-THE-SCREEN.md:41-48,118` | asserted; no citation in the doc | **not on file** — not found in doc 50, doc 49 or the bundle where I looked | the whole G-o gate hangs on it; X0 tests it by reading a frame |
| **substrate grain κ ≈ 0.12–0.16** | `48-THE-FIGURE-AND-THE-GROUND.md:163` | a chosen modulation depth for washi noise in our tokens | **not on file** | presented as ours ("in our own tokens"); tag and move on |
| **parallax `intensity` ceiling 0.18 (band 0.08–0.15)** | `45-PARALLAX-AND-PLATE-MOTION.md:53` | a recommended band over the ComfyUI parallax research | **partly** — `…/COMFYUI_PARALLAX_TECHNICAL_STANDARDS_RESEARCH.md` is on file; the exact ceiling's derivation is not stated | it is a live FAIL threshold in G-c, so the tag matters more here than anywhere |

Not derived, for contrast: 0.30 s / 0.8 of the gap / ≤ 25 % mid-word (measured on the reference,
E38, `OPERATOR-RULINGS.md:1196-1208`); our own `STROKE`, `SPRING`, `SOAK_STEP` dials (declared ours,
42 §42.5, `47-FINDINGS-TO-CHECKS.md:145`).

---

## 4. Decisions only the operator can make

The residue after R1–R8. Recommendation in brackets.

**D1 — the fourteen judge-only devices with no owner row.** CHECK-RESPONSIBILITIES R4
(`patterns/CHECK-RESPONSIBILITIES.md:62-66`) says everything the tools do not reach is the agent's
*by name*, yet fourteen devices in the craft map carry `judge` and appear nowhere in §3
(`docs/CRAFT-MAP.md:35,48,49,53,57,63,66,67,97,103,108,110,111,114`). Add them to §3 as one
additive block, or accept that the craft map is now the roster and point §3 at it?
**[add the block to §3 — one edit; the craft map is generated and will drift into a second roster]**

**D2 — the Deegan dark rim: R1 or the deprioritisation?** R1 (`BACKLOG.md:413`, found by the
retrieval benchmark 2026-09-05) says build it behind a flag as the cue that separates sumi from a
filled vector shape. The operator deprioritised the coffee-ring edge on 2026-09-04
(`BACKLOG.md:441-442`, "worth nothing on a page that still draws like a plotter") and ruled K-M off
the soak after six rounds (`BACKLOG.md:218`). **[BACKLOG: the ink verdict this week was
"actually paint it with ink" (E40 / plate reveal, `BACKLOG.md:219`), not another filter term]**

**D3 — how far into the figure does the stick lane go?** R4 made FK/IK and zero-slip TOP. Balance
(XCOM, `48-THE-FIGURE-AND-THE-GROUND.md:39`) and joint skinning (48.3, `ANIMATION-REGISTRY.md:366`)
sit next to them: a stick figure that gestures needs hips, and a stick figure with bending elbows
needs DQS. Does the lane animate a rig, or swap approved poses?
**[poses first: E40 says stills are the asset (`OPERATOR-RULINGS.md:1245-1247`); rig only what the
first shot cannot fake — that keeps T5/O9 in BACKLOG]**

**D4 — does "a camera move alone is not a visual event" become code?** The restored rule (X1/X2)
contradicts the shipped counter: `gate_motion_density.py:108-111` credits `punch` / `push` /
`focus_zoom` as visual events, and M01/M10/M16 verdicts are built on that count.
**[restore the prose now (R1), change no threshold until one episode is measured both ways — a
flip here re-verdicts every build we have judged]**

**D5 — the 2–6 s plate-hold ceiling from doc 16.** `16-EDITORIAL-MOTION-SYSTEM.md:42` sets six
seconds as a hard ceiling; the shipped gate holds 20 s (`gate_motion_density.py:64`), and M10
already applies a 4–6 s ceiling inside the first minute (`GATES-REGISTRY.md:121`).
**[lift it as a DELTA into 29 worded as the first-minute rule it already is; do not generalise 6 s
to the whole runtime without a measurement]**

**D6 — where the `[DERIVED]` tag stops.** §3 lists thirteen figures. Three of them are live FAIL
thresholds (G-c's `intensity ≤ 0.18`, G-o's 12 px floor and its ≥ 59 px consequence). Tag only the
research-derived numbers, or tag our own computed thresholds too?
**[tag all thirteen: a threshold computed from our own template is exactly the class E38 warned
about — "our own practice is never the calibration source" (`OPERATOR-RULINGS.md:1203`)]**

**D7 — which cadence threshold does O1 test?** The source contradicts itself: 250 px/s at
`briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md:187`, 100 px/s at `:545`.
**[test 250 px/s against our own stepped clocks (`SOAK_STEP.FPS 8`, `SP.LIFE_FPS 10`) and report
the crossover we actually see; adopt neither number on the page]**

**D8 — the gates registry's scope claim.** It reads five script-side checkers and reports "116
distinct ids" (`GATES-REGISTRY.md:19`), while four shipped gate scripts are outside the scan (W1).
Extend the scan, or state the scope in the header?
**[state the scope now, extend when the comfy/grounding gates next change — a wrong scope claim is
how M13 could have been read as present]**

---

## 5. Counts

| recommendation | rows |
|---|---|
| **TOP** | 5 |
| **BACKLOG** | 31 |
| **EXPLORE** | 6 |
| **RETIRE** | 12 |
| **total triage rows** | **54** |

Counted mechanically over §1a–§1h (one row = one decision; the §2 theme table and the §3 derived
table are views, not extra decisions).

**TOP (5):** O1 the cadence module (R2) · T3 zero-slip (R4) · T4 FK/IK (R4) · X1 the
motion-authoring order (R1) · M13 the cut-gap gate (R8). Every one of the five is a ruling already
made; this pass added no TOP of its own — the newest candidates (T10 Deegan, C8 the unowned
devices, F4 the flag-roster test) are all BACKLOG with a reason.

**EXPLORE (6):** S2 figure kinematics · S5 secondary motion (the T8 read) · T7 the phase lag ·
T13 the 0.22 · X2 the camera-move clause · X3 the plate-hold ceiling.

Registry misreads corrected in the table: **T4** (FK/IK read as retired — `BACKLOG.md:402` is X10
*closed by doc 48*, a graduation), **T12** (two-handed CKC read as retired — the retire keyword
matched the word "closed" inside the section title; `BACKLOG.md:431` says deferred with a trigger),
**S1 / S4 / S8** (arc-length reparameterisation, the damping regimes and the LTX frame law read as
orphaned while shipped in `kinetics/stroke.mjs`, `kinetics/spring.mjs` and `gate_comfy_config.py` —
the modules cite our numbered docs, never the research file, so the source-side heading never
links), **T15** (two doc-47 section headings captured as laws).
