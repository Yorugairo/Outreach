# CAPABILITIES — what is already built

**Check this file before building anything.** Three asset classes were
rediscovered in one session (2026-08-29) — the curtain-era wipe grammar, the
typewriter record document, the live chart draw — each rebuilt or nearly
rebuilt because nothing indexed what existed. The plate library indexes
plates; the choreography ledger indexes motion events; this file indexes
**capabilities** — renderers, engines, gates, prototypes.

Format: what it does · where it lives · state · proof. Update it in the same
commit that adds or retires a capability. A capability that is not in this
file will be rebuilt by someone who doesn't know it exists.

## Rendering & playback

| Capability | Where | State | Proof |
|---|---|---|---|
| **Scene-evidence player** — the review renderer: cross-reveal wipe with carried light, coalesced docks, snap-to-boundary, dock-owned sides, interval wash, finance geometry (864/1056), one-baseline spring pills, text-tier palette | `docs/content-video-engine/samples/scene-evidence-player.template.html` | LIVE — the lane | Steel and Paper build F |
| **Record-document species** — typewriter + per-word highlighter, stroke synced to the NARRATOR's word timings **Use when:** the sentence QUOTES someone's claim and the words themselves are the proof - the record types the line and the highlighter lands on the phrase as it is said | template (`drawRecord`) + prototype `steel-and-paper/evidence/prototypes/record-document-motion.html` | LIVE (Karp wired); leases/macdonald await payloads; PORTRAIT on the Tokyo short 2026-09-10 (the pledge row below the camera rows) | 3:10 in build F |
| **Live-chart species** — the ANIMATED EVIDENCE LAYER: line draw (+area fill revealed with the line), BAR charts (staggered grow, notes land with bars), two-panel small multiples, annotation MARKS that land when the line reaches them, reference hlines (list, per-color), SHARES proportion bars (the fill IS the claim - replaces the banned stat-tile species) — all from `.series.json` sidecars emitted by the chart builders from the same data as the PNGs (PNG = fallback + review artifact) **Use when:** the proof is THEIR chart, or a series re-plotted to be read rather than built as ours (§9.28 B) - it docks as a card and draws on its own clock | template (`drawChart`) + `emit_sidecar` in both evidence builders | LIVE — 10 of 10 charts draw themselves; narration-keyed draw binds post-re-record | railway marks 1:27, capex bars 4:46, tnx panels 2:14 |
| **Narration-keyed chart draw** — DRAW_KEYS bound to word timings ("the crash lands on the words") | prototype `evidence/prototypes/data-document-motion.html` | BUILT, unwired — binds after re-record | prototype replays |
| **Kinetic + quiet captions** — word-punch groups at canonical timings; quiet mode under any docked evidence - **amended E62 (2026-09-11): under a card the caption keeps its stage size and MOVES to the band the page leaves free (`caption_band`); quiet only when no band fits, and quiet's floor on a short is 48 px / 800** **Use when:** always - stage mode when no dock is up (the caption is the motion), the stage size in the free band under a card, quiet only when no band fits | template + `caption-pages.json` from the words sidecar | LIVE | doc 29 Part 5 |
| **Caption STAGE mode** — when no dock is up the caption IS the motion: centred at 40% in the plate's quiet zone (a ledger page's declared zone), 64px/800, each word pops at its own spoken time (scale 1.16→1.0, alternating ±2.5° tilt); a live dock moves it to the band the page leaves free at the same size (E62, 2026-09-11) and demotes it to the anchor via `quiet` only when no band fits. The build stamps `cap_mode` per page and declares `caption_modes`; gate M08 counts stage pages as events and FAILs a declaring build's bare >12s stretches **Use when:** no dock is up and the plate or page holds - the words carry the motion; under a card the same size in the free band (E62), the anchor only when no band fits | template `#caption.stage` + `build_scene_timeline_f.py` (`_dock_live_at`) + `gate_motion_density.py` M08 | LIVE 2026-09-03 (P34 T5) — operator's pick on the side-by-side: STAGE is the default ("awesome") | doc 29 §9.25 "Shipped"; ep1 rebuilt: 110 stage / 354 anchor pages |
| **THE CAPTION MOVES UNDER A CARD (E62), WIRED** (2026-09-11; gate 1's first read: "I don't quite understand why our captions sometimes lose their size/punch" - the anchor caption under the two-fingers card at 0:38 read 33 px = 12 CSS px on a phone; the operator: "A. sounds like the right solution") — the demotion is in POSITION, not in size. The compiler writes `caption_band: {y, h, band}` (stage px) on every dock entry whose window carries a caption: the strip is two lines at the stage size (64 px x 1.12 x 2 = 143 px); the candidates in order are `below` (the free band under the plot, above the source line), `above` (over the plot, across the title - where E45 parks a card), `quiet` (the strip's home); the first that holds the strip CLEAR of the plot's data box and of every card live in that window by a margin of one line wins; none -> `null`. The bands come from `free_bands` / `page_boxes` - the measured fixture when the page is on file, the estimate otherwise (P50 T16) - the same truth every card is placed against. The engine: under a card the caption KEEPS `.stage` (64 px / 800) and takes the TOPMOST live band (`top: <y>px`, a layout move on the dock's enter clock, no transition - a scrubbed render and a play-through agree at the instant; the caption never rides the camera, E59); only a live card with NO band (`null`, and every timeline compiled before this rule) switches it to `.quiet`, whose floor on a short is now 48 px / 800 in the safe box (17 CSS px on a phone; two lines at 48 x 1.24 = 119 px inside the 150 px strip); the landscape quiet size stays 33 px / 600 (its 82 px anchor strip is exactly two lines, and 16:9 sits above the proportional floor). Measured on Tokyo (its pages ESTIMATED - the fixture has no dense-line 9:16 entry for this ink): 4 of the 6 dock windows keep the stage caption in the home strip (y 1297, clear of the card) - at 0:38 the caption under the two-fingers card reads 66 px / 23.7 CSS px, 619 px clear of the card, where gate 1 read 33 px / 12.1; 2 windows fall to the anchor (0:57: the fab card runs into the strip and the band under the plot is 42 px, so the quiet anchor at 48 px / 17.4 CSS px); M25 PASS over 61 instants, the caption no longer the smallest type on the page; the compiled timeline gains the 6 `caption_band` fields and nothing else (stripped, equal to the pre-E62 baseline). Two things to judge: under a card the short's caption is now the full stage caption with the phrase mode's keyword punch (the anchor used to lay the whole page at once); the move between bands is instant on the dock's enter clock, not eased (a wall-clock ease never lands in a captured frame). Goldens: `dock-pair-9x16` regenerated on purpose (two stacked cards leave no band: the caption at the 48 px floor, before 33 px); the rest byte-identical. **Use when:** always on a short with cards - the punch is the size; a card is a reason to move, not to shrink; O8 reads it (a shrink under a card is a FAIL unless no band fits). | `build_scene_timeline_f.py` (`caption_band`, `stamp_caption_bands`, `caption_strip_h`, `caption_home_y`); the engine (`capBand`); the template (`#caption.quiet` 9:16); `tests/test_caption_band.py` | WIRED 2026-09-11 | the Tokyo probes at 38.0 / 57.0 + the 3-up, 2026-09-11 |
| **NO CARD READS OVER A CHART (E63, widened the same evening: drawing OR finished), WIRED** (2026-09-11; the operator, on Tokyo at 0:09.5: "docking over the plate while it's drawing is not a good standard practice ... as a rule we should probably use better handling now that we can manipulate scale/depth/placement easier") — the compiler: for a placed dock whose reading pop (`[enter, enter + read_s]`; the row's `read` box or the card's solo CSS box) meets the page's plot by more than 5 % - drawing or finished (the first cut said "while it draws"; the operator's read of the third form, after the clock re-fit let E45's reading pop back over the FINISHED chart: "you just left the dock over the chart now too. something went backwards" - so the qualifier is gone; the compiled `build_windows` only word the reason) - the READ moves and the word never does: into the free band above the plot (E45's own park), then below, then the foot, at the reading scale if it fits, else the widest that fits, never under the card's parked width, with real air from the plot (`READ_PLOT_PAD` 48 px on an estimated page, 16 on a measured one - the estimate puts Tokyo's plot 24 px below where the player draws it, plus the camera's push); when no band holds even that, the read is DEFERRED: `read_deferred: true` and `centre` on the entry, the card at its parked box from its first frame. The entry records the decision (`read_moved {from, to, why}` or `read_deferred`); the build prints one line. A dock entering after the landing, a dock on a plate, a centred card that never pops: byte-identical. The gate: **M27** reads `layout-probe.json` - a NOT-parked dock's box on the plot by more than 5 % of the smaller is a FAIL naming the dock, the instant, the px, and whether the chart was drawing (the scene's compiled `build_windows`) or finished; inside the plot's box under 5 % a WARN; INFO "not measured" without the probe. The limit found the same evening (no band on Tokyo's real pages held even the floor card, `page_place` returned None) is closed by E65's placer (the plot's empty room by the measured data mask); M27 now scores the INK, so a card in that room passes. Tokyo: the panel card's read covered 72-77 % of itself on the plot at drawn 0.50 (M27 FAIL on the old build); the page's one usable band (208 px above the plot) cannot hold E45's parked card plus air, so the read is deferred - the card enters at its parked box top-right and the line draws unobstructed (M27 PASS, 0 FAIL / 2 WARN / 18 PASS). The cost on that page: no reading pop at all; the big read comes back only if the cut leaves a band (park the chart before the card enters) - the rule never concedes. **Use when:** every ledger row with a dock inside a build window - the chart's arrival is never hidden behind a card. | `build_scene_timeline_f.py` (`read_over_build`, `page_build_windows`, `dock_read_box`, `_read_fit`); `gate_motion_density.py` M27 (`_over_build_gate`); `tests/test_dock_over_build.py` (14) | WIRED 2026-09-11 | the Tokyo 4-up at 9.5-11.0 + M27 before/after, 2026-09-11 |
| **THE MOUNT ON THE PAGE'S OWN CLOCK; THE CAPTION ABOVE EVERY WASH, WIRED** (2026-09-11, BACKLOG R26-50; the operator on Tokyo at 1:01: "the random back dip to black") — a mounting ledger page (`mount=<s>`) no longer fades the world that left over its own `mount_s`: the page that left is behind the opaque incoming world on the scene's first frame (a `cut` cuts; a declared exit keeps E47's clock - dip 0.47 s, dissolve 0.8, blur-zoom 0.27), the cream is the ground at once, the SOAK (E22's signature) opens on the first frame and runs `mount_s`, then ink -> punch -> build at their unchanged offsets (a mount skips the roll and the savor). `#caption` sits at z 50 - above every world, the wash and the plate's foreground cutout - with the dip / blur veils at 60 above it, so a caption never dims with a page and is never hidden behind an occluder (the `occluder-dock` golden had that violation frozen in; regenerated on purpose). Measured on Tokyo: the dead time under the two sentences at 1:01 from ~4.5 s to the soak's 2.43 s; the bars stand at 65.5. Note the 0.47 s dip that remains at 61.76 is `DEFAULT_EXIT_DOCKS` (E47 #3), not the mount. **Use when:** always - every mounting page; the gate's `_page_land_offset` follows the same clock. | the engine (`paintLedger` mount phase; the boundary paint), the template (`#caption` / the veils' z), `tests/test_page_performs.py` (+4, RED then GREEN) | WIRED 2026-09-11 | the Tokyo sheet 61.5-66.5, 2026-09-11 |
| **A CHART BECOMES ANOTHER CHART BY RE-WRITING (E64): the data-keyed recast and the axis hand-over for every recast, WIRED** (2026-09-11, BACKLOG R26-49; the operator on Tokyo at 0:50: "i think the chart needs to either re-write/re-draw itself or morph. right now it basically just cuts a new chart") — the compiler keys a `chart_to {to: "recast"}` on DATA when the bars ARE the line's consecutive changes (over the line's last n+1 data, bar k = line[k+1] - line[k] within 0.5 %, months matching - a rule on the two states' data, not a pair list), DERIVES the key when the author named none (series -> `true`, tags -> `"tags"`, data -> `"data"`; `keyed_derived: true`, one compile line), writes `key_map [{datum, bar, from}]` so the engine never re-derives the correspondence, and names a pair that shares nothing `keyed: null`. The engine, one clock: the line un-draws BY HISTORY to the data the bars need; each keyed datum carries its place to the top of its bar while the bar grows beneath to the verified change; the anchor datum fades as its difference becomes ink; the categories write under the bars; the bar's number takes the landed datum's place - no number drawn twice; and across the clock the AXES HAND OVER: the standing labels un-written glyph by glyph, the arriving ones written, the gridlines lit and sliding by rank to the target's places - for EVERY recast, keyed or plain (the plain recast keeps its un-draw-then-draw and gains the hand-over; a target with no axes keeps the old form). `KEYED_DATA.MIN_S` 1.6 s (the row's dur wins when longer). Pure function of t (a cold seek inside the hand-over equals the warm one; the clock's first frame is R26-48's class). Tokyo at 0:50: the history un-draws with the x axis mid-sweep, the four data fly to their bars as the new axis writes, `Mar Apr May Jun` stands at 51.5, the values at 52.2. **Use when:** a page's chart becomes another chart over the same money - a line into its own changes (data), the lines into their bars (series / tags); a pair that shares nothing is a new row. | `build_scene_timeline_f.py` (`recast_data_key`, `derive_recast_key`, `RECAST_KEYS_ALL`); the engine (`KEYED_DATA`, `AXIS_HAND`); `tests/test_recast_data.py` (14), `test_chart_transitions.py` (+5); goldens `data-to-bars` (new), `tags-to-bars` + `ledger-keyed` (regenerated: the axes now hand over) | WIRED 2026-09-11 | the Tokyo sheet 49.8-52.2 + the close-up at 50.8 / 51.1, 2026-09-11 |
| **VALUE LABELS FIT THEIR SLOT; THE PILL CLEARS ITS NEIGHBOURS, WIRED** (2026-09-11, BACKLOG R26-53; the operator: "why are we now crashing text?" - the Meta page's four values at the 9:16 size touched and the callout's pill covered the third; identical on the approved build, never seen by a gate) — `lpFitValues`: after every bar exists the row measures its widest label in the page's own face (`getComputedTextLength`, a per-glyph metric only when the page is not laid out) and takes the largest size at which it fits the bar pitch with half a figure of air each side (`max(12 px, 0.5 x the figure width)` - a flat gutter is a no-op on the fallback face); ONE size for the whole row; the floor is the tick-label size, below it odd bars' values ride one line further out; nothing is written when everything fits, so every page that fit is byte-identical. `lpPillBand`: a callout pill wider than its slot is measured before it is placed and rises to a band above the values (its bottom a gutter clear of the tallest value) with a dotted leader to the bar's centre (the breakthrough capsule's idiom); it outranks the old negative-bar shove; its place is a constant in t on the callout's own count-and-fade (a travel would cross the labels the law protects); a breaking bar's pill keeps its own law. Tokyo: 59 -> 54.5 px, the gap 24 -> 34 stage px (8.6 -> 12.3 CSS px on a phone), the pill 17.8 / 34 / 48.6 px clear of the three values; M26 unchanged (the same strings, the same heights), determinism ok. **Use when:** every bars page - the row is read at one size and no number sits on another. | the engine (`lpFitValues`, `lpPillBand`); `tests/test_chart_transitions.py` (+5) | WIRED 2026-09-11 - M28 (text-on-text among the page's labels) still to write | the before/after crop at 1:10, 2026-09-11 |
| **THE PLACER ALWAYS FINDS A PLACE (E65): the plot's empty room, the axis band, scale - and the measured fixture by ink, WIRED** (2026-09-11, BACKLOG R26-51 + R26-54; the operator: "inside of the empty data would be good, but it can also land underneath partially over-lapping the axis ... there's no reason we shouldn't have the physics engine to be handling better than this as a base level") — `assets/page-boxes.v1.json` gains a top-level `pages` section keyed by `page_ink_key` (`measure_page_boxes.py --project <dir>` measures every ledger page and state a project's compiled timeline carries; `projects` recorded so `--write` reproduces; the builders' representatives untouched) and, per entry, a 16x16 `data_mask` of the plot (the cells the data's ink touches - bars, series paths sampled along their length, wedges, values, callouts, the treemap's `.lp-cell` tiles) and the `axis` bands (the x tick labels' band, the y tick column); `ledger_page.measured_boxes` reads the ink-keyed entry first. `page_place` never returns None: `outside` (a band outside the plot, byte-identical to E45 s1 when one exists) -> `empty` (every maximal empty rectangle of the mask, scored by the declared quiet side, width and corner-ness; the card at its width or the widest that fits, pushed into the rectangle's outer corner, discounted 10 % when it would cover the tick labels) -> `axis` (underneath, over the x tick labels, never on an ink cell) -> `corner` (the emptiest quadrant at the legibility floor, 120 px tall on 9:16 / 80 on 16:9, and the build prints `[WARN] E65: N card(s) found no room` naming the page); `read_in_room` gives the READ the room that holds the park joined to the axis band; `centred_place` falls through to the same room; `place_room` on every entry. Tokyo, all five pages MEASURED (the estimate had the title two lines where the player writes one and the story plots up to 188 px shorter): the panel card `empty [479, 937, 328, 208]` in the holdings line's empty lower right (the row's `:right`; the upper left was the other room), the line readable at every instant, 0 px on the data; the four authored-centre cards back at their boxes (they had been lost to the solo 800x474 while `page_place` returned None - the frame the operator sent). **Use when:** every card on a ledger page - the page's own room decides, the author's hand is for taste. | `build_scene_timeline_f.py` (`page_place`, `mask_rooms`, `_corner_box`, `read_in_room`, `PLACE_FLOOR_H`), `ledger_page.py` (`measured_pages`), `measure_page_boxes.py` (`--project`, `data_mask`, `axis`), `tests/test_page_boxes.py` (+9), `test_dock_over_build.py`, `test_caption_band.py` | WIRED 2026-09-11 | `build-short-r51/E65-placer-sheet.png`, 2026-09-11 |
| **M28 - text on text among a page's OWN labels; M27 scores the INK, WIRED** (2026-09-11, BACKLOG R26-53 / E65; the operator: "why are we now crashing text?") — the probe's READ_DOM emits every label's box with its role (`val`, `tick`, `sname`, `pill` - the callout's text unioned with its capsule rect -, `bracket` with its sub folded in, `span`, `wedge`, `capsule` - the rail's own HTML pill -, `note` when it is on the plot) as `labels: [{role, text, box}]` on each instant, and writes into `overlaps` every pair of labels that MEET by more than a hairline (2 px each axis) and more than the text box's own leading (0.18 of the shorter height - a `<text>` box is the line box; the June figure's empty descent brushed the series name's ascent with nothing touching); the DATA read gains the treemap's `.lp-cell` tiles (M25 and the mask were blind to a card on a treemap). **M28**: FAIL when two of a page's labels meet, OR two of the VALUE ROW (values, the callout pill) stand under a QUARTER of a figure apart (`LABEL_TOUCH_EM` 0.14 x the page's value size - the eye reads one string; the Meta page's 6 px gap, 2 CSS px on a phone, is the operator's "crashing text"); WARN under half a figure (`LABEL_AIR_EM` 0.28, the fit's own air); PASS with the pair count; INFO without the probe. On the approved Tokyo build: FAIL `val:$604 and pill:$577 at 1:15 (s05), 6 px of air`; on the fitted side build: PASS over 2,460 pairs. **M27 on the ink**: a not-parked card FAILs on `page.data` > 2 % or a line of the page's ink > 8 % (M25's own lines); inside the plot's BOX clear of the ink is the WARN tier ("E65's own placement; listed so the author sees where the read landed") - so a card in the plot's empty room passes. **Use when:** every build - the page's own type never sits on itself, and a card's read is scored against the ink, not the box. | `probe.py` (`labels`, `overlap_wh`, `label_key`, `LABEL_TOUCH_PX`, `LABEL_LEAD_SHARE`); `gate_motion_density.py` (`_label_faults`, `_labels_gate`, `LABEL_TOUCH_EM`, `LABEL_AIR_EM`; `_over_build_faults` on the ink); `tests/test_probe.py` (+2), `test_gate_motion_density.py` (+5), `test_dock_over_build.py` (+1), the ep1 baseline re-pinned (6 INFO) | WIRED 2026-09-11 | the approved build's M28 FAIL line + `build-short-vals` PASS, 2026-09-11 |
| **THE ART-EMBED SURFACE - a press card lands ONTO a painted surface inside a narrative plate, WIRED** (2026-09-11, P50 T7; Bravos has the chart world and the TV world; we have the chart world - the ledger page - and the ART world - our narrative plates; the operator's order approved the same night, the two stills driven through Flow at zero credit, quarantined) — a narrative plate declares a NAMED set of flat surfaces in the sidecar HF-17's foreground layers use, `<plate>.layers.json` -> `{"embed": {"poster": {"quad": [[x,y] x4] (stage fractions, TL TR BR BL), "darken": "<word>"}, "paper": {...}}}`; a dock row names the one it lands on (`{"embed": "poster"}`); a camera move may take the surface as a target (`{"kind": "embed", "name": "poster"}`, E59 reason 4 - the quad's bounding box through `resolveTarget`'s region path). The compiler validates the quad (four corners on the stage, convex, in order, at least 25 % of the stage wide - a card must read on a phone), refuses an unknown name (the message lists the plate's surfaces), refuses a ledger page and a chart card outright (B1: the argument's charts never embed), resolves `darken` to a second against the build's words, and writes `embed: {name, quad, darken, img}` onto the dock entry (`img` the picture's aspect read off the file - the player never waits for a decode). The engine: `kinetics/homography.mjs` (pure: the 3x3 H from the unit square to the four corners with h22 = 1, and the CSS `matrix3d` from it; 9 node tests) - the card's box is the surface's WHOLE rectangle (E66, 2026-09-12 - the operator: "isn't the whole point of the TV to use it as the entire surface?"; the inscribed card of the first cut is retired) and the card REFLOWS to it - every px the card's CSS is written at multiplied by one number, the box's width over the card's design width (`pressTypeScale`), the picture fitted on one axis (`pressPictureFit`), never a stretch (the phrase's size over the box's width agrees within 0.2 % across the four surfaces); a SCREEN surface (`kind: screen` in the layers file; the TVs, the laptop) DISPLAYS the card ("it should read as inside of the tv"): the plate's own pixels inside the quad composited above the card at `sheen` 0.4 in screen blend - dimming with the room to 0.224, never to nothing -, an inner edge falloff of 4.5 % of the surface's width, the paper's drop shadow gone, a 1.04 brightness lift (the cream's red channel caps it; no cool cast - it fights E22's ground); a `paper` surface (the poster, the washi) gets none. The `.dock` element takes the homography, so the masthead, the badges, the underline (riding the phrase's projected bottom edge) and the typewriter live in the projected space; E45's pop-and-park is replaced for an embedded card by the plate's `arrive` onto the surface (a throw lands with its impact; a land fades up in place), the idle stays (E49), the surface is the park; the room darkens outside the quad on `darken`'s word (radial, 0.35 s, min-jerk, a function of t). The poster's corners read from the still by a luminance fit (residual < 1 px): `[[0.3685,0.1941],[0.8307,0.1560],[0.8307,0.6247],[0.3685,0.6106]]`, 0.462 of the frame wide. Golden `art-embed` (the press card on the poster's quad, a still card on a synthetic desk paper with both badges in the projected space; two cold renders identical); every other golden byte-identical. Known limit: the pulled phrase is a RASTER cut from the source screenshot (`press_card.py`), so on a tall poster it takes the full width but cannot re-line larger - the phrase as live type is R26-55; the poster reads at 13.4 CSS px on a phone, the laptop at 9.5 (a record's surface, not a press card's), the TVs at 19-20. **Use when:** a press quote or a record lands in the ART world - on the poster, the TV, the laptop, the paper of an approved plate; never a chart. | `build_scene_timeline_f.py` (the `embed` grammar, +213), the engine (+334), `kinetics/homography.mjs` (154), `tests/kinetics/homography.test.mjs` (9), `tests/test_art_embed.py` (19), golden `art-embed` | WIRED 2026-09-11 - the stills quarantined until the operator approves the frames; then `art-embed-study-poster.layers.json` beside the plate with the measured quads | the golden frame + the crops, 2026-09-11 |
| **LEDGER PAGE species** — the channel signature (E22): a world plate that IS a chart. `world.kind == "ledger"` + `world.page` (a `ledger_page.v1` spec): plain cream page rolls out (a GENERATED washi plate via `page.plate`; CSS cream is the fallback) → half savor → the FIELD (generated inked plate cross-faded via `page.field_plate`; fallback `page.field` = scribble strokes with a nib, or soak) fills the board to a definite rounded edge → the outline draws EXACTLY on that edge → ink writes title/source per glyph (seeded tilt) → the crisp build: story bars landing on the exact value strings with the accent callout, or the dense line (dash-offset, tip head, de-collided inline names). All from `t`, seeded by `lpHash`; identical stage hash from any seek order **Use when:** the proof is OURS (A1-A3: first person, a series we own, a turning beat) - the chart is the world (E61); RANKS, COMPARES, DIVIDES and TURNS live on it | template (`const LP`, `buildLedger`/`paintLedger`; the species block only) + `scripts/ledger_page.py` (series.json → spec; builder by data shape; values verbatim) + shot row `ledger:<series>:<variant>[:<emphasize>[:<quiet_zone>]]` (`world_for_plate`) | LIVE 2026-09-03 (P35 T3/T4) — the field DECIDED: on the cream ground the charcoal fills the generated page to its DECKLE and the line is the deckle's innermost rectangle (`page.field_plate` + `page.board`; `edge_path` optional); then PUNCH IN on the board, the build on the punched page, and the page's declared FOCUS action (callout / spotlight / punch) on the datum at the build's end (E22 addendum 6, E25: the page ends pointing at the proof); refused on sight: blob bleed, ink/outline gap, coffee stains, uneven halo, fibre | proof `build-f/ledger-species-proof.html`; doc 29 §9.26 + E22 addenda; hyperframes candidates `compositions/ledger-page-v1-{A,B,C}.html` |
| **The page VORTEX** — how a ledger page leaves and how it returns. RETRACT: on the scene's last 1.0 + 1.0 s every colour on the page is a particle (each ink glyph, each bar / value / tick / label, each pill, every POINT of the series line) sucked down a whirlpool at the board centre - the centre taken first, radius falling slowly then fast, differential rotation (three turns, the core ~4x the rim), area-preserving stretch along the flow - then the crisp charcoal fades to the stains and the stains go down the same drain, bare cream at the cut. RETURN: `enter=spiral` runs the same map backwards over 1.6 s with NO page wipe (the transition IS the spiral) and no roll/soak/ink/build - a chart that comes back is never drawn like new (E25). `exit=cut` skips the retract for a beat that must land on the last line. Analytic in t (FINDING-the-animation-math s3), idle frames touch nothing (goldens byte-identical) **Use when:** the page's argument is over and the next thing is a different page or a plate - it leaves by the vortex; the same data in another form is E58's verb, never a vortex | template `LP_RETRACT`, `lpVortex`, `lpParticles`, `lpSpiral` (called last in `paintLedger`); shot row `ledger:<series>:<variant>:<emph>:<quiet>[:spiral|:mount=<s>][:cut]` (`parse_ledger_id`; `mount=<s>` = the page MOUNT entry, its own row below); gate M15 no species over a retract, `_page_events` credits the beats | LIVE 2026-09-05 (operator: "now we're cooking with hot grease"; "a way tighter vortex, almost celestial" - three turns) | Tokyo short 0:31-0:33 (out), 0:44.6-0:46.2 (back); doc 29 §9.31 |
| **Kinetics modules + sync** — the template's math lives in `content/video_engine/scripts/kinetics/*.mjs` (ease, spring, stroke, ink, squash) and is INLINED between `/* KINETICS:BEGIN <name> */` … `/* KINETICS:END */` markers by `sync_kinetics.py` (`--write`); `--check` fails on drift, a missing region, an orphan module or a wrong import order; `node --test` reaches the math; the template stays standalone | `scripts/sync_kinetics.py`, `scripts/kinetics/`, `tests/kinetics/*.test.mjs`, `test_kinetics_sync.py` | LIVE 2026-09-05 (P43 T1) | 26 node tests + 8 sync tests; four goldens byte-identical through five template changes |
| **Curvature stroke** — every dash-drawn path is a HAND: the two-thirds law `v = (|k| + k0)^(-1/3)` (42 §42.1) on the min-jerk clock, kappa from the path's own geometry, width/ink coupling in the module; six draw sites (callout ring, squiggle, trace; dock chart lines; ledger decline; combo lines; sketch strokes; dense-line paths), each keeping its own ease as the else branch | `kinetics/stroke.mjs`; template `strokeProf` / `strokeFrac` / `drawOn`; flag `curvature_stroke` | LIVE 2026-09-05, **ON in the Tokyo short** - the operator ruled from a side-by-side clip in motion ('curvature stroke should be on') | `stroke.test.mjs` 7 (the sliding mask's failure is a test); flag goldens `chart-callout@curvature_stroke`, `ledger-page-mid-build@curvature_stroke` |
| **Kubelka-Munk ink** — two-flux layer model per channel (44 §44.1), K/S from the ink's own colour; the soak's stains sum coverage (`mix-blend-mode: plus-lighter`) and a per-channel table maps it through the K-M curve so overlaps DEEPEN and one full stain IS the ink; the highlighter band is a K-M layer over the chart ground | `kinetics/ink.mjs`; template soak filter + `kmHex`; flag `km_ink` | LANDED 2026-09-05; **RULED OFF for the soak** the same day after six rounds in motion (operator: 'our original applications were better, with the ink pooling/blotchiness ... the ideal feeling is that we actually paint it with ink') - carbon hides too fast under K-M for a tonal soak; kept behind the flag for ink over ink. The soak's next step is a plate reveal (BACKLOG) | `ink.test.mjs` 6; rendered probe `test_kinetics_km_probe.py` within 3/255; flag golden `ledger-soak-page@km_ink` |
| **The full spring** — three damping regimes with position, velocity AND acceleration (42 §42.2); the inverse model (Mp -> zeta, settle -> omega) is the one entry point; `springPop` a thin caller, bit-identical to the shipping formula | `kinetics/spring.mjs`; flag `analytic_spring` | LIVE 2026-09-05 (P43 T5), ON in the Tokyo short | the seek test (frame N direct === frames 0..N); peak exactly 1 + Mp; flag golden `ledger-soak-page@analytic_spring` |
| **Area-preserving squash** — `A = R diag(1+a, 1/(1+a)) R^-1`, alpha from speed and deceleration (42 §42.3); the vortex's stretch routes through it (bit-identical), the badge pop stretches along its travel from the spring's velocity | `kinetics/squash.mjs`; flag `area_squash` | LIVE 2026-09-05 (P43 T4), ON in the Tokyo short | `det == 1` across the sweep; flag golden `ledger-soak-page@area_squash` vs its spring-only twin |
| **The Remotion kit outro** — the network-nodes end card ("It's not magic. It's mechanics." / "follow for the next teardown" / `@MoneyPhysicsHQ`; dark `outro-v2`, cream `outro-brand`, YouTube `outro-yt` with "subscribe") and the first reel's yen chart, captions and film frame - a Remotion project rescued 2026-09-05 from a session scratchpad, never committed before | `content/video_engine/remotion-kit/` (source + README); renders gitignored beside the Tokyo clips (`tokyo-tea-break/outro/*.mov`); `build_short.py` DISSOLVES the 6.2 s card in as the last word ends (exit `dissolve`, a plain cross-fade with the outgoing clip playing underneath), stitches the recorded BRAND LINE ("Not a panic. Not a plot. Mechanics." - one ElevenLabs pickup, a channel asset at `channel-assets/money-physics/outro/`, recorded once 2026-09-05) 0.7 s after the last word under the card, pads the audio, declares `life` (compiler `SPECIES_KINDS`, gate `SPECIES_EVENTS`: a self-animating world, verified by eye, credited as continuous); the template clears the last caption page 0.4 s after its end | LIVE 2026-09-05 on the Tokyo short (1:28.3, motion gate PASS) | `t7-outro-tail.png`; GATES-MOTION.md M16 PASS, longest gap 2.4 s |
| **Page MOUNT entry + the short's sound map** — `enter=mount` on a ledger row (`ledger:<series>:<variant>:<emph>:<quiet>:mount[:cut]`): no roll-out and no savor: THE DANCE (operator, 2026-09-05: 'the world fading, beneath it the cream building, and by the time we reach the actual transition time the full cream is built and the chart starts drawing ... begin at went home') - `enter=mount=<s>` carries the mount's length: over it the OUTGOING clip rides above the page and fades in five steps while it keeps playing (he walks away as it fades), the page's cream plate builds beneath it in the other half of each step (`lpDance`) and rises with the steps; at mount_s the cream is full and the page's own clock starts at ROLL+SAVOR (the soak, then the ink, then the chart). On the short: from 'went home' (1:01.76) to the cut at 1:04.19, 2.43 s. A mount is a dissolve on a word, not a cut, so M13's gap rule does not apply. The sound map, second pass: fs-whoosh-2 is -26.9 LUFS (13 dB under the rest - every cue on it was inaudible); the VO is -17.9 LUFS; a roll-in page enters on the page-roll at 0.18, a mount has no sound, a page that will return leaves on a LIGHT FLIP (whoosh-1 0.5), a spiral-returned page leaves on a WHIRL (whoosh-4 0.55 timed to end on the cut), a spiral in is a WHOOSH (whoosh-3 0.7), the suck is the tear (page-roll 0.45 - operator's fallback) **Use when:** the next page must build UNDER the leaving world so the chart draws at the transition ('begin at went home') - a mount, not a cut into the roll-out | compiler `LEDGER_ENTERS`; template `mountIn` / `LP_MOUNT_RISE`; `build_short.py` cue map; `sound/SOUND-PLAN.json` | LIVE 2026-09-05 on the Tokyo short (the meta page at 1:04) | `t8-mount-entry.png`; five cues embedded, motion gate PASS |
| **Beds and the press pack** — the short's music: the ep1 Suno beds reused (the operator's Pro account, commercial), matched to one level and limited flat (blueprint s4b), the hook bed from 0:00 and the turn bed fading in under it at 0:40 to the end (continuity, s5); level by the PLATFORM's rule - YouTube −28 LU under the voice, Facebook −26 (operator, 2026-09-05) - `bed_gain = 10^((VO_I − LU − bed_I)/20)` from measured loudness, 0.0226 on YouTube. The press: four CC0 shutters composed at build time into a seeded PRESS PACK the length of the panel clip (clusters of 2-3 clicks, sample and level varied; A dense, B sparse), one cue at 0:09 at ~9 dB under the voice | `build_short.py` (`BED_LU`, `PLATFORM`, `bed_gain`, `press_pack`), `sound/SOURCES.md` (ledger + provenance), `docs/research/audio/SUBTHRESHOLD_BACKGROUND_MUSIC_RESEARCH_BLUEPRINT.md` | LIVE 2026-09-05 on the Tokyo short (8 cues) | `sound/SOUND-PLAN.json`; the review strip's A/B and gain sliders |
| **PHRASE captions (shorts)** — the strip is READ, not chased (operator, 2026-09-05: 'letting people read the captions on shorts is how we reduce mental friction; right now they're hanging on every word, not just words we want punctuated'): the whole page lands as one phrase at the page's start (a 30 ms stagger, so it reads as a hand laying the line), every word legible at once, the spoken-word flip retired; only a keyword is punctuated - it re-pops and brightens at its own spoken time and stays; and THE BLEND (2026-09-05, after four marker rounds by eye - a per-word pop and a per-word box both pin the eye, a smooth front loses the voice): the phrase lands as a unit with ONE spring pop and a rise, flashing GOLD (operator: 'the highest retention is yellow/gold') and settling to white over 0.3 s; only a KEYWORD moves with the voice - on its exact spoken time it pops a third larger and takes the RED box as punctuation, staying gold on it to the page's end; every other word is still once the phrase has landed; a soft dark shadow under every word carries the contrast (the reference); the next phrase lands with the same rise so the strip never goes still. Painted inline as a pure function of t. Declared by the build (`timeline.caption_style = "phrase"`); absent, the long-form stage behaviour is untouched **Use when:** a short - the strip is read as one phrase, never chased word by word | compiler `CAPTION_STYLE`; template `PHRASE` / `#caption.phrase`; `build_short.py` | LIVE 2026-09-05 on the Tokyo short | `t9-phrase-captions.png`; ten goldens identical |
| **Video dock, WIRED** (2026-09-06) — ruling E44 / backlog R26-7 (operator: "use the chart plate/ledger AND THEN DOCK the animation videos"): a dock asset may be a CLIP, so a generated animation lands on the card over a ledger page instead of a still. `.mp4`/`.webm` (or a `clip:<path>` dock id) embeds RAW like a clip world and the compiler writes `kind: "video"` on the dock entry and the evidence map; the player mounts the POOLED `<video>` inside the card's slide-frame (16:9 frame, `object-fit: cover`, muted/playsinline/no controls) and seeks it on the world clip's OWN code path (`seekVideo`, awaited via `window.__clipsSeeked`) from the dock's clock — clip-local `t − enter`, looping when the card outlives it. Mount/retract choreography and the STAGE→anchor caption demotion are unchanged (s9.25 #2 is the stage POSITION, not the card's stillness). The motion gate credits a live video dock as continuous motion (1 event/s, like a `life` species) for M01/M10/M16; an image dock keeps its enter/exit events **Use when:** a generated clip depicts or proves the sentence over a standing page - it lands as a card, never as the world | `samples/scene-evidence-player.template.html` (`seekVideo`, `paintDockClip`, `dockIsVideo`) + `scripts/build_scene_timeline_f.py` (`is_video_asset`, `dock_uri`, `dock_entry`, `dock_asset_path`) + `scripts/gate_motion_density.py` (`_video_dock_events`) | LIVE (E44) — synthetic ledger scene + 2s docked clip: gate M01/M10/M16 PASS with the dock, FAIL as an image card | tests `test_video_dock.py` (compiler + one-seek + browser: the clip paints inside the card, advances with t, runs on the dock clock and loops, same pixels on two renders), `test_gate_motion_density.py::test_a_video_dock_counts_as_continuous_motion_and_an_image_dock_does_not` |
| **Dock placement on a page, WIRED** (2026-09-06) — ruling E45 §1 (operator: "the dock needs to not cover the middle of the chart, it needs to find less busy space and be smaller"): a dock over a LEDGER PAGE no longer takes the 800px solo card. The compiler reads the page's own geometry (`ledger_page.page_boxes` — title, sub, chart, plot incl. the basis label and the x ticks, source, rail, the mobile safe box and the anchored caption's strip, all in stage px), subtracts the four boxes E45 forbids, and parks the card in the widest free band (`above`/`below`/`foot` or a side column beside the plot), pushed to the declared `quiet_zone` end and parked against the PLOT — width `DOCK_ON_PAGE_W` = 0.48 of the stage (9:16 518px, 16:9 922px), shrunk to what the band holds, floored at 240px. It rides the dock entry as `place: {x, y, w, h}` and the player applies it as inline width/left/top over the `.solo` defaults; the 16:9 slide-frame, the intrinsic-height image case and the mount/retract choreography are untouched, and a dock on a plain plate carries no `place` and compiles byte-identically. The callout and badge species still land: both draw inside boxes the search already forbids (the plot, the rail) **Use when:** any card lands on a ledger page - the compiler finds the room; author `centre` + a box only when the sentence needs the card centred | `scripts/ledger_page.py` (`page_boxes`, `PORTRAIT_LAYOUT`, `SAFE_BOX`, `CAPTION_ANCHOR`) + `scripts/build_scene_timeline_f.py` (`dock_place`, `page_place`, `free_bands`, `dock_card_h`) + `samples/scene-evidence-player.template.html` (`d.place` → the card's width/left/top) | LIVE (E45) — 9:16 golden ledger scene + 2s docked clip: card 518×315 at (346,435), clear of plot/title/source/caption; the golden page's own 2-line sub leaves a 186px band, so its card shrinks to 240×159 rather than covering the chart | tests `test_video_dock.py` (half-width card in the quiet space, purity, the tight-page shrink, the landscape quiet zone, a plain plate keeps the solo card, the player reads `place`, and the frame proof: the clip's own pixels lie inside `place` and none inside the plot) |
| **Dock choreography, WIRED** (2026-09-06) — ruling E45, the choreography (operator: "drawing on the heading, springing the dock, then shrinking it while we slide it to the corner or over the title, so that the graph gains its readability back" / "you're just cutting the docks in instead of using our strong maths/springs"): a dock on a ledger page no longer CUTS into its parked rectangle. Phase A READ — on its word the card springs in at READING size (its own `.solo` geometry, 800px centred in the 9:16 safe box) on the analytic spring's `POP` preset (42 §42.2, Mp = 4%, settle 6), scale 0.85→1, opacity over 0.12s, and holds `read_s` (1.2s [DERIVED: one caption page, doc 51]). Phase B PARK — over `park_s` (0.7s [DERIVED: the roll-out's 0.7]) width, x and y run Flash & Hogan's minimum-jerk quintic on ONE clock, so the shrink and the slide are one move; the card then holds at `place`, live, with the chart fully readable beside it. Retract is the same spring in reverse over 0.35s, never a fade. The move rides the card's LAYOUT box, so the parked rectangle IS `place` to the pixel and every frame resolves from `t` alone (the renderer seeks). The compiler emits `read_s`/`park_s`/`park` beside `place`; a dock whose span is shorter than READ+PARK gets `park: false` and simply stays at reading size. E45 also opens the TITLE block (and the sub) as ground the parked card may take — plot, source line, badge rail and caption anchor stay forbidden — which lifts the 9:16 golden page's parked card from 240px to 406px. Docks on plain plates keep the 0.75s expo-out rise (a lift + scale, not a dissolve) **Use when:** a card must be READ before it parks - the pop at reading size, then the park; `read` / `read_s` / `park_s` when the reading box differs from the slot | `samples/scene-evidence-player.template.html` (`dockReadRect`, `dockGeom`, `DOCK_READ_S`/`DOCK_PARK_S`/`DOCK_POP_S`/`DOCK_RETRACT_S`) + `scripts/build_scene_timeline_f.py` (`DOCK_READ_S`, `DOCK_PARK_S`, `dock_entry`, `free_bands`) | LIVE (E45) — measured in the browser on the 9:16 golden ledger scene: 800px at enter+0.6s, 518×315 at (346,435) — `place` to the pixel — at enter+2.1s, and x/y/w at u = 0.2/0.5/0.8 on the min-jerk curve within 2.2px | tests `test_video_dock.py` (the entry's clock, `park: false` on a short dock, the kinetics are the engine's own, the title band, and the browser proofs: reading size → min-jerk park → `place`, a short dock never parks, two browsers one hash mid-park) |
| **Dip and blur-zoom exits, WIRED** (2026-09-06) — ruling E47 (operator: "we should add both ... we're wrong about the wipe, which we only implemented as a solution because we didn't know how to make sense of wiping the OG scenery plates together"). The two world-change transitions the MEASURED reference actually uses (doc 46 §46.5: 36 hard cuts, 35 dips, 28 blur-zooms, 0 wipes) enter the kit as shot-row exits. **`dip[:<s>]`** — a fade to black and back over `DIP_S` 0.47 s `[DERIVED: the reference, 14 frames at 30 fps, all 35 dips]`, a plain LINEAR ramp (the reference's dip is linear to the frame), half in the outgoing scene and half in the incoming one, the cut inside the black; the veil sits above every layer, so the docks, the STAGE captions and the species ride the ramp down with the world. **`blurzoom[:<s>]`** — over `BLURZOOM_S` 0.27 s `[DERIVED: the median duration_frames of the 28 blur-zooms, 8 frames at 30 fps]` the outgoing world magnifies 1 → `BLURZOOM_SCALE` 1.35 on min-jerk while a `BLURZOOM_BLUR` 18 px backdrop-filter takes the frame’s detail, the switch, then the incoming world rises 1/`BLURZOOM_IN` → 1 out of the softness (1.10, the widest crop a `cover` plate affords — a `cover` plate under 1/1.10 cannot fill the stage, which the reference’s figures-on-cream escape; flagged to the operator). **The mechanical default changes: `docks → dip`, `bare → cut`** (was `docks → wipe_right`); doc 29 §9.16 §3’s authored override stands and `wipe`/`wipe_right` stay reachable BY NAME as effects, the default nowhere. The motion gate credits a dip’s 14 frames and a blur-zoom’s magnification as the boundary EVENT, never stillness (M01/M10/M16). Both are pure functions of `t` with an explicit numeric radius per frame; no build authored either exit, so every golden frame is byte-identical **Use when:** the world changes argument (E61's reset): the dip between ideas, the blur-zoom into the next; the mount when the next page builds under the old world | `samples/scene-evidence-player.template.html` (`DIP_S`/`BLURZOOM_*`, `exitName`/`exitSecs`, `#dipveil`/`#bzveil`) + `scripts/build_scene_timeline_f.py` (`SCENE_EXITS`, `parse_exit`, `scene_exit`, `exit_s`) + `scripts/gate_motion_density.py` (`_transition_events`) | LIVE (E47) — synthetic 9:16 two-plate build through `render_baseline.render_frame`: the dip’s boundary frame is mean L 0.000 against a plate’s 230.0 (0.0 %), monotone 230 → 157 → 79 → 0 → 68 → 136 → 200; the blur-zoom’s sharpness falls S 3.91 → 0.04 between b−0.30 s and b−0.05 s and its band measures 703 → 949 px = 1.350× at b−0.01 s | tests `test_transitions_e47.py` (frame proofs, two-browser determinism, the compiler default, the timed exits, the unknown-exit build error), `test_gate_motion_density.py::test_a_dip_splits_the_still_tail_it_ends_so_m10_passes` (+ the blur-zoom twin) |
| **The idle, WIRED** (2026-09-06) — ruling E49 (operator: "nothing ever goes truly still ... we let things be completely still instead of being at a subtle idle"). Every held thing carries a NAMED subtle idle, a pure function of `t`, behind `kinetics.idle` (ON for every compiled timeline; a build sets `KINETICS["idle"] = False` for stillness; a plate id takes `;idle=<none **Use when:** always (E49) - every held thing names its idle; `none` only on purpose |breath|drift|pulse|figure>` per scene). Kinds in `kinetics/idle.mjs`: **breath** — a scale that inhales ABOVE rest and never below it, `BREATH_AMP` 1.2 % `[DERIVED: HyperFrames /prompting/motion "1-2 %"; measure on ours]` at `BREATH_HZ` 0.25 (doc 48 §48.4); **drift** — a bounded Lissajous walk `DRIFT_PX` 2 px on two rates; **pulse** — a luminance dip `PULSE_AMP` 3 %; **figure** — doc 48 §48.4's asymmetric breath (I:E 1:1.75, a 0.7 s post-expiratory pause; never a sine) with the two-rate sway; **none** — explicit, declared. Class defaults in the template (`IDLE_CLASS`): the page body, an image plate (a ledger page and a clip carry their own motion), the placed dock, each pill at its own phase, the caption strip outside phrase mode (whose boil is its idle). Ken Burns and the parallax push go back to being camera moves, not the stillness cure. The idle is NOT an event for M01/M10/M16; the gate gains **M18 frozen frames** — `measure_frozen_frames.py <build>` renders the player at 12 fps through `render_baseline`, hashes every frame to `frame-hashes.json`, and a run of bit-identical frames over `FROZEN_MAX_S` 0.5 s `[DERIVED: HyperFrames' "the final 1-2 seconds"; halved]` WARNs (INFO until measured — no silent skip). Goldens byte-identical (their sources carry no flag) | `scripts/kinetics/idle.mjs` (synced into `samples/scene-evidence-player.template.html`: `IDLE_CLASS`, `idleOf`, `idleCssFor`) + `scripts/build_scene_timeline_f.py` (`IDLE_KINDS`, `split_idle`, `build_kinetics`) + `scripts/gate_motion_density.py` (`frozen_runs`, `_frozen_gate`, M18) + `scripts/measure_frozen_frames.py` | LIVE (E49) — see P47 T5 evidence | tests `tests/kinetics/idle.test.mjs` (the math), `tests/test_idle_e49.py` (the flag, the compiler option and default, M18 on synthetic hashes, the browser proof: two hold frames identical with the flag off and different with it on, the page's box breathing under 2 %), `test_kinetics_flags.py` (+ `idle`, the `ledger-soak-page@idle` golden) |
| **remotion-ui harvest: seven components as references, triaged** (2026-09-07) — `content/video_engine/remotion-ui/` (pinned `remotion-ui@0.9.0`, installed only; loading one is a Remotion 4 project with the lib aliases, so for us a port re-expresses the maths in the template or a kinetics module, as the HyperFrames harvest did). `HARVEST-2026-09-07.md` per component: the source's props verbatim, our nearest, the gap, the optimal use, the verdict; `docs/content-video-engine/REMOTION-UI-INTAKE-2026-09-07.md` RU-1..RU-7: **badge-stamp priority integration** (a clamped scale spring plus a free trailing rotation spring — the two-spring offset our single-spring landings lack, straight into `stopaction.mjs` `land`), svg-mask-reveal + news-ticker-bar backlog, transition-card-flip explore, spatial-push + map-flight index (spatial-push read against E51: its translate is a seam, its tilt is the untethered flourish E51 calls filler; HyperFrames page-slide already has the move), transition-clock-wipe reject (E47 retired the wipe as a default; the sweep is an uninstalled import) **Use when:** a species is being ported - the source's maths re-expressed in the template or a kinetics module, never loaded as a runtime | `content/video_engine/remotion-ui/HARVEST-2026-09-07.md` + `docs/content-video-engine/REMOTION-UI-INTAKE-2026-09-07.md` | REFERENCE — nothing loaded into a workspace; RU-2 is the next port | — |
| **Chart card: a ledger page rendered once as a dock, WIRED** (2026-09-07) — R26-19 / E50 (operator: "there has to be another one that we can pull in of value"). A fresh ledger page needs ~7.4 s (ROLL + SAVOR + FIELD + PUNCH + BUILD) to land its chart, so a 2–3 s beat can never carry a new page; a CARD can. `chart_card.py` compiles the series object through `ledger_page.py` (the compiler's own spec, never a hand-built page), instantiates it in the player on the golden `ledger-soak-page` skeleton, seeks to the landing (8.6 s), finds the charcoal page's box on the pixels (`page_box`) and writes it at the dock width at device scale 2 — the same painter, fonts and chalk as every page. `build_short.dock_card(aid, series)` renders it into `build-short/docks/` when the object or the renderer is newer and registers it as a dock still; `DOCK_META` species **"chart"**, so M11/M12 read it as the chart it is (E25: it proves one sentence and leaves). First use: `ev-fed-vs-yields-v1` — the HOOK'S OWN PROOF ("The Fed hasn't moved, but your borrowing costs climbed anyway"), never charted until now: `evidence/fetch_fed_vs_yields.py` pulls FRED DFEDTARU / DGS10 / MORTGAGE30US, finds the Fed's last move on the data (2025-12-11) and writes the object with a proof line (URL, sha256, fetch date) per series and every fact read from the CSV — thrown (paper) onto the ring on "The Fed still" and gone on "Tokyo is still" **Use when:** the beat is 2-3 s and needs a chart of ours - a card lands; a fresh page needs ~7.4 s to land its chart | `scripts/chart_card.py` (`page_spec`, `page_box`, `render_card`) + `tokyo-tea-break/build_short.py` (`dock_card`, `DOCK_META` chart) + `tokyo-tea-break/evidence/fetch_fed_vs_yields.py` | LIVE — v3 row 6; the labels are tight at 720 px on a portrait stage (a portrait-typed card is a dial) | tests `tests/test_chart_card.py` (the page box on a synthetic stage; the Fed object's proof lines and measured facts; the rendered card is the page, not the stage) |
| **Build-on: the page performs on a word, WIRED** (2026-09-06) — P47 T2 (operator: "we're being a bit too lazy with the world plates ... perform some transformations on the chart"; SHOT-TABLE-V3-PROPOSAL part B). Four PAGE species on the ledger clock, authored in a shot row's species list, painted on the page's own surfaces (never the overlay), pure functions of `t`, a ledger page only: **`build_to {at, dur, target: datum}`** — the drawn series is CAPPED at the datum: the build beat is spent on the first cap (the line draws to the February peak and stops) and each later `build_to` draws from the previous cap to its own on its word, the same stroke engine, the nib visible (`capFrac`: the path knows its data via `pts`/`si`/`k0`, a highlighted tail is offset by its `k0`); **`bracket {at, dur, from, to, label, sub?, series?, color?}`** — a measured vertical span between two data: it stands to the RIGHT of them (ticks pointing back), the span draws by min-jerk over `BRACKET_DRAW` 0.5 of its dur, the ticks land by the spring, the label writes glyph by glyph in Kalam; when the chart has no room beside (a series ending at the right edge — the holdings page) the label stacks ABOVE the peak, never over the line's history; **`retitle {at, dur, text}`** — the title erases glyph by glyph over `ERASE_S` 0.4 s, then the new one writes per glyph as the build did (a page may retitle more than once; the new glyphs ride the vortex on a retract); **`relight {at, dur, ref: bracket **Use when:** the sentence turns to a DATUM (build_to), SPANS two data (bracket), RENAMES the page (retitle) or RETURNS to a number (relight) - the page performs, no new chart |title, index?}`** — a sunflower twin beneath the bracket rises and falls on a sine over its word. The compiler validates every field by name and refuses a page species on a plain plate; the motion gate credits `at`/`end` of each (a relight is one flash) and lists the build_to HOLDS as **M19 INFO** (the pen resting at a datum until the next word is the hold the sentence asked for, not stillness). Found on the way: an EMPTY caption-page list threw at load (`PG[-1]`) and silently skipped every later definition — fixed. Goldens byte-identical (no golden declares a page species). **E50 (2026-09-07, P47 T6): a chart's deployed life is 6–8 s from its LAST data mark, 12 s at most — then it un-draws or becomes the next thing.** Two more page species: **`undraw {at, dur, target: datum, series?}`** — the drawn series unwinds from where it stands back to the datum (index 0 = to nothing) by the same stroke engine, the nib retracing, the axes standing; no series named = every series; **`figure {at, dur, target: datum, text, sub?, color?, dy?, series?}`** — the number the sentence turns to: a dot pops on the datum, the figure writes beside it glyph by glyph on the side with room (a peak at the right edge writes leftward), chalk unless a colour is named, `dy` in lines of its own size. **M21** reads each ledger page's deployed life from its last data mark (the build's landing, a returning page's entry, every build_to / bracket end) to its exit or its first undraw — over 12 s WARN, over 8 s INFO (a dock's clip may hold it), annotations neither restart nor end it. Tokyo v3 row 2: the line un-draws on "The Treasury's table" and the two treasury figures write from the object's `facts`; row 4 un-draws line AND bracket on "The Treasury prints" (an undraw with no series named takes the standing brackets with it) and writes the June print as the first number. **The third watch (P47 T7):** an un-drawn path is HIDDEN (a zero-length round-capped dash painted a dot at its start) and the figure carries no pin; **`note {at, dur, text}`** — a line of handwriting at the sub's size in the page's quiet zone, stacked, or under the source line and the caption band when the chart leaves no column; **`snap`** page enter (`ledger:…:snap=<dock asset>`) — the page arrives BUILT and grows from the landed card's box to the stage over `SNAP_S` 0.45 s (min-jerk), the outgoing world beneath until covered, the card hidden once the page has it (the box comes from the dock loop's layout offsets, `SNAP_RECT`); a `centre: True` dock option parks the card centred (`centred_place`); **M22 (E51)** names every push with no landing on its scene inside (at − 1.5 s, at + 0.3 s). **The design pass (E52, P47 T8):** `src_style: compact` on an object → the page cites in one short line at the small size (26 px portrait, 60 % ink; opt-in, goldens untouched); a `centre: True` dock sits at its box from its FIRST frame (no reading size, no park), sized by `card_aspect` at the reading width (`CENTRE_W` 0.74), capped at `CENTRE_MAX_H` 0.58 of the stage and centred in the band above the caption strip (`CENTRE_BAND` 0.64); the study of where a solo card should sit is `tokyo-tea-break/DESIGN-PASS-2026-09-07.md` §3. **The fourth watch (P47 T9):** `build_to` and `undraw` are ONE time-ordered sequence per path (each event moves the level from the previous event's target to its own), so a page can un-draw and then RE-DRAW; a `paths` selector (`all` | `tail` | `history`) picks which strokes an event touches, so only the months the sentence is about come back. Inline names push apart by the face's own line height and lift off the axis. The **combo builder** honours SIGN (a drop goes down from a zero line and is blood red - E28), places a bar by its own `x` on the lines' time axis when it carries one, gives the lines their own right axis in `line_unit`, and registers their points so a bracket can measure them - so a bar chart can be laid against lines on one page instead of arriving as a dock. **The macro-chart intake (E53, MC-1/MC-2):** `tiers` on a page object splits the plot into two bands sharing one x - the lines above on their own scale, the bars below on their own zero - because a signed flow and a rate level have no honest shared zero; each band owns its gridlines, and the line band drops its axis for DIRECT TERMINAL LABELS in a reserved right gutter (the detached legend is the sin, the terminal tag the cure). A `centre: True` dock centres in the page's tallest FREE band (`free_bands`), not on the stage, and shrinks to it rather than covering the plot. **The fourth watch corrected the tier reading (E53 §4):** the bars keep the whole plot and their own zero and the line OVERLAYS them on its own scale mapped into the upper share — separating them into bands throws away the comparison; and the ring page ships in ONE unit (both rates, one scale), with the combo kept exercised behind `FED_WITH_BARS=1`. With two drawn series a portrait page names each line at its own end instead of parking both in the lower-right corner. **The fifth watch (E53 §5–§7):** `axes.hlines` on a ledger line page draws a REFERENCE RULE (`{y, label, color}` — dashed, in its colour, named at its right end, inside the scale) because a policy rate is a constant, not a series, and drawn as a line it is a step that reads as a fault; the ring page's window opens on the Fed's last move so its rule never steps. **`spread`** is a new page species — the region between a drawn series and a second series or a rule, bled full of ink on a word (`{at, dur, from, to | to_rule, color}`, `PS.SPREAD_A` 0.30, `PS.SPREAD_BLEED` 0.55): the polygon is rebuilt per frame from the two edges' points so it bleeds left to right at the pen's pace, under every line and label. A series that declares a colour now keeps it — the lone-series sign rule was painting a rising COST green. **`zoom_clip()` / `dock_zoom()`** (`tokyo-tea-break/build_short.py`) crop a region of a clip's frame and scale it back up into a keyframed clip, so a card can be LIVE footage zoomed out of another scene — the tea cup on "its tea break" is the opening shot's own cup, still steaming. Dock options `centre_w` (width as a share of the stage), `centre_band` (which free band) and `centre_y` (the card's centre outright, for where `page_boxes` and the player disagree — R26-27). `callout` gains **`pad`** (a datum resolves to a POINT, so the hand's ring had nothing to inflate — `pad` is how wide it draws around it) and `spread` gains **`from_index`** (the fill begins at a datum, so the red is exactly the distance the page named, not the whole window) | `samples/scene-evidence-player.template.html` (`PS`, `capFrac`, `buildPerform`, `paintBracket`, `paintPerform`, `__lpProbe`) + `scripts/build_scene_timeline_f.py` (`PAGE_SPECIES`, `_validate_page_fields`) + `scripts/gate_motion_density.py` (`SPECIES_EVENTS`, `_build_to_gate` M19) | LIVE — see P47 T2 evidence; Tokyo v3 authored on them (build_short.py rows 2/4/6) | tests `tests/test_page_performs.py` (the grammar, the gate, and browser-measured through `__lpProbe`: the capped line's tip is the datum's x within 2 px and HOLDS until the next word; the bracket's span is exactly the two data's y and stands right of them, its label waits for the span; the retitle erases first-glyph-first before the new title writes; two browsers, one frame) |
| **Stop-action mechanics: throw and land with weight, WIRED** (2026-09-06) — P47 T1 (operator, three times: "operationalize stop-action mechanics to be able to throw things on page or land things with weight"). `kinetics/stopaction.mjs` behind `kinetics.stop_action` (ON for every compiled timeline; the authored `arrive` is the switch): **the cadence rule** `cadence(v, kind)` — on 1s above `ON1_PX_S` 250 px/s or for a camera, on 2s below, on 3s for a background boil `[DERIVED: the brief :185-193; its E2 §7 says 100 px/s — measure on ours]`, and `stepped(t, hold, fps)` quantising on the INTEGER frame index (`frame = round(t·fps)`, `step = floor(frame / hold)`, HyperFrames HF-1) so a renderer seeking at frame/fps never lands a ulp short; **materials** `MASS` — the brief's mass-spring-damper presets (paper / metal / liquid / ink `[DERIVED: :226-232]`), ζ and ω0 from m, k, c, evaluated by `spring.mjs`'s closed form in real seconds; **`throwXf(from, mass, t)`** — a ballistic arc from an offset onto the spot: the chord by min-jerk, the arc `ARC` 0.22 of the chord, a `SPIN_DEG` 9° tumble, the flight stepped by the cadence rule, the landing an IMPACT (the material's spring answers the landing velocity: it sinks `IMPACT_PX` 10 and springs back with its own overshoot) with the area-preserving squash along the travel; **`landXf(mass, t)`** — weight sold BEFORE the drop (doc 48 §48.6: the anticipation lift `ANTIC_PX` 6 over `ANTIC_S` 0.18 s with a clamp `ANTIC_SQUASH`), then the drop easing IN (HF-6: impacts ease in), the impact squash decaying over `IMPACT_S` 0.08 s, the material's settle; **`lag(t)`** — what a thing drags settles `LAG_FRAMES` 1 later (HF-2). Wired: a dock entry `{"arrive": throw **Use when:** a card ARRIVES on a sentence that names a thing - `throw` for paper, `land` for weight; the spring for a plain cut-in |land, "mass": ...}` (the shot row's dock tuple's optional 5th element) flies in from off its side / drops onto its spot, then E45's park choreography is untouched and its shadow reads the clock one frame late; a page's pills by `;arrive=land;mass=metal` on the plate id. Gate: a throw's or a landing's impact is an event; **M20 INFO** names each arrival's speed and hold. NOT moved to the frame-index form (deviation, stated): `ink.mjs` `stepClock` and the template's `plate_life` step — the change would move rendered goldens by up to a frame at a boundary, which is a golden refresh with the operator, not a slice; HF-1 is built where new code steps | `scripts/kinetics/stopaction.mjs` (synced into the template after `squash`) + `samples/scene-evidence-player.template.html` (`arriveOf`, `STOP_THROW_DX/DY`, the dock and pill branches) + `scripts/build_scene_timeline_f.py` (`split_plate_opts`, `dock_opts`, `ARRIVALS`, `MASSES`) + `scripts/gate_motion_density.py` (`_arrivals`, `_arrival_events`, `_cadence_gate` M20) | LIVE — HG2 first read (2026-09-07): "the throw comes on, but it doesn't land with much weight/impact, no shadow coming in" — four mechanisms were missing, not dials, and were built the same morning: the CONTACT SHADOW pinned to the landing spot (`contactShadow`: scale 0.55→1.05, alpha 0.25→0.85 by height, spread by the hit — doc 48's floating-sticker rule, the stop-motion-cadence reference), the hit's 5-step squash ENVELOPE on the stepped clock about the ground contact (`impactSquash` × the material's `impact`: paper 0.7, metal 1.5, liquid 0.5, ink 1.0), the ground's three-frame ANSWER on the world layer (`groundShake`, the headline-slam reference); dials for the ear, the research brief `docs/research/motion/BRIEF-WEIGHT-DENSITY-MASS-2026-09-07.md` for the numbers; Tokyo v3's panel and fingers carry them for the next watch. **Read onto the sourced report the same day** (`WEIGHT_DENSITY_MASS_RESEARCH_BLUEPRINT.md`, repaired): the squash is ONE frame for a deformable thing and NONE for a rigid one, the contact frame uncompressed (`MASS.*.squash_frames`, Williams pp. 93-94/263 on file); the shadow's depth is its BLUR 16 → 0.8 px by height (`contactShadow().blur`, Kersten 1997 on file), alpha the weakest cue; mass is the SURFACE dipping 2–6 px and settling in 4–8 f (`groundDip`, `DIP_PX` 4 × impact) — the stage shake is violence and opt-in (`"violent": true` on the dock); the throw's chord is ballistic (no deceleration into the contact, Whitaker & Halas p. 30 on file) and the thing never sinks (`IMPACT_PX` deleted); restitution `MASS.*.e` as `rebound()` [UNVERIFIED]; the landing SOUND on the contact frame − 1 (`build_short.py` `stop_dials()` → `landing N` cues, Williams pp. 263/309–311 on file) | tests `tests/kinetics/stopaction.test.mjs` (the rule's thresholds, the frame-index clock a ulp either side of a frame, the presets' ζ, a throw that starts at the offset, arcs above the chord, holds its pose inside a frame pair on 2s, lands with an impact and settles to rest; a landing that lifts and clamps before it drops, eases in, and settles by its material — paper overshoots, liquid never; det = 1 in every phase), `tests/test_stop_action.py` (the grammar, the gate, and browser-measured: a thrown dock is well off its spot mid-flight and lands within 1 px of where the spring's pop rests, its impact carries the tensor; a landing pill sits 40+ px above its rest during the anticipation and rests exactly where the pop rests) |
| **Card-then-snap: the chart card is thrown, then SNAPS to the world, WIRED** (2026-09-08) — E50 (the third watch; operator: "use the same throw as we did for the docks in tokyo, then we should snap to the full plate size, not grow it"; "zoom pan frame would work if the zoom is fast with a woosh"). A shot row throws the chart card as a DOCK on the Tokyo stop-action throw (`{"arrive":"throw","mass":"paper","centre":True,"card_aspect":...}` via `chart_dock_card()`), and the page enters with `:snap=<dock>` (`SNAP_S` 0.45, `SNAP_BLUR` velocity-envelope blur, a whoosh cue the build emits). The CARD PAGE: rounded like a phone (36/44 px), no black frame, the cream IS the edge, an offset shadow, frameless dock (`.lp-page.card`, `CARD_SCALE` 1, `card=yes` / `card=no`); it lands centred and fills the frame at rest. Refused on the watch and kept as opt-ins: `enter=throw` with in-flight growth (`throw=<grow>,<from>,<s>`; "it shouldn't grow") and `enter=drop` **Use when:** the hook's own chart must be on screen inside a second and then BECOME the world; the camera arrival is its successor (E59 HG2) | template (`enter=throw`/`drop`/`snap=`; the card page CSS) + `build_scene_timeline_f.py` (`LEDGER_ENTERS` throw/drop, `PLATE_OPTS` card) + `gate_motion_density.py` (snap cards out of M11) + `scripts/chart_card.py`; the tariff short's `build_short.py` rows 1/2 and 5/6 | LIVE 2026-09-08, **approved on the tariff short** ("approved, huge success") | `test_page_performs.py` (throw); goldens byte-identical; the P49 camera plan is the next step (the growth is the camera's, 4-6% perspective in flight) |
| **The light HOLDS: `dur:"hold"` spotlights follow the sentence, WIRED** (2026-09-08) — E25 amended (operator: "it should hold until it has a reason not to"). A spotlight with `dur:"hold"` is resolved by the compiler to the earliest of: the next species on the row, a dock arriving, an explicit `until`, the row's end; the build sets `until` = the first word of the NEXT SENTENCE from the take's punctuation (`next_sentence_start`); a resolved hold under `HOLD_MIN_S` (1.0 s) is dropped with a printed notice (the 0.61 s flash on the black card). `SPOT_R_PORTRAIT` for 9:16 **Use when:** the light must stay on its datum or picture until the sentence has a reason to leave (the next species, a card, the row's end) | template + `build_scene_timeline_f.py` (`dur:"hold"` resolution) + `build_short.py` (`next_sentence_start`) | LIVE 2026-09-08 | the tariff short's five held lights; `test_targeted_species.py` |
| **Trace HOPS + stacked stamps + the UNDER species layer, WIRED** (2026-09-08) — the crossings map beat: a trace species carries `hop {from,to,bow,draw_s,width}` (a bowed arc between two named points, drawn on the curvature stroke) and a callout carries `label_scale` / `pad` so six 25% stamps stack (Detroit up-left x3, Ontario up-right x2, Mexico x1). Two species layers: `#species` above the docks, `#species-under` beneath them — a DATUM target on a page that a card will land on goes under (the circle stays under the thrown card; operator: "the drawn circles should be on the layer beneath the card"). The under layer's viewBox is fitted to the stage at start-up (the portrait "glitch") **Use when:** the sentence NAMES places and flows on a still (a route with hops) or stacks figures at points (stamps) | template (`#species-under`, hop drawing) + `build_scene_timeline_f.py` (hop validation) + `build_short.py` `crossings_species()` | LIVE 2026-09-08 | `test_targeted_species.py` (hop tests); golden `chart-callout` unchanged (a ring on a card stays above) |
| **Idle `live`: nothing truly still, WIRED** (2026-09-08) — E49 (operator: "nothing should ever be truly stable - even our charts need to have some sort of life ... even if it's just 1 pixel shifts"). `;idle=live` on a ledger row = breath + drift (`IDLE_KINDS`); the default `IDLE_CLASS` breath keeps a fixed point at the centre. The frozen-frames gate (M18) passes on the tariff short with no frame held over 0.5 s **Use when:** a chart page holds and the breath alone reads still (a chart at the centre) - live moves every pixel | template (`idle live`) + `build_scene_timeline_f.py` (`IDLE_KINDS`) | LIVE 2026-09-08 | frozen-frames gate; goldens byte-identical |
| **The bed BREATHES + a strip that reads in dB, WIRED** (2026-09-08) — the bed blueprint's rule 3 built: a cue carries `env: [[t, dB], ...]` against its own gain (linear between keys, a pure function of t: `envGain`), and a short's build keys +4 dB (`BED_SWELL_DB`) from 0.3 s before each thrown card through its snap (operator: "it makes the landing of the thrown card sound much better"). A short's bed rests at −20 LU (E54). The review strip's level slider reads in dB AGAINST THE PLAN (centre = the plan, ±12; the number on screen is the number in the plan) — the linear 0-1 slider could not show a sub-threshold bed and its first notch was +8.5 dB **Use when:** a card is thrown - the bed swells +4 dB from 0.3 s before the throw through its snap; the strip is the mixer's read | template (`envGain`, the strip) + `build_scene_timeline_f.py` (carries `env`) + `build_short.py` (`BED_LU`, `BED_SWELL_DB`) | LIVE 2026-09-08 | measured in the served player: 0.0376 at rest (−20.0), 0.0596 through both landings |
| **Re-time a take: dead space, then tempo, the words exact** (2026-09-09) — `retime_take.py <take.mp3> --gaps --tempo 1.06 --out <stem>`: doc 37 §14's caps on a single scene take (intra >0.40 → 0.30, inter >0.65 → 0.50, cuts inside the silences with a 4 ms join ramp), then `atempo` (pitch-preserving); the words file is shifted and scaled exactly (no second Whisper pass), the output duration is verified against the prediction; a build takes it with `build_short.py --take <stem>`. The tariff short: three candidates (tight 79.2 s, x106 78.6 s, fast 74.7 s at 199.5 WPM) for the operator's ear (E55) **Use when:** a take's silences run past doc 37 §14's caps or the cut needs a tempo - the words file is scaled exactly, never re-transcribed | `content/video_engine/scripts/retime_take.py`; `build_short.py --take` | LIVE 2026-09-09 | gates 0 FAIL / 0 WARN on the fast build; the Whisper gate reads the original and the re-timed take the same (its numeral normalization is the backlog) |
| **A centred dock in an AUTHORED box, and a ring inside it** (2026-09-09) — a still dock with `{"centre": True, "card_aspect", "centre_y", "centre_x", "centre_w"}` lands where the page's MEASURED ink leaves room (the compiler's `centred_place` with an authored centre is deterministic), and `centred_card_point()` in the build maps a fraction of the card to a stage point so a callout can ring the card's subject (the wafer on "semiconductors"). `dock_card()` remembers its source and crop (`.src` stamp) so a swapped still (`DOCK_H=trap`) re-cuts the card **Use when:** the card must sit where the page's ink leaves room and the sentence rings a number ON the card's chart | `build_short.py` (`still_card_aspect`, `centred_card_point`, `PLEDGE_*`) | LIVE 2026-09-09 (E55: the pledge dock) | measured in the built player: no page label peeks past the card; M16 satisfied by the ring |
| **A light that breathes + the life check on an addition** (2026-09-09) — E56: a ring has one use - a number or a point on a CHART (the compiler refuses any other callout); a picture's focus is a LIGHT. `idle: <IDLE_KIND>` on a spotlight species breathes the hole's radius and drifts its centre (opt-in; absent = the still light, goldens byte-identical); the compiler validates the name. The life check: over an addition's hold, sample its region every 0.25 s and hash it — no two consecutive samples identical (the scratch measurement on the pledge card + wafer light: 0 identical pairs; M18 longest run 0.17 s). The shimmer (a specular sweep) is the unbuilt alternative (docs_find 0 hits) **Use when:** the focus is a PICTURE (a light, never a ring) and it holds long enough to need life | template (spotlight `idle`) + `build_scene_timeline_f.py` (species `idle` validation) + `build_short.py` (the wafer light) | LIVE 2026-09-09 | 31 tests (goldens, portrait parity, targeted species) green; gates 0 FAIL / 0 WARN / 15 PASS on the tight build |
| **The camera arrival: the eye goes to the landed card, WIRED, opt-in** (2026-09-10, P49 T5) — `enter=camera=<dock>` beside `snap`: the card lands on the previous scene, then over 0.45 s the camera zooms the outgoing world and the card together until the card's box fills the stage (min-jerk; the whoosh on the velocity envelope; the card composed about its own transform-origin), the page waits and shows at the match at identity, the card hides on the same predicate. The card's box: its `place`, else its layout box; the compiler extends the card's exit to the match (`extend_camera_cards`); M14 exempts the arrival against its own card (E51). The attention law (T4): `attention: "landings"` on a row pulls the eye 1.06 toward a dock that arrives, from the contact frame, released before it leaves - LOCKED by default (Bravos measured); HG1 tunes the dials. The tariff short's side build with the arrival on both chart pages is on :8741 (`build-short-p49`) for HG2; the approved build is untouched **Use when:** a thrown card becomes the world (E59 reason 3) - the eye goes to it; the snap is its predecessor | template (`camArr`, the arrival block in render, the dock loop's prefix), `kinetics/camera.mjs` (`camArrivalState`, `camAttentionState`, `ATTN`), `build_scene_timeline_f.py` (`extend_camera_cards`, `CAMERA_ARRIVAL_S`), `gate_motion_density.py` (`_arrival_moves`, `_attention_moves`) | LIVE 2026-09-10, opt-in per row; **WATCHED 2026-09-10 - "8742>8738": the operator read the Tokyo camera cut over the locked cut; HG1 (the dials) and HG2 (the arrival replaces the snap on the ring) CLOSED (E59 amended); the camera cut is Tokyo's default build.** | `tests/test_camera.py` (20), `tests/test_gate_motion_density.py` (66) |
| **The camera: one persistent 2D similarity per timeline, WIRED** (2026-09-10, P49 T1-T3) — `kinetics/camera.mjs`: the state `{zoom, look, at}` (the world point looked at, the screen point it lands on; at == look is a zoom in place) as a pure function of t; the three camera species (punch / focus_zoom / pull_back) run through it with their envelopes factored unchanged (measured pixel-identical, flag off vs on, 11 instants); a shot row's optional 8th element authors keys `{t, zoom, look, at?, ease: cubic / inout / linear / hold}` (look/at as stage fractions or a declared target) - identity when nothing is authored (Bravos, measured: 37 of 45 held compositions still; LOCKED is the default); keys and a camera species never share a row. The probe `window.__camera(t, target)` returns the frustum in world stage px and, for a target, whether it is in frame, how much, at what scale and where it lands - the engine knows what it is seeing before a frame renders. Behind `kinetics.camera` (on for compiled timelines) **Use when:** a landing pulls the eye (E59 reason 1), a stage wider than the frame needs one move (2), the arrival (3), the three species (4); locked otherwise | `scripts/kinetics/camera.mjs`, template (`camNow`, `camCss`, `__camera`), `build_scene_timeline_f.py` (`validate_camera`, `validate_camera_row`, `camera_identity`) | LIVE 2026-09-10; 13 goldens byte-identical **WATCHED 2026-09-10 - "8742>8738": the operator read the Tokyo camera cut over the locked cut; HG1 (the dials) and HG2 (the arrival replaces the snap on the ring) CLOSED (E59 amended); the camera cut is Tokyo's default build.** | `tests/test_camera.py` (14), `tests/kinetics/camera.test.mjs` (6) |
| **The BREAKTHROUGH bars - two mechanics** (2026-09-10; the operator: "show the average bond market return over the last 10 yrs with the average return of soxx as the breakthrough", then "either the breakthrough shows at the bond level (the comparator) then stacks each frame that same amount til it builds to its real number, OR it builds to 1.52% right next to the bonds, then immediately counts/shoots up"; Bravos MEASURED at 8:01.8-8:03.2 of the SPR chart: a "?" track, the bar shoots to the frame's edge WHILE the axis rescales 0-450 -> 0-1500 and every other bar collapses to a stub, an overshoot, a settle, a value capsule counting up on the axis with a dotted leader - the frame is not broken, its scale changes to hold the number) — a bars page STATES its scale (`domain: [lo, hi]`, an axes key at the object's top level) that one value cannot fit; that bar builds to the COMPARATOR's level (the tallest honest bar) with the others, holds `BT_HOLD` 0.5 s, then runs on the page's own build clock (`st.buildDur` grows by the run): `overflow: "burst"` (Bravos, the default) shoots to its true height over `BT_RUN` 0.6 s WHILE the scale rewrites to the nice ceiling above it (8 % -> 40 %; the honest bar shrinks to a sliver; the old ticks slide with the stretching scale and fade, the new ones fade in), overshoots `BT_OVER` 5 % and settles over `BT_SETTLE` 0.3 s, the pill rides the tip and counts to the exact string, the bar glows (`BT_GLOW` 18 px, held at half); `overflow: "stack"` (the operator's A) holds the scale and grows the bar one comparator per `BT_STEP_S` 0.06 s until its number - out of the plot (the top gridline SNAPS as it passes), behind the title, off the page (the page's edge clips it; the pill sits inside its own bar once the tip has left). The scale and the value are printed at every instant (E28/E53). The zigzag cut of the first pass is retired (a broken-axis mark says "abbreviated", the opposite). A page that declares neither is byte-identical (goldens 48/48). Found on the way: the callout pill was a fixed 160 px and a six-glyph value lost its first digit - the pill now fits its number. **Use when:** the sentence TURNS on a number that BREAKS the honest scale ('it beats our bonds') - `overflow: burst` (the rescale) by default; `stack` when the row names it **Amended 2026-09-11 (R26-39, the self-watch's first read):** the build phase after a recast scaled the laid-out rect (the value on the stated scale, taller than the plot) so the bar rose toward its true height and snapped down at the hold; the breakthrough paint now runs through the build too - the bar's rest is the comparator's level from its first frame, the pill counts to the comparator, the stated ticks stay until the shoot. Proven on the Tokyo side build at 57.5 s and by the mid-build test in both mechanics. **Amended 2026-09-11 (P50 T10 + T13):** the furniture, off by default (the cut byte-identical): `overflow_placeholder: "?"` on the object (`placeholder` already means SOURCES-TO-VERIFY, never render - the key is sibling-named) - the breaking bar's track stands grey with a "?" stamp until the hold begins, then the count and the burst; `overflow_capsule: "axis"` - the value capsule mounted ON the axis under the bar's end, counting through the shoot, a dotted leader routed beside the bar (a line through a vertical bar read as a seam), the capsule shrinking with its band on portrait so it never enters the citation (`breakCapsuleFit`, k 0.71 on Tokyo's page = 17 CSS px on a phone); the pill riding the tip stays the default. A dial to rule on the first cut that uses it: for VERTICAL bars the truer Bravos analogue is a capsule on the Y axis at the tip's height with a horizontal leader. And the operator's BLEND (R26-30): `break_cadence: "stop"` - the shoot and the counter step on stopaction.mjs's cadence (on 1s at 24 fps: the tip travels 359 px/s, past the rule's 250; the frame index taken explicitly because the renderer runs 30 fps), the scale jumping per step, the ticks crossing per step, the glow full on the landing step then the continuous burst's half; it lands on the same final frame as the continuous burst (the rendered PNGs hash equal, asserted). New math in `species/breakthrough.mjs` (pure; registers nothing - the burst predates the module rule and calls it thinly). Four proof pages now: burst, stack, furniture, stop. | template `buildLedgerBars` (`btMode`, `comp`, `st.bt`, the second tick set) + `lpPaintBreakthrough` + `LPX.BT_*`; `ledger_page.AXES_KEYS` += `overflow`; the object `tokyo-tea-break/evidence/objects/ev-bonds-vs-chips-10y-v1.series.json` (iShares AGG 1.52 % vs SOXX 36.59 % ten-year average annual total return, NAV - `evidence/sources/ishares-soxx-agg-returns-2026-09-10.txt`); `scripts/proof_breakthrough.py` -> `steel-and-paper/build-f/breakthrough-proof.html` (burst) + `breakthrough-stack-proof.html` (:8739) | BUILT 2026-09-10; **RULED E60 (the operator, same day): "the re-scale is the way" - `burst` is THE breakthrough, `stack` an option a row may name, the BLEND (a stop-motion burst: stop-action steps + the counter + the stepwise rescale) is R26-30**; **IN THE TOKYO CUT (the operator, same day: "It should go into Tokyo 'Beat our bonds'")** - row 4's third chart state (`;then=ev-bonds-vs-chips-10y-v1:bars:1`, `build_s` 1.2 on the object): the parked monthly bars RECAST into the ten-year bars on "to chips," (the title by the hand), the chips bar stands at the bonds' level through "if that works" with the plant landed beneath, and the burst lands on "bonds." (59.05-59.65 s) in the parked slot; the plant's light is OFF (a spotlight darkens the page the burst lives on - measured). Two rules moved with it: a PARK stands until the next park (a later recast no longer un-parks - the new state stands where the old one did; a park to scale 1 un-parks) and the page's SOURCE LINE rides the park with the chart (it was left full-size under the cards - E52). The gate reads the run: a breakthrough state's landing and M21 data mark is the end of its hold + burst + settle (`_breakthrough_run_s`, the dials mirrored); the object's printed as-of date is R26-29 | `tests/test_breakthrough.py` (6: the axes key, the object, the comparator level in both, the burst's rewrite + landing, the stack's steps + the clip); `burst-sheet.png` 6.4-10.0 s, `stack-sheet.png` 7.6-10.0 s, `bravos-bar/burst-sheet.png` 8:01.8-8:03.2 |
| **A dock that READS at one box and PARKS at another** (2026-09-10; the operator: "we should pop the card centered exactly as it is, and move it and resize it to that slot on the right when we pop the next card") — the dock choreography has had its two phases since E44 (PHASE A the pop at reading size, PHASE B the minimum-jerk park to `place`, a LAYOUT move so a seek lands the same frame); a CENTRED card short-circuited them. Now a centred dock may name `read` (its own centre_w / centre_x / centre_y / card_aspect: the box it pops at) and its own clock (`read_s` the hold there, `park_s` the park) - it pops at the reading box, holds, then slides and shrinks to its centred `place`; a centred card may sit on EITHER slot, so two cards stand at once. The RECORD paper is a scalable object: in portrait its type is set in container-query units of the dock's width (`.dock.record { container-type: inline-size }`, 5.1cqw = 32 px at the 626 px band card), so the park resizes the paper without a rewrap - the lines the viewer read stay the lines. **Use when:** a card must be READ centred, then KEPT beside the chart as the next card lands | compiler `DOCK_OPTS` += read / read_s / park_s, `dock_opts` (checked), `centred_place` for the reading box, `dock_entry(read_place=, read_s=, park_s=)`, a centred slot-1 card is placed; template `dockReadRect` (the named box), `dockGeom` (a centred card with a reading box parks), the `.dock.record` cqw rules | LIVE 2026-09-10 on the Tokyo cut's pledge beat (below) | `tests/test_dock_read_park.py` (7); `beats10-sheet.png` 55.9-60.3 s, `beats10-crop.png` 56.6 / 58.6 s |
| **The record dock in PORTRAIT, on a short - the pledge's evidence** (2026-09-10; the operator: "we don't actually have any evidence for 'japan has pledged 10 trillion yen to chips.' we should grab an article quote and do the typewriter highlight") — the record species (row 19) applied on the Tokyo short: `record_dock()` writes the dock META's `record` from the take's word times (`record_words`: the words before the phrase type at 0.1 s, the highlighted phrase's words land on the NARRATOR's own, the tail at 0.07 s); the Nikkei lede types in the band under the parked bars on "pledged", the stroke lands on "at least 10 trillion yen" as the narrator says "ten trillion yen"; on "to chips," the paper PARKS beside the parked chart (top-right, scaled - the dock read->park row above) as the plant lands in the band beneath, and both stand through the burst until "And here's" (the operator's staging, same day). Three things the frames showed: the 9:16 paper needed its own type (34 px blockquote - the 25 px was sized for a 1920 stage), the build must write `evidence-dock.json` AFTER the rows (a record's payload is filled while the rows are built), and the space after the phrase belongs outside the stroke ("yen($65 billion)"). **Use when:** a short QUOTES a claim (the pledge) - the record in cqw type beside the parked chart | `tokyo-tea-break/build_short.py` (`PLEDGE_QUOTE` / `PLEDGE_HL` / `PLEDGE_SYNC`, `record_words`, `record_dock`), template `drawRecord` + the `html[data-aspect="9:16"] .record` CSS; sources `evidence/sources/nikkei-2024-11-12-japan-chips-ai-support.txt` (+ the Reuters wire via SCMP) | LIVE on :8738 (the default build now carries the P48 cut + the record) and on :8742 | `pledge-record2-sheet.png` 56.71 / 57.31 s; gate PASS |
| **The Tokyo CAMERA variant** (2026-09-10; the operator: "let's test out those camera changes") — `TOKYO_CAMERA=1 TOKYO_BUILD_DIR=build-short-cam python build_short.py` builds the same cut with the three moves E59 allows on it: the attention pull toward the row-2 landings (the panel, the fingers) and the fab card on row 4 (`CAM_ROW = {"keys": [], "attention": "landings"}`, the row's 8th element), and the ARRIVAL on the ring (row 6 `camera=dock-h-fed-vs-yields` in place of `snap`). Nothing moves on a chart beat. It is HG1/HG2 material beside the locked cut, never over it. **Use when:** comparing the locked cut against the camera cut on the test bed (`TOKYO_CAMERA=0` rebuilds the locked variant beside it) | `tokyo-tea-break/build_short.py` (`CAMERA`, `CAM_ROW`), `.claude/launch.json` `tokyo-short-player-cam` (:8742; the worktree copy needs absolute paths) | BUILT 2026-09-10, gate PASS (M24 in-frame PASS); awaits the operator's watch against :8738 **THE CUT (2026-09-10, "8742>8738"): `TOKYO_CAMERA` now defaults to 1, so `build-short` (:8738) carries the camera and `build-short-cam` (:8742) is the same build; `TOKYO_CAMERA=0` rebuilds the locked variant for comparison.** | the `cam-*` stills; `GATES-MOTION.md` in `build-short-cam/` |
| **The AUTHORING KIT - one door for both formats, WIRED** (2026-09-11, P51 T0; the operator's grill: "we need to be able to essentially 1-shot shorts, and start being able to produce longs much more effectively") — `content/video_engine/scripts/authoring/`: `Project` (here / build / take / take_stem / script_name / episode_id), `words` (the take's clock: `take_words`, `shifted_words`, `phrase_start`, `at`, `word_in`, `cut_before` on M13's 0.8-of-the-gap / 0.30 s floor, `next_sentence_start`, `write_timeline` with the sentence split, `apply_edit_pauses`), `docks` (`seekable_clip`, `zoom_clip`, `dock_zoom`, `dock_still`, `still_card`, `dock_png`, `chart_card`, `card_aspect`, `centred_card_point`, `record_words`, `record_asset`, `register` / `meta_set`), `audio` (`probe_duration`, `outro_clock`, `stitch_brand_line`, `bed_gain`, `stop_dials` read from stopaction.mjs, `landing_contact`, `page_transitions`, `bed_envelope`), `table` (`write_shot_table`, `print_rows`, `hold_until` - E25's held light, `caption_pages`, `compile_timeline`). THE RULE: nothing in the package names an episode - every fact, crop, quote, file and level stays in the episode's `build_short.py` and arrives as an argument (a hygiene test greps the package). Both shorts thinned to rows + facts + their own cue maps (Tokyo 785 -> 573 lines, the tariff 537 -> 404) and rebuilt BYTE-IDENTICAL through the kit (timeline.json, evidence-dock.json, caption-pages.json, the compiled timeline, SHOT-TABLE-SHORT.py, SOUND-PLAN.json, GATES-MOTION.md - `cmp -s` on every one, both projects). The compiler learns `;use=landing|bridge|reset` on a plate id (`PLATE_USES`, E61; the lint imports it). The F door (`steel-and-paper/build_scene_evidence_cut.py`, ep1's legacy door with its own WINDOWS and a hardcoded worktree path) shares nothing importable without changing its output and is left as it is. **Use when:** authoring any new cut, short or long - the build script is rows, facts and cue maps; the mechanism is the kit's; a helper that would be copied is a kit function. | `scripts/authoring/*.py`; `tests/test_authoring_kit.py` (31); the two `build_short.py` | WIRED 2026-09-11 - the two shorts build through it; the next long's door is the kit itself (R26-17 step 0) | the byte-identical proof on both shorts, 2026-09-11 |
| **EYES: the probe CLI + the layout gate M25, WIRED** (2026-09-11, P51 T2; the doctrine: numbers first - a DOM probe at t is ~150 tokens and answers overlaps, sizes and clearances exactly; a frame is ~2,800 and answers them worse) — `probe.py <build> <t...> --json` reads the built player's own DOM at t (headless, via `render_baseline`): every visible dock (id, state parked / reading / arriving, `rest`), the ledger page's boxes (title / sub / plot / DATA / source / note / rail - `getBoundingClientRect`, so a parked chart is measured as drawn), the caption strip and its text, every text run's size at the PHONE scale (doc 49 §49.1: 34 stage px = 12 CSS px on a 390 px phone; 844 across a landscape stage), the overlaps as named pairs with their area and share, the clearances to the strip and the safe-zone bands (top 12 % / bottom 20 % / sides 5 %), the camera state, the marks summary - 1.1 KB at Tokyo 58.6, 2.2 s. `--sheet out.png --tile 360` grabs the instants as a contact sheet (12 per sheet). `--gate` probes the instants the motion gate already knows (`_landings`, `_deployed_lives`, `_transition_land`, every species onset, transition end and dock contact - 61 on Tokyo, 7.8 s) and writes `<build>/layout-probe.json` keyed to the player's sha. **M25 (the layout gate)** reads that file exactly as M18 reads frame-hashes.json (INFO until measured; INFO when the player changed) and FAILs, naming the two boxes, the instant, the pixels and the share: a SETTLED card over the chart's DATA (> 2 % of the card - the bars, the drawn series, the values, not the plot box: the approved tea cup parks inside the plot's empty corner), the source line / a note / a title line / a label / a pill UNDER a card (> 8 % of that line of ink), a card or paper IN the caption strip while a caption shows (> 1 %), any unparked text under 11 CSS px (the citation exempt by the 2026-09-07 design pass; parked runs listed, not scored); WARN a settled card more than a tenth inside a safe-zone band. What a card FLIES over is choreography and is not scored (the Fed card crosses the page mid-throw in the approved cut). The three defects of 2026-09-10 rebuilt as fixtures each FAIL with the right pair named (the fab card over the bars and the source line under it with the park removed; the record's paper in the strip with its reading box enlarged); the untouched Tokyo build PASSes (smallest unparked type 11.6 CSS px). **Use when:** after every build and before every watch - the self-watch (T3) runs `--gate` then the gate; by hand, `probe.py <build> <t>` answers "what is on screen at t" without a frame. | `scripts/probe.py`; `gate_motion_density.py` M25 (`LAYOUT_*`, `DATA_OVER_SHARE`, `INK_OVER_SHARE`, `CAPTION_TOUCH_SHARE`, `TYPE_FLOOR_CSS`); `tests/test_probe.py` (8), `test_gate_motion_density.py` (+4) | WIRED 2026-09-11 - M25 in the gate; measured on Tokyo | the three fixtures FAIL by name, the approved cut PASSes, 2026-09-11 |
| **THE ONE-SHOT BAR: the self-watch as a build artifact, WIRED** (2026-09-11, P51 T3; the operator: "we need to be able to essentially 1-shot shorts"; the bar: gates green plus a self-watch BEFORE the operator watches) — `self_watch.py <build> --project <dir> --script <stem>` runs LAST in both shorts' builds (`SELF_WATCH=0` skips): the layout probe (`probe.write_gate`, M25's input), the motion gate with M25 measured, the species-by-sentence lint, the viewer's and the script gates' last verdict lines, then the OPENING as contact sheets (0:60 on a short, 3:00 on a long, 2 s steps, 360 px tiles, 12 a sheet -> `<build>/self-watch/opening.N.png`), and writes `<build>/SELF-WATCH.md`: section 1 the mechanical rows with every verdict filled by a tool; section 2 the ten O-rows the AGENT fills by reading the sheets (O1 the package answered on sentence 1, O2 the promise by 0:45, O3 the first chart lit and legible, O4 every sentence-act answered, O5 no dead band, O6 no overlap the probe missed, O7 citations readable, O8 captions as phrases, O9 cards on their word and off at the turn, O10 the chart is the world) - written as TODO, never by the runner; section 3 ONE verdict line: `NOT CLEAN - <the first failing row>` or `TODO - the agent reads the sheets`; CLEAN is the agent's word after the read. A section-1 FAIL exits 1 (the build exits 1 with it; the report is still written). **The operator's copy** (gate 1's first read, 2026-09-11: "I can't really tell by reading how to pass/fail the video, I'd have to see the video and have a better writeup of what I'm looking for"): the runner also writes `<build>/SELF-WATCH.html` - the verdict first, the gates table, then each O-row as ONE plain question a viewer can answer (`PLAIN`: "Does every card arrive on the word that names it and leave when the sentence turns?"), what PASS and FAIL look like on the tiles, the rule, the agent's read, and the frame at every instant the read names (`instants_in`: `t=38`, `t=24-32`, `0:39-0:45`, `(9.1)`, a bare `25.6`; never a size, a share or a duration; 8 a row, spread) - each frame a link to `<player>?t=<s>` so the operator clicks and watches the beat; the sheets inline at the end; images inlined, one sendable file. `--html [--player-url <served player>]` regenerates it from the filled .md with the frames (the runner's first pass has TODO rows and no instants). Measured on Tokyo: 19.7 s for the whole build with the self-watch; the report reads NOT CLEAN - the script gates' last verdict is `FAIL (1 viewer)` (the viewer never re-ran on the final script): the bar's first bite, for human gate 1. **Use when:** every build, before every watch - the operator's watch begins at CLEAN; a build handed over without it is not finished. | `scripts/self_watch.py` (`PLAIN`, `instants_in`, `parse_report`, `write_html`); `tests/test_self_watch.py` (12); both `build_short.py`; the checklist `P51-THE-ANIMATORS-LOOP.plan.md` T3 | WIRED 2026-09-11 - human gate 1 in progress: the first read named the caption under a card (E62) and the report's form (the HTML, shipped the same day) | `tokyo-tea-break/build-short/SELF-WATCH.md` + `SELF-WATCH.html`, 2026-09-11 |
| **SPECIES AS MODULES - the mechanism, WIRED** (2026-09-11, P50 T2 (A); the operator's module rule: "each a module file inlined by the existing kinetics sync, never written into the template body") — `sync_kinetics.py` scans a second dir, `content/video_engine/scripts/species/<kind>.mjs`, into ONE name space with `kinetics/` (a name in both is an error; the same `/* KINETICS:BEGIN <name> */` region grammar; `--check` names a module with no region and a region with no module; the import-order rule holds across both dirs); the template carries ONE hook: a registry `SPECIES_PAINTERS` (`Object.create(null)`) and, at the top of `paintSpecies`' per-species loop, `const painter = SPECIES_PAINTERS[sp.kind]; if (painter) { painter({sp, k, dur, t, sc, si, seed, svg, el, A, resolveTarget, centre, stageBox, ease, io, clamp, drawOn, springPop, idle, hash, STAGE_W, STAGE_H, PORTRAIT}); return; }` - a module's last statement registers its painter; every built-in kind's branch is untouched (the goldens byte-identical). The gate's `SPECIES_EVENTS` accepts a FIELD name as an edge (`("at", "cross_at")`) so a module's beats count without a gate edit per kind. Found and fixed on the way: `inline_text` dropped any line beginning `import` - a block comment's continuation line included - leaving the comment unclosed and the registration swallowed (the species painted nothing, silently); it now tracks block-comment state (`test_a_comment_line_beginning_with_import_is_not_stripped`). **Use when:** any new species from here on - a file under `scripts/species/`, a node test under `tests/kinetics/`, a golden surface, a `when` line; the template changes only for the species' CSS. | `sync_kinetics.py`; the template's `SPECIES_PAINTERS` hook; `tests/test_kinetics_sync.py` (13) | WIRED 2026-09-11 - `sync_kinetics --check`: 11 modules (10 kinetics, 1 species) | the chip (next row) is the first through it |
| **The icon CHIP - a named thing as one of a set, crossed out on a later word, WIRED** (2026-09-11, P50 T2 (B); Bravos's icon board, shots 26-28: the predictions crossed out one by one) — `scripts/species/chip.mjs`: `{"kind": "chip", "at", "dur", "target": point|region, "icon": <name>, "label", "cross_at"?, "state": on|crossed?, "idle"?}` - a rounded dark square with one glyph from `content/video_engine/assets/icons/<name>.svg` and the label beneath, landing on its word with the badge spring (springPop, the POP preset), holding on its idle (opt-in, phased per chip: one 30 fps step differs by 18,875 bytes on the held board), CROSSED on `cross_at` by a two-stroke X on the curvature stroke, the chip dimming to 0.55 after; `state: crossed` lands it already struck. Pure function of t. The icons are SOURCED (A2a): Lucide v1.45.0, ISC, five files fetched at the pinned tag with per-file URLs in `assets/icons/SOURCES.md` and the upstream LICENSE beside them - nothing generated. The compiler validates the kind (a target, an icon that exists on disk, a label, `cross_at` > `at`, the state, the idle by name) and parses the SVG's GEOMETRY only (path / circle / rect / line / polyline / polygon / ellipse and their attributes) into `uris["icon:<name>"]`; the painter builds each node through `lpEl` - no markup the player has to trust. The gate counts the landing and the cross as events (M24 knows it points). Golden `chip-board` (three chips on a bare plate, the middle one crossed). **Use when:** the sentence names a THING as one of a set - a prediction, an actor, a plant (EXPLAINS, row 6) - and RETRACTS one on a later word (row 10): the board, then the strike. The cross is the squiggle's sunflower today; a blood-red strike for a retracted claim is a dial to rule on the first cut that uses it. | `scripts/species/chip.mjs`; `tests/kinetics/chip.test.mjs` (node); `build_scene_timeline_f.py` (`chip` in SPECIES_KINDS / SPECIES_WHEN / validate_species, `icon_geometry`); `gate_motion_density.py`; `tests/test_targeted_species.py` (+4); `tests/golden/frames/chip-board.png` | WIRED 2026-09-11 - the first species module; judged on the filmstrip (t=5.22 / 6.42 / 10.37 / 11.00) | `chip-board` golden + the filmstrip, 2026-09-11 |
| **The PRESS CARD dock and the STACK hand-off, WIRED** (2026-09-11, P50 T3; Bravos shots 5-10: their claims as cards on the plate, the previous one dimming and sliding back, the newest lit) — `press_card.py <screenshot> --headline-box --source --phrase-box --out --meta`: the crop tool (PIL, no network) cuts a screenshot to its headline box at the dock's width and writes the meta (`kind: press`, the source line, the quoted phrase's box as fractions of the card). A dock tuple's options `press: <meta>` make it `kind: "press"` on the entry (a paper card with the SOURCE stamped as a masthead strip in the record's header type); `stack: True` joins the scene's press STACK - the compiler assigns `stack_index` / `stack_n` in enter order and the player paints press docks outside the two slots (each its own `.dock.press`, DOM order the pile, membership read from `DOCKS` at t so a scrub equals a play-through and a departed card closes the gap). `scripts/species/press.mjs` is the pile's math, pure: the newest lands by the badge spring (POP), every older card slides BACK on one `sin(πp)` push pulse (x / scaleX / skewX per §9.27's push hand-off), settling into a fan - a step of STEP_H 0.22 of the card's own height (every masthead stays legible), STEP_SCALE 0.05 about the bottom edge, DIM 0.22 per step to DIM_MIN 0.34 (a card the sentence may return to never goes to nothing). The quoted phrase: a `callout` with `form: "underline"` and the new target `{"kind": "phrase", "dock": <press dock>}` draws the squiggle's hand-drawn underline under the phrase on the card's LIVE geometry (E56's one exception - a ring on a press card stays refused). E45 guard: a press dock on a `ledger:` plate is a build error naming the dock (the pile's one box is the stage's centre - on a page that is the plot); reverse it when a cut wants quotations beside a parked chart. Nothing new in the gate: each stacked card is a dock enter, the underline a callout at its word (pinned by a test). Golden `press-stack` (three cards on three words, the third underlined, the settled fan at 11.4 s). **Use when:** the sentence QUOTES a headline or a claim with a masthead (row 1) - THEIR claim never pages (B1); one card for one claim, the stack when three claims land in a row; the record dock stays the species for a typed excerpt read word by word. | `scripts/press_card.py`; `scripts/species/press.mjs`; `build_scene_timeline_f.py` (`DOCK_KIND_PRESS`, `press_meta`, `assign_press_stack`, `_validate_callout`, `PHRASE_TARGET`, `UNDERLINE_FORM`, `press_plate_error`); the template's `paintPress` + the underline branch; `tests/test_press_dock.py` (+`test_press_card.py`, `test_targeted_species.py` +5, `test_gate_motion_density.py` +1); `tests/kinetics/press.test.mjs`; `tests/golden/frames/press-stack.png` | WIRED 2026-09-11 - the second species module; judged on the filmstrip (5.30 / 7.55 / 9.95 / 10.75 / 11.40). Open: the build-loop seam (`row_press` -> `validate_species` -> `dock_entry` -> `assign_press_stack`) has unit tests, not an episode build - the first cut with a press row closes it | `press-stack` golden + the filmstrip, 2026-09-11 |
| **The CLOTHOID fitter - Euler spirals for generated geometry, WIRED** (2026-09-11, P50 T14; doc 42 §42.4: a cubic Bezier's curvature is a degree-3-over-degree-6 rational function that ripples and throws parasitic inflections; an Euler spiral holds `dk/ds = const`, G2 at the joins, and minimises the MVS energy - Levien 2009, Moreton & Sequin 1992) — `kinetics/clothoid.mjs`: `clothoid(p0, t0, p1, t1, n)` an Euler-spiral segment between two points with end tangents, the Fresnel integrals by series (the asymptotic form past the series' radius), sampled to a polyline the stroke engine draws by length; `clothoidS` the two-segment G2 fit for an S-curve; `curvatureOf` for the tests; `bezierOf` the reference the test measures against. Its first customers: the flow diagram's arrows (this commit) and the map's arcs (T5); the leaders of T10 / T11 next. Hand-authored art untouched. **Use when:** the engine GENERATES a curve - an arrow, a connector, an arc, a leader - never for drawn art. | `kinetics/clothoid.mjs`; `tests/kinetics/clothoid.test.mjs` (curvature monotone along a segment, the tangents met, the S-fit G2 at the join, the Bezier of the same ends shows the inflection the clothoid does not) | WIRED 2026-09-11 - the eleventh kinetics module; goldens byte-identical (nothing drew a clothoid before the flow diagram) | the flow-swap golden's arrows, 2026-09-11 |
| **The FLOW DIAGRAM - chips, clothoid arrows, the swap, WIRED** (2026-09-11, P50 T4; Bravos shots 82-86: a three-node diagram draws on a word, on a later word ONE node swaps and the rest stands - the rhyme) — `scripts/species/flow.mjs`: `{"kind": "flow", "at", "dur", "target": region (the box), "nodes": [{id, icon, label}], "edges": [[a, b]], "swap": {at, node, icon, label}?, "tag": "1973"?}` - the dashed box draws on by the curvature stroke, the nodes land as CHIPS (chip.mjs's law, the sourced icons through `icon:<name>`), the arrows between named chips are CLOTHOIDS drawn by length with the nib after the chips land, one per `FLOW.EDGE_S`, the tag a year stamp in the box's corner; on `swap.at` the standing node un-draws (the chip's landing run backward, as a recast runs a build law backward) and the new node draws on the same spot while the arrows stand - one event. Pure function of t, registered as `SPECIES_PAINTERS.flow`. The compiler validates the region, 2-6 nodes with unique ids, edges naming nodes, icons on disk, `swap.at` > `at`, `swap.node` a node; the gate counts `at` and `swap.at`. Golden `flow-swap` (PLANTS -> CHIPS -> PRICE after the swap, both arrows and the 1973 tag standing). **Use when:** the sentence EXPLAINS a mechanism - A causes B via C - as named things and the arrows between them (row 6); the swap when a later sentence replaces one link and keeps the rest ("the same machine, a new part"). | `scripts/species/flow.mjs`; `tests/kinetics/flow.test.mjs`; `build_scene_timeline_f.py` (`flow` in SPECIES_KINDS / SPECIES_WHEN / validate_species); `gate_motion_density.py`; `tests/golden/frames/flow-swap.png` | WIRED 2026-09-11 - the third species module; the goldens read by the parent (the arrows smooth, no ripple) | `flow-swap` golden + the filmstrip, 2026-09-11 |
| **The SPAN - a shaded stretch of time with its name, on a ledger page, WIRED** (2026-09-11, P50 T4 / BACKLOG R26-25; the macro-chart intake's Archetype 5, Bravos 107-110 "the decades") — `scripts/species/span.mjs`: `{"kind": "span", "at", "dur", "from", "to", "label", "color"?}` - a shaded REGION between two x positions behind the page's chart, fading in over `SPAN.IN_S`, its name written above by the hand; a PAGE species on the perform layer, resolving its ends on the ACTIVE state and following a rescale on the live scale (R26-28) as the bracket does. A `bracket` MEASURES two data; a `span` NAMES a stretch of time. Golden `span-decade` (the memory-makers page with THE RUN-UP shaded Nov-Mar). **Use when:** the sentence SPANS a period - a regime, an epoch, "the decade", "the run-up" (row 8) - and the chart should carry the period's name, not a number. | `scripts/species/span.mjs`; `tests/kinetics/span.test.mjs`; `build_scene_timeline_f.py` (`span` in SPECIES_KINDS / PAGE_SPECIES / SPECIES_WHEN); `tests/golden/frames/span-decade.png` | WIRED 2026-09-11 - the fourth species module; R26-25 closed | `span-decade` golden, 2026-09-11 |
| **The VECTOR MAP world - a country that lights, an arc that crosses, a figure that stamps, WIRED** (2026-09-11, P50 T5; Bravos shots 57-80: Iran lights, an arc from the Gulf to the US crosses, "1996" stamps, China lights and takes "1.4 Billion Barrels") — a plate id `vecmap[:<A3 list>][;idle=...]` is a WORLD: `assets/maps/world-110m.paths.json` (Natural Earth 110m, 177 countries by ISO A3 on a 1000 x 500 equirectangular box, <= 450 nodes each, the source pinned by blob hash - `build_world_map.py`, commit 06dae01) drawn in the muted ink on the stage's ground, the FOCUS set (the countries the composition frames) outlined a shade stronger; the compiler fits the focus set's box to BOTH aspects (one similarity per composition - `mapFit` - the whole world on 16:9, the focus box to the width on 9:16) and ships the map data once as `uris["map:world-110m"]`; the world holds on its idle (E49). `scripts/species/vecmap.mjs` is the math (the fit, `mapPoint`, the light's ramp, the arc as a CLOTHOID from centroid to centroid bowed toward the pole through `kinetics/clothoid.mjs`, the stamp at the centroid), the template's world branch thin. Three species live only on this world, with two target kinds of their own (`{kind: country, id: A3}` and `{kind: mappoint, x, y}` in map units, resolved by the compiler against the data): **`light`** (`at`, `dur` or "hold", a country: the fill rises to the accent over 0.3 s and breathes - the spotlight's cousin, a fill, never a ring, E56), **`arc`** (`from`, `to`, `crossed`?: drawn by length with the nib, an X at the midpoint on `crossed`, the arc dimming after), **`stamp`** (`target`, `text`, `size: figure|year`: the figure written at the centroid or at a point; "1996" is a stamp at year size - one act, one kind, one event edge). The gate counts `at` (and `crossed`); M24 knows they point. Golden `vecmap-arc` (portrait: IRN lit, the Gulf -> US arc crossed, 1996, CHN lit with its figure). **Use when:** the sentence NAMES places and flows (row 5) - a country on the word, a flow between two, a number put on a place; the camera moves only between focal points on the map (E59 reason 2), one move per composition, then still. | `scripts/species/vecmap.mjs`; `tests/kinetics/vecmap.test.mjs`; `build_scene_timeline_f.py` (`VECMAP_KIND`, the focus fit, `light` / `arc` / `stamp` in SPECIES_KINDS / SPECIES_WHEN / `_validate_vecmap_species`); `gate_motion_density.py`; `tests/test_targeted_species.py`, `test_portrait_parity.py`; `tests/golden/frames/vecmap-arc.png`; the data: `assets/maps/` + `tests/test_world_map.py` (24) | WIRED 2026-09-11 - the fifth species module; the golden read by the parent | `vecmap-arc` golden + the filmstrip, 2026-09-11 |
| **N-TIER pages - small multiples on one shared x, WIRED** (2026-09-11, P50 T9 / BACKLOG R26-24; the macro-chart intake's Archetype 3, Bravos's two-panel SPR shots 35-36) — `ledger_page.py` builder `tiers`: a series object with `tiers: [{name, series|bars, unit}, ...]` (N in [2, 4] - four bands of a 9:16 plot are ~250 px each, the floor a band can carry; the old two-band `tiers: True` form keeps building byte-identically) - one page, N bands stacked, each its own y-scale with an honest zero (E53 §4), ONE shared x (mixed x ranges refused), the tier names as the band titles, one source line; `page_boxes` returns the bands. `scripts/species/tiers.mjs` is the paint math (pure): the tiers draw in turn on their words (`build_to` takes a `tier` index), the drop of one tier as a bar in the accent on a word (shot 36). Golden `tiers-two` (JAPAN | UNITED STATES on 2015-2025, the -96 Mb drop bar). **Use when:** the sentence COMPARES the same quantity across two, three or four subjects over time (row 3) and a dual axis would lie - one x, each its own honest scale; the page is OURS (A1-A3) when the series are. | `ledger_page.py` (`tiers`, `TIERS_MIN/MAX`, `_tiers_block`); `scripts/species/tiers.mjs` + `tests/kinetics/tiers.test.mjs`; `tests/test_ledger_page.py`; `tests/golden/frames/tiers-two.png` | WIRED 2026-09-11 - R26-24 closed | `tiers-two` golden + the filmstrip, 2026-09-11 |
| **The TREEMAP page with X marks - the census exception, WIRED** (2026-09-11, P50 T6; gate 1 ruled 2026-09-10: E53 §1's second amendment - breadth or a named subset, the subset marked and its share written, labels only where they fit, a size claim takes its bar; Bravos shots 89-91) — `ledger_page.py` builder `treemap` from a series of shares: the squarified layout (Bruls, Huizing & van Wijk 2000) with the aspect tuned toward 3:2, never 1:1 (Heer & Bostock 2010's square penalty) inside `page_boxes`'s plot; label tiers by the research floors (value font >= 18 px; no text in a cell under 80 x 36 px; two lines, one stacked line, none - the rest unnamed, "and N others" under the map); the builder REFUSES a series whose title or claim names a size comparison and points at the bars page. `scripts/species/treemap.mjs` is the paint math (pure): the cells land in layout order on the build clock; the `cross` PAGE species (`{"kind": "cross", "at", "dur", "cells": [labels], "text": "3 partners, 26 % of exports"}` on a treemap page - ONE act: the subset marked AND its share written; the compiler refuses a cross without a number in its text, a cell the page does not carry, or a crossed cell whose label the floors culled - no unlabelled cell is ever the argument) draws the chip's two-stroke X on the named cells on the word and writes the share by the hand; the shrink afterwards is P48's `park` (one affine transform, never a re-layout - Sondag 2018). Golden `treemap-cross` (China's exports by partner, three crossed, the share written). **Use when:** the sentence DIVIDES a whole into MANY parts and the argument is the breadth or a named subset of it (row 4) - never for a size comparison, which is the bars page's. | `ledger_page.py` (`treemap`, the squarify, the label tiers, the size-claim refusal); `scripts/species/treemap.mjs` + `tests/kinetics/treemap.test.mjs`; `build_scene_timeline_f.py` (`cross`); `tests/test_ledger_page.py`, `test_targeted_species.py`; `tests/golden/frames/treemap-cross.png` | WIRED 2026-09-11 | `treemap-cross` golden + the filmstrip, 2026-09-11 |
| **M26 - the printed value and the drawn height agree, WIRED** (2026-09-11, BACKLOG R26-40; E28 / E53: the scale and the value are printed at every instant - and nobody checked that the bar's HEIGHT matched the number the pill printed on the scale the ticks printed; R26-39 was exactly that mismatch, 303 px at "0.00 %") — `probe.py` records per bar `{label, h, plot, value text when fully shown, the top tick's value and y}` at every gate instant; M26 reads `layout-probe.json` (M25's sibling for VALUES) and FAILs naming the bar, the instant, the printed and the drawn value when they disagree past `VALUE_TOL`; INFO when nothing is printed. **Use when:** every build, through the self-watch - the bar that lies about its number is caught by the engine, not the eye. | `probe.py` (`page.bars`); `gate_motion_density.py` M26 (`VALUE_TOL`); `tests/test_gate_motion_density.py` (+2: the R26-39 numbers as a fixture FAIL; the Tokyo build PASSes) | WIRED 2026-09-11 | the Tokyo build's M26 line, 2026-09-11 |
| **Morph METHOD A (vertex-based) and the TR-7 refusals, WIRED** (2026-09-11, P50 T12; doc 43 §43.5's decision rule; the operator, 2026-09-10: "we should probably add the additional morph method") — `kinetics/morph_a.mjs`: ring-normalise (orientation, start vertex), resample both rings to N by arc length, the rotational alignment through `arap.mjs`'s `correspond`, a direct vertex lerp per frame and a centripetal Catmull-Rom reconstruction of the intermediate ring (alpha 0.5: the one alpha that can neither cusp nor self-intersect between knots) - pure; the ARAP path untouched. `chart_to {to: "morph", method: "a" | "arap"}` - the default by the rule from the pair's MEASURED principal-axis angle (`METHOD_A_MAX_DEG` 15): doc 43 names 90 deg for real rotation but TR-7 refuses anything past 15, so EVERY ADMITTED PAIR DEFAULTS TO A (doc 43's own "start on A; escalate when a shape collapses") and ARAP is reached by naming it; a timeline with no `method` still runs ARAP, so old builds are byte-identical - ratified by the parent 2026-09-11, the operator reads the two proof pages. THE REFUSALS (TR-7's invariants, never a silent bad match): a morph whose pair fails centroid distance <= 0.06 W, principal-axis angle <= 15 deg, or area ratio >= 0.60 is a BUILD ERROR naming the measured number and the limit; `measure_morph.py` prints the three invariants for a pair. R26-16's rule stands (the source is real or it is a cut); the proof pages `morph-a-proof.html` / `morph-a-arap-proof.html` render one pair by both methods side by side for the operator's read. **Use when:** a `morph_to` between two LINE areas whose outlines match with modest rotation - A is lighter and reads cleaner; ARAP when the shape really turns; the compiler picks unless the row says. | `kinetics/morph_a.mjs` + `tests/kinetics/morph_a.test.mjs`; `build_scene_timeline_f.py` (`MORPH_METHODS`, `METHOD_A_MAX_DEG`, the refusals); `measure_morph.py`; the template's `lpPaintMorphTo` routing; `tests/test_morph.py` | WIRED 2026-09-11 - the twelfth kinetics module; the demo's human read is the operator's | the two proof pages + the filmstrip, 2026-09-11 |
| **Line-end TAGS become the next chart's BARS, and the tip-riding pill, WIRED** (2026-09-11, P50 T11 / BACKLOG R26-34; Bravos shots 104-105: continuity - the four yield lines' tags hand over to four bars with no cut) — `chart_to {to: "recast", state, keyed: "tags"}` on a dense-line page whose named state is a bars page with one bar per series (`RECAST_TAG_PAIRS`; `keyed: true` keys the DATUM as before, `keyed: "tags"` keys each series' TERMINAL TAG - the name at the end becomes the bar's own number): the tag slides and grows into the bar while the line un-draws by length beneath it; the values never re-draw; a page without tags is refused for the tags form; the hand-over runs on the clock, not the eased slide (`KEYED_TAG_HAND` 0.92 - two strings ghosted for two thirds of the transition, read off the frame). The tip-riding pill (`scripts/species/tippill.mjs`, opt-in `;pill=yes|no|<datum index>` ON THE PLATE ID - how this shot draws its data is the row's, as `card=` is; the index is the milestone datum the pill pops at): `X_pill(u) = P_tip(u) + D_offset` on the stroke's fractional tip (30 ahead, 40 above, so the nib is never covered), a leader from the tip, popping on `springPop(Mp = 0.05)` over 6 % of the line's own length (a seek paints what playing would), and over the last tenth of the draw it settles into the terminal tag's place as the tag fades in - one string, one place (E53 unchanged); byte-identical off. Open: the leader can hide behind the pill's own capsule (R26-42); a tag wider than the chart gets no pill and degrades silently to the terminal tag (R26-43). Golden `tags-to-bars` (a two-line page mid-hand-over: the lines un-drawing, the tags standing, the bars rising to take them). **Use when:** a COMPARES page (row 3) turns into a RANKS page of the same series ("where the four stand today") - the tags carry the eye across; the tip pill when the draw itself is the sentence and the series' name should ride the pen. | `build_scene_timeline_f.py` (`RECAST_TAG_PAIRS`, `RECAST_KEYS`); the template's `lpPaintRecastKeyed` (the `tag` role); `scripts/species/tippill.mjs` + `tests/kinetics/tippill.test.mjs`; `tests/test_chart_transitions.py`; `tests/golden/frames/tags-to-bars.png` | WIRED 2026-09-11 - R26-34 closed | `tags-to-bars` golden + the filmstrip, 2026-09-11 |
| **ONE placement truth - the page's boxes measured from the player, WIRED** (2026-09-11, P50 T16 / BACKLOG R26-22, R26-27; the finding: on a portrait page `free_bands` handed `centred_place` y 536 where the player draws the band at 1250 - the estimate and the player disagreed) — `measure_page_boxes.py` renders one representative page per builder (dense-line, story, share, tiers, treemap) at BOTH aspects through the headless player and writes `assets/page-boxes.v1.json` (the title / sub / chart / plot / source / rail boxes and the free bands above, below and beside the plot, in stage px, with the player's sha); `ledger_page.page_boxes` READS the fixture when the builder + aspect is in it and falls back to the estimate otherwise (noted in the compiled timeline), so `centred_place` and the player agree. R26-22: a SOLO card on a page that names no `centre_y` lands centred by E50's clock in the band the fixture says is free at its enter time, instead of the reading rect over the title; an authored `centre_y` still wins. Both shorts rebuild byte-identical (they name their centres). **Use when:** every dock over a page - nothing to author; re-run `measure_page_boxes.py` when the page's CSS changes (the sha tells). | `measure_page_boxes.py`; `assets/page-boxes.v1.json`; `ledger_page.page_boxes`; `build_scene_timeline_f.py` (`centred_place`, `free_bands`, the solo card); `tests/test_page_boxes.py`, `test_dock_read_park.py`, `test_video_dock.py` | WIRED 2026-09-11 - R26-22 / R26-27 closed | the byte-identical proof on both shorts, 2026-09-11 |
| **The CONTINUITY three - arriving from the edge, the thread, the occluder, WIRED** (2026-09-11, P50 T15; HF-15 / HF-16 / HF-17 from the HyperFrames intake) — HF-15, a camera key law (E59 reason 2: one move per composition between focal points on a stage wider than the frame): the next region is visible at the frame's edge BEFORE the move - `validate_camera` refuses a key whose target box is wholly off-frame at the key before it, naming the key's t and the distance ("camera key 1 (t=4.00s) moves to a target that is wholly off-frame at key 0 ... 72px past the left edge. E59 reason 2 / HF-15: the next region ARRIVES from the frame's edge; a target the frame cannot show is a cut wearing a pan's clothes"). HF-16, the wire: ONE element carried across worlds - `;thread=<mark key>` on the ARRIVING page's plate id - a property of the page ("this page starts with that mark already on it"), checked against the page that hands it over; `species/thread.mjs` composes the two fits so the mark stands on the same stage pixels after the cut; E50's clock does not restart; golden `thread-baseline`. HF-17, one foreground occluder on a world plate: `behind: "<layer>"` on the dock, the plate's fronts declared in `<plate>.layers.json` (`{"foreground": {"desk": "...png"}}`), refused when undeclared or missing, the PNG embedded raw for its alpha, painted above the docks and below the species and caption layers; the depth cue by occlusion, never blur (E22, doc 29 §9.27); golden `occluder-dock`. Each is judged by the operator on a real beat when a cut asks (the goldens are the mechanism's proof). **Use when:** a map or wide diagram moves the eye (HF-15 is automatic - a refusal); a line the next page argues from is already drawn (HF-16: carry it, never redraw it - E40 §4); a card should sit IN the painting rather than on it (HF-17: the plate's foreground names its layer). | `build_scene_timeline_f.py` (`validate_camera`, the thread, `behind`); `scripts/species/thread.mjs` if present; the template's occluder paint; `tests/test_camera.py`, `test_chart_transitions.py`, `test_video_dock.py` / `test_targeted_species.py`; `tests/golden/frames/thread-baseline.png`, `occluder-dock.png` | WIRED 2026-09-11 - the last P50 species slice | the goldens + the filmstrip, 2026-09-11 |
| **THE RUNTIME APART FROM THE DOCUMENT - the engine as a module, the player a shell, WIRED** (2026-09-11, P51 T1 / BACKLOG R26-17 step 1; the operator, 2026-09-07: "we need to build the actual engine so that the editor is not the engine") — the reviewed player is now two files beside each other: `docs/content-video-engine/samples/scene-evidence-engine.mjs` (600 KB, 7,688 lines: the whole engine - the painters, the species registry, the kinetics and species regions `sync_kinetics` inlines, the probes - as ONE exported entry `mount(doc)` whose body is the old script verbatim except its first two lines, which now take the data INLINE when the slot carries it or FETCH it from the slot's `data-src`; the two `window.__*` probes stay module-top-level exactly where the old script left them) and `scene-evidence-player.template.html` (46 KB: the style, the DOM, the two data slots with `data-src`, an engine slot). One engine, two forms: `render_baseline.instantiate(timeline, uris)` composes the SINGLE FILE (the engine inlined by the sync's own `inline_text`, the data inline) - every golden byte-identical, and every player built before the split still runs; `write_split(build, timeline, uris, name)` writes the build's form - `player.html` (the shell with the src slots), `assets.json` (the data URIs, fetched not embedded), a copy of the engine beside it, `player.json` with the engine's sha (the compiled timeline stays byte-identical). The build writes the split form; the render, the probe, the self-watch and the review servers load it served (`.mjs` as text/javascript on both servers; `prepare_page` waits for `__mounted`). Proven on Tokyo: `player.html` 32,101,827 -> 45,985 bytes, `assets.json` 31 MB, the gate rows identical before and after, the probe's JSON at 58.6 identical, the self-watch the same bar; two parity tests hash a golden's frame equal in both forms. **Use when:** always - this is how a build is written now; the split changes nothing a cut sees. Next: hot reload (T4) and the sidecar (T5) are clients of this. | `samples/scene-evidence-engine.mjs` + the shell; `render_baseline.py` (`instantiate`, `write_split`, `player_text`, `single_file_shell`); `build_scene_timeline_f.main`; `sync_kinetics.py` (targets the engine); the two `serve_player.py`; `tests/test_golden_frames.py` (+3: the parity pair, the size) | WIRED 2026-09-11 | the Tokyo side build, the gate / probe diffs, 2026-09-11 |
| **THE OVERRIDE SIDECAR - a human's or a flash agent's edit, keyed by row id and field, WIRED** (2026-09-11, P51 T5; the grill: "a sidecar of overrides keyed by row id and field, layered over the shot table the agent authors"; Motion Canvas's .meta, never Theatre's random ids) — `<build>/overrides.json`, a JSON object keyed `<row id>.<field>`: `{"s04.dock.dock-k-pledge-record": {"centre_y": 0.32}, "s04.species.3": {"at": {"word": "pledged"}}, "s02.camera": null, "s04.plate": {"idle": "drift"}, "s04.exit": "dip"}`. The ids are DERIVED, never stored: `s01..` in the compiler's own order (a table where two rows open at the same second is refused - that order is what the key means), a dock by its slide, a species by its index in the row's authored list, plus `.plate`, `.exit`, `.camera`; the compiled timeline writes an `id` on every dock, species and camera so the key is stable. A dock's fields go through `dock_opts`, a species' through `validate_species`, a plate's through `PLATE_OPTS`, the exit through `parse_exit`, the camera through `validate_camera_row` - the same code, the same named refusals (an unknown id, an unknown field, `kind` as an edit, a bad value, a phrase not in the take - each names the row and the field); `at` as seconds or `{"word": "<phrase>"}` resolved through the build's words; `null` restores a field's default. Pure and idempotent; the kit's `apply_sidecar` layers it before the shot-table literal is written, so `SHOT-TABLE-SHORT.py` shows the effective rows and the compiled timeline records `overrides_applied`. Proven live on Tokyo (five overrides: the exit, the idle, a species' word, a dock's centre, the camera off; the gate PASS) and byte-identical with no sidecar (the compiled timeline gains the `id` fields only - stripped, equal to the baseline). **Use when:** a human or a flash agent tunes a cut - the box, the beat's word, the variant - without touching the source; the change report (T6) reads the sidecar's diff. | `build_scene_timeline_f.py` (`apply_overrides`, the derived ids, `overrides_applied`); `authoring/table.py` (`load_rows`, `apply_sidecar`); `tests/test_overrides.py` (36) | WIRED 2026-09-11 | the live Tokyo sidecar run + the stripped-equal proof, 2026-09-11 |
| **HOT RELOAD with the DETERMINISM CHECK, WIRED** (2026-09-11, P51 T4 / BACKLOG R26-17 step 2; the grill: "hot reload proves determinism on every edit - the changed instants re-render warm and cold and hash-compare"; the documented killer: a change that passes preview and dies in render) — `scripts/serve_player.py`, the shared review server (the two projects' `serve_player.py` are 25-line wrappers, CLI and ports unchanged): the no-store Range server with the `.mjs` MIME, threaded; `--watch` polls the shot table, `<build>/overrides.json` and the compiled timeline every 0.25 s and re-compiles IN-PROCESS from `<build>/player.json`'s `compile` block (the episode dir, the timeline name, the shot table, the titles, the aspect, the caption style, the kinetics - and `stamped`, the dock assets the build registered in memory, 92 on Tokyo, without which a fresh process cannot compile); the page long-polls `GET /reload?since=<gen>` (held up to 25 s; `{gen, reload, changed: [instants], why, reason, determinism, compile_ms}`) only when `?watch=1` is on the URL; the engine's `reload(timeline)` rebuilds the state from a new timeline without a page load (the scrub kept, the assets cached), and `?t=<seconds>` seeks on load. `determinism_check.py <build> [--against <old>] [--instants t...] [--all]` diffs two compiled timelines, names the changed instants (docks' enter/exit, species' at / cross_at / swap.at, rows' ends), renders each WARM (a seek from the instant before, as play arrives) and COLD (a fresh page straight to it), hashes the RGB bytes, writes both PNGs on a mismatch, names R26-21's known class rather than hiding it, exits 1; the server runs it after every re-compile. Measured on Tokyo (a 31 MB asset map): a sidecar edit lands on the served page in 658 ms (the re-compile 335 ms), a compiled-timeline edit in ~250 ms; the stateful-bug fixture (a cached box injected into a shell copy, never the engine) is caught as a mismatch with both PNGs. And the check's first catch on the APPROVED cut: two real mismatches (R26-46 the retitle's state depends on the seek path - a warm arrival at 55.31 carries no title where the cold one does; R26-47 a 0.16 % drift on the fab card at its exit). **Use when:** every edit round - the server with `--watch` beside the page with `?watch=1`; the check on every build before the watch; a mismatch is a render that will differ from the preview. | `scripts/serve_player.py` (396); `determinism_check.py` (309); the engine's `reload` + `?t=`; `render_baseline.WATCH_CLIENT`; `authoring/table.py` (`write_compile_manifest`); `tests/test_reload.py` (10) | WIRED 2026-09-11 - goldens byte-identical with the engine changed | the Tokyo measurements + the two mismatch frame pairs, 2026-09-11 |
| **THE EDITOR, THIN - a client of the served build that writes the sidecar and nothing else, WIRED** (2026-09-11, P51 T7 / BACKLOG R26-17 step 3; the grill: "the player stays a pure function of t; the editor is a client. A ghost outline may follow the hand while dragging; the real frame on release"; the operator: "Option A with the ghost outline B") — `GET /editor.html` on the review server serves `content/video_engine/editor/editor.html` (one file, inline CSS/JS, no build step) from the scripts tree on the BUILD's origin - nothing copied into a build, the page always the current file; it holds the served `player.html?watch=1` in an iframe (the engine untouched) and beside it: the scrub (seeks the iframe by the player's own `#scrub`; the player's scrub mirrored back), the scene list from the compiled timeline (click = seek), per scene the species (kind, `at` as seconds and the nearest word from the build's words, dur, idle - each a text box) and the docks (slide, enter/exit, place, `seek to its landing`), the dock boxes as GHOST outlines over the frame (the compiled `place`, scaled to the iframe; a card that pops then parks shows its park box and says so; a placed-not-centred dock is grey and warns that centre_x/y will not bind): a drag moves the ghost only - the frame never moves under the hand - and the release writes `s0N.dock.<slide>.centre_x/centre_y` as stage shares; a row's exit (a select), its camera (a checkbox: on = `{keys: [], attention: "landings"}`, off = a literal null), a plate's idle; the determinism verdict from `/reload` beside the scrub with links to the PNG pair; the sidecar as read-only text so the human sees what the agent will read, with an `x` per line that takes it back; a refused write shows the compiler's message verbatim. The dials (LP / DOCK_* / LPX.BT_* / ATTN) are a disabled panel that says why: the sidecar has no `dials` key yet. **The write door:** `POST /overrides[?literal=1]` `{"<row id>.<field>": <value>}` merged into `<build>/overrides.json` AFTER the compiler's own `apply_overrides` accepts the result over the build's current rows (read through `player.json`'s compile block) - 200 `{ok, gen, keys, overrides}` and the watcher does the rest, 400 with the compiler's message and nothing written; a null deletes the key, `?literal=1` writes it (the camera OFF and the exit default ARE nulls); a dict merges field by field; atomic write; an emptied sidecar is unlinked. Gate 3's proof on a private copy of Tokyo's side build: the three edits of 2026-09-10 (the record 0.015 up, the plant 0.04 down, the camera on) by drag and tick, each one sidecar line, the frame in 0.98 s (12-14 s while the determinism check runs between edits). **Use when:** a human tunes a cut by hand - the box, the beat's word, the variant - and the agent reads the change as the sidecar's diff (T6). | `editor/editor.html` (571); `scripts/serve_player.py` (`POST /overrides`, `write_overrides`, `GET /editor.html`; 541); `tests/test_editor.py` (6); `.claude/launch.json` `tokyo-editor` (:8744, `build-short-t7`) | WIRED 2026-09-11 - human gate 3 OPEN: the operator makes the three edits by hand in the page | the gate-3 strip + the editor screenshot at 57.0, 2026-09-11 |
| **THE CHANGE REPORT - what a human or a flash agent changed, as the agent sees it, WIRED** (2026-09-11, P51 T6; the grill ledger: "the agent sees a change as the diff line, the two frames at the affected instant and the gate delta") — `change_report.py <build> [--since <git sha> | --against <overrides.json> | --before <compiled timeline>]` -> `<build>/CHANGE-REPORT.md` + `<build>/change-report/` (the crops). The BEFORE: by default the build re-compiled IN PROCESS with no sidecar (the agent's authored rows - a temp copy through `player.json`'s compile block, the same door `serve_player --watch` uses, ~1.2 s on Tokyo; never over the build); `--against` the same with another sidecar; `--before` an old compiled timeline dropped into the copy; `--since` that timeline at a git sha (`git show`, read-only, the file's only git). The AFTER is the build as it stands. Five sections: (1) the sidecar field by field in row order, `s04.dock.dock-k-pledge-record.centre_y: 0.335 -> 0.3 [added]`, a word-form `at` shown with the second it resolved to; (2) per changed key the instants it touches - from `determinism_check.changed_instants` on the two timelines, never a second diff (minus two bookkeeping fields, `overrides_applied` and the words path) - and a before/after crop pair at each (the changed element's box padded 10 %, the same crop on both halves, `<key>-<t>-{before,after,pair}.png`); (3) the gate delta M01-M26, the gate run on both builds with the layout probe written for each, every row whose level or text moved as `M24: PASS -> PASS, the reading moved - before: ... - after: ...`, the unchanged rows counted; (4) the determinism check's verdict on the after build at the changed instants (its functions, not a re-implementation); (5) one verdict line `3 field(s) changed on 3 row(s); 5 instant(s); gate delta: M23, M24, M25, M26; determinism: mismatch at 55.31, 59.57`. Deterministic text (the date in the header only); exit 0 always but a usage error (2); a FAIL is stated, never an exit code. Honesty limit: the default before is the authored table AS ON DISK - a kit build that baked a sidecar into the table (`apply_sidecar` rewrites it) leaves no authored rows to diff against; `--since` / `--before` then. The first report (the three hand edits on a copy of Tokyo's side build): the three lines in row order, 15 crops, M23-M26 moved, two warm/cold mismatches on the record's enter and exit (the R26-46/47 family). **Use when:** the agent is summoned after a hand edit or a flash agent's pass - read CHANGE-REPORT.md before the build; the reply cites it. | `scripts/change_report.py` (673); `tests/test_change_report.py` (15) | WIRED 2026-09-11 - human gate 2 OPEN: the operator's three hand edits, the report the agent gets, the agent's reply citing it | `tokyo-tea-break/build-short-t6/CHANGE-REPORT.md` + `change-report/`, 2026-09-11 |
| **The perform layer on a page with chart states, WIRED** (2026-09-10, P48 T7) — on a page that carries `page_states` the page species (bracket, figure, spread) draw on their own svg above every state (`st.performSvg`) instead of inside state 0's chart, which a transition hides; a FIGURE follows the ACTIVE state's datum through `lpMarkDatum` (a datum the window dropped shows nothing), the layer rides the active chart's park, and a cold seek past a transition does not inherit the hidden chart's opacity. **R26-28 (2026-09-10): brackets and spreads follow too** - a bracket's two anchors and a spread's two edges are read from the active state every frame (`lpDatumNow`, `lpPointsNow`, `lpRuleYNow`), LERPED from the leaving state to the arriving one while a rescale or extend moves the scale (the marks' own eased clock), so they travel with the data instead of jumping at the switch; a bracket whose datum left the window hides (nothing to measure), a spread covers only the data both states carry mid-clock; on a recast or morph they leave with the line. The bracket's geometry is a function (`geomOf` / `applyGeom`) re-applied only when it changes. A page with one chart draws exactly as it did **Use when:** a page carries `;then=` states and the sentence still needs a bracket, figure or spread on the ACTIVE state | template (`buildPerform`, `paintFigure`, `paintPerform`) | LIVE 2026-09-10 - the Tokyo P48 cut's two treasury figures on the windowed line (:8740) | `test_chart_transitions.py` (the figure follows the active state; goldens byte-identical) |
| **`rescale`: the chart's axes retarget on one clock, WIRED** (2026-09-10, P48 T2) — `chart_to {at, dur, to: "rescale", ymin?, ymax?, window?}` on a ledger row: the compiler DERIVES a second state from the page's own series (the window sliced, `axes.domain` / `axes.xdomain`, the same builder, month labels for a months-wide window) and the player moves the standing chart's marks to the new scale - the line re-projected from its data, ticks and names lerped by value or key, leaving values fading, arriving ones fading in - then the derived state stands exactly as built; datum targets after the rescale resolve against the active state through the window offset. The kinetics: `chartxf.mjs`. Bravos 46 §46.7: a build inside a held frame, not a cut - the row-2 beat the fourth watch asked for (the sell-off at full width) **Use when:** the same series at another scale - the window the story is about ('since February', 'at full width') | template (`lpPaintRescale`, `lpRestoreState`, `st.scale`), `build_scene_timeline_f.py` (`rescale_state`, `derive_rescale_states`), `kinetics/chartxf.mjs` | LIVE 2026-09-10; the bars branch written, proven on the line only | 22 transition tests + 6 node tests; goldens byte-identical |
| **`extend`: new points (or a later series) draw on at the pen, WIRED** (2026-09-10, P48 T3) — `chart_to {at, dur, to: "extend", to_index}` grows the standing window to a datum: the axes retarget first (T2's rescale, the first 0.45 of the clock), then the new segment draws from the last shared datum by the two-thirds law with the nib visible - every path of the series capped at the pen; `chart_to {to: "extend", series: k}` draws a `later: true` series of the page's file on from its first point (Bravos 29-30: production, then consumption on its word). The golden `ledger-extend` (mid-tail at 13.4 s) proves the compiler and the player together **Use when:** more of the same series ('and then May') or a later series of the same file ('then consumption') | template (`lpPaintExtend`, `extendCap` in lpPaintChart), `build_scene_timeline_f.py` (`derive_rescale_states` in time order), `ledger_page.py` (`later` series) | LIVE 2026-09-10 | 32 transition tests; goldens byte-identical + 1 new |
| **`recast` keyed: n lines become n bars, by series, WIRED** (2026-09-10, P48 T4b) — `chart_to {at, dur, to: "recast", state, keyed: true}` on a ledger row whose page is a multi-line page and whose named `then=` state is a bars page with one bar per series (`RECAST_PAIRS`; the compiler refuses any other pair by name and points at the plain recast, `morph_to` or a cut). One clock, two phases (Bravos 99-105): each line's terminal NAME gives way to its terminal VALUE at the name's settled y; then the line leaves by its history (the dash window slides to the tail), its end and value travel to the bar's top, the bar grows from the baseline beneath, the axes hand over, the bar's name comes up. The plain recast (no `keyed`) stays the hand-over for pairs with no honest correspondence **Use when:** the same data in another form with a 1:1 correspondence - n lines into n bars ('where the four stand today'); the plain recast for the hand-over | template (`lpPaintRecastKeyed`, `KEYED`), `build_scene_timeline_f.py` (`RECAST_PAIRS`, the keyed check in `derive_rescale_states`) | LIVE 2026-09-10; golden `ledger-keyed` | 42 transition tests; 13 goldens |
| **`morph_to`: the area under the line becomes another line's area by ARAP, mid-page, WIRED** (2026-09-10, P48 T5) — `chart_to {at, dur, to: "morph", state}` between two LINE pages (`MORPH_BUILDERS`; the compiler refuses any other pair by name and points at the keyed recast, the recast or a cut): the first 0.3 of the clock the standing line un-draws by length while its area fills in its own colour; the filled strip then morphs (min-jerk, det J > 0 at every frame - `kinetics/arap.mjs`, the strip mesh of the page-enter morph) into the target's area as the standing axes leave and the target's arrive; the target then builds on its own law and the fill leaves with the build. No key correspondence pretended: a change of SHAPE between two different series. M17 is measured PER MORPH (a page's enter morph keyed by scene, every morph_to keyed `scene@at`; `measure_morph.py`); M23 counts it as a data transition. Behind `kinetics.arap_morph`: off, it is the recast hand-over of the same length, byte for byte. Open: the planted-element source (R26-16's tie) is the page-ENTER path's next slice (P48 T5b) **Use when:** a different LINE series in the same frame (the Fed rate against the ten-year) - the area hands over; never a shape invented for continuity (R26-16) | template (`lpStrip`, `lpMorphFor`, `lpPaintMorphTo`, `lpPaintMorphHold`, `XF_MORPH`), `build_scene_timeline_f.py` (`MORPH_BUILDERS`), `gate_motion_density.py` (`_morph_events`, `_morphs`), `measure_morph.py` | LIVE 2026-09-10; proof :8739 at 50 s (the holdings line -> the Fed-vs-yields line) | 47 transition tests, 62 gate tests; 13 goldens identical |
| **`park`: the chart makes room by one affine transform, WIRED** (2026-09-10, P48 T2b) — `chart_to {at, dur, to: "park", scale?, anchor?}`: the active chart shrinks toward a corner of its own box on the min-jerk clock and holds - one transform on the whole svg, every mark in place (the stability rule; Bravos 91: the treemap up and smaller, the next diagram below); no state is derived; datum targets and the vortex follow the transformed rectangle; the title and the words stay **Use when:** the next thing needs ROOM beside the chart (a card, a diagram); a park stands until the next park; `scale: 1.0` un-parks when the cards leave | template (`lpPaintPark`, `lpUnpark`), `build_scene_timeline_f.py` (`PARK_ANCHORS`, `PARK_SCALE`) | LIVE 2026-09-10 **2026-09-10: a park to scale 1.0 is the UN-PARK - the chart grows back to full from the standing park (a park now moves from where the chart stands, and a park stands until the next park; the source line rides it). Tokyo row 4: as the two cards leave on "And here's" the ten-year bars re-take the stage and the burst's result holds large under "what nobody says" (the operator: "the chart should re-take center stage").** | 39 transition tests; goldens byte-identical |
| **The morph: the object becomes the chart, WIRED** (2026-09-07) — P47 T3 / P38 T5 (43 §43.5 method B; E48 §4: the tab as the protagonist; the brief B4). `kinetics/arap.mjs`: **Method A** (resample by arc length, rotational alignment, lerp) and **Method B, ARAP** (Alexa, Cohen-Or & Levin 2000; cotangent weights after Sorkine & Alexa 2007 — not on file): per triangle `J = D′D⁻¹`, the closed-form polar decomposition `θ = atan2(j₂₁−j₁₂, j₁₁+j₂₂)`, R interpolated on SO(2) and S on Sym⁺(2) (eigenvalues on the log scale) so `det J(t) = (λ₁λ₂)ᵗ > 0` at every t, the rest mesh's cotangent Laplacian factored ONCE (dense Cholesky) and back-substituted per frame with one vertex pinned on the chord; the **strip mesh** for a prop that becomes the area under a series — a fan from one centre flips on a jagged series (measured: 12 of 96 triangles on the holdings page); a strip of n columns (a top vertex over a bottom vertex, two triangles a column) never inverts while both shapes are x-monotone, which a till-roll tab and the area under a time series both are; the **match-cut invariants** `morphInvariants` — centroid shift ≤ 6 % W (the AREA centroid, shoelace), dominant-axis turn ≤ 15° (the polygon's INERTIA, not its vertex spread), oriented bounding area min/max ≥ 0.60 (read in the target's principal frame, so a prop turned to the chart's axis is not charged for its tilted screen box) `[DERIVED: brief :390-396]`. **The `morph` page enter**: `ledger:…:morph[=<s>]` with `;morph=tab **Use when:** an OBJECT on a plate is the protagonist that becomes the chart (the tab, the bill; E48 §4) - never a shape invented to hold continuity |plate|card` — the prop strip is placed on the target's area centroid, turned to its dominant axis and sized in its principal frame (TAB_W 0.92 × TAB_H 0.85 of the extents), so the centroid and axis invariants hold by construction and the morph is a change of SHAPE, not a move; over `MORPH.S` 2.0 s on min-jerk the tab becomes the area under series 0 (sign colour, `FILL_A` 0.28), the board is soaked from the first frame, the build starts as the morph ends and strokes the line along the area's top edge while the fill leaves; behind `kinetics.arap_morph` (with the flag off a morph page is a MOUNT of the same length — byte-identical); the world beneath dissolves as under a mount. Gate: the morph's window is continuous motion, the build lands `morph_s + 3.0` after the enter, **M17** reads `morph-invariants.json` written by `measure_morph.py` (INFO until measured, stale on a rebuild, WARN naming the failing invariant, PASS with the numbers) | `scripts/kinetics/arap.mjs` (synced after `stopaction`) + `samples/scene-evidence-player.template.html` (`MORPH`, `morphProp`, `buildMorph`, `paintMorph`, `__morphInvariants`) + `scripts/build_scene_timeline_f.py` (`LEDGER_ENTERS` morph, `MORPH_SHAPES`, `morph_s`) + `scripts/gate_motion_density.py` (`_morph_gate` M17, the morph's beats and landing) + `scripts/measure_morph.py` | LIVE — HG3 first read (2026-09-07): the mechanism reads, the SEAM does not - the source must be a real element of the outgoing world (the red tie at its last frame), never a shape conjured over the clip (R26-16); the demo player (`tokyo-tea-break/build-morph-demo/`, M17 PASS: centroid 0.1 % W, axis 0°, area 0.69, det > 0); v3 keeps its mount until the operator's eye says the shape reads | tests `tests/kinetics/arap.test.mjs` (the resample and the alignment; the polar decomposition exact; a shape turned 150° vertex-locked: the lerp collapses to a sliver, ARAP keeps det > 0 on the target Jacobians AND the solved mesh; the strip on a jagged series never inverts where the fan flips; the three invariants fail by name), `tests/test_morph.py` (the grammar, the gate's states, the flag fallback byte-identical to a mount, and the browser: the tab is mid-morph at u = 0.5, the invariants hold, the end outline sits on the series or the axis within 1 unit, the fill leaves once the line is drawn; measure_morph writes and M17 reads) |
| **Motion-density gate, WIRED** — E21 on the built timeline: M01 no stretch >12s without a visual event, M03 evidence ≤45s apart, M04/M05 plate density and hold, M06 caption cadence, M07 the opening minute never the thinnest, M08 stage captions; dock events from the timeline's OWN `scenes[].docks` (evidence-dock.json sat on an older clock); page beats count as events, page start as an evidence entry. The build writes `build-f/GATES-MOTION.md`; `render_episode.py` refuses a FULL render on FAIL (exit 2) unless `--force`; slices and shards never block **Use when:** every compiled timeline, before the watch - it reads the authored table's events; it never authors them | `scripts/gate_motion_density.py` (`write_report`) + `build_scene_timeline_f.py` + `render_episode.py` | LIVE (P34 T4, P35 T4) — ep1 baseline 5 FAIL (M01 15% still, M03 54s, M05, M07, M08 six dock-held/silent stretches) | `build-f/GATES-MOTION.md`; tests `test_gate_motion_density.py`, `test_motion_gate_wiring.py` |
| **Surface grammar + census** — page vs dock vs plate-life vs none, decided by rule (A1–A3 earn the page, B1–B4 keep the dock, C1–C6 choreography at a boundary, D1–D4 the E21 density link); the ep1 census classifies all 75 shot rows with rule letters and a builder **Use when:** deciding page vs dock vs plate for any row (A1-A3 earn the page, B1-B4 keep the dock, C at a boundary, D the density link) - before the species is chosen | doc 29 §9.28; `build-f/SURFACE-CENSUS.md`; CHECK-RESPONSIBILITIES §3g | LIVE (P35 T0) — the re-script's shot table starts from the census | 6 pages / 49 docks / 4 plate-life / 14 none; 4 defect rows |
| **MOTION MENU species** — the targeting law as code: shot rows carry `species` entries with DECLARED targets (datum \| point \| region \| span); `validate_species` fails the build on a pointing species without a target or two camera moves in one window (gate M09 mirrors it); the player resolves targets at render time. Shipped: plate life (10 fps stepped, squash on land, seeded boil, our cutouts), camera punch / focus zoom / pull-back (one world transform, docks fixed), scribble callout, feathered spotlight glide, squiggle under caption spans. Not yet: beat-freeze exit, radial reveal, push hand-off, weight-shift captions **Use when:** a sentence points at a DECLARED target - punch (a named object), callout (a number on a chart), focus_zoom (a region), spotlight (a picture or datum), squiggle (a word); which one: `SPECIES-BY-SENTENCE.md` | template (`const SP`, `resolveTarget`, `paintSpecies`) + `build_scene_timeline_f.validate_species` + `gate_motion_density.py` (SPECIES_EVENTS, M09) | BUILT (P35 T6/T7) — each species needs its range render for the operator | proof `build-f/ledger-species-proof.html`; `tests/test_targeted_species.py` (14) |
| **Page sound cues** — paper slide (roll-out), drop settle (bleed), chalk stroke (outline draw-complete): CC0 Freesound, trimmed ≤1.5s, matched to the whoosh at −14 LUFS ±1 LU; page-relative `page_cues` in SOUND-PLAN (the `cues` list is flattened onto the episode clock by the mixer) | `sound/fs-page-*.mp3` (gitignored) + `sound/SOURCES.md` + `configs/sound_palette.json` | BUILT (P35 T9) — no CC0 ink sound under 4s exists; the bleed slot is a water drop, flag at the contact sheet | loudness table in SOURCES.md |
| **Choreography ledger + gates** — every enter/exit/side/how-it-leaves, gated per slot; FAIL blocks the build | `content/video_engine/scripts/emit_choreography.py` → `build-f/CHOREOGRAPHY.md` | LIVE — constants mirror the template; change together | caught tnx re-landing on first run |
| **Whiteboard reveal engine** — serpentine SVG mask + hand follower, pose set with per-pose nib calibration | git history `7880c01`, `1e6612f`; poses `hyperframes/assets/hands/` + `nib-calibration.v1.json` | RETIRED from this lane (doc 29 8.17) — earns its place when artwork is drawn, not sourced | doc 29 8.10–8.16 |
| **Remotion Production Console** — local timeline/canvas editor + Python bridge (`127.0.0.1:4317`): scrub/zoom/drag/trim, hash-bound immutable revisions, recompiles without touching narration or evidence approvals; semantic-evidence binding tests | `content/video_engine/production_console/` + `configs/production_console_snapshot*.schema.json` (merged from p31, 2026-08-29) | BUILT — the doc 29 §9.3 production route | P29/P31 gate screenshots in `.claude/PRPs/evidence/` |
| **Remotion composition registry** — single source of truth for editor compositions (Editorial, Documentary, motion variants, finance proofs, production evidence/timeline, 3D prototypes) | `content/video_engine/editor/src/compositions.ts` | LIVE — register here, never in Root.tsx | typecheck + vitest |
| **Editor fixtures** — editorial-motion two-shot with render harness (`render.mjs`), canonical audio fixture | `content/video_engine/editor/fixtures/` (merged from p16) | BUILT | `npm run render:editorial-motion-fixture` |
| **remotion-ui registry** (external, MIT) — ~200 copy-in `.tsx` components: captions, data/live metrics, SVG draw-on paths, TransitionSeries transitions, motion primitives; MCP server (`npx remotion-ui-mcp`) exposes list/search/detail/install to agents | github.com/riaz37/remotion-ui · remotionui.com/docs/components/browse | MCP INSTALLED (.mcp.json, loads on session start); registry index + 8 key components read; THREE techniques already ported into the review player (feathered wipe edge, under-wipe parallax, active-word caption pop). Their EASING.pop == our badge spring — same motion school. Full sweep when the Remotion port opens | port commit 2026-08-30 |
| **Hyperframes** — HTML-to-video rendering framework & motion system (DOM `data-*` timeline, clips, tracks, sub-compositions, 7 runtime adapters [GSAP, Lottie, Three.js, Anime.js, CSS, WAAPI, TypeGPU], seek-safe keyframes, registry blocks, Remotion-to-HyperFrames compilation) | `.agents/skills/hyperframes*`, `content/video_engine/review/hyperframes_assets`, `npx hyperframes` (0.8.27) | LIVE & STANDARDIZED — 8 skills synced across master, global Codex and all worktrees; PLANE ONE preserved (operator 2026-08-30) alongside the Remotion plane | CLI `npx hyperframes --version` -> 0.8.27, doc 29 Part 7 |

## Generative video, 2.5D parallax & driver automation (2026-09)

Rescued onto main 2026-09-03: this stack was written UNTRACKED in the main checkout
while it sat on `claude/outreach-api-and-tooling` (299 commits behind), so it belonged
to no branch. ComfyUI is the host for the first three - they are custom nodes on the
local instance at `127.0.0.1:8188`, not standalone tools.

| Capability | Where | State | Proof |
|---|---|---|---|
| **ComfyUI 2.5D Parallax Engine** - zero-hallucination monocular metric depth (`Depth Anything v2`) + virtual 3D camera trajectory displacement (`Depthflow` GLSL: dolly, zoom, circle, horizontal, vertical, orbital); turns an approved still plate into a camera sweep without AI shape drift, so it never re-generates locked art | `tools/google-flow-driver/src/parallax-runner.mjs`, `comfy-client.mjs`, workflow `content/video_engine/workflows/2_5d_parallax_inpaint.json`, ComfyUI `127.0.0.1:8188` | LIVE - benchmarked on RTX 4070 (3.1s / 30 frames @ 1024x768) | `content/video_engine/assets/test_parallax_dolly.mp4` (untracked - `*.mp4` is gitignored by the binary policy) |
| **Mask-pinned AMBIENT lane (local ComfyUI)** — an approved still, a SAM 2 object mask, a LIFE region, a prompt -> an mp4 where everything outside the region is the still bit-for-bit and only the region is generated. Two backends on the same contract: Wan 2.1 VACE 1.3B (`comfy_vace_ambient.py`, ~8 min / 81 frames at 480p portrait on the 4070) and LTX-Video 2B 0.9.8 distilled (`comfy_ltx_ambient.py`, 12-18 s / 121 frames; `--denoise` < 1 keeps the region's own pixels and adds to them). Doc 49 dials fixed in code; `gate_comfy_config.py` runs before every submission; the job JSON lands beside the mp4. Models linked into the running install from `Comfy-Desktop/ComfyUI-Shared/models` by hardlink (the install's own model folders were empty) | `scripts/comfy_sam2_mask.py`, `scripts/comfy_vace_ambient.py`, `scripts/comfy_ltx_ambient.py` | LIVE 2026-09-05 — the PIN is proven (cup / desk / laptop within compression noise across 5 s, four runs); the EFFECT is not: neither model drew thin steam in a small dark region (VACE rebuilt the room, LTX imagined a second desk at full denoise and shifted tone at 0.6). Use it for ambient life that fills a region (window light, rain, screen glow), not for a thin bright element on a dark ground - that stays a drawn species | `tokyo-tea-break/omni-video/ambient/*.job.json` (mp4s gitignored) |
| **SAM 2 + LaMa occlusion inpainting** (ComfyUI nodes) - the precondition for parallax on any plate carrying an actor: SAM 2 cuts the subject to an alpha PNG, LaMa fills the hole behind it (~0.3s), and the camera then moves two clean layers instead of stretching edge pixels into smears | `custom_nodes/ComfyUI-segment-anything-2`, `custom_nodes/comfyui-inpaint-nodes` | INSTALLED & VERIFIED | ComfyUI node initialization ledger |
| **LTX-Video 2B DiT ambient engine** (ComfyUI node) - local physical motion loops (haze, embers, drifting cloud, water) on a still plate, ~12s on the local GPU, zero cloud credits; SAM 2's mask pins the subject so the model animates only the ground - which is what protects the Graphic Silhouette actor from morphing | `custom_nodes/ComfyUI-LTXVideo` | INSTALLED & LOADED | ComfyUI node initialization ledger |
| **Google Flow driver** - zero-credit multi-reference generative diffusion over an ACTIVE Chrome CDP session (port 9222); the CDP path is the ONLY live one - `src/cdp-driver.mjs` + `dag-engine.mjs` behind `mcp/server.mjs`; preserves project canvas ratio or enforces 9:16 / 16:9; optional FFmpeg `-vf reverse` for pixel-exact ending-frame handoffs, which is what makes a SEQUENTIAL plate chain possible (plate N seeds plate N+1) | `tools/google-flow-driver/src/cdp-driver.mjs`, `scripts/run-batch.mjs` | LIVE - needs Chrome running with remote debugging. **Re-fitted 2026-09-04 for the Flow redesign:** host is `flow.google.com` (matched by project id across both hosts), media CDN is `flow-content.google` / `/asb/`, the `@` picker is a listbox with a focus-stealing search - a Character is typed-to-filter, selected, then **Add to prompt**; a file reference is reused by filename from project assets or uploaded via the picker's file chooser; the prompt is set BEFORE references because both land as `mention-chip`s in the composer; the download rejects any candidate that perceptually matches a reference (uploads re-render as new CDN images and are re-encoded, so byte hashes miss). A missing character chip throws before submit. The `video-engine` MCP wraps this same driver | two A0 model sheets rolled at 0 credits; `provider-jobs/finance-host-model-sheets-001` roll_log |
| **Video engine MCP - TWO surfaces, verified by live handshake 2026-09-03** | `tools/google-flow-driver/mcp/` | **`server.mjs` -> `video-engine`** (4 tools): `create_flow_video`, `create_flow_batch` (`scenes[]` + `outputDir`), `get_flow_status`, and **`create_comfy_parallax_video` (`imagePath` + `outputPath`) - the ONLY MCP route to the parallax engine**. **`index.mjs`** (11 tools) drove the queue/bridge pipeline and is **RETIRED** with the extension lane below  Both registered in the tracked `.mcp.json`; they load on session start and resolve the SDK from the driver's own `node_modules`, so a fresh checkout needs `npm install` in `tools/google-flow-driver` first | both answered `tools/list` over stdio; the 4-tool and 11-tool sets above are what they actually returned, not what a doc claimed |
| **Flow stdio dispatcher + character A/B** (2026-09-08) — `create_flow_image` over the MCP's stdio JSON-RPC, one order file per prompt (`omni-video/dispatch_crossings_map.py`, `*.order.json` v1-v8), the SAME prompt run against `@MikeMasterV3` / `@Mike2` / `@HollowStickMike` and judged side-by-side (HollowStickMike is the truest at small size; the style atom is E39's amended line). Routing (E57): an episode's plate set is a bridge WORK-ORDER to Gemini; a couple of plates are driven from here. Five driver drift fixes (Agent mode hides the pill, the Characters view, the grid refill baseline, `fileURLToPath`, the transient pill retry) are ON MAIN (2026-09-08, operator: "move those all to main"; the interim patch file is retired) | `tools/google-flow-driver/mcp/server.mjs`, `src/cdp-driver.mjs`; the short's `omni-video/` | LIVE 2026-09-08; the multi-agent MCP (a queue per session) is a PRP not yet written | map v9 (the operator's Flow edit) approved 2026-09-08 |
| **Chrome extension + native messaging host + the `flow-queue` MCP surface** - the pre-CDP bridge: a packaged extension (`extension/`), a native messaging host (`native-host/`) and 11 queue/bridge tools (`flow_enqueue_batch`, `flow_bridge_status`, `flow_capture_*`, ...) behind `mcp/index.mjs` + `mcp/tools.mjs` | `tools/google-flow-driver/` - 17 files, 238 KB, plus 8 tests (`capability-capture`, `content-bridge`, `native-protocol`, `image-runner`, `video-runner`, `selectors`, `state-machine`, `media-observer`) | **RETIRED 2026-09-03 (operator): obsolete to the MCP.** The CDP driver reaches Flow directly, so the browser-extension bridge it was built to cross no longer exists as a boundary. Unregistered from `.mcp.json`; the code is kept in git rather than deleted, on the same rule as the whiteboard reveal engine - a superseded mechanism is recorded with its reason, and the reason is what generalises | `server.mjs` imports `dag-engine` + `parallax-runner` and holds zero bridge references; `tools.mjs` is bridge-shaped throughout |
| **Video perception (`/watch`)** - acquisition via `yt-dlp`, frame extraction via `ffmpeg` (scene-aware or keyframe), timestamped transcript from native captions or Whisper | `.agents/skills/watch/` | LIVE & STANDARDIZED | yt-dlp 2026.08.19, ffmpeg 8.1.2 |

**Motion-gate standing:** ambient loops are TEXTURE, not authored events - they never
satisfy M01/M03/M08/M12, or they launder the stillness those gates were built to catch
(E21, E25). A parallax move counts as plate life only when it is bound to a narration
anchor, the same rule as narration-keyed chart draw and `narration_key_delays`.

## Evidence & assets

**Martial Matters episode 1** (merged from p16, 2026-08-29): 192 asset files
(1080p plates), recorded narration + word timings
(`projects/martial-matters/.../transcript/narration.words.json`), the
Marshall Monday editing handoff. The third channel's pilot — indexed in the
plate library under channel `martial-matters` (2026-08-29), semantics from
the word-timed cue ledger, state `candidate` until an approval manifest is
read.


| Capability | Where | State | Proof |
|---|---|---|---|
| **Flow character pack** — the host identity as a validated, hashed, non-renderable contract: `finance-host-v1` (retention: full suit detail, the recognised *woodblock print + vox newspaper, rich anime colors* directive) and `finance-host-stick-v1` (acquisition: locs, glasses, goatee, indigo/copper only); each with front/profile/three-quarter/back, five named expressions and five named poses — the rows double as the cutout rig's slot enumeration. The service FAILs an unenumerated sheet or an orphan variant; tests pin the survivors, the dropped detail, and that the paper-toy medium stays out | `projects/systems-and-blowups/finance-host-flow-character-pack.v1.json` + `configs/flow_character_pack.schema.json` + `src/services/flow_character_pack.py` | LIVE 2026-09-04 (A0 closed) — both sheets operator-approved, promoted to `assets/generated/host/`, catalogued; **`@Mike`** (retention, entity `dab5d902`, bound FROM the approved sheet; the original master binding survives as `@MikeMasterV3`) and **`@StickMike`** (acquisition, entity `fe6a3f2b`, cream deckle page) both bound in Flow project `d171ec1f` with verified chips. A character edit cannot adopt a new reference - it re-renders from the old one; bind from the file | `tests/test_flow_character_pack.py` (7); `provider-jobs/finance-host-model-sheets-001.google-flow-job.v1.json` |
| **THE RAILWAY YARDSTICK** — recurring channel instrument: tech share of ALL US capital formation (narrow 28% all-time high vs 23% dot-com; broad 65%; UK railways ~50% one-technology reference); quarterly updater + dated readings log | `content/video_engine/instruments/railway-yardstick/` | LIVE — operator-adopted 2026-08-30; tripwire pending operator decision | readings.jsonl seeded |
| **Plate library** — 326 plates indexed by SEMANTIC across all worktrees and CHANNEL-AWARE (money-physics 134 / martial-matters 192); channels are identity walls — the resolver refuses cross-channel plates; status from manifests, never paths | `content/video_engine/scripts/build_plate_library.py` → `sources/PLATE-LIBRARY.json` | LIVE — rebuild after any plate wave | resolver falls through to it |
| **Chart builders** — real-data charts (yfinance/FRED), verbatim end labels, month/year axes, series sidecars | `steel-and-paper/evidence/build_evidence_documents.py` (+ railway, HBM builders) | LIVE | ev-divergence-v1 |
| **Live HTML evidence sources** — karp/leases/macdonald records, instrument-memory, mechanism-ladder, three-manias | `steel-and-paper/evidence/*.html` | BUILT — PNGs are flattened renders of these | files on disk |
| **Teacher-stamped catalog** — 86 production slides keyed `image_id` → `extracted_path` | `sources/decks/teacher-stamped-production-visuals/` + manifest (MAIN checkout) | LIVE | episode-build skill §5 |
| **Two-tier palette** — graphic tier for lines/fills, lifted text tier for numerals on dark pills | template `:root` + BUILD-PIPELINE.md | LIVE — never graphic-tier text on dark | contrast 5.7–7.4:1 measured |

| **VERDICT STACK species** — N proofs fly in from depth over the world plate, word-matched; hyperframes focus hand-off (active card LARGE center-stage for its phrase, recedes to rail on the next beat); radial burst on the pivot **Use when:** the sentence lists N proofs of one claim in a row ('three things say so') - each flies in on its word; a list, not one chart | `samples/scene-evidence-player.template.html` (stackbox / drawStack; dock species `stack`, member ids resolve against asset-data) | LIVE — nine-proof wall at s67-70, operator-approved | doc 29 §9.24-9.24b |
| **Chart self-containment gates** — auto-fit checklist columns, skew-pivot highlighter sweeps, nowrap pills, mark backing chips + dotted leaders, named reference series, log-chart date ticks, header shrink-then-squeeze, recap fill (sub-12s checklist hold = 0.8s/row) | `samples/scene-evidence-player.template.html` (template-enforced, every chart inherits) | LIVE — mute-the-narration test is the acceptance gate | doc 29 §9.23 |

## Script & voice

| Capability | Where | State | Proof |
|---|---|---|---|
| **Strength loop** — multi-scale fixpoint (L0–L6 + X1–X5), rewrite budget, oscillation escalation | `patterns/STRENGTH-LOOP.md` + script-writer skill | LIVE | Script F |
| **Doctrine audit + pattern lint** — timed gates from text via dual rate estimators (16.29 c/s, 170.9 wpm) | `scripts/audit_script_doctrine.py`, `lint_script_pattern.py`, `kit_spec.py` | LIVE | |
| **Opening-structure gate** — G01–G44 + J01–J11: the doc-38 / P1 / P2 shape as a real gate (3s grab, 8s paradox, "you" by 0:30, mini-payoff then the promise by 0:60, A1/A2/A3 with A3 = 10% of runtime (E23), classical nodes as DECLARED beats via `beat_tags.py` — archetype, desire, opponent, map, catalyst, debate, signpost, head-fake, loop/loop-close, tricolon, anaphora, reflect, dip — G36 the retention cycle over the WHOLE runtime, G44 one rehook per unit, G34/G35 concession + hedge, JUDGE rows printed never passed) | `scripts/gate_opening_structure.py` + `beat_tags.py` + `kit_spec.a3_anchor_s` / `unit_windows` | LIVE — ep1 baseline 27 FAIL (promise at 1:20, no declared beats, G36 352s from 7:33, G44 unit 4) | `tests/test_gate_opening_structure.py`; CHECK-RESPONSIBILITIES §2 |
| **Script-gate RUNNER + recording refusal** — ONE command runs lint → audit → opening gate → screens through each tool's own `main()`, writes `<script>-GATES.md` (the §5 TOOLS block, every stdout verbatim, `script_hash` of the spoken text, `VERDICT`), exit 1 on any FAIL; both recorders REFUSE a missing / stale / FAIL report (`--force "<reason>"` records and logs the reason into the take manifest); the audit defers doc-38 beats 1–4 to the gate when the report exists (one row, one verdict) | `scripts/run_script_gates.py` + `record_chained_take.py` / `record_master_take.py` + `audit_script_doctrine.py` (`_doc38_opening`) | LIVE (P34 T1/T6) — ep1's promise-at-1:20 could not have been recorded | `steel-and-paper/SCRIPT-G-GATES.md`; `tests/test_run_script_gates.py`, `test_audit_defers.py` |
| **Opening-minute gates (E24)** — the analyst's drop-off review (operator-verified against analytics) as gates, doc 29 §9.29–9.30: G45 packaging echo — the title/thumbnail content words (`--title`, `--thumb`) as the mechanical proxy for "the first sentence answers the thumbnail", J12 prints `--thumb-file` for the agent to open; G09 WARNs a promise past 0:45 (DECISION R7 vs doc 38's 0:60, the FAIL past 0:60 stands); M10 no still stretch > 6s begins in the first 60s; M11 the first chart enters 0:08–0:20 with a spotlight / callout / punch / focus_zoom within 1.5s (+ sound cue or WARN); M12 a chart dock never spans a scene boundary and holds ≤ 10s / ≤ 6s in the opening — re-enter it spotlit (E25: the chart is the proof, not the homework); the runner passes `--title/--thumb/--thumb-file` through **Use when:** every cut's first minute, before the watch - the package answered by sentence 1, the first chart lit by 0:20 | `scripts/gate_opening_structure.py` (G45 / J12 / G09) + `scripts/gate_motion_density.py` (M10–M12) + `run_script_gates.py` | LIVE 2026-09-03 — ep1 red on every row: G45 (none of ai / bubble / real / survives / steel / paper in the first two sentences), M10 3 stretches (0:16+10s, 0:33+16s, 0:57+14s), M11 ev-bravos-original-v1 at 9.5s full and unannotated, M12 25 chart docks held as homework (bravos 0:09–0:50 across 3 plates); opening gate 28 FAIL, motion gate 8 FAIL | `tests/test_gate_opening_structure.py`, `test_gate_motion_density.py`, `test_motion_gate_wiring.py`, `test_run_script_gates.py`; OPERATOR-RULINGS E24 / E25 |
| **G2 short mode (doc 51 s51.2 as gates)** — a MEASURED clock under 3:00 (or `--short`) routes `gate_opening_structure` to the shorts shape instead of the long-form geometry: S01 hook lands in 0:00–0:03; S02 the mechanism (`[post-key]` sentence) by 0:10; S03 ≥ 2 instances (`[new]` / `[catalyst]`) inside 0:10–80%; S05 the ring (token AND the claim's stems, G15b at the short's fraction) in the last 20%; S06 a rehook every 30 s; S07 the brand line never in a script (the outro carries it); S08 WARN a sentence over 18 words (the two-line caption gate); J50 the key lines read as ONE mechanism; J51 the instances share a spine by stem (46 s46.4 / G-g); the platform gates G04/G05 and G45/J12 stay. The runner decides short ONCE (`is_short`) and passes it to the audit too, where doc 35 rule 2 (the late-stage flip) and the MAP sec 1 P1 pin become INFO — a short has no late-stage flip (operator 2026-09-04). The long form is untouched: an estimated clock never routes short | `scripts/gate_opening_structure.py` (`run_short`, `SHORT_*`), `run_script_gates.py --short/--long`, `audit_script_doctrine.py --short` | LIVE 2026-09-05 — the Tokyo take (1:21 measured) routes by itself: 0 FAIL / 1 WARN (S08, the 27-word Meta sentence) / 9 PASS / 3 JUDGE; audit 0/0; the 17 long-form FAILs were geometry, not the script | `tests/test_gate_opening_structure.py` (short green / red / routing / the Tokyo fixture), `test_run_script_gates.py` (`--short` reaches both checkers, `--long` still binds the flip rule) |
| **Docs index (retrieval layer)** — one flat, greppable record per heading under `docs/` (path, line, level, doc id, heading, lead line, up to six bold labels) plus every CAPABILITIES and BACKLOG table row as a record; `rg -i "<term>" docs/DOCS-INDEX.jsonl` returns `path:line` in one call, then `sed -n` the window. SigMap stays the code-symbol index (it ranked the retired Manim scene for "animation math"). Since the same evening also `terms`: up to 12 body-vocabulary search terms per section (inline-code spans, hyphenated / CamelCase tokens, symbols, citations like `Deegan 1997`), so `rg -i "minimum-jerk|deegan|springPop"` lands on the section in one hop; 2010 records / 140 files, 770 KB | `content/video_engine/scripts/build_docs_index.py --write/--check` → `docs/DOCS-INDEX.jsonl` + `docs/DOCS-INDEX.md`; roots/excludes/includes are project-configurable (`docs/DOCS-INDEX.config.json`, `--root/--exclude/--include/--output/--repo`, CLI > config > defaults, printed on every write; runs/ excluded by default, pulled in per run with `--include`) so the cross-repo plan can reuse it; the read-only roles' "Retrieval discipline" says rg it first | LIVE 2026-09-05 — 2000 records / 139 files; built by `implementation_luna` (Opus) from a parent brief in the retrieval benchmark. **It is now one layer of seven — the measured effect of the set is the row below** | `tests/test_build_docs_index.py` (15: synthetic tree, fenced headings excluded, runs/ excluded, determinism, --check, real-docs smoke, `terms`, config roots + `--include` beating `--exclude`, a bad config exits 2 not 1) |
| **The docs retrieval layers** — the index's six siblings, run and checked by one command: the **manifest** (one record per document: bylines, every heading, labels, leads), the **topic index + citation graph** (`DOCS-TOPICS.jsonl` / `DOCS-CITATIONS.jsonl` — which docs argue a topic, and every `doc §x` cite resolved to `path:line`), the three registries — **gates** (from the checkers' `ast`, 128 records / 116 ids, scope stated in its header per P45 D8), **animation and maths** (348 formula records + 89 dials, with `implemented / tracked / retired / orphaned`) and the **craft map** (81 devices, 53 with a gate id, 32 `judge`) — the **standard audit** (`DOCS-STANDARD.md`: 241 files / 2955 headings, 97.3 % lead, 91.5 % termed), and beside the wrapper the **overlap report** (`report_doc_overlap.py` → `DOC-OVERLAP.md`: DUPLICATE vs DELTA per compressed-doc → target pair). `docs_find.py` is the query side: one compact `path:line — heading` per hit across all layers instead of whole JSONL records | `content/video_engine/scripts/build_docs_layers.py --write/--check` (runs the seven layers in dependency order, one summary line each, a missing tool SKIPPED and printed; `report_doc_overlap.py --write/--check` runs standalone) + `docs_find.py`; artifacts `docs/DOCS-INDEX.*`, `DOCS-MANIFEST.*`, `DOCS-TOPICS.*`, `DOCS-CITATIONS.jsonl`, `GATES-REGISTRY.*`, `ANIMATION-REGISTRY.*`, `CRAFT-MAP.*`, `DOC-OVERLAP.*`, `DOCS-STANDARD.md` | LIVE 2026-09-05 — **measured, same harness / model / prompts, layers added** (`evals/RETRIEVAL-BENCHMARK-2026-09-05.md`): rounds 1–2 = the Opus `explorer` before the layers, **338k tokens / 70 tool calls / 390 s — 8.5/9** with one false negative; round 4 after them, **392k / 65 / 488 s — 9/9**: the false negative gone (every negative claim now names the roots searched), tool calls −7 % overall and −25–40 % on the well-specified questions, **tokens +16 %** — the honest price of returning whole records. Round 5 answers that: `docs_find.py` cuts the multi-layer hunt **−18 %** (Q10 70,164 → 57,841) and leaves a single-target lookup unchanged, because the ~30 k dispatch floor is prompt, not retrieval. Round 3 settled the searcher: Sonnet 5 on the same five cost **238k / 25 / 336 s for 3.5/5** against Opus's **155,988 / 31 / 138 s for 5/5** — no Sonnet searcher | `tests/test_build_docs_layers.py`, `test_build_docs_index.py`, `test_build_docs_manifest.py`, `test_build_topic_index.py`, `test_build_gates_registry.py`, `test_build_animation_registry.py`, `test_build_craft_map.py`, `test_audit_docs_standard.py`, `test_report_doc_overlap.py`, `test_docs_find.py`; `evals/RETRIEVAL-BENCHMARK-2026-09-05.md` rounds 1–5 |
| **Declared-beat enumeration** — `<script>-SCREENS.md` gains a DECLARED section: every beat tag with its clock, the gate's window verdict, the sentence under it and a blank verdict column the agent fills true / laundered (rule R2 is enumerated, not remembered) | `scripts/enumerate_strength_screens.py` (`build_screens`, `--timeline/--ring/--counterparty`) | LIVE (P34 T3) — ep1 declares zero beats | `tests/test_enumerate_declared_beats.py`; STRENGTH-LOOP §8a |
| **Recorder preflight** — 16 gates: split-on-pause, tag cap, paragraph density, stacked pauses, credits-for-both-parts | `scripts/record_chained_take.py` | LIVE — both new gates FAIL on the take that shipped bad | doc 37 |
| **Pause compilation** — marks → break tags, backticks consumed, dirty-tag guard RAISES | `src/services/audio_synth.py` `compile_pause_marks` | LIVE | the 0:08 artifact class is unshippable |
| **Scratch take (stage zero)** — free full-script render before any credit: Chirp 3 HD (fast listen, key in local.env) + Kokoro-82M local (word timestamps, jump index, real chars/sec) | `scripts/scratch_take.py` | LIVE | doc 37 §16; Kokoro hook timing 1.8% off the EL take |
| **Tempo field edit** — one-pass PCM edit: dead-space caps, tighten runs, pauses, and a continuous tempo curve (speed limits at reveal anchors, 1.10x cruise, 1.6s ramps; words never split, cuts only in silence gaps) | `scripts/tempo_edit.py` | PROVEN on the probe (operator: "This is it"); chain promotion = warped-timeline emission | doc 37 §20 |
| **Checklist species** — live procedural cards: rows land narration-keyed; question cells TYPE on, answer cells take a marker-highlight sweep (technique ported from remotion-ui: cap-height band, chisel-tilt edge); per-row status colors **Use when:** the sentence walks a PROCEDURE (a question, its answer) - rows land keyed to the narration | template `C.checklist` + build-time `narration_key_delays` | LIVE — test scorecard, tripwire board | doc 29; f428993+ |
| **SCML monitor cards** — the ledger's instruments as evidence: tripwire board (FRED DFF vs Bravos' 5.50 trigger + memory trigger, contagion status verbatim) and the memory monitor (customs $/kg m/m readings) | `ev-tripwire-board-v1`, `ev-memory-monitor-v1` | LIVE | s60-s62 |
| **Whisper gate** — transcribe the take blind, diff vs script; FAIL on insertions/deletions, WER > 5% | `scripts/verify_take_whisper.py` (faster-whisper base.en) | LIVE — caught a real vocalized tail tone on its FIRST run | doc 37 §12; Script G take |
| **Defended join** — part-1 tail faded to silence after last word + generated settle; provider-appended junk dies by construction | `scripts/join_chained_take.py` | LIVE — seam measures −91 dB | doc 37; Script G |
| **Edit-pause insertion** — the ~3-tag practice's owed silences cut into the take at verbatim anchors; timeline shifted | `scripts/insert_edit_pauses.py` | LIVE — 15 pauses, +15.0s | Script G |
| **Retime pass** — docks pin to anchors' new word times; plates warp between control points; same-slide gaps stitch | `scripts/retime_to_take.py` | LIVE — 32 pins; MUST be followed by the topic-exit audit (durations carry, topics move) | Script G |
| **Caption pages regen** — ~3-word kinetic pages with k-flags from the current timeline | `scripts/build_caption_pages.py` | LIVE | 832 pages |
| **Topic-exit audit** — E11 enumeration of every dock vs the narration it serves; exits authored to topic ends | `build-f/TOPIC-EXIT-AUDIT.md` procedure (doc 29 §9.20) | STANDING — runs after every retime or script change | 47 docks walked 2026-08-30 |
| **Choreography clash gates** — same-slot overlap + >2 concurrent docks FAIL | `scripts/emit_choreography.py` | LIVE — caught a real slot clash on first run | |

| **Pause grammar, GENERATIVE** — scans the VO for grammar classes (stat-settle→era-shift FULL; snap settles / era breaths / reveal leads HALF), diffs against the standing plan; run before every master record | `scripts/derive_pauses.py` | LIVE — Script G: 18 candidates, 1 operator full + 7 ratified halves | doc 37 §19b |
| **Micro-repair** — sub-word provider stutters excised free: envelope-localize, fade-free preview candidates, zero-cross butt splice, words.json shifted, verify from the EDITED master | `scripts/repair_microcut.py` | PROVEN — 'Builted it' two-round fix | doc 37 §23-23c |
| **Stutter auto-scan** — NEGATIVE result, do not rebuild: three envelope detectors all failed validation against the known case; ear detects, envelope localizes | (tool deleted by design) | CLOSED | doc 37 §23b |

## External evidence sources

| Source | Where | Gives | Rule |
|---|---|---|---|
| **SCML ledger** — the operator's Korea/memory intelligence base | `~/.claude/Claude Work/Claude Files/scml-ledger/scml-ledger/data/scml.db` | DART-filed financials (cross-validate Yahoo to the won), USDKRW series, **memory export tracker** (customs value-per-kg — primary-source "sold out" pricing), short interest, NPS flows, catalyst calendar | read-only for evidence; .env keys never printed; cite as "cross-checked vs DART filings" |

## Reference builds (locked)

| Reference | Where | Governs |
|---|---|---|
| **current-bubble-five-minute-v4** | `samples/current-bubble-five-minute-v4.timeline.json` (+ rendered build) | THE motion gold standard (doc 29 §9.16) — side-by-side before any motion change ships |
| **Gemini showcases** | `samples/gemini-decoupled-evidence-showcase.html`, `gemini-scene-evidence-pipeline-showcase.html` | ancestor artifacts — where doctrine CAME from; a reviewed refinement outranks them |
| **The viewer (P36)** — a blind, windowed perception test: an agent that knows no doctrine reads the script cold in 15s windows with a two-window memory; a deterministic scorer measures beat recall (a declared beat the reader never felt was laundered) and information gain | `scripts/viewer_windows.py` · `viewer_run.py` (Codex headless) · `viewer_score.py`; `run_script_gates.py --viewer-gate` | **shipped and binding** (P36 HG1 granted 2026-09-03 on the ep1 calibration: recall FAILs, confusion WARNs, gain INFO-only) | ep1: 54 windows on measured timings, all 37 declared beats placed; `test_viewer_windows.py` 17 + `test_viewer_score.py` 14 |

## The research reference layer (2026-09-04)

Docs **42–46** condense the research evidence bundle into what is applicable here.
[`RESEARCH-INDEX.md`](RESEARCH-INDEX.md) dispositions **every heading of every bundle
document** — coverage is the proof of reading — and
`python scripts/check_research_extraction.py` enforces it.

| doc | what it settles |
|---|---|
| [42-DRAWING-KINETICS](42-DRAWING-KINETICS.md) | why our stroke reads as a plotter, and the closed-form overshoot that replaces guessed timing |
| [43-SCENE-GRAPH-AND-TRANSFORM](43-SCENE-GRAPH-AND-TRANSFORM.md) | the anchor sandwich, the Z-stack, both morph methods, the cutout rig |
| [44-INK-AND-SURFACE](44-INK-AND-SURFACE.md) | alpha blending is the wrong operator for layered ink |
| [45-PARALLAX-AND-PLATE-MOTION](45-PARALLAX-AND-PLATE-MOTION.md) | the viability matrix — parallax is banned on the page — and our dial audit |
| [46-REFERENCE-RHYTHM](46-REFERENCE-RHYTHM.md) | the shot distribution recomputed, and the equation spine |
| [47-FINDINGS-TO-CHECKS](47-FINDINGS-TO-CHECKS.md) | which findings can be designed out, gated, judged — or demoted |
| [48-THE-FIGURE-AND-THE-GROUND](48-THE-FIGURE-AND-THE-GROUND.md) | how an actor moves, holds a prop, and stands on a plate without looking pasted |
| [49-GENERATIVE-VIDEO-AND-THE-VERTICAL-STAGE](49-GENERATIVE-VIDEO-AND-THE-VERTICAL-STAGE.md) | Wan/LTX/depth dials, mask pinning, and the mobile safe box our 9:16 layout violates |
| [50-THE-PHONE-IS-THE-SCREEN](50-THE-PHONE-IS-THE-SCREEN.md) | **ep1's real analytics** — the cold cohort leaves at 1:05, 75 % watch on a phone, and our type is illegible there |
| [51-THE-SHORTS-FORMAT](51-THE-SHORTS-FORMAT.md) | **one page, ship in an afternoon** — the four numbers, the shape, and a deliberately lower production bar |
| [52-CONSTRUCT-DONT-INHERIT](52-CONSTRUCT-DONT-INHERIT.md) | **the founding-premise correction** — generated plates were a substitute for the ability to construct, and a generated plate has no addressable coordinate space |
| [53-THE-STICKMAN-LANE](53-THE-STICKMAN-LANE.md) | the tutorial workflow read off the video — CHARACTER LOCK, the two prompt schemas, and why 30-page prompt packs are a missing runtime |

These are **reference, not portable**. A rule graduates to `docs/portable/` once proven
in a shipped build, and its index row says so.

## The enforcement layer (P37, 2026-09-04)

The research of 2026-09-04 as checks that run - each shown failing before passing (47 §5b).

| Capability | Where | State | Proof |
|---|---|---|---|
| **M14** — a camera move never overlaps an evidence build (entrance through the last badge settle) | `scripts/gate_motion_density.py` `_build_clashes` | LIVE in the motion gate | `tests/test_gate_punch_build_overlap.py` |
| **Comfy / parallax config gate** — dials (intensity 0.10-0.12, tiling none, ssaa >= 1.5, quality >= 80, ViT-L), Wan 4k+1 / LTX 8n+1, CFG <= 4.5, FP8/quantized checks, plate-kind matrix | `scripts/gate_comfy_config.py` | LIVE; the runner it FAILed is its fixture | `tests/test_gate_comfy_config.py` |
| **Vertical safe-box gate** — 9:16 docks in x[80,880] y[280,1340], caption in the strip; static CSS reader + rendered rectangles | `scripts/gate_vertical_safe_box.py` | LIVE; FAILs the pre-T0 template verbatim | `tests/test_gate_vertical_safe_box.py`, `tests/test_vertical_safe_box.py` |
| **Transform lint** — scale/rotate without a declared origin | `scripts/lint_template_transforms.py` | INFO ladder; 11 listed on the template | `tests/test_lint_template_transforms.py` |
| **G15b ring mechanism** — the close returns the P1 claim's content stems, not just the token | `scripts/gate_opening_structure.py` | LIVE as FAIL (no grandfathering, operator 2026-09-04) | `tests/test_gate_ring_mechanism.py` |
| **Grounding gates** — eye-line on the horizon, feet-anchored sprites, solver per contact beat | `scripts/gate_grounding.py` | INFO-then-FAIL ladder (shot table carries no horizon/contact yet) | `tests/test_gate_grounding.py` |
| **Portrait parity gate** (2026-09-08) — operator: "how do we make sure that anything that is hard-coded to landscape is actually responsive to mobile?" A lint on the template (no 1920/1080/960/540 literal outside the stage constants and an allowlist) + a runtime check on a 9:16 golden surface (every SVG under `#stage` carries the stage's viewBox; every full-stage layer's box IS the stage; the worlds 1.10x centred by design). Both bugs of the night (the under layer's landscape viewBox, the camera centred at 960/540) would have been refused | `tests/test_portrait_parity.py` | LIVE 2026-09-08 | 2 tests; caught `SPOT_R`, the camera centre, the under layer |
| **The recall receipt** (2026-09-08) — a commit touching the player template, `kinetics/`, the timeline compiler, the motion gate or a short's `build_short.py` must carry `Recall: <path>:<line>` (or `Recall: docs_find 0 hits for <term>`) naming a record that exists; the `commit-msg` hook refuses it otherwise. Installed in `.git/hooks` (shared by main and every worktree). The rule and its order: `docs/runbooks/RECALL-RECEIPT.md`; the triggers in CLAUDE.md's fast routes and `docs/AGENTS-VIDEO-ENGINE.md` rule 4 | `scripts/hooks/recall_receipt.py`, `.git/hooks/commit-msg` | LIVE 2026-09-08 (operator: "install the hooks") | every mechanism commit since 2026-09-08 prints `receipt ok` |
| **M23 — chart transitions** (2026-09-10, P48 T6) — every `chart_to` listed with its scene, verb and clock; WARN inside the page's build beat or within 0.5 s of its exit; FAIL on a page with two chart states and no transition between them; a transition's end is a landing (E51, M22) and a data mark (E50, M21); a park is neither | `scripts/gate_motion_density.py` (`_transition_gate`) | LIVE in the motion gate | `tests/test_gate_motion_density.py` (4 rows) |
| **Muted-caption judge** — a model reads the captionless frame; verdict + `scenery` / `over-dense` diagnosis owned by code | `scripts/judge_muted_caption.py` | CLI, unwired; **one headless Codex run per manifest** (the viewer's lane), tests replay | `tests/test_judge_muted_caption.py` |

## The golden-frame harness (P39 T2/T3, 2026-09-04)

Four committed surfaces - ledger page mid-build, chart with a callout, 16:9 dock pair,
9:16 dock pair - each a small synthetic timeline instantiated from **our** template
(`tests/golden/sources/`), rendered to a committed frame (`tests/golden/frames/`).

```bash
python content/video_engine/scripts/render_baseline.py --check     # PASS, or one line per changed surface + a diff PNG
python content/video_engine/scripts/render_baseline.py --update    # deliberate refresh after an approved change
python -m pytest content/video_engine/tests/test_golden_frames.py content/video_engine/tests/test_render_determinism.py -q
```

A failure names the surface and writes `tests/golden/diffs/<surface>.diff.png` (golden |
actual | diff x8). The suite includes the perturbation that proves the harness catches a
one-value CSS change. The capture is deterministic because `prepare_page()` neutralises wall-clock
transitions, the fit-scaled stage and unawaited fonts - and `render_episode.py` captures
through the same function at native 2560x1440 (B8 fixed, P39 T6). Runbook:
`docs/runbooks/RENDER-REGRESSION.md`.

## The frozen baseline (P39 T1, 2026-09-04)

The last known-good player is tagged **`player-baseline-2026-09-04`** (annotated; the
template last changed in `3a0e092` and is byte-identical there). If a render regresses and
nobody can say why, get back in one command:

```bash
git checkout player-baseline-2026-09-04 -- docs/content-video-engine/samples/scene-evidence-player.template.html
```

Proven on tagging: `git hash-object` of the working file equals `git rev-parse
player-baseline-2026-09-04:<path>` (`a4705a4a`). Golden frames and per-capability flags
are P39 T2–T4.

## What is NOT built yet

[`BACKLOG.md`](BACKLOG.md) — the hand-maintained open work, and the scope shift
behind it. This file says what exists; the backlog says what is asked for and
what is blocked on an operator decision. `STATE-OF-WORK.md` is neither: it is
the auto-generated worktree census.

## The recall rule

1. `ls` and read the index before searching (PIPELINE.md, this file, PLATE-LIBRARY, CHOREOGRAPHY).
2. When a doc cites a source artifact, the audit reads the artifact.
3. A capability added or retired updates this file **in the same commit**.
4. A capability that ships **closes its BACKLOG.md row in the same commit** — a
   backlog that outlives its work is worse than none.
