# Backlog — content video engine

Hand-maintained. `docs/WORKTREE-REGISTER.md` is the board of every checkout (P62); this is the
work itself. Rewritten 2026-09-04 after the full research-bundle read.

**Route:** capability that exists → [`CAPABILITIES.md`](CAPABILITIES.md) · what the
research settled → docs [42](42-DRAWING-KINETICS.md)–[46](46-REFERENCE-RHYTHM.md), indexed
in [`RESEARCH-INDEX.md`](RESEARCH-INDEX.md) · which findings can be enforced →
[47](47-FINDINGS-TO-CHECKS.md) · what is waiting on a DECISION (the open human gates and
every open question, 2026-09-12) →
[`OPEN-GATES-AND-QUESTIONS-2026-09-12.md`](OPEN-GATES-AND-QUESTIONS-2026-09-12.md).

## The founding-premise correction (52)

> *"The stick figures work because you can build the world you need to convey; I was
> trying to avoid building that world because I didn't have the skill to do it, so I tried
> to inherit it by image generating plates."* — operator, 2026-09-04

**Generated plates were a substitute for the ability to construct — in the evidence lane.**
*Scoped 2026-09-04: this holds where a chart or callout must sit somewhere specific. For an
illustrative scene where the image IS the claim and nothing is docked to it, generation is
correct and cheap (52 §52.3).* The technical form:
a generated plate has **no addressable coordinate space** — a diffusion model decided
where everything is, so you cannot place a callout against a pillar you cannot locate. A
constructed scene has `M_world = M_parent × M_local` and a Z-stack; every object has an
address, and a prop can sit `at: "datum"` because both live in one coordinate system.

**This explains E25 retroactively.** *"A chart never survives a plate change"* reads as a
motion rule; it is actually scar tissue from the plate not being addressable. On a
constructed page it is unnecessary.

**And whiteboard animation — the original intent — is not a new capability.** It is the
curvature stroke (P38 T2) plus the hands (A5) plus ink (44), composed. See
[52-CONSTRUCT-DONT-INHERIT](52-CONSTRUCT-DONT-INHERIT.md).

## The scope shift, named

The engine was scoped to **assemble plates and dock evidence over narration**. It is
becoming **a narration-timed 2D animation system**. What did it:

1. **The chart engine was never chart-specific.** `drawOn` draws any path;
   `resolveTarget` resolves against data.
2. **The ledger page is a working surface**, so metaphor → evidence is a transformation
   rather than a cut.
3. **Cuts belong in acoustic gaps** — and our cut *rate* already matches the reference,
   so this is placement only, at no structural cost.
4. **The reference composites; it does not generate.** Proven forensically — three
   incompatible illustration styles on one canvas. The actor is a rig with swappable
   slots, not a per-shot image.

Consequence: **props, not world plates, are the unit of visual work** — and the actor
joins them.

---

## The ground truth we hold: one retention curve — and now the analytics behind it

**Updated 2026-09-04 with operator-supplied YouTube analytics.** See
[50-THE-PHONE-IS-THE-SCREEN](50-THE-PHONE-IS-THE-SCREEN.md).

| | |
|---|---|
| **cold cohort** (arrived from an impression) | **1:05 average view duration** |
| **everyone** | **4:39** |
| **device** | **75.2 % mobile phone** |
| suggested-video traffic | **11.5 %** — search + channel + direct is 76.9 %, i.e. people came looking |
| funnel, 2 days | 77 impressions · 6.5 % CTR · 5 engaged views |

**The finding that reprioritises everything: our type is illegible on a phone.** A 16:9
video in YouTube's default portrait player is a 4.9× downscale, so a font must be ≥ 59 px
on our 1920 stage to reach 12 px on screen — and **14 of the template's 16 sizes fall
below that.** Axis labels render at ~5 px for three-quarters of our watch time.
It is arithmetic, not yet observation: **render one frame at 390 px and look.**

**The uncomfortable version: we spent the week making the drawing better, and the most
likely reason people leave may be that they cannot read it.**

## The retention curve, and why n=1 still binds

Ep1 is still the only episode with a retention curve. Its curve drops at **0:45–1:00**. That is the whole of our evidence about actual
viewer behaviour — everything else here is inference from shot tables, comparison against a
reference, or agent judgment.

`SCRIPT-G-VIEWER-CALIBRATION.md` used it to calibrate P36, and the result is the precedent
this backlog runs on: **information gain scored 5 at the drop window against an episode
median of 3** — green exactly where the video failed — so it was retired to INFO rather
than promoted. Recall and confusion found the window, so they carry authority.

**The asymmetry matters and P40 inherits it: one curve is enough to *kill* a measure, not
enough to *bless* one.** A metric that lights up at 0:45–1:00 could be coincidence; with
four metrics and one event, something will correlate. Any threshold from n=1 is a
hypothesis until a second episode's curve exists.

One more prior worth carrying: the calibration found the script is **information-dense
everywhere, including where it loses people** — *"density is not the problem, so a density
measure cannot find it."* That lowers the odds that a motion-energy metric separates w3
either, and P40 should expect a null result rather than hope for a hit.

## The shorts question, 2026-09-04

The operator, on the mass-produced listicle format (*"You can become a millionaire just
by starting these 10 businesses"* — one figure, ten icons, slow zoom, bold yellow caption
per item):

> *"It's hard to not just want to pump out stick figure content... I'm trying to add value
> everywhere all at once and getting bogged down in production... But I suppose if we get
> the drawer right — even drawing shorts like that just becomes a part of the drawer."*

**The closing line is the resolution.** That format is not a different business; it is
*our engine with the complexity dial at zero*. One page, ten props, ten captions, a slow
zoom — that is the object page (T5) plus the prop library (A2) and nothing else. Once the
drawer exists, the format is nearly free.

**And it is not beating us on quality. It is beating us on the one thing 50 §50.2 says we
fail:** every element in it is enormous. One idea per beat. It is tuned to a phone by
construction, which is exactly what our 5 px axis labels are not.

**The error is not too much quality — it is uniform quality.** Doc 29's bar is applied to
everything. A listicle short needs legible type and one clear image; it does not need the
deckle, ARAP, or a curvature stroke.

**And this was already ruled on, then not applied.** The shorts lane has a phase-1
standard (hook + compression + dissolves + Ken Burns + doc-29 captions in tokens + safe
zone; generated plates and chart overlays **rejected**, yen short, 2026-09-03). Against
that, **Tokyo is 11 evidence pages, 10 beat tags, 290 words and 118 s against a 90 s cap**
— a compressed long-form episode wearing a short's runtime. 49 §49.6 independently says
short-form wants **exactly one mechanism**.

**Four things now say the same thing:** the operator's own shorts ruling, 49 §49.6's
cognitive atomicity, 50 §50.2's legibility floor, and the format he is looking at.
**Shorts are a different machine and we have been building them on the long-form one.**

**The resolution is sharper than "two products, two jobs": the format carries substance
and we already hold the proof.** *Wealth Logic* is the same listicle shape — "6 ways rich
people make money with debt" — and every item is a real mechanism with real arithmetic.
**262,684 views.** The format is not the slop.

What separates them is the spine. Finance Theory's ten items share nothing — ten nouns,
no reason it is those ten or ten rather than twelve. Wealth Logic's six are
`Spread = (Return − Cost) × Leverage` six times. **One is a pile; one is a thesis**
(46 §46.4, gated as G-g).

And they are not winning on *no* merit — they win on legibility, completion, aspiration
and cadence. Those are real crafts, just not informational ones. Naming them as craft is
what makes them learnable. We cannot beat them at aspiration and should not try; we can
be the one that is true, in a format people already know how to watch.

**Plates are a retention asset, not an acquisition asset** (operator, 51 §51.7). A
semiconductor fab in woodblock rewards someone who already chose the channel; it asks a
stranger to interpret before they have agreed to care. **We hold 326 retention assets and
zero acquisition assets** — hence A2a.

**Motion is not animation, and the shorts format needs no new capability** (51 §51.8).
Every event that satisfies E21 — scene boundary, dock entry, badge reveal, stage caption,
page build, targeted species — is free, deterministic and already shipped; the gate's
docstring explicitly excludes Ken Burns drift as "what a viewer reads as stillness". The
stick-figure format is a page flip, a caption pop and a slow push: three shipped event
types. **It needs icons and a script, not an engine.**

**→ [51-THE-SHORTS-FORMAT](51-THE-SHORTS-FORMAT.md) is the one-page spec.** Shorts fund
the long-form's existence: reach subsidises depth.

*Action: the shorts-lane standard is in memory and in a ruling but not in
`docs/portable/OPERATOR-RULINGS.md`. It should be, before the next short is built.*

## The standing frame: we lose people in the first minute

Operator ruling 2026-09-04: *our failure is holding the audience; we never even get the
chance for a long hold.* Everything below is ordered against that. Richness in the middle
is a luxury for an audience that already stayed.

---

## Blocked on an operator decision

| # | item | the decision |
|---|---|---|
| ~~B1~~ | ~~Tokyo short is 118 s against a 90 s cap~~ | **CLOSED 2026-09-05** — v11 records at 82 s (the head-fake, debate, threshold/flip and the triad cut; E40 #4). |
| B2 | **3.9 GB pool + `f10b` worktree** | 18.4 GB, of which 8.68 GB is regenerable animatic frames. Needs a home that is not a worktree. |
| B3 | **~350 unpushed commits (348 on 2026-09-05)** | `main` is local-only since 2026-08-30. A week of doctrine, the generative stack, the portrait page, the vortex, the ambient lane - all on one disk. |
| B4 | **Gitignore on `review/`** | 46 plate waves' approval manifests are untracked. Narrowing the ignore to images tracks the manifests. |
| ~~B5~~ | ~~Speech-rate contradiction~~ | **WITHDRAWN 2026-09-04 — there was no contradiction.** The "145–165 doctrine target" was mine; the operator asked for its origin and it has none. The doctrine's actual number is **140, an estimation constant** for pre-TTS duration guessing (`04-STORYBOARD-CONTRACT.md`: *"an estimate, not a promise"*), never a delivery rate. We run near 180 and always did. See 46 §46.6. |

## The day's read — 2026-09-05 evening (the Tokyo short is done: 1:28.8, motion gate PASS)

**What landed today.** P43 complete (the stroke ON, the spring, the squash ON, K-M ink kept for ink over ink); the Remotion kit
rescued and its dark card on the tail; the brand line recorded once and paced; the water on the drain and the warps; the beds at
the platform's LU rule and the press pack; the mount (a dance: the world fades above, the cream builds beneath); phrase/kinetic
captions with the keyword box; the two-line word gate.

**What we learned (each has a memory or a doc line).** A filter chain does not make ink - the image does, the engine keeps the
motion (soak-ink-is-a-plate). Anything that moves word to word pins the eye; a flash is an event, not motion; energy is continuous
voice-timed motion (caption-energy-lessons). The vertical word gate is lines, not words. Judge motion in LIVE play from the
player's Play button (the in-app pane throttles frames while hidden). A whoosh warped to the transition's own clock reads as the
transition. A mount is a dissolve on a word, not a cut, so M13's gap rule does not apply. Sound files must be gain-matched before
a cue gain means anything (whoosh-2 sat 13 dB under the rest and four cues were silent for a day).

**Next, in order of leverage.**

| # | item | why | cost |
|---|---|---|---|
| ~~1~~ | ~~The script skill is behind the gates~~ | **DONE 2026-09-05 (P44)** - SKILL.md rewritten against the gates as shipped (Stage 0 long/short, G15b, G2's S01-S08 + J50/J51, M13, E40, E41, the viewer's real three-tool order, the runner's real flags), `references/SHORTS-SHAPE.md` added; the kit gained the shorts section + hard gates 6-8 (v1.2); the three rulings the skill had to cite landed as E41 | — |
| ~~2~~ | ~~G2 short mode in `gate_opening_structure`~~ | **DONE 2026-09-05** - `run_short` (S01-S08, J50/J51) routes on a measured clock under 3:00; the runner decides once and the audit's flip rule / P1 pin are INFO on a short; the Tokyo take: 0 FAIL / 1 WARN / 9 PASS / 3 JUDGE, audit 0/0. The pattern kit's shorts section is P44's | — |
| ~~3~~ | ~~The full render of the short~~ **RENDERED and APPROVED 2026-09-06** (operator: "approved"; record `build-short/render/APPROVALS.json` with the sha256) - `build-short/render/tokyo-short-full-1440p.mp4` (1440x2560, 30 fps, 88.83 s, four workers, ~4 min). The command, from the repo root with `serve_player.py 8731 build-short` up: `RENDER_ASPECT=9:16 RENDER_BUILD=<build-short abs path> RENDER_URL=http://127.0.0.1:8731/player.html RENDER_NAME=tokyo-short python content/video_engine/scripts/render_episode.py` - **without `RENDER_ASPECT=9:16` the portrait stage is captured into a landscape viewport** (2560x1440; that is what the first attempt produced). The viewer re-read on the final take landed the same day (E43). Still open: the muted-caption judge (V-a) on the render | the render proves the player | done / an hour |
| 4 | Backlog 9: the soak as a plate reveal | the operator's ideal for the ink | one image batch + a mask mode |
| 5 | X0 / G-o: read a 9:16 frame at 390 px | still the cheapest gate in the set | minutes |
| 6 | B3: ~380 unpushed commits on main | operator decision | — |

## Ready to pull in — read 2026-09-05 after the hands-on day (operator: "see if we're ready to pull anything else in")

Ordered by leverage against what the Tokyo build now exercises. Each names the artifact it lands on.

| # | item | why now | cost |
|---|---|---|---|
| ~~1~~ | ~~T1 curvature-reparameterised stroke in `drawOn`~~ | **LANDED flag-off 2026-09-05 (P43 T2)** - six draw sites, the sliding mask's failure is a test, flag goldens captured; **ON in the Tokyo short** - the operator ruled from the side-by-side clip in motion, 2026-09-05 ('curvature stroke should be on') | — |
| 2 | **X0 / G-o: render a 9:16 frame at 390 px and read it** | the portrait page set its type from doc 50's arithmetic; the arithmetic has never been looked at on a phone-sized frame | minutes |
| 3 | **V-a the muted-caption judge on the short** | shipped unwired; the short is compiled with the promise plate and three pages - the first build worth judging | an hour (Codex CLI batch, E37) |
| ~~4~~ | ~~G2 short mode in `gate_opening_structure`~~ | **DONE 2026-09-05** - see the evening read's item 2; the long form is untouched (an estimated clock never routes short) | — |
| ~~5~~ | ~~X14 short-form pulse~~ | **DONE 2026-09-05 — operator: "the gate is the pulse; we could have much more animation and it would be fine."** `M16`: on a short no gap between visual events over 2.5 s, no ceiling; INFO on long form. The Tokyo short FAILed it on first run (a 2.5 s hold at 1:20, the ring clip's tail). **CLEARED 2026-09-05: the Remotion kit outro** (rescued from a session scratchpad into `content/video_engine/remotion-kit/`; the dark `outro-v2` card the operator showed) is appended after the last word - wipe at 1:22.1, runtime 1:28.3, audio padded, a declared `life` species carries the pulse; motion gate PASS, longest gap 2.4 s | — |
| ~~6~~ | ~~G-h Kubelka-Munk on overlapping ink~~ | **LANDED 2026-09-05 (P43 T3), RULED OFF for the soak the same day** after six rounds in motion (mesh, grain, creep, step motion): 'our original applications were better, with the ink pooling/blotchiness ... the ideal feeling is that we actually paint it with ink. The fade in from cream to the ink surface has still been one of our best variants' -> the alpha soak ships; K-M kept behind the flag for ink over ink | — |
| **9** | **The soak as a PLATE REVEAL** - a generated ink-on-cream plate (E40: the image model carries the ink - pooling, rims, grain) revealed by the growing stains as a MASK, the stepped clock and jitter (`soakStepped`, `soakJitter`, shipped behind km_ink) driving the mask, the cream-to-ink plate fade (`field_plate`, 2026-09-03, 'one of our best variants') as the end state | the operator's ideal, stated 2026-09-05: 'the ideal feeling is that we actually paint it with ink' - no filter chain reproduced it in six rounds; the engine keeps the motion, the image keeps the ink | one image batch (the plate), a mask mode on the soak, a before/after on 0:18-0:21 |
| ~~7~~ | ~~T3 area-preserving squash as a helper~~ | **DONE 2026-09-05 (P43 T4)** - `kinetics/squash.mjs`, the vortex through `scaleBy` bit-identically, the badge pop stretches from the spring's velocity; ON in the Tokyo short | — |
| ~~8~~ | ~~T2 the remaining damping cases + the seek test~~ | **DONE 2026-09-05 (P43 T5)** - three regimes with x, v, a; the seek test passes and an Euler spring fails it; `springPop` bit-identical | — |
| — | T4-T6 (ARAP, the object page, object -> chart) | no object page in the short; **not yet** | — |
| — | X17-X19 (Flow clip tests) | the Flow lane is paused by E40; **not now** | — |

## The plan stack

| # | plan | status | note |
|---|---|---|---|
| ~~P39~~ | **[Render baseline & kill switch](../../.claude/PRPs/plans/P39-RENDER-BASELINE-AND-KILL-SWITCH.plan.md)** | **COMPLETE 2026-09-04** — tag, four golden frames, determinism proven (and the renderer fixed where it was not: B8), six flags defaulting off, runbook | **first.** Tag, golden frames, and a flag per capability. Today there is no rollback point and no way to detect a visual regression |
| ~~P37~~ | [Enforcement layer](../../.claude/PRPs/plans/P37-RESEARCH-ENFORCEMENT-LAYER.plan.md) | **COMPLETE 2026-09-04** — 14 checks shipped or on the ladder, each shown failing first (47 §5b); G15b FAIL, judge on the Codex lane | 13 gates + V-a; T0 fixes the 9:16 docks, T9 the parallax dials |
| P38 | [Kinetics capability](../../.claude/PRPs/plans/P38-KINETICS-CAPABILITY-LAYER.plan.md) | draft | the six designed-out items; changes what the video looks like |
| ~~P40~~ | **[Measure the first minute](../../.claude/PRPs/plans/P40-MEASURE-THE-FIRST-MINUTE.plan.md)** | **COMPLETE 2026-09-04** — gap threshold from the reference (0.30 s, 0.8 of the gap); E1 metrics in numpy; the first-minute report: the drop follows the stillest window (0:30–0:45, a 21 s bare hold) and lands on the late first chart at 0:50 | E1 metrics against **the one retention curve we hold** (drop at 0:45–1:00). **T1 settles X3 and unblocks M13 with no render and no dependency — the cheapest useful thing in the stack** |
| ~~P41~~ | The vertical stage — 49 §49.1 | **COMPLETE 2026-09-05** — the portrait page (49 §49.1 landing note): the chart IS the world, type floors from doc 50's arithmetic, the caption strip on every world; plus the vortex retract / spiral return (29 §9.31), the suck, the still-life species, the since-2000 holdings page | the 3-zone short-form stage. Folds in the caption strip; X14 (cadence) is measurable on the build now |
| P43 | [Kinetics wave 2](../../.claude/PRPs/plans/P43-KINETICS-WAVE-2.plan.md) — the curvature stroke, K-M ink, the squash helper, the rest of the spring | **COMPLETE 2026-09-05** — modules + sync, the stroke (ON in the short, ruled from a side-by-side clip), K-M ink (landed, RULED OFF for the soak after six rounds in motion, kept for ink over ink), the squash (ON), the full spring; ten goldens incl. one flag-ON per capability | takes P38's T1-T4; P38 keeps T5-T8 |
| P42 | Grounding & compositing — 48 §48.7-48.8 | **backlog** | dual shadow, light wrap, substrate grain, horizon lock, ground binding. **Gives G-i/G-j/G-k a path to green** — today they check what nothing builds. Folds in actor idling (48 §48.4) and the Z-stack (43 §43.3) |

## Build — Tier 1, designed out (47 §1) → **[P38](../../.claude/PRPs/plans/P38-KINETICS-CAPABILITY-LAYER.plan.md)**

The strongest results of the day: once built they cannot be violated, because the wrong
path stops existing. Each ships with the test named in 47.

| # | item | the test that fails without it |
|---|---|---|
| ~~**T1**~~ | **Curvature-reparameterised stroke** in `drawOn`, with the κ₀ regulariser and width/ink coupling — **landed flag-off 2026-09-05 (P43 T2), human gate pending** | `v(max κ) < v(min κ)` on a cornered path; a straight line returns finite v; `w(max κ) > w(min κ)` — all pass; the sliding mask fails |
| **T2** | **Analytic spring evaluator**, three damping regimes, the only spring API. **Partly shipped 2026-09-05:** `springPop` (underdamped, with the M_p inverse) behind `kinetics.analytic_spring`, on the badge and caption pops; the critical / overdamped cases and the seek test remain. `minJerk` shipped beside it behind `kinetics.min_jerk` (the wipe front, the suck) | seek test: frame N direct == frames 0..N sequential, bit-identical |
| ~~**T3**~~ | **Area-preserving squash**, driven by velocity and deceleration — **shipped 2026-09-05 (P43 T4)** | `det(A(t)) == 1` for all t and all α — passes |
| **T4** | **ARAP morph** with polar decomposition inside | morph through >90° of rotation; `det(J(t)) > 0` at every t → P45 T1 |
| **T0** | **Fix the 9:16 dock geometry** — `scene-evidence-player.template.html:155-162`. Ours: `width 952, left 64`. Safe: `width ≤ 800, x ∈ [80,880]`. We are **136 px into the right rail** where like/comment/share sit. | render one 9:16 frame; assert every dock's bounding box is inside the safe canvas |
| **T7** | **DQS joint blending** in the figure rig (48 §48.3) | flex a joint 180° at w=0.5; assert `det(T_blend) == 1`. **Linear blend skinning returns the zero matrix** — the candy-wrapper elbow as one assertion → P45 T5 (held for D3) |
| **T8** | **Prop attachment by cached offset matrix** (48 §48.5) | pick a prop up mid-move; world position continuous across the handover frame, and the node's parent never changed → P45 T6 |
| T5 | Object-page renderer — the template draws registered props on the LP clock | spec side already ships (`--variant object`, 12 tests) |
| T6 | `object → chart` transform on one page | declared by two Tokyo pages; T4 is its prerequisite |

**Read `06_unified_ledger_drawing_engine_and_comfy_spec.md` before T5** — it carries a
`ledger_page.v2.json` contract built on our existing LP clock, plus the Z0–Z5 stack.

## Build — Tier 2, gates (47 §2) → **[P37](../../.claude/PRPs/plans/P37-RESEARCH-ENFORCEMENT-LAYER.plan.md)**

**Shipped 2026-09-04 (P37, see 47 §5b):** G-a (`M14`), G-b/c/e/m/n (`gate_comfy_config`), G-d (lint, INFO), G-g (`G15b`, FAIL - no grandfathering, operator 2026-09-04), G-i/j/k (`gate_grounding`, ladder), G-l (`gate_vertical_safe_box` + the T0 fix), V-a (judge CLI, unwired). **Open:** G-h (44's ink work), G-o (X0), M13 (P40 T1). The rows stay below as the record of what each one was built against.

| # | gate | FAILs on |
|---|---|---|
| **G-a** | punch must not overlap a build | **our renderer today** — camera motion during a data build, so saccadic suppression eats the numbers |
| **G-c** | parallax dial lint | **every value we ship today**: `mirror`, `ssaa 1.0`, `quality 75`, `intensity 1.0`, `vits` |
| G-b | parallax plate eligibility (the viability matrix) | Depthflow on the page, on @Mike, on any text |
| G-d | no unanchored transform | the diagonal-drift class |
| G-e | LTX `num_frames % 8 == 1` | a 120-frame job that silently produces garbage |
| **G-g** | **G15 strengthened** — the ring closes on the *mechanism*, not a repeated token | a close that echoes a phrase while the argument has drifted. Not a new gate; not the "equation spine" I first wrote, which was a listicle's shape mistaken for the finding (47 §6) |
| G-h | Kubelka–Munk compositing on overlapping ink | alpha blending, which is the wrong operator |
| **G-l** | **mobile safe box** — every 9:16 element inside `x[80,880] y[280,1340]` (49 §49.1) | **our shipped 9:16 dock CSS.** The one gate that fails code we already have |
| **G-m** | **generative frame-count law** — Wan `4k+1`, LTX `8n+1` (49) | an 80-frame Wan job or a 120-frame LTX job; both flash |
| **G-n** | **generative guidance ceilings** — CFG ≤ 4.5 both, scaled FP8 encoder, unquantized VAE (49) | frame burning, NaN black frames, VAE banding — a wasted render each |
| **G-i** | **eye-line invariant** — a composited figure's eyes sit on the plate's horizon (48 §48.7) | the "standing in a pit" read, the commonest tell in composited 2.5D |
| **G-j** | **zero-slip anchoring** — grounded sprites anchor `50% 100%` and bind to floor velocity (48 §48.7) | foot slide, and the floor-shear paradox |
| **G-k** | **solver declared per contact beat** — contact resolves IK, free gesture resolves FK (48 §48.1) | a pointing arc flattened straight, or a planted foot that slides |
| G1 | **M13** — cut lands in an acoustic gap | 68 % of ep1. Free: our cut rate already matches the reference |
| **V-a** | **The muted-caption judge** (operator, 47 §2b) — a model reads the test scene with captions removed and states the claim it makes | a plate that is scenery. And a FAIL is *diagnostic*: either the plate does not throw the punch, or the argument is denser than a decent model can follow — which means it is denser than the average viewer can. **The first check that tests image against claim rather than either alone.** |

## Build — carried, unchanged by the research

| # | item |
|---|---|
| ~~A0~~ | **CLOSED 2026-09-04 — contact sheet approved; both sheets promoted and catalogued; `StickMike` bound in Flow (entity `fe6a3f2b`) with a verified `@` chip.** Stick Mike is a stick figure on the cream deckle page (v5) — the house ground carries both styles. Nine attempts in the job's `roll_log`. Earlier state: Found on the way in: the finance pack had **never validated** (`fictional_presenter` was not in its own schema's enum, and only the BJJ pack had a test). Done: schema gains `back` / `pose_sheet` views, `expressions`, `poses`, `variant`, `variant_of`; the service FAILs a declared sheet that is not enumerated and an acquisition variant with no `variant_of`; `finance-host-v1` carries five expressions, five poses, front/profile/three-quarter/back; `finance-host-stick-v1` added (locs, glasses, goatee, indigo/copper only — a test enforces the survivors and the dropped detail). **Operator rulings folded in:** the paper-toy (crinkle-paper puppet) medium is rejected — only the host identity survives; the recognised Flow directive is *"A light application of woodblock print and vox newspaper with rich anime colors"* (in eight shipped Tokyo prompts). **Remaining, and it is a human gate:** roll `provider-jobs/finance-host-model-sheets-001.google-flow-job.v1.json` (two 16:9 model sheets, quarantined, `ready_for_confirmation`), operator approves the contact sheet, then `reference_asset_ids` is filled. Original: **Upgrade the character pack, and add a stick Mike.** `finance-host-flow-character-pack.v1.json` declares `reference_views` but **`reference_asset_ids` is empty — the sheet was never generated**, and `render_eligible` is `False`. Bring it to the §53.9 standard: add **side** and **back** views, **enumerate five expressions** (happy, neutral, surprised, worried, angry) and **five poses** (waving, thinking, presenting, explaining, shrugging), generate the sheet, register the assets. Then add **`finance-host-stick-v1`** — the same person at acquisition abstraction, carrying only **locs, glasses, goatee, indigo/copper**; the lapel pin, shoe textile and tailoring do not survive simplification. **The expression and pose rows double as the cutout rig's slot enumeration** (43 §43.6). See 53 §53.10 |
| **A2a** | **The icon ring — sourced.** Flat, single-object, nameable: liquor store, gas pump, hospital. MIT sets (Phosphor, Tabler) recoloured to tokens, indexed like plates. Cheap, an afternoon |
| **A2a'** | **Hero illustrations — generated.** **Spec now exists: [53](53-THE-STICKMAN-LANE.md)** — the eight-field image schema and seven-field animation schema, with **bound `@Mike` in the character slot** (no paragraph needed; the binding already works). Flow: Nano Banana 2 9:16 for stills, Omni flash 4 s for motion. **The fields we do not currently send: `SUBJECT COUNT`, `NEGATIVE RULES`, `CAMERA: static hold`, `ANTI-GLITCH LOCK`.** *Corrected 2026-09-04: an earlier note here said do not generate these. Wrong — that art is polished illustration, not stick art, and the tutorials teach the prompt.* Operator supplies the prompt pattern; the engine makes it repeatable (locked template + style tokens + the existing review quarantine). **Generation works for a listicle because each item is a new figure — style consistency, not character consistency, so the @Mike problem never arises** (52 §52.3) |
| A2b | **Prop library — retention.** The five Tokyo props validate but do not exist as art: toll gate, empty chair + cold cup + bill, crate stamped with a future year, locked lever, lit fab |
| ~~A4~~ | **CLOSED 2026-09-05 by the page VORTEX** (29 §9.31): every colour on the page down the drain, then the stains; the reverse is the spiral return. Original: The recede species ("whirlpool") — `spotlight` inverted, radius → 0 |
| A5 | Actor on the page = hands; `build-f/ledger-hands.html` is an untracked proof |
| A6 | Expose the chart's coordinate mapper — 43 §43.2's anchor work is its general form → P45 O6 |
| ~~G2~~ | **CLOSED 2026-09-05 (G2 shipped: `run_short`, `--short/--long`, the audit's short flag; CAPABILITIES row).** Original: Short-runtime mode for `gate_opening_structure.py` — 17 Tokyo FAILs are long-form geometry on a 90 s script. **Measured again 2026-09-04 on the rewrite:** the pinned P1 (0:35) + G18 (≤ 3 marks/min) + G12 (tricolon inside P1) leave a two-second slot for the promise, and P2's seven beats must sit in 0:35–1:05 — fitting a clear script to it clipped the prose until the operator said the sentences did not make sense (v3 → v4 → v5 in `tokyo-tea-break/`). The clean cut passes at 2:20; a 45–60 s short (doc 51) cannot. **Destination: a declared short mode — operator, 2026-09-04, on the Tokyo short (v9): the head-fake and the debate are CUT, the back end is cut, the signpost is one clause ("So, the first number:"); hook → mechanism → the ring survive; **and a short has no late-stage flip** (operator, same day: the threshold / where-we-sit / flip block is a long-form draw-out, cut for 1:30 - so `audit_script_doctrine` doc 35 rule 2 must not bind in short mode either); the brand triad is cut when the take runs over because the outro carries it. Spec it from v11 (`tokyo-tea-break/SCRIPT-90S-VO.claude.txt`, 3 FAILs = exactly the cut beats) with v8-long and ep1 as the long-form fixtures.** |
| G3 | Stop compressing inter-paragraph silence in `tempo_edit.py` |
| ~~G4~~ | **CLOSED 2026-09-04** — `tokyo-tea-break/build_short.py` compiles the short onto the 9:16 template (six CLIP worlds, three ledger-page entries); `player.html` is retired. Original: Tokyo `timeline.json` on the 9:16 template; retires `player.html` and its three wrong TIC figures |
| **P41** | **The PORTRAIT ledger page (operator, 2026-09-04, on the first Tokyo render).** On a short the page IS the world, not an evidence card: (a) re-stage the chart for 9:16 - the chart box fills the safe box `x[80,880] y[280,1340]` and the chart's own coordinate system takes a tall viewBox (`W/H` are literals 1000×560 in `buildLedgerLine` / bars today); (b) every text ≥ 59 px on the stage (doc 50) - title 34 / labels 24 today; (c) the charcoal fills the paper to its DECKLE, no rounded board and no cream margin on top (doc 29 §9.26 deckle rule; the field is a 6%/8% inset rounded rect today); (d) the ground is the warm textured cream deckle, not flat `#F4E6C7` (`page.plate`, none set today); (e) the bleed is an INK SPILL - splotchy, spreading from a spill point (feTurbulence-displaced blobs), not nine blurred seeps; (f) no highlight species over the build (M14 extends to spotlight/callout; M11 treats a ledger page's build as its own annotation). **DECISION for the operator:** doc 49 §49.1 says the ledger card keeps 16:9 inside the vertical frame (zone 2, 1000×562); the operator's ruling today is the chart becomes the world sized for 9:16 - P41 follows the ruling and 49 §49.1 gets the addendum. Fixture: `tokyo-tea-break/build-short/`. |
| G5 | The 8 carried P34 reviewer mediums |

## Planning gap found 2026-09-04: gates whose fix has no owner

An audit of P37 and P38 against docs 42–49 found **three gates that check behaviour
nothing builds**, and four clusters of research neither plan claims.

| gate | checks | but who builds it? |
|---|---|---|
| G-i | the composited figure's eyes sit on the plate horizon | **nobody** — no plan locks the horizon → P45 T8 |
| G-j | grounded sprites anchor `50% 100%` and bind to floor velocity | **nobody** — no plan builds the ground binding → P45 T3 (TOP) |
| G-k | a contact beat declares IK, a free gesture declares FK | **nobody** — no plan builds an IK solver → P45 T4 (TOP) |

I excluded G-h (Kubelka-Munk) from P37 precisely because it would FAIL every build with no
path to green, then did not apply the same test to these three. **P37's T7 lands them on
the M08 INFO-then-FAIL ladder, which is the correct mitigation** — but the build still
needs an owner before they can promote to FAIL.

**Unclaimed research clusters.** Each now has a home rather than a lane:

| cluster | where it goes |
|---|---|
| dual shadow, light wrap, substrate grain, horizon lock (48 §48.7-48.8) | **P42** when written — and it is what gives G-i/G-j a path to green → P45 O10, P45 T8 |
| the 3-zone vertical stage (49 §49.1) | **P41** when written. T0 fixes dock geometry; the stage itself does not exist and we ship shorts |
| the E1 measurement harness (X1/X2/X3) | **P40** — the only planned work that addresses retention |
| **multi-plane inpainting, SAM 2 + LaMa (45 §45.5)** | **B6 below — ours, and load-bearing.** G-b bans Depthflow on actor/prop/evidence/text plates and nothing builds the replacement, so today the ban just means those plates hold still → P45 T9 |
| parallax dial fix | **P37 T9** — folded in, no longer deferred |
| actor idling (48 §48.4), Euler spirals (42 §42.4), the Z-stack (43 §43.3) | slices inside P40/P41; too small to plan alone |

### B8 — the shipped 1440p render is an upscale of a 1398px capture (found by P39 T2, 2026-09-04)

`fitStage()` scales `#stage` to the `#fit` container, so `render_episode.py` screenshots a
0.728x stage and `resize()`s it to 2560x1440. Every frame of ep1 that shipped was a LANCZOS
upscale. Alongside: CSS transitions run on the wall clock during a seek-render (dock
entrances smear at whatever pace capture runs), and `document.fonts.ready` is not awaited.
**FIXED 2026-09-04 (P39 T6):** `render_episode.py` captures through `render_baseline.prepare_page` at native 2560x1440 and raises on a wrong size; two `--test` renders of ep1 hash identical. Ep1 and Tokyo rebuild on the fixed capture.

### B7 — the Flow driver should verify, not the prompt contain

**Operator ruling 2026-09-04:** negative rules in a positive prompt can summon the very
failures they name — *"trust and verify instead of try to contain."* Observed on our stack;
scoped to negations placed in the positive prompt.

**Refined 2026-09-04 (53 §53.9): exclude content, do not name defects.** *"No background
music"* and *"no on-screen text"* describe what is not in the scene and are fine. *"No
morphing, no extra limbs, no duplicate characters"* describes failure modes back to the
model and is what summons them.

So the prompt carries only what **is** there plus content exclusions, and the driver checks
for what must not be:
**duplicate figures · extra limbs · style drift against the bound `@Mike` · watermark ·
stray text.** Re-roll on failure. This is an automated **pre-filter before the contact
sheet**, not a replacement for operator approval.

It is the same missing-runtime point as 53 §53.8: they write negatives because a human
clicking tabs can only contain in advance. We have a driver that can look at the output.

*Cheap first version: subject/face count, and an OCR pass for text. Style-match against the
reference is the harder half.*

### B6 — the multi-plane lane has no owner and the viability matrix needs it

45 §45.2 **bans** Depthflow on any plate carrying a foreground character, an isolated prop,
or vector evidence and text. That is correct — it is what stops the melting. But the
replacement path (SAM 2 segment → LaMa clean plate → composite as Z-stack cards, 45 §45.5
+ 43 §43.3) **is not built and not planned**, so a banned plate currently gets no motion
at all rather than better motion.

*Recorded 2026-09-04 after the operator corrected a framing error of mine: I had filed this
under another agent's "lane." There are no lanes. Research agents explore; code has an
owner, and the owner is whoever is planning.*

## Explorations — what we still cannot answer

**Landed 2026-09-04 from the research agent: [54](54-GOOGLE-FLOW-OMNI-NANO-BANANA-PRODUCTION-RESEARCH.md)
+ `.agents/skills/google-flow-production/SKILL.md`, read in full and reconciled in a header note
(170 WPM → ~180; negatives narrowed to E29; the cream/charcoal monoline is the ledger token set,
not the host directive; five claims moved to SOURCES-TO-VERIFY). Three of its claims are cheap
to test and worth it:**

| # | question | test |
|---|---|---|
| **X17** | Does an explicit transition verb **at second 5** defeat the late-clip motion freeze in a 10 s Omni clip? | same scene twice, with and without the second-5 verb; look at seconds 6–10 |
| **X18** | Is the **cowboy shot** (mid-thigh up, 65–75 % of frame height) the legibility floor for a stick figure in 9:16 720p, and does an extreme wide really dissolve the limbs? | one 9:16 roll at each of EWS / cowboy / ECU with the bound stick Mike |
| **X19** | `Portrait → Create Body` with no upload, and **15 credits per 10 s** — both asserted, neither observed | verify both while binding `finance-host-stick-v1`; read the meter on the first 10 s roll |


Ordered by what the first-minute frame makes urgent.

**New, from doc 49:** our shorts run on long-form cadence. Short-form wants a visual pulse
every **1.2–2.5 s** against our ASL of 6–10 s, and **cognitive atomicity** — exactly one
mechanism per short, which is the short-form form of the equation spine. Logged as X14.

| # | question | how to settle it |
|---|---|---|
| **X0** | **Render one frame at 390 px and read it.** The mobile-legibility finding (50 §50.2) is arithmetic from declared font sizes, not observation. **This is the cheapest check in the entire backlog and it may be the most important.** | Open the player at 390 px wide, screenshot, try to read the axis labels. |
| **X1** → **[P40](../../.claude/PRPs/plans/P40-MEASURE-THE-FIRST-MINUTE.plan.md)** T3 | **What actually loses people in our first minute?** We have never measured our own failure mode — only inferred it. This is the question everything else is serving, and it has not been on this list before. | P36 (the viewer) on ep1's opening, windowed and blind; plus the E1 metrics once X2 lands. Run the reference's opening on the same instruments and compare. |
| **X2** → **P40** T2 | **E1 metric thresholds.** Motion energy, centroid of change, saliency and flow coherence are the right metrics; the ranges are not ours yet. | Build the measurement, run it over ep1 and both references, derive from that. **Never adopt a guessed threshold** — that is precisely the error the metrics exist to catch. |
| ~~X3~~ → **CLOSED 2026-09-04 (P40 T1), reference-first on the operator's correction: 0.30 s, cut at 0.8 of the gap, mid-word ≤ 25 % (46 §46.3)** | **Gap threshold: 0.30 s at onset, or 0.45 s at midpoint?** Different gates. | Count both across ep1's word timeline; check the 0.45 s set is large enough to carry every boundary that needs one. **Blocks M13.** |
| ~~X12~~ | ~~Wan is not covered~~ **CLOSED 2026-09-04** by [49](49-GENERATIVE-VIDEO-AND-THE-VERTICAL-STAGE.md) §49.2 — the 4k+1 law, CFG ceilings, TeaCache, and the two dials that fail loudly (unscaled FP8 encoder, quantized VAE). Original: | The bundle specs LTX-Video thoroughly (frame-count law, CFG ceiling, mask-pinning, re-stitch) and mentions Wan **zero** times. The *architecture* transfers — mask-pin the subject, composite the pristine cutout over the decode — but every dial is LTX-specific. | A targeted pass, or one calibration roll per lane. Do not assume LTX numbers carry. |
| ~~X13~~ | ~~9:16 composition~~ **CLOSED 2026-09-04** by [49](49-GENERATIVE-VIDEO-AND-THE-VERTICAL-STAGE.md) §49.1 — the Universal Clean Canvas and the 3-zone vertical stage. **It caught a live defect in our own template.** Original: | Brief item D2 asked this and **no pass answered it.** All the compositing work (48 §48.7) assumes a 16:9 stage with a horizon; nothing addresses a vertical frame whose bottom third is captions. We ship shorts. | Measure the reference shorts lane, or derive from the safe zone we already enforce. |
| **X14** | **Our shorts are built on long-form cadence.** 49 §49.6: short-form wants a visual event every 1.2–2.5 s and exactly one mechanism; we run ASL 6–10 s and compound argument. The Tokyo short is 118 s carrying a full six-phase structure. | Re-cut one short at short-form pulse and compare. Cheap, and it bears directly on X1. |
| X4 | **Does a prop library actually compose?** The claim is that 5–8 props cover most metaphors. | Brief three unrelated episodes against one prop set and count the misses. |
| X5 | **Cut ON the pause or THROUGH it?** The measurement says references cut on it; it does not say we are wrong to sometimes cut through. | A/B by ear on one scene pair. |
| ~~X15~~ | **CLOSED 2026-09-04 - prose, from our own artifacts, no test roll needed.** `scene-01-02-red-arrow-transformation_meta.json` is 768 chars, zero newlines, no field markers, and it shipped. I had recorded a tutorial's field schema over our own working practice. Original: ~~Fields or prose for Omni Flash?~~ Two tutorials using the same model disagree flatly: one uses a field schema for animation prompts, the other says field formatting is *"the single most important formatting rule"* and breaks the model. **This decides the shape of every animation prompt we send.** | One test roll: same scene, same bound `@Mike`, once as fields and once as prose. Until settled, **prose is the safer default** (53 §53.9). |
| ~~X16~~ | **WITHDRAWN 2026-09-04 - moot.** Voice is decoupled to ElevenLabs, so the video model never speaks and there is no per-clip word constraint to verify. Original: ~~Does 6 s hold exactly 13 words?~~ A hard production constraint from the same source — over 13 and the line is cut off mid-sentence. If true it is a mechanical gate on any 6-second-clip script. | Measure against our own Kokoro/ElevenLabs timings, which we already have. Cheap. |
| X6 | **Depth model precision** — `vitl_fp16` (05) vs `vitl_fp32` (06, "fp16 strictly banned, logit underflow"). | One test roll. ViT-Large either way. |
| X7 | **Euler spiral quality threshold** — how much curvature ripple is too much. | No basis yet. Build the generator; gate later or never. → P45 T2 |
| ~~X10~~ | ~~How does a figure move in our register?~~ **CLOSED 2026-09-04** by [48-THE-FIGURE-AND-THE-GROUND](48-THE-FIGURE-AND-THE-GROUND.md) — the FK/IK boundary, XCOM balance, DQS/BBW rigging, breath and sway, reach and grasp, grounding. Original question below.<br><br>~~ 42–47 cover strokes, springs, morphs, ink and plate motion and say almost nothing about **actor motion** — generated or composited. Exposed by withdrawing G-f: we argued about *how the actor is made* and never wrote down how it should *move*. | Name the motion vocabulary a figure needs (enter, gesture, settle, exit), then decide which is designed out, gated, or judged. Prerequisite: none. |
| X11 | **Per-material `ζ` / `ω₀`, and the secondary-motion ratio** | T2 gives the model; these are the numbers. Derive by eye against reference footage, or declare them ours and tune. → P45 T13 |
| X8 | **Two unverified citations** in the pass-2 rewrite of `08` (Martinez-Conde year/volume, Hasson volume/year). Nothing in 42–47 depends on either. | A library check. Low priority. |
| ~~X9~~ | ~~Parallax + object page~~ | **CLOSED 2026-09-04** — 45 §45.2 bans parallax on vector evidence and text. Depth on the page comes from the Z-stack instead, which costs no shader and cannot tear. |

## Found by the retrieval benchmark — 2026-09-05 (evals/RETRIEVAL-BENCHMARK-2026-09-05.md)

Two rounds of delegated lookups (Fable `Explore` vs the Opus `explorer` role) surfaced these while answering; each is doc-grounded and its absence was proven by grep.

| # | item | where it goes |
|---|---|---|
| R1 | **The Deegan dark rim** — 44 §44.2 specifies a one-to-two-pixel edge density at the ink boundary (coffee-ring, Deegan 1997); nothing in `kinetics/ink.mjs` or the template draws a perimeter term | Build behind a flag next to `km_ink`; the one cue that separates sumi from a filled vector shape → P45 T10 |
| R2 | **Syllable-locked STAGE type** — `FINDING-the-animation-math-and-what-it-changes.md` §(147-150) and its build-order row: pops phase-locked to the 4–8 Hz speech envelope; no `syllab` anywhere in the template | Exploration; needs a syllable timeline from the take (Whisper word times are the input) |
| R3 | **`arap_morph` is a stub flag** — declared in `KINETICS_DEFAULTS` (template :446) and never read; T4 above is the real item | Fold into T4; the flag-roster test should refuse a declared flag no site reads → P45 F1, F4 |
| R4 | **The docs index** — SigMap indexes code symbols only, so "animation math" ranked the retired Manim scene; `build_docs_index.py` (**shipped 2026-09-05**, and the body-vocabulary slice the same evening: `terms` per section - inline-code spans, hyphenated/CamelCase tokens, symbols, citations - 2010 records / 140 files; doc 42 now names "the minimum-jerk law" so the term it is searched by is in its own text) emits `docs/DOCS-INDEX.jsonl` (every heading, lead line, bold labels, CAPABILITIES/BACKLOG rows) for one-call `rg`; follow with a labelling pass — every doc's H2 numbered `NN.x`, a lead sentence under every heading | Build; then the explorer role's brief says "rg the index first" |
| R5 | **A persistent explorer** — continuation is `/resume` in the agent view (CLI only, not the desktop Code tab - docs agent-view.md); `memory: project` is now set on explorer / docs_researcher / reviewer (`.claude/agent-memory/<role>/MEMORY.md`, version-controlled, auto-loaded up to 200 lines) so the map of the tree accumulates across sessions; re-run the benchmark in a later session and compare tool calls, not tokens (an agent's growing context never touches the Fable cap) | Harness; after the docs index lands |
| R6 | **Negative claims from a delegated agent are verified by the parent** — one Opus miss in ten hard lookups was "07 §5.3 does not exist" (it is `sources/reference_analyses/complete_research_evidence_bundle/07_academic_literature_drawing_and_2_5d_animation_engine.md:311`); the brief now says "not found where I looked", never "does not exist" | Runbook rule; doc 42 §42.2 now names the path |

## Registry read — 2026-09-06 (what is written but never built, now that the layers show it)

The first read of `docs/ANIMATION-REGISTRY.md` after P45: the 66 "orphaned" records collapse to nine ideas, and most are
already doctrine under another name (Darcy → 44 §44.3, breathing → 48, the log-normal → 46, charcoal-to-the-deckle → E22,
the punch/build decoupling → M14, the parallax intensity → G-c, silence snapping → M13, the ARAP morph → T4). Four were
never pulled in. Rule (operator, 2026-09-05): a recent stumble → TOP; not implemented → BACKLOG or EXPLORE.

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| **R26-8 BACKLOG** | **Cross-posting from one master** — one 1440x2560 render per short (the operator's standard, 2026-09-06: YouTube assigns higher-bitrate codecs to 1440p+ uploads and the same file serves Facebook/Instagram, so no per-platform render), plus a publish package per short: the YouTube description, the Facebook description with the link in the first comment, the pinned-comment text, the tags, the first frame; a `publish/` folder written by the build and a checklist so a post is one pass across platforms. Scheduling/API posting is out of scope until the package is routine | `SCRIPT-90S-DESCRIPTION.md` (both descriptions exist by hand for Tokyo); operator: "ideally we start being able to cross-post" | CLOSED 2026-09-12 (P52 T16, b00cba7) - `publish_package.py <build>` writes `publish/` (the YouTube description, the Facebook description with the link as the first comment, the pinned comment, TAGS, SOURCES, MANIFEST, the first frame, a one-page CHECKLIST of the seven human steps) from the build's own artifacts, no hand text, byte-identical on every run; the one-shot bar writes it; the master stays the one 1440x2560 render path. Gate 6 (the first use) is the operator's read of `tokyo-tea-break/build-short-t16/publish/`; scheduling / API posting stay out |
| **R26-7 EXPLORE** | **The world is the stage; the clips dock** - a short lives on one ledger world from the hook to the ring; generated video arrives as docks over the page (doc 29 docks), never as a scene swap; the engine's own capabilities (the drawing engine, hyperframes stop motion, compositing and drawing ON the world, the species) carry the motion, with B-roll as the exception. First application: Tokyo v2 (`SHOT-TABLE-V2-PROPOSAL.md`), where every clip between two pages becomes a dock | operator 2026-09-06: "we created the ability to build the whole world - we don't actually even have to scene swap if we keep improving the engine… we just have the world and maybe some B-roll"; the n = 51 curve: pages hold, clips bleed | Tokyo v2 first (no spend); then the unused capabilities in order of what the next short needs |
| **R26-12 → P47 T5 BUILT 2026-09-06** | **The idle: nothing ever goes truly still (E49)** - every held thing carries a named subtle idle (breath 1-2 %, drift, pulse; the figure's asymmetric breath), a pure function of t; the frozen-frames gate row; Ken Burns and parallax go back to being camera moves, not the stillness cure | operator 2026-09-06: "this is what my eyes are seeing and my brain is searching for, but we've been trying to do it too complicatedly, or too bluntly. Parallax / Ken Burns was our answer to get enough motion - because we let things be completely still instead of being at a subtle idle" | a kinetics module + defaults + one gate row; the smallest slice in P47 and the first |
| **R26-13** | **M18 per layer** - the whole-frame hash `measure_frozen_frames.py` writes cannot see a frozen page beneath a boiling phrase caption or a moving video dock (Tokyo v2 pre-E49: 1036 distinct frames of 1066, longest identical run 0.33 s, M18 PASS - yet its pages and pills were still); hash the frame with the caption and dock layers hidden (a `?layers=` switch in the player, or per-element `getBoundingClientRect` + transform hashes) so M18 reads the page, the docks and the captions each on their own | P47 T5 evidence, 2026-09-06 | CLOSED 2026-09-12 (P52 T15, 161c558) - the shell's `?layers=page,docks,captions` switch (off by default, never a build-time write; `visibility: hidden !important` so the hidden layer keeps its geometry; the species overlays and the veils are the page's), `measure_frozen_frames.py --layers` (one loop per layer, `frame-hashes.<layer>.json`), M18 reading each measured layer on its own - the row's level the worst of the whole frame and the layers, the whole-frame verdict still reported, windowed by what the timeline paints on a layer (an empty opening is not a frozen dock). Tokyo's page layer: longest identical run 0.08 s (E49's live idle) - the defect proved on the fixture and on the soak golden in the browser |
| **R26-15** | **The HyperFrames harvest, twelve components verbatim** (`content/video_engine/hyperframes/HARVEST-2026-09-07.md`) - references, tools and play objects, never a change to what works. The lessons named for a slice when a short asks: a `path` arrival (arc-motion-path), a `slide` page seam with the two-layer parallax (page-slide - "a much cleaner page slide than ours"), a caption `fade_up` arrival with the blur-in and one stagger envelope (staggered-fade-up - "more caption motion without overcrowding"), the recontextualise recipe for `pull_back` (pull-back-reveal - "live on the world longer"), the camera pan clamp and the drift-that-ends-at-zero (ui-focus-zoom), the tilt on `punch` (yt-camera-move), the cloud callout body (hw-text-cloud - "call outs onto the ledger world"), the dock-to-dock `condense` hand-over (morph-swap - "presenting more than 1 dock"); parallax-zoom / unzoom and vignette as play objects and a look; **drift-hold** (rotation + light-sweep idle kinds - E49 built by someone else) and **morph-text** (the gooey threshold: the operator's "melt the chart, turn it into a stop-motion ink ball, splash it everywhere or toss it off the page" - a `melt` page EXIT chaining what we own: the area polygon → blur + the K-M alpha threshold → a blob on the stepped clock → `throwXf` or the soak's splash) | operator 2026-09-07 | each lesson its own bounded slice behind a flag, ported as math on our painter (the P35 way); the operator's ear decides on a short |
| **R26-16 (the rule stands; Method A shipped 2026-09-11)** | **The morph's source is a real element of the outgoing world, never a conjured shape** - P50 T12: `kinetics/morph_a.mjs` (the vertex method for an outline-to-outline morph with modest rotation) and TR-7's invariants as compiler REFUSALS naming the number; the planted element's first Method-A use is the proof page `morph-a-proof.html`. Original: - operator on the HG3 stills, 2026-09-07: "we can't have an overlay of a chart portion on the screen that doesn't exist for 3 frames just to pretend that we're holding on-screen continuity ... pull it off of the red tie at the last frame or two." What it needs: an authored source outline in STAGE px on the outgoing scene's last frame (a polygon traced on the clip's still - the tie - the way species take a `region`), the morph spanning the boundary (the outgoing world holds its last frame while the lifted shape leaves it; no dissolve beneath the morph), the fill sampled from the source's own colour (the tie's red is the sign colour already), and the strip mesh from that outline to the area under the series (arap.mjs `stripMesh` takes any x-monotone pair; a tie is one) | P47 T3 follow-up; amended after the watch (operator 2026-09-07: "it reads but not effectively ... a tough sell without building something into every scene that allows it ... you can't do it on a mount like that, it's too long of a duration") | the third slice on the morph: `morph_from: {scene, outline}` on the page enter, the source PLANTED per scene (a scripting + asset discipline: every scene that hands to a chart carries the element that becomes it), the morph a SEAM's length (a few frames to ~0.5 s - measure the reference's blur-zoom 0.27 s and dips 0.47 s as the scale), never on a mount; M17 unchanged; HG3 again on the tie | **BUILT 2026-09-16 (P61 T3, P48 T5b): the planted source - `world.morph.poly` traced off the world's own raster; RULED E99 s52: "The actual morph is fine, but going from the ink splotch to the full fill on the board instantly around it is the problem" - the morph page's ground arrives by its field entry, never in one frame (P61 T3b)** **E99 s62 (2026-09-16), on the real Tokyo beat (the panelist's tie into the holdings chart): "the morph mechanism which is the point is valid" - CLOSED; the beat's second light on the crowd refused as authoring, not engine.** |
| **R26-23 → P48 PLANNED 2026-09-07** | **Chart-to-chart transitions as a first-rate feature** - operator: "we should be able to re-draw, change chart types/shapes, add additional points, or morph on page." Today a page's builder is decided once (`ledger_page.pick_builder`) and the marks are built once, so showing the same data another way means leaving the page. The plan makes the chart's SHAPE a function of `t`: every builder emits KEYED marks (T1, goldens the proof), a page carries up to three states, and `chart_to {at, dur, kind}` blends between them - `rescale` / `extend` (add points) / `recast` (change type) / `morph_to` (ARAP on page, which is also R26-16's mechanism). Gate M23; E50's clock restarts at a transition | operator 2026-09-07 | [P48](../../.claude/PRPs/plans/P48-CHART-TO-CHART-TRANSITIONS.plan.md) - T1 alone first (the risk), then the verbs; HG2 decides whether `recast` ships |
| **R26-32** | **The studio display stage (Bravos's TV lane)** - other people's claims on a monitor in a lit studio: a `tv-embed` WORLD with four depth layers (wall / bezel / content / callouts) each under a CSS `perspective()` transform (a planar homography), a sine micro-pan `Camera_X(u) = A sin(wu)` that yields the parallax between them, card drop-shadows; the external lane's docks land on it. From Gemini's un-filed summary (REPORT.claude.md §"mapped", technique 1). Note: E49 retired parallax as a cure for stillness; here it is the world's own depth. | P50 T8 blend -> T7 (Bravos's TV lane: a card on a painted surface by the homography; the sine pan dropped with parallax) - SHIPPED 2026-09-11/12 as the ART-embed surface (301f076, E66 e6e9902): the poster, the TV, the laptop, the washi paper; closed |
| **R26-33 (half, 2026-09-11)** | **The dual-lane stage's map + the sonar ping** - the MAP lane shipped as the `vecmap` world (P50 T5: a country lights, an arc crosses, a figure stamps); the `ping` species (a radial pulse at a point on a period) is still open - it rides the map as a fourth vecmap species when a cut asks. Original: - Bravos shot 28: a chart left, a dark map right, one clock; we have the split (a parked chart beside a docked card, Tokyo row 4) but not the map lane (the vector map, review §2 #7) nor the `ping` species: a radial pulse at a point, `r(t) = r_max ((t - t0) mod T) / T`, `alpha = 1 - r / r_max`, fired on the word. From Gemini's un-filed summary, technique 3. | P50 T8 blend -> T5 (the map: SHIPPED as the vecmap world; the sonar PING - r(t) = r_max ((t - t0) mod T) / T, alpha = 1 - r / r_max, at a lit country - not built; the light and the arc carry the beat) - open for the ping only |
| **R26-34 CLOSED 2026-09-11** | **The tip-riding pill** - SHIPPED with P50 T11 as `scripts/species/tippill.mjs` (opt-in `tip_pill`; the pill rides the fractional tip with a leader, pops at its milestone, and IS the terminal tag at the end). Original: - a country/series pill locked to the drawing tip `X_pill(u) = P_tip(u) + D_offset` with a leader from the tip, popping on `springPop(Mp = 0.05)` at a milestone; ours names a series at its END (E53) and draws with a tip head. The bloom Gemini pairs it with (a two-pass halo + hot-core stroke) is against E22 unless ruled - the burst's glow is the one bloom we use. From Gemini's un-filed summary, technique 5. | open 2026-09-10 |
| **R26-35** | **The Tokyo bed swell misses the camera arrival** - the short's bed envelope keys the +4 dB swell to a thrown card's SNAP (`:snap=<dock>`); under `TOKYO_CAMERA=1` (the default since "8742>8738") the ring's card arrives by `:camera=`, so its swell has always fallen back to throw + 1.2 s instead of holding through the arrival. Preserved by P51 T0 on purpose (the kit's `bed_envelope` defaults to both keys; the episode pins `(":snap=",)` so the approved cut's sound plan stayed byte-identical). Fix on the test bed when the sound is next re-checked (a one-word change in `build_short.py`; the plan changes, the watch says). | open 2026-09-11 |
| **R26-36** | **A page row with `;then=` / `;idle=` after `:cut` is not read as a cut by the sound map** - `page_transitions()["cut"]` is the literal `:cut` SUFFIX test both shorts were built on; Tokyo row 4 (`...:spiral:cut;then=...`) therefore gets a "page retract 4 (whirl)" cue at its end although the Meta page MOUNTS over it. Preserved by P51 T0 (documented in the kit's docstring); the fix is to strip the `;` options before the suffix test - it moves a cue the operator has heard, so it lands with the next sound pass on the test bed, named in the plan. | open 2026-09-11 |
| **R26-37** | **The template's own probes answer for the FIRST ledger world** - `__lpProbe` / `__camera` / `__lpDatum` take `[wA, wB].find(ledger)`; when both worlds hold a page (a mount: Tokyo 0:58 and 0:81) they answer for the page UNDERNEATH. `probe.py` (P51 T2) reads `#wB` (the player paints wA = prev, wB = this scene) and falls back to the drawn DOM. Fix in the template with P51 T1's split (the runtime apart from the document): the probes take the ACTIVE world. | CLOSED 2026-09-12 (P52 T3, 8186f1a) - the three probes take `[wB, wA]`, the page ON TOP (the order `resolveTarget` always used); `__lpProbe` returns `scene` and `world`. Tokyo 0:81 before: the Meta page underneath (38 glyphs, 0 series); after: s06 on wB (50 glyphs, 1 series), identical cold and after a backward seek; eight Tokyo frames byte-identical HEAD engine vs this one - the paint was never wrong |
| **R26-38** | **A world's `__lp` can point at the previous page after a backward seek** - at 0:58 after a seek to 0:30, `world.__lp` holds the previous page's state whose chart elements are detached; the rendered frames are byte-identical (the player is fine), the bookkeeping is stale. `probe.py` reads charts from `world.querySelectorAll('.lp-chart')` and uses the state object only for its declared plot box; `test_probe.py::test_a_seek_is_the_play_for_the_probe_too` pins it. Fix with P51 T1 / T4 (the determinism check reads the same DOM). | CLOSED 2026-09-12 (P52 T3, 8186f1a) - `paintLedger` re-points `el.__lp = st` on every painted frame (the cached path of every backward seek never did) and a non-page world clears it; probe.py's stale-state work-around removed, the DOM read kept; `probe.at(58.6)` cold vs after a seek away and back now identical (M26's bars no longer lost); the gate artifact byte-identical either way |
| **R26-39 CLOSED 2026-09-11** | **A recast into an overflow state paints the breaking bar TALL during its build phase** - FIXED the same day: `lpPaintChart` now runs `lpPaintBreakthrough` for negative secs too (the build phase: the bar's rest is the comparator's level, the stated ticks the visible ones, the pill's count left to the bars law); `test_mid_build_the_breaking_bar_rises_to_the_comparator_s_level` (burst + stack); the Tokyo side build probed at 57.5 s: the data box 102 px (was 303), the sheet shows the chips bar rising beside the bonds bar. The original finding: - measured on the Tokyo build-short (the self-watch's first read, 2026-09-11): at 57.5 and 57.65 s (sought and played, identical) the chips bar stands 303 px on the rewritten 0-40 % scale while its pill reads 0.00 %; at 58.0 both bars sit at the comparator level (63 px, 0-8 %); the shoot then runs 59.0-59.6 as designed. E60 says: build to the comparator's level, hold, THEN shoot - the build phase after a `recast` draws the final geometry for ~0.7 s before the hold snaps it down (the page's own roll-out path, proven on breakthrough-proof.html, does not). Fix in the template's recast draw-on for an overflow state (`lpPaintBreakthrough` / the state build's `cb` clamp) - queued behind P50 T2's template edit; a test in test_breakthrough.py at a recast + 0.3 s. | open 2026-09-11 |
| **R26-40 CLOSED 2026-09-11** | SHIPPED as M26 (the probe records every bar's height, plot, printed value and top tick; the gate FAILs a mismatch past VALUE_TOL). Original: **M26 - the printed value and the drawn height agree at every instant** - E28 / E53 say the scale and the value are printed at every instant; nobody checks that the bar's HEIGHT matches the value the pill prints on the scale the ticks print. R26-39 is exactly that mismatch (303 px at "0.00 %") and the probe already reads every bar's box, the pill's text and the tick labels: `probe.py --gate` can record (bar height / plot height) x (visible top tick) against the pill's number and the gate can FAIL a difference over a few percent while the pill is visible. A layout row's sibling for VALUES; lands with the burst's furniture (P50 T10) or before. | open 2026-09-11 |
| **R26-41** | **One painter registry for PAGE species too (P51 T1)** - `SPECIES_PAINTERS` hands a painter the stage-px overlay, `resolveTarget` and the scene's clock; a PAGE species (bracket, figure, spread, span) is built and painted by the page's perform layer inside the chart's viewBox, on the page state, under the active state's park transform - a different space and clock. So `span` (P50 T4) keeps its MATH in `species/span.mjs` (pure, tested) and the perform layer calls it in ~14 lines (`paintSpan`), its region placed with the kinetics laws because `buildPerform` closes over the consts hundreds of lines above the species block; the module registers nothing (nor does `press`, a dock kind). The split (P51 T1) gives both layers one registry keyed by space - stage overlay vs page perform - so a page species is a module end to end. Until then a page species is a module with a thin call. | BUILT 2026-09-12 (P52 T5, c115f81) - `PAGE_PAINTERS` (the page space: the chart's viewBox, the page state, the park transform; `PAGE_CTX` = pointsNow / datumNow / markDatum / clamp / el / PS) beside `SPECIES_PAINTERS` (the stage); the perform layer's hook routes by the DECLARED kind; `span` is a page module end to end (`SPACE: page`, `paintSpan` registered as its last statement); `sync_kinetics --check` refuses a module in the wrong registry by name and holds a page region before `paintPerform`. Open half: `bracket`, `figure`, `spread` and every page BUILDER still live in the engine - each moves with its builder in the next plan (named at the hook) |
| **R26-42** | **The tip pill's leader hides behind its own capsule** - the leader runs from the tip to the pill's ANCHOR point, which for an end-anchored tag sits inside the box; the fix gives `pillAt` the pill's box (not just its horizontal span) so the leader ends on the near edge - a small `species/tippill.mjs` + test change (P50 T11's report). | CLOSED 2026-09-12 (P52 T2, 4f698cd) - `leaderEnd` in `species/tippill.mjs`: the leader's far end is where the ray from the pill's anchor toward the tip leaves the capsule's BOX (pillBox's ink about the anchor, handed in by the engine as `box`), never inside it; an anchor outside its box ends at the anchor, a box over the tip cuts at the tip; the span fallback bounds x alone. 11 node tests; 19 golden surfaces byte-identical with the HEAD template (the leader has faded at a settled frame) |
| **R26-43** | **A tag wider than the chart gets no tip pill, silently** - measured: 1,070 units of name on an 800-unit chart hung half off the stage, so the pill degrades to the plain terminal tag with no word; a compiler line would make it a refusal (or a shorter label the author must write). Decide when a cut names a long series. | open 2026-09-11 |
| **R26-44** | **The wire (HF-16) becomes visible when the chart layer does, not on the page's first cream frame** - the carried mark lives inside the chart svg whose opacity the build drives, so through a mount it appears with the axes, not over the cream; lifting it clear of the build's opacity so it stands through the mount is a doctrine call (does the wire belong to the page or to the world between pages?). Ask on the first cut that threads a line. | open 2026-09-11 |
| **R26-45** | **The approved shorts' pages are placed by the estimate, not the fixture** - `page-boxes.v1.json` holds five representative pages keyed by ink; Tokyo's and the tariff's pages are not on file, so their docks keep the 09-10 estimate (byte-identical by design). Measuring them moves four dock rectangles (Tokyo s02 {594,297,270,175} -> {624,289,240,159}) and flips two non-centred docks to E50-centred - a re-cut of approved work. Measure on the NEXT cut (`measure_page_boxes.py` needs an `--episode` mode); the treemap squarifies into the plot it feeds, so `--passes 2` converges it. | open 2026-09-11 |
| **R26-46** | **The retitle's state depends on the seek PATH** - found by the determinism check on the approved Tokyo cut (P51 T4): a page sought 0 -> 1.99 -> 38.96 -> 44.88 -> 55.31 (warm) carries NO title at 55.31 where a fresh page straight to 55.31 (cold) carries "The opponent: a balance sheet" (the retitle carried into row 4 at 41.88); a sequential warm pass (the self-watch's sheets) shows the title. So the retitle's glyph state is not a pure function of t once the page was mounted from an instant before its own scene - the class the check exists for (a render seeks sequentially, a golden renders cold). Frames: `tokyo-tea-break/build-short-t0/determinism/55.31-{warm,cold}.png`. Fix in the engine's retitle (the `--w` per glyph must be recomputed from t on every paint, never from a "seen" flag); a test at that path. **CLOSED 2026-09-11 (hardened; the symptom never reproduced):** the frame with no title did not reproduce in five attempts on today's build and the per-glyph `--w` was provably equal warm vs cold at 55.31 / 57.0 / 59.57 on every path walked; the one genuine function-of-history in the chain - the erase multiplied the CURRENT `--w` read back from the DOM, cumulative when a frame paints twice, and the base title (chain index 0) is never re-written from t on the way in - is removed: `setW` records the written width beside the painted one and the erase scales that; two tests in `test_page_performs.py` (five paints of one erase instant give one width; a retitle carried into a scene paints the same whether the page mounted before or after its word, per world layer). No golden moved. | closed 2026-09-11 |
| **R26-47** | **A 0.16 % drift on the fab card at its exit (59.57), warm vs cold** - max channel delta 24 inside `dock-i-fab-wafer`'s box; neither dock is a video, so not the decode class; reproduced on the pre-T4 build with stable hashes. Frames: `build-short-t0/determinism/59.57-{warm,cold}.png`. Likely the card's idle or its leaving transform sampling a cached box; find with the probe at 59.5-59.6 warm vs cold. **CLOSED 2026-09-11 (the capture path, not the engine):** the DOM is byte-identical warm vs cold (box, transform, opacity, filter, every child); the same DOM rasterises differently on its first two paints and settles on the third (warm 98703e3a5ae1 / cold once a4782129ab55 / again with no seek adff2810172e / sought again 98703e3a5ae1 - Chromium re-rasterising a scaled bitmap after its first cheap pass). `determinism_check.Frames.cold` now paints the instant twice and hashes the second, as `probe.Probe.seek` already seeks twice for the same reason; a real path-dependence cannot hide behind it because the painter re-runs at the same t. 55.31 and 59.57 both ok; `--all` 23 ok / 36 mismatch / 2 known -> 26 / 34 / 1, no new mismatch; the 34 are R26-48's class. | closed 2026-09-11 |
| **R26-48** | **The sub-pixel text class: the same SVG `<text>`, the same DOM, transforms and `--w`, renders with an ink box up to 1.5 px taller and 0.6 px higher on a page that has painted many frames than on a fresh one - and at 79.03 with an identical box and different pixels.** Found by the determinism check on Tokyo's side build (2026-09-11) once R26-47's raster settle was in: each arrival is stable against itself (three fresh cold pages -> three identical hashes at 7.69 / 23.10 / 79.03; three warm walks -> identical too) and warm and cold disagree REPRODUCIBLY - `text.lab` at 7.69: warm y 1008.64 / h 44.49, cold y 1008.04 / h 46.00, same svg box, same viewBox; fonts loaded and identical on both. Not glyph state, not fonts, not the raster settle (it survives two paints). `determinism_check.py build-short-t0 --all`: 26 ok, 34 mismatch (all this class), 1 known. A render seeks sequentially (warm); a golden renders cold; the two are 0.6 px apart on every label. Candidates to test: Chromium's text subpixel positioning drifting with the compositor's layer state (a `will-change` / transform-flattening difference between a page that has animated and one that has not); the SVG text's `dominant-baseline` resolved against a font metric that differs before/after layout. The cure is likely `text-rendering: geometricPrecision` + an integer-snapped text y, or rasterising labels on a canvas the engine owns. Evidence: `tokyo-tea-break/build-short-t0/determinism/<t>-{warm,cold}.png` (the pairs on disk), the check's `--all` counts above; the element-by-element diff of 2026-09-11 (only `text.lab` / `text.val` client rects differ) lived in the agent's scratchpad (`ink_diff.py`, `zoom-7.69.png`) and is re-run from the check. The row was announced in 6924b84 and its patch's own guard skipped it (the closing text of R26-47 already named it); written 2026-09-12. | CLOSED 2026-09-12 (P52 T4, 81ccef9) - `.lp text, .lp-chart text { text-rendering: geometricPrecision; }` in the player template: Chromium's default `auto` let Skia pick HINTED metrics (an integer-rounded ascent, hinted advances) on one page and the unhinted outline metrics on the other, by the font cache's state - the cold ink box 0.80 px taller and 0.84 px higher, the bottom edge fixed. After the rule not one text box, advance or attribute differs warm vs cold at any instant; the check on build-short-t4: 38 mismatch -> 7, the seven attributed (three video-dock DECODE, three one-to-eight-level raster noise, one a thrown dock's flight - R26-58). The integer-snap cure is measurably unavailable (the viewBox is not 1:1 to the stage); the canvas not built. Eleven goldens regenerated on purpose. Pinned by test_render_determinism (RED with the rule stripped) |
| **R26-49** | **E64: the recast re-writes or morphs, never cuts (the operator, 2026-09-11, on Tokyo at 0:50: "i think the chart needs to either re-write/re-draw itself or morph. right now it basically just cuts a new chart")** - today the plain recast (`chart_to {to: "recast", state}` with no key) un-writes the sub, un-draws the whole line by its history in ~0.6 s, swaps the axes to the new scale in one frame, then grows the bars (frames: the parent's `scratchpad/frames/recast-050.png`, 49.8-52.0 at 0.3 s). The mechanism: the compiler derives the key when the two states share data (a line whose next state is bars over the same series -> `keyed: "tags"` / `true`; over the same DATA - the line's last n points ARE the n change bars, Tokyo's case: `ev-japan-holdings-v1` datum 312-315 -> `ev-japan-selling-v1`'s four bars - a new datum-keyed pair in `RECAST_PAIRS`, `keyed: "data"`); the engine's recast gains the axis hand-over for every pair (the tick labels un-write and re-write, the gridlines slide) and the datum travel for the data-keyed pair (each keyed datum carries its value to its bar's top; the bar grows beneath; the rest of the line un-draws by history); a pair that shares nothing is named in the compile as a cut the author owns with a new row. M23 reads the hand-over. **CLOSED 2026-09-11 in two halves.** The compiler (66c4220): `keyed: "data"` when the bars ARE the line's consecutive changes (a rule on the two states' data, within 0.5 %, months matching), the key derived when the author named none (`keyed_derived`), `key_map [{datum, bar, from}]` written for the engine, a pair that shares nothing named `keyed: null`; Tokyo 50.3 keyed data (Mar -47.7 = 1191.6 - 1239.3 ... Jun -26.4 = 1116.7 - 1143.1, exact), 56.72 null. The engine: the line un-draws BY HISTORY (the dash window slides to the first datum the bars need, 0-0.35), each keyed datum carries its place from the line to the top of its bar while the bar grows beneath to the verified change (0.25-0.90, min-jerk), the anchor datum fades as its difference becomes ink, the categories write under the bars, the bar's own number takes the landed datum's place (0.90-1); across the whole clock the AXES HAND OVER - the standing labels un-written glyph by glyph (0-0.55), the arriving ones written (0.35-1), the gridlines lit and SLIDING by rank to the target's places - for every recast, keyed or plain (a target with no axes - a pie, a treemap - keeps the old hand-over). `KEYED_DATA.MIN_S` 1.6 s (at 1.4 s the four data cross ~700 px in 22 frames under a running axis re-write - three things at once; the row's dur wins when longer; M23 still reads the authored 1.4). Tokyo sheet 49.8-52.2: the history going and the x axis mid-sweep at 50.6, the line gone and the data in the air at 50.9, the categories arriving in order at 51.2, `Mar Apr May Jun` at 51.5, the four bars at their values at 52.2. Determinism inside the hand-over exact (50.8 / 51.3 / 51.7 ok; 50.3 is the R26-48 class - the clock's first frame, the same page mismatches at 46.0 / 49.8 with no transition on). Goldens: `data-to-bars` NEW (the derived key, the four data in the air, the axes handing over); `tags-to-bars` and `ledger-keyed` regenerated on purpose - both were captured mid-keyed-recast with NO axis at all (the one-frame swap E64 outlaws); now the gridlines stand and both label sets are mid-hand-over. The rest byte-identical. | closed 2026-09-11 |
| **R26-50** | **The page mount reads as a dip to black at 1:01 (the operator, 2026-09-11: "the random back dip to black at 1:01")** - s05 (`ledger:ev-meta-yield-v1:bars:3:right:mount=2.43:cut`) mounts by a cream wash that CROSSFADES over the finished Ten-years page for ~2.6 s (61.8 -> 64.4: the old bars dim to grey, then to cream), then the soak's charcoal blobs fill the deckle 64.8-66+, and the new chart is legible only past 1:06 - ~4.5 s under two sentences with nothing new to read; AND the caption ("home, and the tab stayed") is painted UNDER the wash and dims with the page (frames: `scratchpad/frames/dip-101.png`, `dip-101b.png`). Two defects: (1) the caption is the viewer's layer (E21, E59) and never dims - the wash must sit below `#caption`; (2) a `cut` exit cuts: the old page leaves on the exit clock (E47's forms, or the plain cut) and the cream lands as the NEW page's ground at once, the soak (E22's signature, kept at its 2.43 s) starting on the scene's first frame - never a 2.6 s fade of the previous chart. The mount's clock is the page's, not the previous page's. Gate: M01/M05 miss it (motion exists); O5 caught the class; a row for the gate: a page's `mount` window with the previous page still visible past 0.5 s is a FAIL. **CLOSED 2026-09-11 (the engine + the shell):** the DANCE is retired - a mounting page no longer fades the world that left over its `mount_s` (the branch that painted the old plate at a stepped opacity on z-index 3 is gone; the page that left is behind an opaque incoming world on the first frame); `mount_s` is the SOAK's seconds on the page's own clock (the cream is the ground at once, the soak opens on the scene's first frame, ink -> punch -> build follow at their unchanged offsets; the savor is skipped on a mount only); a declared exit keeps E47's clock (dip 0.47, dissolve 0.8, blur-zoom 0.27). The caption is the viewer's layer in the stacking order too: `#caption` z 50, the dip/blur veils 60 above it - the direct cause of the dimming was the old plate raised to z 3 over it. Four browser tests RED on the old engine then GREEN (a cut cuts; the soak opens on the first frame; the caption never dims and nothing the mount paints covers it, by z-rank and by hit test; a declared dip keeps its own clock). Tokyo: the finished page gone at 61.9, the soak 62.3-63.5 with the caption bright, the title writing at 64.5, the bars standing at 65.5 - the dead time under the two sentences drops from ~4.5 s to the soak's 2.43 s; determinism at the boundary 2 ok. Goldens regenerated on purpose: `occluder-dock` (the anchored caption had been hidden behind the plate's foreground cutout - an E21/E59 violation frozen into the golden; it reads now) and four sub-pixel edge changes under the caption's new compositing layer (chip-board, press-stack, vecmap-arc, flow-swap - R26-48's class, invisible by eye). The 0.47 s dip at 61.76 that remained (E47 #3's `docks -> dip` in front of s05's MOUNT) - the operator (2026-09-12): "The black flash is back on the scene change" - closed by E47's correction (the operator: "the dip was associated with any DOCK, not the dip being associated to the scene change. That's 2 very different things"): the dip is the default when the WORLD actually changes at the boundary and never in front of a signature enter; a dock decides nothing; s05 (a mount) cuts into its cream on the scene's first frame. The gate's `_page_land_offset` for a mount follows the new clock (mount_s + PAGE_BUILD_END_S - ROLL - SAVOR - FIELD). | closed 2026-09-11 |
| **R26-51** | **The measured page-boxes fixture is stale against the template** - `tests/test_page_boxes.py` asserts the fixture's `player_sha256` equals the template's sha; E62 changed the template (the 9:16 quiet caption) and R26-50 changes it again, so the fixture's pages are "read from" an older player. Re-measure ONCE when the template settles (`measure_page_boxes.py`, the same surfaces), in a commit that says why; until then the loader's fallback is the estimate for every page (E62's bands and E63's placement on Tokyo ran on estimates anyway - Tokyo's dense-line 9:16 ink is not in the fixture; add it when re-measuring). **CLOSED 2026-09-11:** the fixture gains a top-level ink-keyed `pages` section (the builders' representatives untouched) and a `projects` list; `measure_page_boxes.py --project <dir>` measures every ledger page and state a project's compiled timeline carries (Tokyo: five pages at 9:16; the estimate had the title two lines where the player writes one, the plot up to 96 px off in y and 188 in h on the story pages); `ledger_page.measured_boxes` reads the ink-keyed entry first; `--check` reproduces; the tool's DATA selector gained `.lp-cell` (the treemap's tiles carried no class - the probe's blind spot, fixed in the M28 slice). | closed 2026-09-11 |
| **R26-52** | **The japan-tariff short's build beat is authored on the roll-out clock** - its `build_short.py` carries the same local `PAGE_BUILD_START_S = 4.4` that Tokyo's did, so on the mount clock (R26-50) its first page lands 3.2 s before the authored `build_to` and the cap no longer shapes the line's arrival. The approved cut is not rebuilt (R26-45); when it is next re-cut, take the landing from `gate_motion_density._page_land_offset` as Tokyo's `build_short.py` now does (t_build = the scene's start + the landing - PAGE_BUILD_S). | open 2026-09-11 |
| **R26-53** | **Value labels crash on a bars page (the operator, 2026-09-11 evening: "why are we now crashing text?")** - the Tokyo Meta page's four values "$665 $633 $604 $577" at the 9:16 value size (59 px) over a 660 px plot (165 px a bar) touch each other and the callout's pill on bar 4 covers bar 3's label. IDENTICAL on the approved 09-09 build (1:11) and on today's side build (1:08) - not a regression, a defect the page always had that no gate sees (M25 reads cards over ink, M26 the value's height; the probe's `overlaps` has no value-vs-value). The fix: a value label fits its slot (one size for the row, the largest at which the widest fits, floor the tick size, then a second row); the pill rises to a band above the values when it is wider than its slot, never over a neighbour; a gate row M28 - text-on-text among the page's own labels (values, ticks, pills) - from the probe's boxes. Evidence: the parent's `scratchpad/frames/values-crash.png`. **CLOSED 2026-09-11 (the engine only; the template's base size stays the one source):** `lpFitValues` - the row takes the largest size at which the WIDEST label fits its slot with half a figure of air each side (measured in the page's own face by `getComputedTextLength`; a flat 12 px gutter was a no-op on the fallback face), one size for the row, floor the tick size, below it odd bars ride one line further out; no inline style when everything fits, so every existing page is byte-identical. `lpPillBand` - a pill wider than its slot rises to a band above the values with a dotted leader to its bar (the capsule idiom), never over a neighbour; its place is a constant in t (a travel would cross the labels the law protects). Tokyo: the values 59 -> 54.5 px, the neighbour gap 24 -> 34 stage px, the pill risen 17.8 / 34 / 48.6 px clear of the three values; s04's selling page's -$26.4 pill also rises (it sat 46 units inside the neighbour's column). M26 reads the same strings against the same heights; determinism 2 ok; 224 tests; no golden moved. M28 written the same evening: the probe labels every text box by role and writes the pairs that meet; the row FAILs a met pair or a value-row gap under a quarter of a figure (the approved build's 6 px: FAIL at 1:15), WARNs under half; PASS on the fitted build over 2,460 pairs. | closed 2026-09-11 |
| **R26-54** | **E65: the placer's base level - the plot's empty room, the axis band, scale; never "no place"** - found by R26-51's measured boxes (2026-09-11 evening): on Tokyo's real pages `page_place` returns None (the band above the plot is 184 px as the player draws it, 6 px short of E45's floor card + air; 136 in the fixture) and the engine paints the solo 800x474 centred card over the chart - the frame the operator sent ("this is docked over our chart now too"). The mechanism: the fixture records per page a DATA MASK (a grid over the plot, the cells the data's ink touches, from the player's DOM) and the axis bands; `page_place` falls through E65's order (outside band -> the largest empty rectangle in the mask, the quiet side first, else the emptiest corner -> under, over the x-axis band -> scale down to the floor -> the emptiest corner at the floor with a build warning) and records `place_room`; the READ takes the same room enlarged toward the axis; the PARK is the corner. Ships WITH the ink-keyed fixture (R26-51 step 2, held until this lands). **CLOSED 2026-09-11 (with R26-51):** `page_place` falls through outside -> `empty` (every maximal empty rectangle of the data mask scored by the quiet side, width and corner-ness, the card pushed into the outer corner and lifted 44 px off the tick labels) -> `axis` -> `corner` + a build WARN; `read_in_room`; `centred_place` falls through to the same room; `place_room` on the entry. Tokyo (`build-short-r51`): every page MEASURED; the panel card `empty [479, 937, 328, 208]` in the line's lower right, 0 px on the data at 10.0 and 12.0; the fingers, the record and the plant back at their authored boxes; no `corner`, no warning. M25 PASS, M26 PASS; M27 read the plot's BOX and failed the card in the empty room - moved to the ink (the M28 slice). 162 tests, no golden moved. | closed 2026-09-11 |
| **R26-55** | **The press card's pulled phrase as LIVE TYPE** - today `press_card.py` cuts the headline out of the source screenshot as a raster, so on a tall surface (E66: the card fills the poster) the phrase takes the surface's width and cannot re-line larger over more lines; on the poster it reads at 13.4 CSS px on a phone against the 17 floor. Live type (the phrase's words on the card, the source's face approximated by the house display face, the raster kept as the provenance strip) lets the reflow set the phrase at the size the surface allows. `press_card.py` + the compiler + `species/press.mjs`. | CLOSED 2026-09-12 (P52 T18, dce2e71) - the phrase travels as WORDS beside the crop (`press_card.py --phrase-text` -> `press_meta` -> `phrase_text` on the dock entry); `species/press.mjs` `pressPhraseFit` sets the largest quantised size whose greedy wrap fits the paper on both axes, one element per line, measured once per card and face; `pressColumn` keeps the crop as the provenance strip (30 % of the paper, one-axis fit, still cited) so a pile card's box and the fan never move; the underline rides the last line. The poster: raster 16.33 CSS px on a phone -> live type 39.76 (six lines); the TV 32.85; the pile card 18.6. Gate 7 (the face) is the operator's: `press-stack.png` (house, the default), `press-stack@face-serif.png`, `press-stack@face-condensed.png`; no new webfont; the dial is `kinetics.press_face` (engine-side; no compiler grammar writes it yet). `press-stack` and `art-embed` (the TV) regenerated on purpose |
| **R26-56** | **The golden source `treemap-cross.timeline.json` is stale** - `build_golden_sources.py` regenerates every source and the treemap's cell px differ from the committed file (`h_px` 284.3 vs 329.6; since the E65 page-boxes re-measure, 207b00c). Two agents reverted it rather than ship it. Find which change moved the treemap's cells, regenerate the source and its frame on purpose in a commit that says why. | CLOSED 2026-09-12 (P52 T1, 5c076e0) - the cause was 3eba5de (P50 T16, the MEASURED page-boxes fixture overriding page_boxes' estimate: plot 663x402 -> 768x466, every cell x 1.158), NOT E65 / 207b00c as this row guessed - E65's treemap entry differs from its parent only in data_mask / axis, the boxes byte-identical. Regenerated on purpose: Japan tier 1 -> 2 (prints 4.7), Russia named, the legend honest at 6, the fonts at their clamped size. Open half: test_page_boxes' player-sha check fails against the working tree - a re-measure on the settled player can move this golden again, on purpose, in a commit that says so |
| **R26-58** | **A thrown dock's in-flight state depends on the path** - T4's residual at Tokyo 75.79 (build-short-t4): `dock-h-fed-vs-yields` enters on a throw and its contact shadow (`DIV.dock-contact`) sits at y 371.5 on a page walked in warm and y 1761.5 on a cold seek - 1390 px apart, 33 435 px of the frame (1.6 %); every text box at that instant identical. The flight is `throwXf` on the stepped clock (`kinetics/stopaction.mjs`), a pure function of t - so the shadow's y is being read from a layout the cold page has not settled, or the dock's `from` offset is measured off a rect that differs before first paint. Reproduce with `determinism_check.py build-short-t4 --instants 75.79` and `px_diff.py`; the cure is in the dock loop's contact placement, not in the throw law. CLOSED 2026-09-12 (P53 T10). Reproduced on `build-short-t4`: MISMATCH at 75.79 only - 76.00 and 76.25 agreed - so the fault was the throw's FIRST frame. The shadow read the card's LIVE layout (`el.style.top || offsetTop`, `offsetHeight || 450`), and on a cold seek at the entering frame the card's image has not decoded. The cure, where the row said it would be: the shadow reads the PLACED box, a timeline constant - `dockGeom`'s x, y, w and the place's own aspect for the height - and falls back to the live layout only for an unplaced card. Proved on a fresh private build (`build-short-p53t10`): 75.79, 76.00 and 76.25 all warm == cold, and the landing frames read right (the stray shadow near the frame's foot is gone). The build's one motion-gate FAIL is M29 at 9.51 s, identical on t4 - the open dock-landing ruling, not this change. | **CLOSED 2026-09-12** |
| **R26-59** | **The head above the newsreel is a framed card, not a cutout** (STALE 2026-09-13: P53 T7 built the no-frame `cutout` dock and the golden `newsreel-band.png` shows the frameless head; close after the operator reads P53 human gate 4; someone else's words on video now go on a TV surface, E86) - T6 (P52) docks a head PNG as an image dock, and every dock renders inside the template's card frame (`.dock`, 4 px charcoal, 14 px radius) on paper; a head cutout wants NO frame and no paper - a `cutout` dock kind (or the occluder's `fg:` route, P50 T15) in the template and the dock loop, then gate 1's 16:9 frame re-rendered with Bessent's real head (`myth-of-historical-normal/assets/heads/head_bessent.png`, approved E68). Two smaller halves from the same slice: `centred_place` does not yet take the band's `reserve` (one parameter + two call sites in the dock loop); `cap_band: "above"` only moves the caption where E62 wrote a band on a DOCK entry - a crawl with nothing docked is refused rather than shipped with the caption on the tape. | open 2026-09-12 | **RULED 2026-09-15 (E99 s45): "both should be options, right now I would lean towards the framed render" - the framed card and the cutout are both dock forms for a head, the framed card the lean; CLOSED** |
| **R26-60** | **A transition that TAKES the world needs the page's `:cut`** - found by P52 T9 on the melt and true of the shipped `suck`: a ledger page whose row does not say `:cut` runs its own RETRACT vortex (`pg.exit !== "cut"`) and has emptied the sheet before the boundary, so the suck spins a blank cream page into its point (`scratchpad/p52/dbg-wA-suck:0.5,0.5-15.15.png` on the session's machine) and the melt would drip a blank one. CLOSED 2026-09-12 by P53 T6: `stamp_transition_pages` (`content/video_engine/scripts/build_scene_timeline_f.py`) runs once the scenes are built - where both sides of every boundary are visible - and stamps `exit=cut` on a ledger page under a `suck` or a `melt`, printing what it stamped (a default nobody can see is a default nobody can argue with). A row that declares its own exit is never touched. Pinned by `test_transition_stamps.py`. **CORRECTED 2026-09-13 (the exit convention):** a scene's `exit` names the transition INTO the scene it sits on (E47; `SCENE_EXITS`), so "a ledger page under a `suck` or a `melt`" is the OUTGOING page - the scene BEFORE the one whose exit is the suck or melt - and that is where `exit=cut` is stamped; P53 T6 had read the exit as the one OUT of its scene (`stamp_transition_pages` docstring, caught by P54 T9's seam measure). | **CLOSED 2026-09-12** |
| **R26-61** | **The rig's bibliography - bundled, not lost** (the operator, 2026-09-12: "i think we may still eventually build the rig, but yes, we should bundle it and mark it properly"). The figure's own math, read and not built: **2.2 Postural Equilibrium: Ankle vs. Hip Gesturing Strategies**, **6.1 Biomechanics, Motor Control & Mathematics**, **A. Physiological Respiration Dynamics**, **A. Mathematical Formulation** and **B. Virtual Parent Constraints with Cached Offsets** (all `09_2d_and_2_5d_body_animation_object_handling_and_grounding.md`, the file doc 48 was extracted from); its deformation: **A. Linear Blend Skinning (LBS) Volume Loss**, **B. 2D Dual Quaternion Skinning (DQS)**, **C. Bounded Biharmonic Weights (BBW)** and **2.3 Bounded Biharmonic Weights (BBW) & 2D Dual Quaternion Skinning**; and our own two sections that state it in our tokens, **48.2 Balance — why a gesturing figure must move its hips** and **48.8 Harmonisation, in our own tokens**. The 2026-09-05 triage put these at EXPLORE / BACKLOG (its rows S2, S3, O9, O10) with the reason "read when the rig is built, do not plan them separately"; the buildable halves that exist are the ARAP morph and the DQS pair already tracked on this list. THE TRIGGER: the first short that asks a figure to gesture, bend or hold weight - until then this row IS their disposition and the registry reads them as tracked, not as unattached findings. | deferred with a trigger 2026-09-12 (P52's orphan triage) |
| **R26-62** | **The ground's physics and the transform chain - tracked where our own rows already bound them.** **Anisotropic Darcy Flow in Washi Paper (Chu & Tai 2005)** and **3.3 Spacetime Constraints & Elastic Paper Mechanics** are the research behind 44 s44.3's wicking, which is a JUDGE row bounded by E22 (the ink is a plate, and the soak we ship is the K-M chain); **3.2 Matrix Concatenation & The Dirty Flag Pattern** and **C3: Nested Coordinate Spaces [Tier 1 Architecture — CLOSED]** are the transform stack, whose buildable parts are P42's grounding, 43 s43.3's Z-stack and A6's chart coordinate mapper. The 2026-09-05 triage put all four at BACKLOG (rows S6, S7, O6) with those owners named; this row is what attaches them, so the registry stops reading them as unattached. | tracked 2026-09-12, deprioritised under E22 and 44 s44.3's JUDGE row |
| **R26-63** | **The research the triage told us to drop, dropped.** **2. Dynamic Nib Pooling [RECLASSIFIED: Engineering Heuristic Proposal]** (our own width and ink dials ship instead), **2.1 The 5 Overlapping Synergies to Capitalize On** (prose the registry matched on a stray `$`, not a formula), **B5: Rhythm as a Distribution [Tier 1 Science — CLOSED]** (the tail item it implies is deprioritised by ruling) and **C6: What Rive / Lottie / Flash Got Right [Tier 1 Architecture — CLOSED]** (P39 proved and locked the frame-addressed render: a frame is a pure function of t, and the determinism check is the proof). The 2026-09-05 triage said RETIRE for each (its rows O2, O3, O5, O7) and nobody wrote it down, so they read as open findings for a week. VERIFIED 2026-09-14 (P57 T5): the regenerated animation registry reads all four items `retired` (`docs/ANIMATION-REGISTRY.jsonl`, status by name - the WITHDRAWN verdict in this row is what the classifier reads), none `tracked`. | **WITHDRAWN 2026-09-12** - the triage's own verdict, executed |
| **R26-64** | **Two dials nothing reads** (2026-09-13: `IMPACT_S` deleted in dd859b6; the STROBE ceiling's space awaits the Gemini order sent 2026-09-13 - see R26-85) - `CADENCE.STROBE_PX_S` (300 px/s, `kinetics/stopaction.mjs`: "on-2s above 300 px/s strobes", Watson et al. 1986, not on file) and `STOP.IMPACT_S` (0.08 s, "the impact squash's speed-driven part decays over two frames") are declared and read nowhere - not by their module, not by the player, not by a test. Both surfaced the moment the registry stopped reading the engine's inlined copy of a module as a use (P52's classifier fix): each was being credited by its own copied declaration. Read 2026-09-12, and they are two different cases. **`STOP.IMPACT_S` is SUPERSEDED**: its note says the impact squash's speed-driven part decays over two frames, and that decay is implemented - by the MATERIAL, not by a time constant. `impactSquash` counts frames off the stepped clock and scales `IMPACT_SQUASH` by `1 - (f - 1) / squash_frames` (paper 1 frame, liquid 2, metal 0), which is the 5-step envelope the same object's own note describes `[DERIVED: HyperFrames stop-motion-cadence SQUASH_ENV, verified 2026-09-07]`. So nothing is missing from a hit: what makes a throw land harder is `IMPACT_SQUASH` (the squash depth), the material's `impact` (how hard it reads on the receiver), `DIP_PX` (the surface's deflection), `SHAKE_PX` (the ground's three-frame answer), `squash_frames` and `e` (the rebound). IMPACT_S is the vestigial time constant of the formulation the envelope replaced: delete it, or state in its place why a time decay should return. **`CADENCE.STROBE_PX_S` is a CEILING with no enforcer**: `cadence()` only consults `ON1_PX_S` (250), so anything over 300 px/s is already on 1s and the threshold can never bind on the helper's own choice. It binds where a cadence is DECLARED against the speed - a `break_cadence: "stop"` burst, a boil on 3s, an authored hold - and nothing checks that case today (P50 T13's own burst travels 359 px/s, past both numbers). That is a gate row: a thing stepping on 2s or 3s faster than `STROBE_PX_S` strobes and must go on 1s. **MEASURED ON OURS 2026-09-12 (P53 T5), and the disagreement has never changed a hold in anything we shipped.** `cadence()` has exactly two callers - `throwXf` (`kinetics/stopaction.mjs:154`) and the declared burst (`species/breakthrough.mjs:126`) - and every shipped throw runs 1188-2479 px/s of a 1080-wide stage (the approved tariff short's two cards at 2479, `japan-tariff-trick/build-short/GATES-MOTION.md:47`; Tokyo r51's 1311 and 2336, measured in flight at 1259 and 2090 off the probe's own boxes), 5-25x BOTH candidates; a landing drops 48 px in 0.14 s (343 px/s mean, 1029 at contact). What our footage actually moves at inside the 100-300 band is continuous motion that never asks the rule: the newsreel crawl at 140 px/s (`species/newsreel.mjs:39`), the racing bars at a measured mean of 13-61 with peaks to 344 (`build-short-t17/arm-a/RACE-MOTION.json`), an idle drift at 1.4 (`kinetics/idle.mjs:22`). **So the SPACE is the question before the threshold**: the burst's 359 is in the chart's viewBox units, which is 291 stage px/s once the page is drawn at the one-shot's fit (an 810-wide chart box over a 1000-unit viewBox) and half that again parked - UNDER `STROBE_PX_S` in the space the eye reads it and OVER it in the space the cadence is computed in, so whichever space the rule names decides whether P50 T13's own shipped proof passes. Until a declared cadence exists at a middling speed, `ON1_PX_S` may stay at 250: no shipped frame can tell 250 from 100. **`STOP.IMPACT_S` IS DELETED** (P53 T5, with the grep that proved nothing read it - not the module, not the player, not a test - and the comment that replaced it names the envelope). THE STROBE ROW IS PLACED BUT NOT WRITTEN: it belongs beside M20 in `gate_motion_density.py` (which already mirrors `STOP_ON1_PX_S` and computes a throw's px/s, and can ship INFO and become FAIL in one word when the threshold is ruled), never in the compiler, whose species validation is pure grammar with no geometry - and two of this row's three clauses have no authoring surface to bind: `break_cadence` exists on the proof pages only, a boil is hard-coded on 3s by KIND and carries no speed, and there is no authored-hold grammar at all. RETIRED / RULED 2026-09-14 (P57 T5, the parent, per the operator-accepted recommendation on R26-85): `STOP.IMPACT_S` stays deleted (`dd859b6`); `CADENCE.STROBE_PX_S` 300 and `ON1_PX_S` 250 are KEPT as OPERATOR-SET CONSTANTS with the psychophysical derivation STRIPPED from their comments (`kinetics/stopaction.mjs:25-26`, engine region re-synced: `sync_kinetics: 31 region(s) written`, `--check` in sync) - the R26-85 verification found the 15-arcmin limit misattributed (Braddick 1974's random-dot limit) and Watson 1986 removing the ceiling at 12/8 fps; the comment now says STROBE_PX_S is read by nothing and names the space it waits for (a gate on a DECLARED cadence against the speed, R26-85). RED's 154 px/s cinema-parity reference is recorded beside it, not as a dial. `stopaction.test.mjs` 11/11. | **RETIRED / RULED 2026-09-14 (P57 T5)** - IMPACT_S gone (`dd859b6`); the two strobe numbers stay as operator constants, derivation stripped; the gate row is R26-85's | REVIEWED 2026-09-14: 154 px/s set from the evidence (E99 s30) - the 250/300 labels 'OPERATOR-SET' were false; 300 stays unread pending a sharpness term |
| **R26-57** | **The stage-space text at default `text-rendering`** - T4's rule (R26-48) covers the PAGE's glyphs (`.lp`, `.lp-chart`); `#species .lab`, `.chiplab`, `.flowtag`, `.vmstamp` and the chartbox tier (`.ct / .cs / .csr / .cl`) write SVG text at `auto` and carry the same latent warm/cold defect, unmeasured because Tokyo exercises none of them. Measure on the first build that does (a chip board, a flow diagram, a map) with `determinism_check.py --all` and the per-element diff (`build-short-t4/determinism/ink_diff.py`); widen the rule in that commit and regenerate the goldens it moves on purpose. | open 2026-09-12 |
| **R26-65 CORRECTED 2026-09-12** | **The page CAN land built - `enter=built` has been in the grammar since 2026-09-08 and the one-shot simply did not use it; what is missing is the AXES register and a default** - measured on the first pages-only short (`normal-for-which-bridge/build-short`, 69 s, five worlds): frame 0 is bare cream, the title types on at 4 s, the chart's line exists at 6 s and the page's build lands at 7.4 s (the motion gate prints the instant). The 2026-09-12 retention research (`docs/research/retention/CHART_APEX_RETENTION_ARCHITECTURE_RESEARCH_BLUEPRINT.md`, from the vidIQ watch) asks for the opposite: coordinate axes pre-drafted, the ink already at the apex and the caliper resting on it AT FRAME 0, because the autoplay feed judges the first still frame. Our page has no such enter - `spiral` returns a drawn page and `mount=<s>` fades a world above one that is building underneath, but neither starts BUILT. THE OPERATOR'S FORM (2026-09-12): *"we need to build the ability to land with the ledger already built. I'm surprised we don't already have that capability, shouldn't be hard to add"* and then the register: *"the other option is showing the charcoal built with only the axes drawn, then immediately drawing the chart and starting the analysis, that gives us the first initial frame of motion"*. So TWO enters, not one: `enter=built` (the page lands complete - ground, frame, title, axes, line, the last figure - for a cut that opens on a finished read) and `enter=axes` (the charcoal page lands with its ground, frame, title and AXES only, and the line draws from the first frame, which is the opening's own motion). The second is the default for a hook: frame 0 carries a built page AND movement. M11 reads either as annotated on arrival when the light lands inside 1.5 s of the line's own finish. Measured against the same page in a 9:16 build: 7.4 s of a 69 s short is 11% of the runtime spent arriving, five times over. **THE CORRECTION (2026-09-12, an hour after this row was filed):** the row's first half was wrong. `LEDGER_ENTERS` (`content/video_engine/scripts/build_scene_timeline_f.py:56`) has carried `built` since 2026-09-08 - *"the page ARRIVES with its chart already drawn, by the row's own transition - it NEVER mounts"* - and `test_page_performs.py:552` pins it (*"enter=built arrives drawn"*). The one-shot's opening row declared no enter at all, so it took the default roll-out, and the 7.4 s arrival I measured was my authoring, not the engine. Re-built with `ledger:ev-federal-load-v1:line:240:right:built:cut` in `build-short-built/`: FRAME 0 carries the title, the sub, the y-label, the line drawn to 123% and the source line, and the first caption word lands at 0.4 s. One grep of the enter grammar before filing would have caught this - the Recall rule applies to a row as much as to a build. WHAT IS ACTUALLY MISSING, and it is the register the operator named: `enter=axes` - the charcoal page lands with its ground, frame, title and AXES only and the line draws from the first frame, so frame 0 carries a built page AND motion (the operator, 2026-09-12: *"showing the charcoal built with only the axes drawn, then immediately drawing the chart and starting the analysis, that gives us the first initial frame of motion"*). Plus two smaller halves: nothing makes an enter the DEFAULT for a hook row (an opening page with no declared enter still takes the roll-out), and M11 reads the annotation window off the page's build, which for a built or axes arrival is the line's own finish. | **HALF BUILT 2026-09-12** (P53 T1: `enter=axes` ships - the charcoal page lands on its ground, title, labels and axes and the line draws from frame 0, read on `build-short-axes/self-watch/axes/`; M11 measures its annotation window off the LINE's finish at 3.0 s; `_page_events` also stopped crediting a page that arrives built the six roll-out beats it never plays. STILL OPEN: nothing makes an enter the DEFAULT for a hook row, and which register a hook opens on - axes or built - is P53's human gate 1) |
| **R26-66** | **A transition empties the stage while the narration keeps talking** - the same build, read frame by frame: the suck at 13.8 s leaves cream with one caption from 14.0 to 17.5 s while the sentence "Start with the number. Two weeks ago the ten-year paid four point six six" is already spoken over nothing, and the melt at 40.5 s does it again from 41.5 to 45 s ("And the load sends a bill"), as does the vortex return at 58.5 s. Three holes of 1.5-3.5 s in a 69 s short: 8% of the runtime is a stage with no world under a live voice. The dip is the one transition that is ALLOWED to empty the stage (E-dip: a dip is a world change). A suck, a melt or a spiral is a hand-off, so the next world's cream should be under the outgoing one before the last word of the sentence that motivates it - or the cut lands later, on the gap the M13 rule already finds. MEASURED AND CLOSED 2026-09-12 (P53 T2). `measure_stage_gaps.py <build>` seeks the player across every scene boundary, asks the same DOM the layout gate reads what is ON STAGE, and names the words spoken inside each empty run -> `<build>/stage-gaps.json`. The first read on `build-short-axes` (69.8 s): **6.2 s empty, 8.9% of the runtime** - the suck at 13.79 s and the melt at 40.51 s each 3.1 s and each SPOKEN OVER, while both `cut -> spiral` boundaries measured 0.0 s (the vortex return was never the fault). THE CURE, measured rather than argued: the page that FOLLOWS a world-taking transition arrives with ink (`enter=axes`, P53 T1) - the same build then measures **0.0 s, 0.0%** with the motion gate still PASS - so it is a compiler DEFAULT (`stamp_transition_pages`), not a restaged transition, and no engine work was needed. M31 reads the measurement the way M25 reads the layout probe: INFO until measured, FAIL when a non-dip transition's empty stage is spoken over. THE DIP stays licensed (a dip is a world change). ONE THING LEFT FOR THE OPERATOR: the suck's vortex and the melt's drip are correspondingly shorter on stage - the frames are in `build-short-axes/self-watch/handoff/` and the register is human gate 2 of P53. **CORRECTED THE SAME DAY (E73):** the operator does not see the defect this row claimed - *"I dont think suck and melt get less screen time"* - and the measurement counted no page INK as no world, when a vortex and a cream roll-out are the world on stage. The stamp that put `enter=axes` after every suck and melt is withdrawn; the hook alone opens on axes; M31 is kept as an INFO reading. **RESTORED FOR CHART TO CHART (the operator, the same evening):** *"The empty cream stage isnt supposed to be on stage during the exits ... It doesnt make sense to do that when transitioning from chart-to-chart, that is used for mounting a ledger plate to a narrative plate."* The withdrawal misread the operator; the stamp is back for a ledger page after a suck or a melt out of another ledger page, and M31 FAILs that case (E73 2', 4'). **CORRECTED 2026-09-13 (the exit convention):** `exit` names the transition INTO its scene (E47), so chart to chart is a ledger page that follows a ledger page WHATEVER the transition - the stamp covers a cut between two charts too, and M31 FAILs any non-dip boundary with a page on both sides; the review cut's cream at 0:34 followed a CUT, not the melt (E73, the 2026-09-13 note). | **RESTORED 2026-09-12 (E73 corrected)** - chart to chart only; a plate after the transition is the mount's | REVIEWED 2026-09-14: the clipped boundaries were cuts - build-short-axes authors its suck and melt on s01 and s03 (the pre-E47 exit reading), so no hand-off transformed; the rows are rewritten and the whole sequence returns (E99 s19) |
| **R26-67** | **The ring's flag chip has no room at an apex datum** - P52 T8's flag lands beside the ring, and on the closing frame of the same build (69.0 s, `self-watch/close/opening.png`) the ring sits on the last datum at the right edge of the page, so the chip is painted half outside the page's card in the cream margin with its glyph clipped. `flag_side` is documented as "absent = the side with the room" and the auto choice took the side with none. CLOSED 2026-09-12 (P53 T3, and the frames corrected the first fix). The choice is the module's, not the author's: `ringXExtent` asks the engine's own `resolveTarget` for the DRAWN series' x extent (index 0 and one past the end, which the engine clamps), and `flag_side` still always wins. THE FIRST FIX SENT IT LEFT AND THE FRAME REFUSED THAT: the left of the last datum is where the engine writes the series' terminal tag, so the chip landed in the middle of "x3.9 Federal debt" (`build-short-axes` 68.5 s). A stage painter cannot read the page's labels to dodge one - the ring is a `SPECIES_PAINTER` in stage px, the page's marks live in `PAGE_CTX` and only a `PAGE_PAINTER` is handed them, which is why `span.mjs` can and `ring.mjs` cannot - so past the last 15% of the extent the flag goes UNDER the ellipse, into the plot's own empty room below the tip. Read at 67.8-69.3 s: the chip stands under the ring, clear of the tag and inside the page. THE ELLIPSE'S OWN HALF is narrower than the row asked: `ringMark` takes ink that stands clear ABOVE the ring and inside the ellipse's x reach, so a peak ring is bit-identical and only a ring with a spike through it grows; on this frame the spike is 46-75 px back against an rx of 54, so it fires or does not depending on where it is measured, and the frame is unchanged either way. Widening the reach was measured and refused: it swells every dense-line ring (a mid-chart datum 54x40 -> 70x52, a sharp peak -> 74x114), which moves the `ring-dashed-chip` and `species-proof@proof-ring` goldens - a form change for the operator, not for a fix. | **CLOSED 2026-09-12**, the ellipse's reach noted | REVIEWED 2026-09-14: the ring's flag accepted (E99 s19) |
| **R26-68** | **A `span` paints over the series label and the ink it is measuring** - the same build at 38-40 s: the span's shaded stretch (1981 to today) is drawn as a grey block ON the line, and the page's own series label ("x3.9 Federal debt") sits inside the shade, unreadable, while the span's own label sits above it. M28 passes because the two labels do not overlap - the SHADE is not a label and nothing checks what it covers. CLOSED 2026-09-12 (P53 T4), both halves, and read on the frames it was found on (37-40 s of `build-short-axes`): the line and both figures now read OVER the wash. THE LAYER: a shade is ground, so the painter sinks its rect to the bottom of the ACTIVE chart state's own svg every frame - idempotent, so a cold seek lands it where a play does and a rebuilt state gets its shade back - rather than leaving it in the perform svg that stacks above every state. THE LABEL: the band's PAD is room, not an edge, so the pad YIELDS - where one of the page's own direct labels (a series' terminal tag, a printed value, read off the engine's mark model) stands inside the band's x range and above the ink's highest point, the shade's top stops `LABEL_CLEAR` short of its baseline and the label is left on the ground. The EDGES never move (they are the stretch of time the span names) and the span's own name keeps the band's RAW top, so M28 reads the same geometry it already passed. | **CLOSED 2026-09-12** | **RULED 2026-09-15 (E99 s46): "yes" - the dark span at span_alpha 0.30 reads right; it becomes the default in P61 T13 (the dial a9e8653 shipped off flips on, goldens re-run; approved cuts untouched)** |
| **R26-69** | **A free-ASR clock is not an authoring clock** - P53 T9 built `align_take.py` (local faster-whisper 1.2.1, `word_timestamps=True`, never a cloud STT call) so a Chirp take can carry a clock at all, and measured its limit on the same audio the same day: `small.en` returned **141 words where kokoro's own tokenizer had 155**, and the last word's end lagged **0.84 s**. A shot table anchors every row on a PHRASE (`authoring/words.at` walks the take's words), so a clock missing one word in eleven cannot carry the rows - the tool is for LISTENING and for beat timing until this is fixed, and its own docstring says so. THE FIX is forced alignment to the SCRIPT rather than free-form recognition: the script text is known, so the words are known, and only their times are in question (candidates: a larger model as a stopgap, `initial_prompt` seeding, a Needleman-Wunsch merge of the recogniser's words onto the script's, or a proper forced aligner). Wanted before any Chirp take is authored against. | **CLOSED 2026-09-14 (P57 T1) - shipped in `3642f2a`** (2026-09-13, after this row): `align_take.py --script` makes the output's words the script's own spoken tokens, matched to local faster-whisper's words by Needleman-Wunsch with digits spelled as the script writes them (`word_units`); measured in that commit on the same audio: kokoro take **155 of 155 matched, 0 interpolated**, start error mean 0.086 s (p95 0.345 s - Whisper's early onsets after pauses); the Chirp take **155 of 155, all 20 of the short's phrase anchors resolve** - so a Chirp take can be an authoring clock. `tests/test_align_take.py` covers it. Residual carried, not hidden: the p95 0.345 s onset error is the recogniser's, so a row anchoring on the first word after a long pause may still land early by up to that - the shot table's tolerance question stays with the next Chirp-authored cut. Whisper stays local |
| **R26-70** | **The metric-to-comparator morph (E76)** - show the figure the market quotes (a P/E of 24.8x against 21.5x), then morph it into the number a viewer feels (dollars per share, the change on $10,000), as one repeatable mechanism. Recall: doc 43 §43.5 morphs an object into a chart; E60's counter steps one number up a rescale; nothing turns one number into another. Operator, 2026-09-13: *"showing the P/E and then morphing it to a more visual number would be a great repeatable mechanism"*. **BUILT 2026-09-14 (P57 T11 + T12, implementation lanes, parent-read diffs + frames).** THE GRAMMAR (T11): a SIXTH `chart_to` verb `to: "compare"` (`CHART_TO_KINDS`; M36 counts any `chart_to`, so it is a chart-to-chart transform with no gate change): `metric{value,text,label}` the figure the market quotes, `comparator{value,text,label}` the number the viewer feels, `inputs` + `derive` the AUTHORED arithmetic (parsed and walked by `ast` against a whitelist, never eval; must reproduce the comparator within `COMPARE_TOL` 0.5 % - a near-miss is a refusal, never a tween), `source` (`[DERIVED:`/`[SOURCE:`, E77), `hold: metric|gone`; E50 enforced in `validate_species`: the page must already write a `figure` with `metric.text`. `tests/test_metric_comparator.py` 40 tests. THE PAINT (T12): `species/compare.mjs` (223 lines, a PAGE painter, `PAGE_PAINTERS.compare`, dials `COMPARE` COUNT .72 / SWAP .5 / CROSS .2 / LABEL_AT .55 / GHOST_A .62 / GHOST_F .74 / GAP 14 / SUB_DY 1.3 / WIDTH_EM .56 / PAD 4 / OVERLAP 1.6), dispatched in `paintPerform` after the figures. THE FORM IS E60's COUNTER, not the glyph-outline morph: a page figure is SVG `<text>` with one tspan per character, and Method A morphs OUTLINES - the only path to glyph outlines is a font-to-path library, a second type engine on the page, refused; the digits count from the metric to the comparator on the clock, the label crossfades, the metric ghosts beside (`hold: metric`) or leaves (`gone`). Pure function of t (18 node tests: u=0 exact metric, u=1 exact comparator, identical on two calls). Golden `compare-morph` (+ `@proof-quoted` 11.9 s, `@proof-mid` 12.84 s reading `20.2x` in transition, the end `15 % dearer` with `24.8x` ghosted and `dearer than its own history` beneath) read by the parent; 47 existing goldens byte-identical. `sync_kinetics: in sync (32 module(s): 14 kinetics, 18 species)`; kinetics node tests 366/366; golden_frames + kinetics_sync + metric_comparator 114 passed; Japan/Tokyo motion gates unchanged; `effects_catalog_check` 0 failures (the check learned to pair a compare example with its E50 figure). Card `chart_to:compare` wired, `lives.form: module`. Nit left: the golden's comparator text carries a space (`15 % dearer`) - an authored string in the golden source, not the painter's. CORRECTED 2026-09-14 (P57 T12b, the operator: *"just collapse or melt then re-draw... I don't see why we would need a 'vendored font to path library'"* - E76 s4): the compare's DEFAULT is no longer the counter - the quoted figure MELTS as ink at its datum (\`form: melt\`, composed from \`species/melt.mjs\`'s own \`meltRun\` / \`meltTextFilterMarkup\` - the words' run, one streak under each glyph, on the K-M stepped clock; two exports added to melt.mjs with no change to what it paints) or the hand takes it back (\`collapse\` - the figure's write run backwards), and the comparator is then RE-WRITTEN there by \`figure.mjs\`'s own \`figureGlyph\`; T12's counter kept whole as \`form: count\`, byte-identical on the new \`compare-count\` golden (cmp against the old \`compare-morph\` bytes). Dials \`TAKE_SHARE\` 0.55, \`MELT_BOX\` 3.2 (the one to tune by eye). The two region relocations the module-order rule forced (melt 10514 -> 2180 after morph_a; compare 896 -> 7224 after figure) moved no pixel (67 goldens identical before any compare change) - and the rule itself is R26-115. Frames read by the parent: \`@proof-melt\` (24.8x sagged and streaking, the chart whole), \`@proof-write\` ('15 % de' arriving by the hand at the datum). node 482/482; pytest 187 passed; 70 goldens identical; Japan gate unchanged; catalogue 0 failures (131 cards, 65 options). E76 s5 (the operator, same day: *"it's just math"*) orders the FULL glyph morph too - T12c, \`form: morph\`, outlines computed by canvas raster + marching squares, never a library. THE FULL MORPH 2026-09-14 (P57 T12c, E76 s5 'it's just math' + E88 s5 the ball first): the compare's DEFAULT is now the BALL - the quoted figure's outlines are measured off the page's own ink (`compareShape`: the string drawn on an offscreen canvas at the face read from its own `<text>`, RASTER_S 4 device px per page px, alpha thresholded at 0.5, `kinetics/contour.mjs` walking the 0.5 level by MARCHING SQUARES with the saddle rule stated and tested - the foreground is 8-connected - holes told from outlines by containment parity, Douglas-Peucker at 1.2 raster px; cached by (text, face) once `document.fonts.check` passes; NO font file parsed, no library), then SAG (SAG_GAIN 3.2), BALL UP into a disc that holds that ink's area (r = BALL_SWELL 1.0 x sqrt(area / pi), floored at BALL_MIN 0.34 fs - `MELT.BALL_R` is a share of a PAGE and would ball a number into a 3-px speck), then `then: morph | splash | throw` (morph default): the ball's ONE ring is carried by morph_a into the comparator's glyph rings under the pairing rule (`comparePairGroups`: glyphs by centroid x, rank-paired; a surplus ring born from / collapsing into a degenerate ring DEGEN_R 0.6 at its nearest counterpart's centroid; holes with holes - node-pinned on 5 -> 3 and on 1 -> N, the ball opening); one `<path>` per pair, fill-rule nonzero with holes wound the other way. Grammar `form: melt | streak | collapse | count` (T12b's text melt kept byte-identical as `streak`; `then` on a non-ball form refused by name). Frames read by the parent: `@proof-sag` (the number leaning and sinking in its own hand), `@proof-ball` (one clean white disc under where it stood), `@proof-050` (u 0.775 - 0.50 of the window is still the ball - the ball an oval opening into '% dearer' with the label writing: neither ball nor number). u=1: outlines vs `<text>` max per-channel delta 204 on 686 of 11,400 px - a 1-px antialias rim, interiors identical. 67/67 pre-existing goldens byte-identical; node 507/507; `sync_kinetics: in sync (40 module(s): 16 kinetics, 24 species)`; pytest 194 passed; Japan gate unchanged; catalogue 0 failures (`kinetics:contour` carded). NOTE for HG1: at a figure's scale the ball is small by construction (a 40 px number's ink); the ball that shows is the PAGE melt's - R26-118 gives both the mass and the living surface. | **BUILT 2026-09-14 (P57 T11+T12+T12b)** - `compare`: melt (default) | collapse | **BUILT 2026-09-14 (P57 T11+T12+T12b+T12c)** - `compare`: melt (the ball, default) | streak | collapse | count x then: morph | splash | throw; the outlines are ours (contour.mjs); HG1 | | REVIEWED 2026-09-14: the compare-morph proof is not a morph proof - a morph transforms the whole chart (E99 s1) | **BUILT 2026-09-15 (P61 T2): the SEVENTH `chart_to` verb `remake` - a FULL line chart becomes a FULL bars chart and back on one clock (every keyed datum's ink as one ring column by column, the marks travelling, the axes handing over whole, the labels re-written by the engine's hand, the title transformed); goldens `remake-line-to-bars` / `remake-bars-to-line` + @proof-025 / @proof-075; at u 0.5 the frame is neither chart; HG1 open on `r26-70-compare-morph` with the two clips** (CAPABILITIES 'THE WHOLE-CHART MORPH'; the compare verb stays as built, E99 s1 refused a proof on it) | **RULED 2026-09-15 (E99 s39): "this does work, especially line-> bars"; bars -> line REBUILT in P61 T2b - "collapse to the single apex point and then draw the line back to the root instead of sliding and snapping together"; a morph is "proof of form/function to the audience ... not tricking them"; HG1 ruled, T3 unblocked on line -> bars** | **CLOSED 2026-09-16 (E99 s50): "bars-to-line approved." - the whole-chart morph `remake` ships both ways; HG1 / HG1b closed** |
| **R26-71** | **A bracket's figure is printed across the line it measures** - M34 (the collision ledger, 2026-09-13) re-probed on a private copy of `normal-for-which-bridge/build-review` FAILs three times: `bracket:31% of GDP` on line s0 at 0:27, `bracket:123%` at 0:31, `bracket:31%` at 0:52 - the frame shows "31%" over the debt line. Kept strict (no own-line exemption: text over a line is unreadable wherever it belongs). Fix in the bracket builder's label placement; pass 2 of the bridge short restages these beats anyway. | FIXED 2026-09-14 (P57 T10, implementation lane, parent-read diff + frames). Corrected first: the three instances are `figure` species, not brackets - M34 names them `bracket:` only because `paintFigure` writes into the same `bklab` class, and the gate already read PASS (the operator's own-line exemption, `5aa0f8f`, is kept - it is a ruling), so the fix was proved on the PAIR COUNT, not a verdict: figure-on-its-own-line overlaps on `normal-for-which-bridge/build-review` **20 pairs before -> 0 after** (10 label-instants x the `s0`/`s0:h` paths). THE FIX is the figure's STEP-OFF in `paintFigure` (`scene-evidence-engine.mjs`, +64/-6, not a synced module): `segMeetsBox` (Liang-Barsky), `figBox`, `figClearY` - the label is created at the AUTHORED place, its written advance measured, and only if the box meets the stroke of its OWN series (`st.linePts[series]` / `lpPointsNow`) does it step away in quanta of `PS.FIGURE_STEP` 0.6 x fs, the authored `dy`'s side first, capped at `FIGURE_STEPS` 4 and the chart's box (`FIGURE_PAD` 5, `FIGURE_UP` 1.08 / `FIGURE_DOWN` 0.53, `FIGURE_CHAR_W` 0.62 the fallback advance) - a pure function of the datum, the live points, `dy` and the width, so a cold seek lands where a play does (R26-28) and a clear figure does not move by a thousandth. `tests/test_figure_placement.py` 5 tests on the built player (red before: `box [398.6, 394.3, 140.9, 42] is crossed by its own series`; green after); golden_frames + targeted_species 122 passed; **47 goldens byte-identical** (112 files). Frames read by the parent: `build-review/self-watch/p57-t10/before-t31p78.png` (`31% of GDP` printed through the orange rise) vs `after-t31p78.png` (under the 1980 trough, clear of both series) and `after-t53p92.png` (`31%` clear below the trough). The review build's own engine copy was patched with this diff only (it is 36 KB behind HEAD) so the frames could be read - a review build, never an approved cut. Risk named: the advance is measured on the written glyphs, so a font that has not loaded at a cold seek would measure the fallback face; the cold-seek test passed on the built player. | **FIXED 2026-09-14 (P57 T10)** - 20 pairs -> 0, 47 goldens identical; HG1 frames named | REVIEWED 2026-09-14: the figure clear of its own line accepted (E99 s19) |
| **R26-72** | **E79's shared scale as the enforced default on a tiers page** - `ledger_page.py` now WARNs same-unit tiers on different y-domains and computes `shared_tier_domains`, but the engine's `tierDomain` reads only its own band, so the compiler cannot SET the shared domain. Needs a per-tier `axes.domain` the engine honours (then the compiler writes the shared domain unless `independent: true`). |
| **R26-73** | **The newsreel strip's default is the opposite of E84** - measured 2026-09-13 on the 9:16 whole-reel default: the caption sits at 0.676-0.750 of height and the crawl band BELOW it at 0.78-0.92; E84 puts a whole-reel crawl around the top of the bottom third with the captions above it. The `cap_band: "above"` alternative (band 0.675-0.815, caption 0.596-0.671) is close, but `validate_newsreel_strip` (`build_scene_timeline_f.py:3127`) refuses `"above"` unless a card is docked above the band, so a crawl with nothing docked cannot use it. E84's other case - a crawl on a secondary surface (a TV, a card) with the captions centered - is not supported: the band always draws on the whole frame. Dials: `NEWSREEL_CAP_ABOVE` / `newsreel_caption_band`, the row's `target` y0/y1, `CAPTION_HOME_BOTTOM` 480, `NEWSREEL_STRIP_PAD` 8. |
| **R26-74** | **An embed surface holds images and video (E86; BUILT 2026-09-13, committed `3a6a645`: `fit` cover/contain, clips fill by default, the camera push carries embedded content; E95 BUILT 2026-09-13, committed `3a6a645`, parent-reviewed: `embed_fit(dopt, asset, evidence)` returns the row's `fit`, else None for a press card (E66 reflow) or for a dock whose evidence carries badges / a record / a chart / a stack (a CARD is not a picture - `embed_card_reason`; an explicit `fit` on such a card raises by name), else `cover` for a bare still or clip; the parent's ruling on the agent's question "is a badge card a still?" - the engine draws any entry with `fit` without paper, masthead or rail, so covering a card would crop its badges; `art_embed()` in the golden builder now routes its surfaces through `embed_fit` and its two-badge card stays a card; 102 golden frames + sources byte-identical; `test_art_embed_media.py` +6 tests (a still covers, contain letterboxes, a clip unchanged, a press card no fit, a badge/record/chart/stack card no fit, an explicit fit on a card refused); 193 passed over the five suites; proof frame `scratchpad/proof/media@04.00.png` read by the parent: the CNBC still fills the TV glass to the bezel, no bars)** - the ART-embed surface (P50 T7; a plate's `<plate>.layers.json` `embed` quads; E66 fills it whole) accepts only press cards today; an image dock and a video dock must land on it, perspective-mapped to the quad under its `darken`, the clip on the stage clock, a camera push into it (E59) still resolving. In build 2026-09-13 (implementation lane, proof frames on the tv embed). | **DONE 2026-09-14 (P57 T1) - `3a6a645`** (`embed_fit`, `tests/test_art_embed_media.py` +6) **+ `364ecb9`** (the P55 card): 102 golden frames + sources byte-identical; proof frame `scratchpad/proof/media@04.00.png` read by the parent |
| **R26-75** | **The slide transition (E87 s3)** - the incoming frame pushes the outgoing frame off along one axis, both moving together, so the worlds stay spatially continuous. Named `slide` (our `push` is the camera push-in). Recall: docs_find 0 hits for "slide transition" and "push transition"; the editor already imports `@remotion/transitions` (wipe, book-flip - `editor/src/TransitionEvidence60sProof.tsx`), whose `slide` presentation is the reference; E47 retired the wipe only as the world-change default. Build in the engine's transition clock beside the dip and blur-zoom (min-jerk, lands in the caption gap like a cut), an `exit` kind the compiler accepts with a direction, goldens at two instants. BUILT 2026-09-14 (P57 T13, implementation lane, parent-read diff + goldens). GRAMMAR: `slide:<left|right|up|down>[:<s>]` (`SLIDE_DIRECTIONS`, `SLIDE_S` 0.6, `_slide_parts`/`slide_direction` in `parse_exit`; `slide` in `SCENE_EXITS` + `TIMED_EXITS`); refused by name: no direction, `slide:push` (push is the camera push-in), an unknown direction, a trailing extra, a bad length; `scene_exit` never chooses it (a bare `slide` stays refused, `test_transitions_e47.py:262`). PAINT: the engine's boundary clock (`slideOn`/`slideU` on min-jerk beside `dipIn`/`bzIn`; dials `SLIDE_S`, `SLIDE_AXES`, `slideOpts`): the outgoing world at sign*u*span, the incoming at sign*(u-1)*span - one distance, opposite ends, both mounted, abutting at every instant; the span is the STAGE's own and each world is clipped to its own stage rect (the first try used the `.world` element's 110 % span and the frame showed a 192 px cream gutter - the overhang is Ken Burns' room, not picture); WHOLE-PIXEL translates (a fractional one resamples the layer: 13 grey levels on the E47 band at u=0.005). Ken Burns underneath untouched; no veil, no wipe front (`!slideOn` in `wk`). STAMP/M31: no code change and that is the point - `slide` is not in `WORLD_TAKING_EXITS`, so the incoming chart is stamped `enter=axes` and M31 reads it as chart-to-chart (`test_a_slide_hands_over_and_never_takes_the_outgoing_world`). GOLDENS `slide-mid` (both boards abutting on the centre line, the outgoing legend half off left, the bars page arriving, only the deckle notch at the seam) and `slide-landed` (the outgoing gone, no sliver), read by the parent; the 50 existing goldens' sha256 table identical before/after. Card `exit:slide` planned -> wired (`callable.today: true`, `proof.golden slide-mid`, the remotion `slide` read for its enter/exit pairing, never vendored); `effects_catalog_check` 0 failures and the `SKIPPED_EXAMPLE exit:slide` line gone; CAPABILITIES row added. Tests: chart_transitions + transition_stamps + golden_frames 156 passed; transitions_e47 15; Japan/Tokyo motion gates unchanged. | **BUILT 2026-09-14 (P57 T13)** - `slide:<dir>[:<s>]`, goldens `slide-mid`/`slide-landed`; HG2 watches it on a real seam | |
| **R26-76** | **The melt reworked to E88: the chart melts, not the board** (IN PROGRESS 2026-09-13, uncommitted: mechanics built - board stays, `melt:throw` / `melt:splash:chart` / `melt:splash:plate`, bare `melt:splash` refused, only melt goldens changed + `melt-plate` new, 210 tests; the LOOK failed the parent's frame read - a green jelly slab not ink, hollow green rings not splats, peach discs + maroon rectangles for the plate - then took three look passes; PASS 3 ACCEPTED on the parent's frame read 2026-09-13 (`scratchpad/lanes/melt/proof/{throw,chart,plate}-{015,045,075,100}.png`): 015 each glyph single and legible with one continuous streak of its own ink below it, 045 a crisp round ball with one highlight, 075 splats with a wet rim fading under half opacity so the new chart's labels read, 100 clean; 210 tests + 16 node tests green, only the 7 melt goldens + 6 sources changed; RESIDUAL for the operator's watch in motion (not yet served): the ball is bright orange (the deepened first-series stroke, an operator dial) not near-black ink, and the two lowest series bands striate at 015; the ball's colour is an operator dial; `test_gate_motion_density` still passes an unvalidated `melt:splash` fixture) - `species/melt.mjs` (P52 T9) sags, balls and throws the WORLD's area polygon, so the whole charcoal board leaves. E88: extract only the page's chart information (series paths, bars, marks, labels, annotations) as the melting body while the board, deckle and ground stay; morph the fused drips into a dense heavy ink ball on 2s (weight read in the squash and the stepped fall); then the authored ending - `throw` (off the stage; a new chart then draws on the same board), `splash:chart` (the splatter lands on the board and resolves into the next chart's marks), or `splash:plate` (the splatter seeds a narrative plate that springs up reading as painted - the ink-bloom / K-M soak chain, INTAKE-INK-BLOOM-2026-09-08). Dials start from the current MELT set; proof frames at the four instants per ending, judged by the operator. Queued behind the embed lane (R26-74) - both touch the engine's scene loop. | REVIEWED 2026-09-14: the melt and splash refused as they look; the melt gathers like the vortex into one dense, heavy, vibrating point and splashes into a scenic plate or a full chart - an ink splat into the ink-bloom transition is accepted (E99 s2) | **BUILT 2026-09-15 (P61 T6, proof A): `melt:gather` - the page vortex's map aimed at the ball's centre, every mark and word amassing into the point, the point wearing T5's material and vibrating on drop.mjs's floor, the splash painting the world plate up through the ink-splat -> bloom; ink spread 265.5 -> 59.9 px at constant contrast, no blur, no wipe; goldens `melt-gather` + @proof-point / splash / plate; the default melt byte-identical; proof B (the splash into a FULL chart) waits on T3 and the card `r26-76-melt-endings-in-motion` stays owed until both (HG6 asked once)** | **RULED 2026-09-16 (E99 s51): the gather approved - "This is good, but I'd want to see the ball actually picked up and then thrown forward to splat on the canvas ... I want it to be thrown" - the splash endings gain the THROW (P61 T6b)** | **RULED 2026-09-16 (E99 s56): "approve" - the thrown splash ships; the off-screen throw variant is R26-157; CLOSED** |
| **R26-77** | **The Steel and Paper caption, alive (E90)** - start from the shipped STAGE-mode caption (Steel and Paper build-f; `#caption.stage`, `build_scene_timeline_f.py`, the per-word pop 1.16 -> 1.0 with +-2.5 degrees tilt) and blend: a slightly stronger pop leading, P52 T10's stagger envelope under it (`DUR_S` 0.34, the 0.05 s anticipation), and E49's idle on a held caption page; keep E62's band move under a dock and the caption-energy lessons (no per-word cursor/flash). Proof on Tokyo's private build (never a render): the pop, the stagger and the blend side by side at the T10 instants (25.23 / 25.33 / 25.43 s) plus a player, for the operator's eye and ear. BUILT 2026-09-14 (P57 T14, implementation lane, parent-read frames) as a DIAL with three named settings, opt-in, per the operator-accepted recommendation 5: `cap_life: pop|stagger|blend` on a stage caption page (`caption_life` on the timeline, the page's wins; absent = the shipped caption byte-for-byte; an unknown setting refused by name). The composition lives in `kinetics/stagger.mjs` (`popAt`, `lifeAt`, `lifePage`, frozen `LIFE`: `POP_LEAD` 1.22 over the base 1.16, `POP_S` 0.20, `ANTICIP_S` 0.05, `OPACITY_K` 2, `MP` 0.04; the stagger's `DUR_S` 0.34 / `RISE_PX` 22 untouched), the engine's caption paint calls `lifeAt` by name; a held page breathes on E49's existing `breath`. The BLEND = the pop's scale + tilt riding the stagger's RISE - and, read on the first frames and corrected: NO blur (with it the word was unreadable at the instant it was spoken) and the stagger's own scale dropped (two scales multiplied put the word at 0.97 at onset - the quiet register, not a pop leading). PROOF on a PRIVATE copy of Tokyo `build-short.v2` (never rebuilt, never rendered - R26-45): nine frames at the T10 instants 25.23 / 25.33 / 25.43 s for the three settings + a contact sheet (`scratchpad/p57/t14/tokyo-private/self-watch/p57-t14/`), served for the ear at `http://127.0.0.1:8761/player-{pop,stagger,blend}.html` (not in launch.json). The parent's read of the band sheet: pop crisp and tilted from the onset; stagger invisible at 25.23 and a ghost at 25.33 - the voice outruns the read; blend legible at every instant with 'been' arriving lower and larger mid-rise, nothing pins the eye, the strip in its E62 band. 52 goldens byte-identical; Tokyo's GATES-MOTION.md hash unchanged; `test_caption_life.py` new; golden_frames + kinetics_sync + caption_life 89 passed; `stagger.test.mjs` 17/17; `sync_kinetics` in sync (32); Tokyo motion gate unchanged; card `caption` gains the three options + `dials: LIFE`; CAPABILITIES row added. HG2 rules which setting becomes the default; until then nothing is authored. | **BUILT 2026-09-14 (P57 T14)** - `cap_life: pop|stagger|blend`, opt-in; HG2 picks by eye AND ear at :8761 | |
| **R26-78** | **The race's two path settings (E91)** - port P52 T17's arm B into the engine as an opt-in: a race page's `path: "eased"` (default, the engine as is) or `path: "clothoid"` (the substitution in `tokyo-tea-break/build-short-t17/engine-clothoid.mjs`, `clothoidFit` through the period knots, the period clock unchanged); the compiler accepts the key on a race, goldens byte-identical for the default, a golden pair for the clothoid. Queued behind the melt lane (R26-76) - both touch the engine. BUILT 2026-09-14 (P57 T15, implementation lane, parent-read). GRAMMAR: `ledger:<series>:race;path=eased|clothoid` (`RACE_PATHS`, a `PLATE_OPTS` key; `path=spline` refused by name; `path=` on a non-race page refused naming the page's builder; absent writes no key - the default is the engine as it was, arm A). THE SWITCH: `lpRacePos(R, u, j)` beside `lpRankPos` in the engine - eased returns `valueAt` + `lpRankPos`; clothoid reads `lpRaceFits` (`kinetics/clothoid.mjs`'s `clothoidFit`/`clothoidAt` through the PERIOD KNOTS in track px, Catmull-Rom tangents) at `smoothstep(u - i)`; `paintLedgerRace` calls it once per row; dials `LPX.RACE_PATH`, `RACE_FIT_VMIN`, `RACE_FIT_CHORD`. Nothing moved into `clothoid.mjs` - the knot/tangent mapping is the race's own geometry, the fit is the module's. THE PERIOD CLOCK IS IDENTICAL IN BOTH (the knots do not move; `test_race_path.py` 14 tests, 13 red before / green after, pin positions equal at the knots and different between). GOLDEN PAIR `race-path-eased` / `race-path-clothoid` at t 5.1 s (u 0.5, mid-period, no swap in flight so T16 cannot disturb it): identical rows, order and values; in the clothoid frame CHI rides ~2 px up and ALPHA/BETA ~5 px down toward their next lanes - the eased arm holds its lane, the clothoid leans into the turn. The 52 pre-existing goldens identical (table 04cc4fef... before and after). golden_frames + race_path 71 passed; kinetics node 374/374; `sync_kinetics` in sync (32); `effects_catalog_check` 0 failures; the race card gains the option (130 cards, 62 options); CAPABILITIES row 'THE RACE'S TWO PATH SETTINGS' added. E91 s1 honoured: both ship as named settings, neither discarded; HG2 watches the pair. | **BUILT 2026-09-14 (P57 T15)** - `path=eased|clothoid`, golden pair, period clock identical; HG2 A/B | |
| **R26-79** | **A race's rows collide at a rank swap (E91 s2)** - measured on the fixed T17 players 2026-09-13: arm B at 7.5 s prints BETA and ALPHA in one row ("ALBETA") with "136" over "138"; the older stills showed DELTA over CHI at 8.7 s. Two rows passing must separate their labels and values (a lane offset while crossing, or the passing row's label yielding for the crossing's duration); M34's collision ledger should catch it on a probed race page. FIXED 2026-09-14 (P57 T16, implementation lane, parent-read golden). THE CURE IS A LANE, and it had to be HORIZONTAL: a vertical lane is topologically impossible (two labels that exchange vertical order must pass through each other - built, measured, it only moved the collision from t 7.407 to 7.227), and the name column (150 px) has no room beside it, so the PASSING row's label and value step INTO its own bar and step back out; a YIELD was rendered too (`race-swap-yield.png`: it deletes BETA's name and number at the instant of the overtake) and stays as `LPX.RACE_CROSS: "yield"`, one branch, no compiler key. Dials beside `RACE_PERIOD`: `RACE_LANE_NEAR` 1.0, `RACE_LANE_FULL` 0.7, `RACE_LANE_PAD` 12, `RACE_LANE_HALO` 3, `RACE_CROSS` 'lane'. The WINDOW is the pair's own separation (`swaps` names who passes whom; openness is the gap on the page), not periods - the clothoid path reaches a crossing up to 0.23 of a period off the solved centre. A pure function of u on BOTH settings (`test_race_swap.py`: 57 collisions before / 0 after on eased and clothoid; the passing row is the one in the lane; the lane is shut wherever no pair crosses; a cold seek lands where a play does; the golden unchanged). GOLDEN `race-swap` at 7.407 s (the solved ALPHA/BETA crossing), read by the parent: the two bars coincide at the crossing (the nature of a swap), ALPHA outside at the column, BETA haloed inside the bar, 135 inside the bar's end, 134 outside - two names, two numbers, none on another; HG2 judges whether it reads as two rows. THE GATE: M34 reads INFO on a race page (no series lines, no ring/callout marks) - M28 is the row that catches this: pre-fix `[FAIL] M28 ... val:133 on val:138 at 0:07 (s01), 150 px`, after `[PASS] M28 ... (171 label pair(s) checked)`. 54 existing goldens identical; golden_frames + targeted_species + race_swap + race_path 144 passed; `sync_kinetics` in sync (32); `effects_catalog_check` 0 failures. | **FIXED 2026-09-14 (P57 T16)** - the horizontal lane; M28 FAIL -> PASS at the crossing; golden `race-swap`; HG2 | |
| **R26-80** | **The numbered agenda, beautified (P52 gate 3; E93: each row carries a catalogued icon stamped on after its sentence is read; the model is Steel and Paper's three-question TEST card)** - the operator, 2026-09-13: *"the frames themselves are fine, thogh I think we need to beautify the agenda list a bit"*. Today (`species-proof@proof-agenda.png`, 28.5 s) it is three plain rows - a gold numeral, white bold sans, a grey hairline under each - parked bottom-right with the upper two-thirds of the page empty. Candidates to prove side by side: rows placed and scaled to the page's quiet zone; a medallion per row carrying the numeral or a catalogued icon (the operator's 44 cutouts, `content/video_engine/assets/icons/finance_icons_catalog.v1.json`, woodblock red-sun/gold/navy, matched by `semantic_tags`); the house display face for the row text; a deckle-edged row card or an inked underline drawn on the curvature stroke instead of a flat hairline; the sub-value ($4,500) set as a figure, not grey body text. The icons render only once the operator has approved them - E94 (2026-09-13) approved the 44 (`operator_approved`, `render_eligible: true`) and the compiler's `catalogue_icon` refuses by name any id not in that state. Motion judged on the served species-proof player. | REVIEWED 2026-09-14: still owed: the beautified agenda page, the plate version of the list effect (E99 s16) | **BUILT 2026-09-15 (P61 T8): `form: "page"` on the agenda - a titled board, each row on its own mount with a medallion numeral the nib draws, the sentence written on its beat, the figure in gold, the CATALOGUED icon stamped on (a fall with a squash) only once its sentence is read (E93); the compiler's `catalogue_icon` is the first render path to the icon catalogue (refuses by name on kind / approval / render_eligible / sha); the dock form byte-identical; goldens `agenda-page` + @proof-first-row / stamp / full; HG8 open on `r26-80-agenda-page-owed` (the clip beside today's corner list)** | **RULED 2026-09-15 (E99 s44): "Good concept ... the actual build process is good" - the page form ships; the proof's text and icons are authoring, not the mechanism; CLOSED** |
| **R26-81** | **The three-question TEST card on a short (lifted, never used)** - recall 2026-09-13: the engine already draws it (`scene-evidence-engine.mjs:3587-3625` `if (C.checklist)`: question cells type on, answer cells get the `hlband` highlighter sweep, columns auto-fit; data `steel-and-paper/evidence/objects/ev-test-scorecard-v1.series.json`, docked at 492.3 s and 778.6 s of build-f), but no short has used it (0 hits for `"checklist"` in Tokyo, Japan, the bridge) and no frame of it is on disk. To use it on a short: the palette in the cream/charcoal tokens (defaults `#3bc9b0` / `#ff8a8c` are for a dark card), a 9:16 layout (four columns become two stacked cells per row), each row's `delay_anchor` keyed to the short's word times; it stays a dock (`ledger_page.py` UNCHARTABLE). The operator's pointer (2026-09-13) makes it the model for the agenda (R26-80, E93). |
| **R26-82** | **The VERDICT STACK on a short (the evidence wall, lifted, never used)** - recall 2026-09-13: doc 29 s9.24 / s9.24b, CAPABILITIES "VERDICT STACK species" (LIVE, operator-approved at Steel and Paper s67-70: `build-f/evidence-dock.json:1198` `ev-holds-stack-v1`, nine proofs 702.87-723.69 s, `clear_at` 726.98; the operator's frame shows `ev-weight-check-v1` "We checked. It's bigger." pulled forward at 720.5 s); motion in `scene-evidence-engine.mjs` (mount 3230-3272, `drawStack` 3720: fly-in from depth, focus hand-off, idle drift, radial burst on the pivot line). Not Remotion (remotion-bits was a reference only; mosaic/fracture never built). No short has used it and no rendered frame is on disk. To use it: a short with enough earlier docked evidence to recall; the 16:9 rail spots and the -940 depth fly-in re-laid for 9:16; the item times and the host dock window in ONE file (the split caused a 0.77 s dimming drift, s9.24); proof frames + a player for the operator. | REVIEWED 2026-09-14: right direction; owes Steel and Paper's choreography, cards that move rather than a horizontal line, and the burst it earned (E99 s21) | **BUILT 2026-09-15 (P61 T7): `VERDICT_9X16` - the five phases re-laid for 9:16 (nine rail spots in the safe box, the focus card 63.9 % of the stage, the burst radial from the mosaic's centre, the railed idle a named kind) and the compiler's `stack_entry` deriving the payload and the host dock's window from one set of times (the 0.77 s drift made mechanical); goldens `verdict-stack-9x16` + @proof-enter / focus / idle / burst; `VERDICT` and the 16:9 goldens untouched; HG7 open on `r26-82-verdict-stack-choreography` beside the Steel and Paper reference clip (build route 702.87-727.6 s)** | **RULED 2026-09-15 (E99 s43): "we probably don't want to be sending peoples eyes scattered everywhere ... 1-2 on top, left to right ... then 3-4, on bottom, 5-6 on top, 7-8 on bottom" - the rails re-laid in reading bands, P61 T7b** | **RULED 2026-09-16 (E99 s59): "The pair method is good ... the last card should land in the middle so that we can have a gather before the burst ... we're missing the gather, and then the burst should be tighter" - P61 T7c, read off Steel and Paper's beat** **E99 s61 (2026-09-16): the 9:16 stack APPROVED as built by T7c (the last card at the centre, the 0.9 s gather, the burst 35 ms / 0.42 s); the 16:9 stack keeps Steel and Paper's own choreography - T7d returns its dials and goldens to the pre-T7c bytes.** |
| **R26-83** | **Commit the 2026-09-13 evening batch** (DONE 2026-09-13 night, the operator: "commit todays work" - five commits on main `3a6a645` engine, `364ecb9` P55 catalogue, `3e7f736` P56 recipes + floor, `7c59dfa` rulings/docs/memories, `2684c2c` projects/assets; 9 ahead of origin, NOT pushed; left out by rule: the five never-stage files, every player/proof HTML, the root scratch files, `.agents/`) - uncommitted in main after `5aa0f8f`: OPERATOR-RULINGS E80-E95 (faces, numbers as starting points, the audience target, dock cues, the strip law, presentation as moat, surfaces hold media, the transition kit, the melt, the serif press card, the Steel and Paper caption base, the race settings, the publish folder, agenda icons, icons approved, stills fill); M29 widened (E83) + tests; R26-74 embed media (`build_scene_timeline_f.py` `fit`/`embed_fit`, the engine painter + camera carry, `tests/test_art_embed_media.py`); the melt rework (R26-76, after its look pass); BRAND-SHEET / SOUND-SOURCING / doc 21 / doc 51 amendments; OPEN-GATES marks; BACKLOG R26-73..R26-92; the P53 plan's HG2 mark; the icon catalogue (44 approved, v2 re-cuts, v1s in the Recycle Bin; `assets/icons/**` is untracked - decide what of it is tracked: catalogue + cutouts + sheets, per doc 27 durability classes); the race T17 generator fix (`tokyo-tea-break/build-short-t17/build_race_arms.py`, private proof); the P55 plan (copy from the sweet-villani worktree `.claude/PRPs/plans/` to main); ADDED 2026-09-13 evening: the whole P55 implementation (cards, generator, gate, modules, goldens, editor routes), the studio readiness review, the pipeline grill ledger + rulings E96-E97 + rows R26-102..108, the three P55 test-pin fixes, the re-measured `page-boxes.v1.json`, and the untracked Bravos dossier `sources/reference_analyses/bravos-the-bubbles-final-phase-has-begun/` (09-12). Before committing: melt look pass done, docs layers regenerated and in sync, touched suites green, NUL scan, allowlisted staging (never bridge_send.py, the two reference_analyses REPORT.md, steel-and-paper build-f GATES-MOTION.md, tokyo SHOT-TABLE-SHORT.py), one `Recall:` line per hit in mechanism commits. `main` is 4 commits ahead of origin: push only on the operator's word. |
| **R26-84** | **Astra (Codex lane) back on the engine - the conditions** (operator, 2026-09-13: Astra hands off for 2-3 days during the changes; asked whether the editor is safe for multiple agents) - not yet: (1) R26-83 committed; (2) Astra works in its OWN git worktree on a branch off that commit, never the main checkout while Claude lanes write; the engine (`scene-evidence-engine.mjs`) and compiler (`build_scene_timeline_f.py`) take one writer at a time, handed out explicitly; lane table PRP_EXECUTION "Lane write sets"; (3) for AUTHORING episodes, after P55 T3-T4 (the catalogue + drift gate) so a less-informed agent resolves effects by name. Editing a build through the editor's override sidecar is safe once (1)-(2) hold. | REVIEWED 2026-09-14: not yet - no separate engine worktree; the route is saved states or per-agent engine and compiler copies (E99 s23) | **DESIGNED 2026-09-15 (P61 T10): three routes costed in `docs/research/runs/p61-saved-states/OPTIONS.md` - (1) the override sidecar (built; solves five row fields, not the engine), (2) SAVED STATES (a named, restorable, diffable snapshot of a build's inputs + the engine revision, proven by the determinism check) - the recommendation, as its own plan P62, (3) per-agent engine copies - refused as E99 s23's sprawl with fewer tools; one P61-sized slice named: `apply_sidecar` stops rewriting the shared episode shot table (`authoring/table.py:59`, `:69`); HG10 open on `r26-84-engine-saved-states` (rule)** | **RULED 2026-09-15 (E99 s47): "work has to be parallel ... worktrees with both agents aware of each worktree, and accept that there will be sprawl at times until things get sent back to main" - s23 amended; P62 = the worktree register + merge discipline, not saved states; route 3 stays refused** **CLOSED 2026-09-16 (P62): the operator - "isn't a better way to just check git before pushing to main and always preferring non-destructive methods?" - so the register is the hand table `docs/WORKTREE-REGISTER.md` (one row per checkout, a ruling number claimed there before it is written), the merge-before-main checklist lives in PRP_EXECUTION "Worktrees and lanes" and the steward, and `test_worktree_register.py` fails a merge on a duplicate number or an unregistered worktree; Astra's worktree is cut off main (`git worktree add`, never a clone) when its slice is named.** |
| **R26-85** | **The strobe ceiling: read the Gemini reply, then rule and gate** (VERIFIED 2026-09-13 night: `docs/research/runs/strobe_stop_motion/VERIFICATION-2026-09-13.md` - the arithmetic reproduces (phone 279.6 / 186.4, monitor 120.6 / 80.4 px/s; centre-local desktop ~114 / ~76) but the 15-arcmin limit is Braddick 1974's RANDOM-DOT correspondence limit misattributed to Baker & Braddick 1985; Chapiro tested nothing below 24 Hz and no mean luminance above 40 cd/m2 (the "0.1-300 cd/m2" is fabricated); RED's 1/7 picture width per second is CONFIRMED verbatim = 154 px/s on the 1080 stage at 24 fps WITH a 180-degree shutter; the operator's Watson 1986 REMOVES the ceiling at 12/8 fps (r_max = (w_s - w_l)/u_0 < 0 for w_l ~ 30 Hz) and gives the law's shape as speed x edge sharpness; the OLED hold argument is contradicted by Watson Exp. 2. RECOMMENDED for the ruling: keep 250/300 as operator-set constants, strip the derivation from the docs, add 154 px/s as the cinema-parity reference and a sharpness term on r*u_0; `nasa_tp2211.pdf` is NASA TM-84352 (Watson & Ahumada 1983), misnamed) (REPLY LANDED 2026-09-13: `docs/research/runs/strobe_stop_motion/findings.md`, 155 lines - the space is ANGULAR: Baker & Braddick 1985 d_max ~15 arcmin per step (reported CONFIRMED), derived ceilings phone 6.1" at 32 cm on a 1080 stage ~279 px/s on 2s / ~186 px/s on 3s, 24" monitor at 60 cm on a 1920 stage ~121 / ~80 px/s; contrast not significant, luminance is (Chapiro 2019); the RED 154 px/s pan rule PLAUSIBLE; every number is [DERIVED] by Gemini - the parent has NOT yet verified tiers, DOIs or the arithmetic; the operator's own `1986_WatsonSampledMotion.pdf` sits untracked at the repo root) - order sent 2026-09-13 18:22 (conversation `083c2b7c-c869-4865-9157-8e439f4a82b5`, packet `5ce6177c...`), output `docs/research/runs/strobe_stop_motion/findings.md`, marker `strobe-order-2026-09-13`; builds on `docs/research/runs/strobe_units/findings.md` (screen space; 300 px/s UNSOURCED; RED ~154 px/s). Verify tiers and URLs (research-gate-tiers), bring the operator the space + a range, then write the gate row R26-64 decision two named (a declared cadence stepping on 2s/3s faster than the ceiling FAILs) and fix `ON1_PX_S` vs E2 s7's 100. | REVIEWED 2026-09-14: RULED (E99 s30): ON1_PX_S = 154 px/s, the CONFIRMED cinema-parity reference; review on real motion |
| **R26-86** | **P55 THE EFFECTS CATALOGUE - approval and its two gates** (DONE 2026-09-13 except HG2, uncommitted: approved by the operator's `/prp-implement P55`; HG1 delegated to the parent; T1-T11 complete with evidence in the plan; `python content/video_engine/scripts/effects_card.py "<name>"` resolves any effect; OPEN: HG2, the operator's one read of the gallery served on launch `p55-effects-gallery` :8758; the commit batch needs the operator's word) - `.claude/PRPs/plans/P55-THE-EFFECTS-CATALOGUE.plan.md` (drafted 2026-09-13 in the sweet-villani worktree, `scripts/prp_validate.py` PASS; `npm run prp:validate` has no package.json in either checkout). Awaiting the operator's approval; then HG1 (the name ledger: "evidence wall" -> the verdict stack, "test card" -> the checklist, titles for the three `stack` tokens) before T3, HG2 (the gallery earns its keep) after T9. T1-T5 and T8 run off the contended engine files; T6-T7 queue behind R26-76 and R26-74. | REVIEWED 2026-09-14: the operator asked why the gallery was offered as keep / trim / retire - the gate question had no recommendation behind it (E99 s14) | REVIEWED 2026-09-14: the gallery does its job; it owes a faster build and motion examples (E99 s20) | **HG9 CLOSED 2026-09-15 (P61 T9): the gallery builds in 0.098 s median (was 0.201; 0.488x) by one `scandir` (`FrameIndex`) + `copy_if_stale`, is pinned by three page goldens (180 tiles asserted), and three effects a still cannot show carry clips by a stated rule (`chart_to:compare`, `exit:melt`, `dock_payload:stack`); approved on its number (queue card `p55-gallery-speed-and-motion`, ruled)** |
| **R26-87** | **Open human gates across the running plans (operator reads)** - P52: gate 3's MOTION on the served species-proof player (`content/video_engine/review/gates-2026-09-13/species-proof-player/`, launch `gate3-species-proof` :8756, frames accepted 2026-09-13; the ring instant shows charcoal to the frame edge, not the golden's cream rim - operator to say if wrong). P53 (running): HG1 the opening register (frame 0 and 0.5 of the axes register vs the built one), HG3 the strobe row (R26-85), HG4 the cutout dock's first frame (R26-59). P51 (running): HG1-3 - read the self-watch checklist, three hand edits in the editor, the editor's first controls (an operator session). P54 (running): HG3 / T7 the Astra-vs-Fable bake-off brief and verdict. | REVIEWED 2026-09-14: P52 gate 3 approved in motion (E99 s16); P51 gates 2 and 3 closed as not yet meaningful - the editor exposes integers and the timeline comes first (E99 s17) |
| **R26-88** | **The bridge short's pass 2 - the story-first restage** (first-pass-additive-capability-led; the one-shot `normal-for-which-bridge` is gate-clean and thin) - comparators before series (rate vs its history band, the two loads compared directly, the same rate on 31% vs 123%, the bill vs a sourced comparator), capability inventory per beat (plates + docks, stop-motion, transforms that land on a new chart, the chart-to-chart transitions), `build_short.py` rows rewritten to the INTO exit convention (E47; the stamps were corrected 2026-09-13), and the natural test bed for R26-81 (the TEST card) and R26-82 (the verdict stack) and M34 (bracket labels now pass). Fetch FRED/Yahoo/SCML and send Gemini bridge orders without asking. |
| **R26-89** | **Two bridge replies waiting** (DONE 2026-09-13 night: both closed with `result.md` under `docs/research/runs/bridge/done/<packetId>/`; `f95ebada7174` was the P54 reasoning chunk 03, verified clean, nothing to ingest; `f4541f7f6808`'s open requests became R26-109) - `f4541f7f6808` (claude review of "The Myth of Historical Normal": REQUEST CHANGES, unread) and `f95ebada7174` (gemini paths-written, POSITION done - likely the icon tagging/catalogue the operator commissioned; the catalogue was verified by the parent 2026-09-13: 44 paths + sha256 match, 214 tags). Read and close each through `bridge_reply.py` / the daemon; the review's changes become rows or a rewrite order. |
| **R26-90** | **CAPABILITIES rows owed for 2026-09-12/13 work** - add or amend rows (the owning lane, one row each): ART-embed media (`fit` cover/contain, clips + stills fill, camera push carries embedded content - R26-74, E86, E95); the melt's three endings (R26-76, E88); M32 / M33 / M34 the collision ledger (probe exports series lines, ellipses, owners; brackets on their own line and names near their own line's end pass; a mark over its own target WARNs toward the spotlight); G13 by function, G46-G48; the E79 scale WARN (`ledger_page.py` `independent`); `check_gated_pipes.py` + the `~/.claude/hooks/gated_pipe_guard.py` PreToolUse hook; the race A/B proof players fixed (silent clock + real plates); the verdict-stack row's stale template path (P55 T5 owns that one). | **DONE 2026-09-14 (P57 T3)**: eight rows written into `docs/content-video-engine/CAPABILITIES.md` - SIX added (an embed surface holds pictures and clips, E86 / E95, `3a6a645`, `tests/test_art_embed_media.py`; the race page's A/B proof players fixed and read, the 14 s silent clock and the real plates verified playing in `build-short-t17/proof/player-check/player-check.json`, E91 -> R26-78 / R26-79; M32 / M33 / M34 the seam's black frame, the spoken visual and the collision ledger, `5aa0f8f`; G13 by FUNCTION with G46 / G47 / G47b / G48, `5aa0f8f`; the E79 scale WARN, `ledger_page.scale_warnings` + `independent`, `5aa0f8f`; `check_gated_pipes.py` and the `~/.claude/hooks/gated_pipe_guard.py` PreToolUse hook, `5aa0f8f`) and TWO amended (the melt exit reworked to E88's three endings - the chart melts, the board stays - `3a6a645`, goldens `melt-page@proof-{015,045,075,100}`, `melt-plate`, `melt-splash`; the icon CHIP row for E93 / E94 - the operator's 44 cutouts approved, `render_eligible: true`, 220 semantic tags, the agenda stamp RULED and unbuilt at R26-80), the verdict-stack row's stale template path deliberately untouched because P55 T5 owns it, and the generated layers deliberately not regenerated because T4 owns them - `build_docs_manifest.py --check` reads `in sync (412 documents, 79178 bytes of Markdown)`. |
| **R26-91** | **Pre-existing reds to triage (not caused by 2026-09-12/13 work)** (TRIAGED + FIXED 2026-09-14: 62 reds -> 4; the whole video-engine tree `4 failed, 2964 passed, 26 skipped`; root SEO tree 432 passed; Flow driver node 65/65. Root causes: (1) `core.autocrlf=true` with no attribute rule handed out CRLF bytes for the pilots' hash-bound artifacts - `.gitattributes` now marks `**/pilots/**`, `**/animatic/**`, `**/*.svg` as `-text`, and 48 stale CRLF pins were re-pinned to the LF (index) hash with a per-pin proof in `projects/systems-and-blowups/pilots/LINE-ENDINGS-2026-09-13.md` (0 content drift); (2) the engine's own `src` package shadowed the repo-root `src.config` in a full run (`cli.py` loads the root config by path; `test_footage_harvester` imports by full name); (3) moved assets re-bound by sha256 (86 deck slides, 20 review plates, the ep1 master) into their gitignored expected paths; (4) two shipped defects fixed - the audio cache's invalid-entry branch crashed instead of resynthesising, the editorial-motion schema rejected `layer_activation`; (5) 11 stale pins re-pinned; (6) 19 tests skip with the missing file named (render outputs and plates never committed) - see the skip reasons for the builder commands. 2026-09-14 close: the whole video-engine tree `0 failed` after the finance channel closed (see R26-111); the earlier state - LEFT RED (4): `test_finance_channel` x3 - the Money Physics `asset-catalog.v1.json` carries v2 fields (`style_families`, `style_version`, `kind: world`, `review_state: operator_approved`) under a v1 declaration with `additionalProperties: false`, and `generation-manifest.v1.json` has 19 assets against a pin of 15, with 11 referenced cutouts on no disk - a finance-lane decision (bump to v2 + re-pin 19 + restore or strike the cutouts); `test_production_editor::test_real_current_bubble_snapshot_v2...` needs the two never-committed plates `default-bubble-reaction-v1.png` / `wrong-bubble-elevators-v1.png`) - proved on a clean HEAD worktree 2026-09-13: `test_field_ink_e67` (field ink one table), `test_video_dock::test_the_placed_clip_paints_inside_its_rectangle_and_never_on_the_plot` (also red on the embed lane's HEAD engine), `test_dock_over_build::test_each_drawing_beat_is_its_own_window`, `test_motion_gate_wiring::test_ep1_report_is_the_four_fail_baseline`, `test_measure_motion_energy::test_headless_sampling_returns_stage_elements_smoke`, Tokyo M11; the full suite was 66 failed / 2,568 passed with most in old August lanes. S02's cite test was fixed 2026-09-13. Each: fix, re-baseline with its reason, or retire the test with a note. |
| **R26-92** | **Docs-layer and review-server housekeeping** - the docs manifest markdown is 77,864 B against the 80,000 cap (~2 KB headroom; P55 adds an effects layer - measure before and after); regenerate all 8 layers after the engine lanes settle (the animation registry goes stale while `scene-evidence-engine.mjs` is mid-edit). Review servers started 2026-09-13 in the sweet-villani `launch.json` (gates-review :8752, gate5 race arms :8754/:8755, gate3-species-proof :8756, bridge-build-review :8751) - stop them once their gates are ruled; the five-server limit per worktree is already hit. | **DONE 2026-09-14 (P57 T4, the parent)**: (a) measured - `netstat` shows NOTHING listening on :87xx (58 listeners, none in range), so 'stop them' was already true; main's `.claude/launch.json` pruned 13 -> 5 configurations (episode-player :8731, brand-cards :8732, tokyo-short-player :8733, japan-short-player :8734, tokyo-short-player-v2 :8738 -> `build-short.v2`), the dead ones (candidates, p48/p49/cam test builds, the t7 editor, t0 side, a build-f proof under another project's server) deleted, and the rule written INTO the file (`_rule`): at most FIVE per worktree, one per build a human may be asked to watch; a lane's proof build is served from scratch and never enters the file. (b) the manifest headroom RECORDED: **79,178 B of `MD_MAX_BYTES` 80,000** (`build_docs_manifest.py:97`), 822 B left; `MD_LADDER` (`:100-103`) drops a rung rather than failing, so the cost is detail per line, never a break - the rung is visible in the next STALE. (c) the layer-regen rule is one paragraph in `docs/runbooks/RECALL-RECEIPT.md` s3: regenerate all ten with `build_docs_layers.py --write` after an engine or doctrine lane settles, never mid-edit of the engine, never inside a slice that does not own the layers. (d) `build_docs_layers.py --write` then `--check`: `every layer in sync (10 layers)` - docs-index 4130 records / 339 files, research-ledger 20 runs (0 orphan), effects-catalog 129 cards / 39 recipes, manifest 412 documents / 79178 B, gates-registry 160, animation-registry 810 (535 implemented, 86 tracked, 14 retired, 0 orphaned). | **DONE 2026-09-14 (P57 T4)** - launch.json 13 -> 5 + the rule; manifest 79,178/80,000 B; runbook line; 10 layers in sync |
| **R26-93** | **REGRESSION: `idle=live` paints nothing - the approved Japan short's pages would hold still on a rebuild** (FIXED 2026-09-13, committed `3a6a645`, parent-reviewed: `kinetics/idle.mjs` `IDLE_KINDS` + the `live` branch restored verbatim from 72b7516, engine region re-synced by `sync_kinetics.py --write` (1 `kind === "live"`, `--check` in sync); the gate `test_idle_e49.py::test_every_compiler_idle_kind_is_a_kind_the_module_paints` FAILED before the fix (`compiler IDLE_KINDS the module never paints: ['live']`) and passes after; node `idle.test.mjs` 11/11 incl. live = breath scale + drift dx/dy exactly; pytest idle + kinetics sync + golden frames 84 passed; 44 golden frames byte-identical. Nit left: the restored branch is indented 4 spaces where its neighbours use 2 - fix on the next idle.mjs touch, not worth an engine re-sync while other lanes write the engine) (found by P55 T1 group A 2026-09-13, traced by the parent) - 72b7516 (2026-09-08, E49 amended on the first watch) added the `live` idle (breath + drift: "a chart at the page centre is never still") INLINE in the player template's kinetics copy and in the compiler's `IDLE_KINDS`, but never in the source module `content/video_engine/scripts/kinetics/idle.mjs`; 235832a (P48 T2, the rescale) re-synced the inline region from the module and deleted it (`-    if (kind === "live")`). Today `build_scene_timeline_f.py` `IDLE_KINDS` still accepts `live`, `idleXf("live")` returns the identity, and the engine carries 0 `"live"` branches. The approved render (2026-09-09) and `japan-tariff-trick/build-short/player.html` (built 2026-09-10) still carry the branch, so what shipped moved; any rebuild of the 6 `;idle=live` rows (and the spotlight's `idle: "live"` at 58.8 s) would not. The only test (`test_idle_e49.py::test_split_idle_reads_the_option_and_refuses_an_unknown_kind`) checks parsing. FIX (queued behind E95 as the engine's next writer, before P55 T6): restore `live` in `kinetics/idle.mjs` (`IDLE_KINDS` + the `idleXf` branch, verbatim from 72b7516), `sync_kinetics.py --write`, a node test that `idleXf("live", t)` moves at the page centre (dx/dy non-zero AND scale != 1 over a second), and a Python test that every compiler `IDLE_KINDS` token is in the module's `IDLE_KINDS` (the drift class that let this ship - the module rule's own gate); goldens byte-identical (none uses live). | **DONE 2026-09-14 (P57 T1) - `3a6a645`**: `kinetics/idle.mjs` +8 (`IDLE_KINDS` + the `live` branch), `tests/kinetics/idle.test.mjs` +20, `test_idle_e49.py` +13 (`test_every_compiler_idle_kind_is_a_kind_the_module_paints` red before, green after); 44 golden frames byte-identical; the indentation nit stays for the next idle.mjs touch |
| **R26-94** | **DEFECT: the `cutout` dock option cannot be authored - `dock_opts` raises KeyError** (FIXED 2026-09-13, committed `3a6a645`, parent-verified with a read-only call: `dock_opts({'cutout': True})` -> `{'cutout': True}`; a `cutout` branch mirroring `centre` (`v is not True` -> ValueError), refused together with `press` (dock_entry's press kind overwrote the cutout kind), `stack` (a pile of press cards), `embed` (`embedPaint` re-draws a card shadow / vignette over the cutout - the ghost edge P53 gate 1 removed) and `fit`; `tests/test_dock_cutout_option.py` 13 tests, FAILED before on `KeyError: 'cutout'`; the test drives `dock_opts` then `dock_entry(cutout=...)`, the dock loop's own path - no full shot-row compile exists to mirror; 102 golden files byte-identical; art-embed 14, golden frames 47, press dock 21, motion density 104 passed) (found by P55 T1 group B 2026-09-13, reproduced by the parent with a read-only call: `dock_opts({'cutout': True})` -> `KeyError 'cutout'`, and the same for `'head'` and a dict) - `cutout` is in `DOCK_OPTS` (`build_scene_timeline_f.py`, the tuple with `embed`/`fit`) and the dock loop reads `dopt.get("cutout")` to write `kind: cutout` (`DOCK_KIND_CUTOUT`, P53 T7 / R26-59: "a head above the crawl is a person, not a document"), but `dock_opts` has no `cutout` branch, so the value falls through to the generic option check and raises. The `newsreel-band` golden reaches the painter only because `tests/golden/build_golden_sources.py` calls `dock_entry(..., cutout=True)` directly; no shot row can, and no test compiles one. P53 human gate 4 (the cutout dock's first frame) therefore reads a surface no episode can author. FIX (the compiler's next writer, after the E95 `embed_fit` follow-up): a `cutout` branch in `dock_opts` mirroring `centre` (`v is not True` -> ValueError "dock: cutout must be True - the dock is a cutout, no card"), refused with `press`, `stack`, `embed` and `fit` (a cutout is neither a card nor a picture on a surface - confirm against the painter), plus a test that a shot row carrying `{'cutout': True}` compiles to a dock with `kind: cutout` and the golden builder's direct call still matches (goldens byte-identical). The P55 card `dock_option:cutout` carried `callable.today: false` until this landed; it reads `true` with `proof.test: tests/test_dock_cutout_option.py` (verified 2026-09-14). | **DONE 2026-09-14 (P57 T1) - `3a6a645`**: `tests/test_dock_cutout_option.py` 13 tests (KeyError before, green after), 102 golden files byte-identical; the card `dock_option:cutout` `callable.today: true` |
| **R26-31 REVIEW** | **The exploration review of 2026-09-10** - [EXPLORATION-REVIEW-2026-09-10.md](EXPLORATION-REVIEW-2026-09-10.md): the Bravos maths accounted for (Gemini's pacing figures carried / re-measured / rejected; the phase windows now in 46 §46.7), the species Bravos has that we do not (the press card, the TV-embed world, the vector map, the flow diagram, the burst's furniture, the treemap ruling, the count array), every open row's maths, the reference layer's unbuilt maths (Euler spirals, morph Method A, the viability matrix), and the order to explore them in. | open 2026-09-10 - the operator picks the next |
| **R26-30 CLOSED 2026-09-11** | SHIPPED as `break_cadence: "stop"` (P50 T13): the shoot and the counter step on the stop-action cadence, the scale per step, the glow on the landing frame. Original: **The breakthrough BLEND - a stop-motion burst** (the operator, 2026-09-10, E60: "A blend would also work, basically a stop-motion version of what bravos does. essentially blending our stop motion + break through + counter mechanics"). The burst as it stands is continuous (min-jerk over `BT_RUN`); the blend runs the same shoot on stopaction.mjs's frame-index cadence - the bar and the counter step together, the scale rewrites stepwise, the ticks cross over per step, the glow on the landing frame. Built when a cut asks for it (`overflow: "burst"` + `break_cadence: "stop"`, or a fourth `overflow` value). | open 2026-09-10 ("But this works" - the continuous burst is the ruled default) |  |
| **R26-29** | **The breakthrough object's printed as-of date** - `ev-bonds-vs-chips-10y-v1` carries iShares' standardized ten-year average annual total returns (AGG 1.52 %, SOXX 36.59 %, NAV) from the product pages, which publish the table without its printed as-of date (the fact-sheet PDFs returned 104/290-byte redirect stubs to curl). Before any cut renders the page: open the two PDFs in a browser, read the printed quarter-end, write it into the object's `src` / `fetched` and the source file's header. Until then the page says "fetched Sep 2026", which is true and not the same thing. | open 2026-09-10 (the page is not in a cut yet) |  |
| **R26-28 → BUILT 2026-09-10 (the operator: "fold in the proper bracket/spread handling before review")** | **Brackets and spreads follow the active chart state** - on a page with states the perform layer now draws above every state and a FIGURE follows the active state's datum (P48 T7), but a bracket or spread is built once on the page's own geometry and WAITS (hidden) while a derived state stands; the Tokyo P48 cut has neither on a rescaled page, so nothing shows wrong - the first cut that measures a span on a windowed line will. Build the bracket's geometry per frame from `lpMarkDatum` (two data, the room beside them) and the spread's edges from the active state's `linePts` | P48 T7, 2026-09-10 | a bounded slice on `buildPerform`/`paintBracket`/`paintSpread`: half a day; a browser test on the rescaled holdings page |
| **R26-27 CLOSED for measured pages, 2026-09-11** | **`page_boxes` disagrees with the player's own layout on a portrait page** - FIXED by P50 T16 for every page on file: the boxes are MEASURED from the player per builder per aspect, keyed by the page's ink, into `assets/page-boxes.v1.json` and `page_boxes` reads the fixture (the estimate stays for a page not on file - the approved shorts, by design: R26-45). Original: - `centred_place` asked `free_bands` for the gap between the source line and the caption and was handed y 536, which the player draws inside the plot; the x maths is exact, only y is off. Until it is reconciled an author can name the card's centre (`centre_y`). The fix is to derive the bands from the same numbers the template uses (the chart box percentages), or to have the player publish its boxes | the sixth watch 2026-09-07 (the tea cup landed on the chart) | half a day; a test that compares `page_boxes` against `__lpProbe`'s rendered rectangles |
| **R26-24 CLOSED 2026-09-11** | **N-tier pages (small multiples, shared x)** - SHIPPED with P50 T9: `ledger_page.py` builder `tiers` (N in [2, 4]), `species/tiers.mjs`, the golden `tiers-two`. Original: - the macro-chart intake's Archetype 3: three bands, one x, one unit each, so correlated macro series compare without a dual axis. We built the two-band case (`tiers`); the general form is a list of bands with a builder each | macro-chart intake 2026-09-07 (MC-7) | a bounded slice on the tier code: half a day |
| **R26-25 CLOSED 2026-09-11** | **A `span` species: a shaded region with a label** - SHIPPED with P50 T4 as `scripts/species/span.mjs` (a page species on the perform layer; the golden `span-decade`). Original: - the intake's Archetype 5 (regime / epoch shading behind a line, with stage brackets). We have `bracket` (a measured span between two data) but nothing that shades a REGION of time and names it | macro-chart intake 2026-09-07 (MC-9) | a page species like `bracket`: half a day |
| **R26-26 → BUILT 2026-09-07 (the `spread` species; the ring page's Fed-vs-10-year gap)** | **The divergence spread (two lines, the gap filled, terminal tags)** - the intake's Archetype 4. New to us; our `decline` builder is single-series. The Meta-yield beat ("a higher yield discounts it") is the candidate | macro-chart intake 2026-09-07 (MC-8) | explore first: one page, judged by eye |
| **R26-22 CLOSED 2026-09-11** | **Centred placement by E50's clock** - SHIPPED with P50 T16: a solo card with no `centre_y` on a MEASURED page lands in the fixture's free band at its enter time; an authored centre still wins; on an estimated page the old rule holds (centring against an estimate is what put the tea cup on the chart). Original: - today a solo card sits at the reading rect over the title while the page builds and is centred only when the author says `centre: True`; the compiler could flip to centred by itself whenever the page's last data mark is behind the dock's enter (the chart has left or holds) - the study is DESIGN-PASS-2026-09-07.md §3 | the design pass 2026-09-07 | an afternoon in `dock_place` with the E50 marks; a test per band |
| **R26-20 (half, 2026-09-11)** | **badge-stamp's two-spring landing (remotion-ui RU-2)** - the chip (P50 T2, b13e61e) and the press card (P50 T3) land on the badge spring with the drop on the same clock; the free trailing ROTATION spring is still open - port it when a stamp asks for it. Original: - a clamped scale spring plus a free trailing rotation spring on the stamp: the weight cue our single-spring `land` lacks; port the offset into `stopaction.mjs` `landXf` as a `stamp` arrival (E51: a stamp is a legal push anchor) | remotion-ui intake 2026-09-07 (REMOTION-UI-INTAKE-2026-09-07.md) | P50 T8 blend -> T2 (the chip lands by the same two-spring law, `arrive: land` with its mass - shipped with the chip module 2026-09-11); the badge-stamp's own re-port stays half |
| **R26-21** | **A cold seek into a snap window** - the `snap` page reads its card box from the dock loop's layout offsets, so a cold seek into the 0.45 s window shows the page full for one frame (sequential rendering is exact). Make it pure: compute the card's read rect from the dock's declared geometry (`dockReadRect` + the card's intrinsic size) at the snap scene's start | P47 T7 deviation | an hour; a test that cold-seeks u = 0.5 |
| **R26-19 → P47 T6 BUILT 2026-09-07 (the Fed-vs-yields object from FRED with proof lines; `chart_card.py`; v3 row 6). Open: the JGB-vs-UST fetch; a portrait-typed card** | **A second chart of value - the hook's own proof, never charted** - v3 deploys the holdings line three times (rows 2, 4, 6) and one other chart (Meta yield, row 5). The hook "The Fed hasn't moved, but your borrowing costs climbed anyway" and its ring "The Fed still hasn't moved" are sourced in `tokyo-tea-break/EVIDENCE-DOSSIER.md` (30-year mortgage 6.55 % -> 6.71 %, 10-year 4.63 % -> 4.79 %; FRED `MORTGAGE30US`, `DGS10`) and never drawn. Build `ev-fed-vs-yields-v1.series.json` (FRED `DFEDTARU` flat vs `DGS10` / `MORTGAGE30US` rising, the same window, proof lines per E28 / the dossier's form) by a `meta_yield()`-style builder and put it on row 6, the ring, in place of the holdings' third return. Second candidate: JGB 10-year vs UST 10-year for "it beats our bonds" (54.6-58.4) - a fetch (MoF Japan / FRED), not on disk. Third option, no fetch (operator, same read: "or even just a different display method, or a comparison of it"): the holdings object's own `facts` already carry the comparison - Japan $1,116.7B against the UK $939.9B and China $633.4B (`rank_2_uk`, `rank_3_china`), the -$122.6B as a tenth of the $1,239.3B pile (`drop_bn`, `peak`, `share_pct` 12.0) - so row 4's "a tenth of the pile" can be a BAR page (the `ledger_page.py --variant bars` form the Meta page already uses) and row 6's ring a COMPARISON (the three holders), the same sourced object shown two more ways instead of the same line three times. | operator 2026-09-07 (HG2 second read): "we also show the same chart a couple too many times, there has to be another one that we can pull in of value" | the FRED pull + the builder + the row: an afternoon; the JGB fetch a bridge `fetch` order |
| **R26-18 → P47 T6 BUILT 2026-09-07 (`undraw`, `figure`, M21; v3 rows 2 and 4)** | **A chart's deployed life is 6-8 s from its LAST data mark on average, 12 s at most, then it un-draws or becomes the next thing** (E50, ruled 2026-09-07: the clock starts when the last data point lands, not at the first cap; the 12 s ceiling lets a chart hold under a dock's clip; gate: > 8 s INFO, > 12 s WARN - v3 rows 2 and 4 read 18.9 s and 12.7 s) - v3's holdings line is fully deployed for 19 s after its June stroke (30 s after its first cap) and the operator's read is that it stays up too long. Two mechanisms: (a) the `undraw` page species - the line unwinds from its tip back to a datum or to nothing on a word, the reverse of `build_to` (which is `Math.max`-guarded today and never retracts); (b) the line's ink BECOMES the next figure or object (R26-16's morph mechanism, planted). The proposed cut for v3 row 2: un-draw on "The Treasury's table shows" (20.9), the peak figure $1,239.3B written on "over a trillion" (22.5), the June figure $1,116.7B on "selling since February" (24.4), both holding under the anecdote to the cut. Gate: a page whose line has been fully deployed and unchanged for more than the clock WARNs (the frozen-frame idea at the species level, M18's cousin) | operator 2026-09-07 (HG2 second read): "we're leaving the chart up for too long ... un-draw it or morph it into something else after it's been fully deployed for 6-8 seconds ... reverse the draw / transform the graph into the treasury" | `undraw` + the figure write: an afternoon on P47 T2's species (the retitle's hand already writes glyphs); the morph form rides R26-16 |
| **R26-17 TOP -> P51 (approved 2026-09-11; step 0 the kit DONE - T0; step 1 the split DONE - T1; step 2 hot reload with the determinism check DONE - T4, an edit on the served page in ~250-660 ms; the sidecar DONE - T5; step 3 the thin editor BUILT - T7, gate 3 open)** | **Build the actual engine, so the editor is not the engine** - the grill of 2026-09-11 ([GRILL-ANIMATOR-ITERATION-2026-09-11.md](GRILL-ANIMATOR-ITERATION-2026-09-11.md)) made it `P51-THE-ANIMATORS-LOOP`: the authoring kit, the probe + the layout gate, the one-shot bar, the split, hot reload with the determinism check, the override sidecar + the change report, the thin editor, the chart-as-world doctrine. - operator 2026-09-07: "we need to build the actual engine so that the editor is not the engine, and so that we can develop much more iteratively on screen instead of rebuilding." Today the player template IS the engine: a 4,000-line single file instantiated per build with the timeline and every asset embedded as data URIs (Tokyo's v3 player is 42 MB); any change to the shot table, a dial or a species is `build_short.py` (~1 min), a static file server with no-store, a reload, and the seek - no live edit, no hot reload, no way to nudge a dial and watch. What it needs: (1) the RUNTIME apart from the DOCUMENT - the painter and the kinetics as a module the player loads, the timeline and the asset map as files it fetches (not embedded), so the build writes data and the engine is one; (2) hot reload of the timeline and the dials on the served page (a watcher, or a `?reload` endpoint) so a shot-table edit lands on screen in a second; (3) an EDITOR surface over it - the scrub, the scene list, the species per scene, the dials (LP, DOCK_*, PS, MORPH, IDLE, STOP) as live controls writing back to the shot table / a dials file, never to the template; (4) the render path unchanged (render_episode still seeks the same pure function of t) and the goldens still byte-identical - the engine's determinism is the contract, the editor is a client of it. The remotion-ui production-console port (memory) is the earmarked shell for (3). | operator 2026-09-07 | a plan of its own (P49 - P48 is the chart-to-chart transitions): the split first (1), the reload second (2), the editor last (3); every slice proven by the goldens | REVIEWED 2026-09-14: P51 gate 3 closed as not yet meaningful - the editor exposes integers, so the timeline comes before any further editor gate (E99 s17) |
| **R26-11 TOP** | **The HyperFrames intake** - [HYPERFRAMES-INTAKE-2026-09-06.md](HYPERFRAMES-INTAKE-2026-09-06.md): Gemini's 32 KB drop gated (16 proof lines, 12 unsourced figures, 0 DERIVED tags, one claim not on the cited page, our own TR-7 numbers reflected back) and triaged. **Priority integration** HF-1..6 (frame-index quantisation of the stepped clocks; the one-frame lag for what a thing drags; the stagger law + focal priority; three SVG draw-on hazards; min_jerk on by default = TR-3; the easing grammar into doc 29) → P47 T1 / TR-3 / doc 29. **Backlog** HF-7..12 (the four film invariants block, the One Breather, focal + roles on the shot row, the negative list for plates, text behind the subject, the encoder-size stillness signal). **Explore** HF-13..18 (does a held page breathe; does the camera ever rest; arriving not cutting; the three threads; occlusion vs blur; a shader seam). **Reject** HF-R1..R8 (the black-hole cut, the s8 settlements, beat-grid cuts, 6x parallax, 1.1-4 s for long form, back.out as house physics, the third-hue ban, the ARAP attribution). **Index** the rest | operator 2026-09-06: "HyperFrames has a GREAT resource library / prompt guide; we should be calling on it more for faster iterations, lessons, troubleshooting" | HF-1/2/4/6 ride P47 T1 and the stroke engine (hours); HF-7..10 are doc/table fields (an afternoon); HF-13/14 are measurements |
| **R26-10 → P47 T2 BUILT 2026-09-06 (build_to / bracket / retitle / relight; v3 first pass authored)** | **The cut ledger and the page's performance (Tokyo v3)** - plan [P47](../../.claude/PRPs/plans/P47-STOP-ACTION-BUILD-ON-AND-THE-CHART-MORPH.plan.md): T1 stop-action (the stepped clock, throw, land with weight - the operator's third ask today), T2 build-on (build-to-datum, the bracket, retitle), T3 the chart morph (ARAP, the bar tab becomes the line), T4 Tokyo v3 - every cut on a word it proves, or cut (the v2 ledger names three that add nothing: the host swap at 15.3, the two-fingers dock, the host cameo, and two misplaced: the 10.8 callout and the Meta mount 2.5 s early); and the page PERFORMS every sentence it is under (56 of 89 s) - build-to-datum on a word, the bracket, the title rewrite, focus-zoom, numbered badges; `tokyo-tea-break/SHOT-TABLE-V3-PROPOSAL.md` parts A/B/C | operator 2026-09-06 on v2: "we need to actually match the cuts of the video properly - some cuts either add no value or don't make sense where they're placed; being able to spring the news anchors at that moment is genuinely good. We're being a bit too lazy with the world plates - we have to play more if we're going to leave it on screen; learn the backlog skills, perform some transformations on the chart" | A + the existing species: an afternoon; V3-1 build-to-datum + V3-2 the bracket: a lane day; V3-3 retitle: half a day; V3-4 the object->chart morph waits (T4) |
| **R26-9 TOP** | **The transitions review** - [TRANSITIONS-REVIEW-2026-09-06.md](TRANSITIONS-REVIEW-2026-09-06.md): the research asked the right questions (Track B) and recorded them unanswered; the reference's 100 cuts are all `unclassified` by kind; five shipped transition numbers have no source; the beat-freeze exit, J/L offsets and the ARAP match cut are unbuilt; doc 51 s51.4 contradicts E44/E45. Its TOP rows: **TR-1 DONE 2026-09-06** (measured: 36 hard cuts / 35 dips through black / 28 blur-zooms, 0 dissolves - review §6, doc 46 §46.5); **TR-12 DECISION** whether the dip and the blur-zoom enter our kit for world changes, **TR-2 DONE 2026-09-06** (one placement rule: the picture changes 3 frames before the next word's onset; dips centred on the onset; no J/L grammar - review §7, doc 46 §46.6); **TR-13 BUILT 2026-09-12 (P52 T11, 35d0cec)** the engine form of M13 (cut = onset − 3 frames; dip black = onset) - `authoring/words.py` `cut_before(rule="onset")` the default for a measured take, the two approved shorts pinned to `rule="gap"` by name and rebuilt clock-identical; `measure_cut_offsets.py --scenes` the verdict on our own build; doc 46 §46.6 carries the engine form, **TR-3** tag and test the unsourced numbers (dissolve 0.8 vs 0.35-0.5; `min_jerk` on), **TR-4** reconcile doc 51 s51.4 with E44/E45, **TR-5** the E44 s2(b) gate Measured from the reference run on disk: `docs/research/runs/wealth-logic-cuts/` (1489 files - `download/video.mp4` + `video.en.vtt`, extracted `frames/`, `check/` strips, `followup-01.md`/`followup-02.md`), synthesised in `docs/research/motion/WEALTH_LOGIC_TRANSITIONS_MEASURED.md` - the source of TR-1's 36/35/28 and TR-2/TR-13's onset - 3 frames (R26-110, 2026-09-14). | operator 2026-09-06: "I don't think we've gotten them nailed properly"; the Tokyo curve (n = 254) bleeds on every clip-to-clip cut | TR-2/3/5 are a day of lane time with no spend; TR-1 is a Gemini research order; TR-4 is a ruling |
| **R26-95** | **Promote `species:trace` to a module (P55 T11 queue)** - engine symbol paintSpecies `sp.kind === "trace"` (34 lines); rank 476 - 7 uses, live in the approved Japan short (t 9.22); golden first: a `trace-hop` golden (the hop drawn, a stamp stacked); queue: an engine writer slot; beside R26-78/79 (race) only if those do not touch paintSpecies. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T18, implementation lane, parent-read golden). GOLDEN FIRST: `trace-hop` from today's inline code (a still with the bowed `hop`; `FRAME_T` 8.54 - hop 1 drawn and landed on the ringed point with the callout's '25%' stamped there, hop 2 leaving the same point bowed the other way mid-draw with no head yet, the plain still-life zigzag mid-draw on the hillside - read by the parent). THE LIFT: `species/trace.mjs` (145 lines, `/* SPACE: stage */`, WHEN quoting `SPECIES_WHEN['trace']`, THE LAW the still-life/E49 one, the compiler named as the owner of what may be declared - its `hop` validation at `build_scene_timeline_f.py:1194-1202` untouched); frozen `TRACE` dials with NO value changed (PERIOD, DRAW_S, N, MIN_DRAW_S, BOW, BOW_K, JAG_K, HASH_SALT, X0, X_SPAN, Y0, Y_SPAN, WIDTH, WIDTH_K, WIDTH_MIN, FADE_AT, FADE_SPAN, IN_S, HEAD_K, HEAD_MIN, HEAD_W, HEAD_A, COLOR); pure math exported (`traceHopPoints`, `tracePlainPoints`, `traceHead`), `trace.test.mjs` 18/18; registered as the last statement `SPECIES_PAINTERS.trace = paintTrace`, the 33 inline lines deleted. `sync_kinetics: in sync (34 module(s): 14 kinetics, 20 species)`; `render_baseline.py --check` PASS 56 golden frames identical - the 55 existing byte-identical, 1 new; kinetics_sync + golden_frames + catalog_drift 130 passed; card `species:trace` `lives.form: module`, `proof.golden: trace-hop`; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T18)** - `species/trace.mjs`, golden `trace-hop`, 55 goldens identical, 34 modules | |
| **R26-96** | **Promote `species:spotlight` to a module (P55 T11 queue)** - engine symbol paintSpecies `sp.kind === "spotlight"` (20 lines); rank 320 - 8 uses, live (Japan, dur: hold, idle live - R26-93); golden first: a `spotlight-hold` golden (the dim, the lit datum) + a proof at the idle's two phases; queue: after R26-93 is committed (the light's idle). Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T19, implementation lane, parent-read golden; the queue condition - R26-93's `idle=live` - was `3a6a645`). GOLDEN FIRST: `spotlight-hold` (FRAME_T 8.0: the whole stage dimmed with one feathered pool around the first datum on the line page) + `@proof-idle-a` 12.0 / `@proof-idle-b` 14.0 (the pool landed on the peak, the two differing by 99,623 px from the `live` idle alone - half the 4.0 s breath period; `dur: hold` resolved by the compiler's own `_hold()`), rendered from the inline code BEFORE the lift. THE LIFT: `species/spotlight.mjs` (116 lines, WHEN quoting `SPECIES_WHEN['spotlight']`, THE LAW E56 / E25 amended); frozen `SPOTLIGHT` dials with NO value changed (GLIDE_S 0.6, IN_S 0.4, DIM 0.49, FEATHER 0.12, W_MIN 240, PAD 60, R_PORTRAIT 960, R_LANDSCAPE 0.5, HASH_SALT 991, INK '#000' - `SP.SPOT_DIM` and `SPOT_R_PORTRAIT` gone from the engine); pure math exported (`spotlightGlide`, `spotlightRadius`, `spotlightStops`, `spotlightFade`), `spotlight.test.mjs` 14/14; registered as the last statement `SPECIES_PAINTERS.spotlight = paintSpotlight`. `sync_kinetics: in sync (35 module(s): 14 kinetics, 21 species)`; sha256 59 identical / 0 moved of 59 (`render_baseline.py --check` PASS 59); kinetics_sync + golden_frames + catalog_drift 133 passed; card `species:spotlight` `lives.form: module`, `proof.golden: spotlight-hold`; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T19)** - `species/spotlight.mjs`, golden `spotlight-hold` + two idle proofs, 35 modules | |
| **R26-97** | **Promote `species:callout` to a module (P55 T11 queue)** - engine symbol paintSpecies `sp.kind === "callout"` + calloutPath (17 lines); rank 306 - 9 uses, live; golden `chart-callout` exists; golden first: none new: `chart-callout` pins it; queue: the cheapest extraction: its golden already exists. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T17, junior lane, parent-read). `species/callout.mjs` (105 lines, `/* SPACE: stage */`, WHEN quoting `SPECIES_WHEN['callout']`, THE LAW quoting E56), the inline `calloutPath` + the `sp.kind === 'callout'` branch deleted from the engine's body and the region `KINETICS:BEGIN callout` opened after the ring's; frozen `CALLOUT` dials with NO value changed (DRAW_S 0.7 - was `SP.CALLOUT_DRAW` - RX_PAD 22, RY_PAD 18, SEGMENTS 28, START_A 0.6, SWEEP 2.08, JITTER 0.12, HASH_SALT 21, LABEL_AT 0.8, LABEL_POP_S 0.25, LABEL_FROM 0.7, LABEL_SPAN 0.3, LABEL_DX 34, LABEL_DY -10, PAD_DY_K 0.4); pure math exported (`calloutPad`, `calloutPath` taking `hash` as the vecmap precedent does, `calloutDrawF`, `calloutLabel`), `callout.test.mjs` 14/14; registered as the module's last statement `SPECIES_PAINTERS.callout = paintCallout` - the registry dispatches it, the slot does not call it by name (unlike verdict). `sync_kinetics: in sync (33 module(s): 14 kinetics, 19 species)`; ALL 55 golden frames byte-identical by sha256 (the golden `chart-callout` already existed - no new golden); kinetics_sync + golden_frames + catalog_drift 129 passed; card `species:callout` `lives.form: module`; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T17)** - `species/callout.mjs`, 55 goldens identical, 33 modules | |
| **R26-98** | **Promote `page_species:figure` to a module (P55 T11 queue)** - engine symbol paintFigure (17 lines); rank 204 - 6 uses, live (Japan t 77.02); golden first: a `page-figure` golden (the written number landed); queue: a ledger-page writer slot. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T20, junior lane). GOLDEN FIRST: `page-figure` (FRAME_T 14.4: '1,074 index' in the memory makers' coral above their peak, written LEFTWARD because `fits` is false, 'the peak' under it, clear of all ink - the first candidate at datum 120 printed across its own climbing line, R26-71's fallback, so the golden moved to the peak rather than pretend). THE LIFT: `species/figure.mjs` (163 lines, `/* SPACE: page */`, a PAGE painter stated in the header, WHEN `SPECIES_WHEN['figure']`, THE LAW E50 + R26-71); frozen `FIGURE` (13 keys, NO value changed: FIGURE_STEP, FIGURE_STEPS, FIGURE_PAD, FIGURE_UP, FIGURE_DOWN, FIGURE_CHAR_W - the six R26-71 keys keep their PS names because `test_figure_placement.py` reads them off the engine's text - X_PAD, BASE_DY, LINE, SUB_DY, WRITE, SUB_WRITE, OVERLAP); lifted `paintFigure`, `segMeetsBox`, `figBox`, `figWidth`, `figClearY`, the write clock; WHAT STAYED: the BUILDER in `buildPerform` (it makes DOM via `lpEl` and owns `PF.figures`, the record `species/compare.mjs` reads - byte-identical shape) now calls the module's `figurePlace`/`figWidth`/`figClearY` by name, one law for builder and painter; registered `PAGE_PAINTERS.figure` as the last statement (the dot form - `test_kinetics_sync` pins one hook into the page registry). `figure.test.mjs` 16/16; `sync_kinetics: in sync (36 module(s): 14 kinetics, 22 species)`; sha256 60/60 identical before vs after (`compare-morph*` and every `chart-*` figure included); kinetics_sync + golden_frames + figure_placement + catalog_drift 139 passed; card `page_species:figure` `lives.form: module`, `dials.object: FIGURE`, `proof.golden: page-figure`; `effects_catalog_check` 0 failures. Pinned, not fixed: a sub's last glyph holds at 0.625 of the word's end (the inline clock's own tail) - changing it would move pixels. | **PROMOTED 2026-09-14 (P57 T20)** - `species/figure.mjs` (a PAGE painter), golden `page-figure`, 60 goldens identical, 36 modules | |
| **R26-99** | **Promote `dock_payload:record` to a module (P55 T11 queue)** - engine symbol drawRecord (the typewriter + highlighter); rank 140 - group B's rank (below the checklist 488, now a module via T7); golden first: a `record-typewriter` golden mid-type and highlighted; queue: the plan guessed the record heads the queue; T1 measured it fourth among dock payloads. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T21, implementation lane). GOLDEN FIRST: `record-typewriter` (the cream paper, the masthead, the quotation mid-type with the marker at rest on one word and its trailing space OUTSIDE the stroke, two characters of the next word cut, the block cursor after them) + `@proof-attr` (t 10.26: the quotation whole, the attribution and the source line landed), from the inline code before the lift. THE LIFT: `species/record.mjs` (103 lines, WHEN = CAPABILITIES 'Record-document species' verbatim, THE LAW: the stroke is the NARRATOR's, a string cut never per-character opacity, the phrase's space outside the marker - Tokyo 2026-09-10); frozen `RECORD` dials with NO value changed (TYPE_FRAC 0.72, SPAN_MIN 0.08, SWEEP_S 0.2, ATTR_AFTER 0.15, SRC_AFTER 0.45, HL_CLASS 'hw', CUR_CLASS 'pcur'); pure math exported (`recordTyped`, `recordTypeClock`, `recordCut`, `recordSweep`, `recordLanded`, `fillDock`), `record.test.mjs` 11/11 pinning the type clock against the word onsets; it is a DOCK payload so it follows verdict's precedent exactly - NO registry, the engine's dock slot keeps `recState[slot]` and calls `paintRecord` by name, `DOCK_PAINTERS` still recorded and unbuilt. `sync_kinetics: in sync (37 module(s): 14 kinetics, 23 species)`; sha256 62/62 identical; kinetics_sync + golden_frames + catalog_drift 136 passed; card `dock_payload:record` `lives.form: module`, `proof.golden: record-typewriter`; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T21)** - `species/record.mjs`, golden `record-typewriter`, 62 goldens identical, 37 modules | |
| **R26-100** | **Promote `exit:dip` to a module (P55 T11 queue)** - engine symbol the render loop's THE DIP block + dipIn/dipOut (10 lines); rank 180 - 9 uses, live (Japan x6); golden first: a `dip-boundary` golden (the black boundary frame and the ramp's midpoint); queue: with the scene-loop transitions as one module `species/transitions.mjs` beside R26-75's slide - decide at that slice whether dip, blurzoom, wipe, dissolve and suck share one module. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T23, implementation lane). THE TRANSITIONS DECISION, recorded in the module header: `kinetics/transitions.mjs` (101 lines), NOT `species/` - a transition registers no painter and declares no SPACE, it is a law about time at a boundary that every layer obeys at once, and the boundary clock is kinetics. It holds the exit grammar (`exitName`/`exitSecs`), the STRADDLE clock (`straddleSecs`) and the dip's linear ramp (`dipAlpha`), because on disk the dip and the blur-zoom are the only two transitions that straddle the boundary and ride it through the same four lines; the suck, dissolve, slide, wipe and melt arrive ON THE CUT off `clamp01((t - sc.span[0]) / S)` - a different clock whose only shared code is the engine's clamp - so they stay inline, each named with its reason in the header. E47 s1 survives unchanged: a plain LINEAR ramp to black over the last DIP_S/2 and back; dials `DIP` = HALVES 2, OPACITY_DP 4 (the engine's `/2` and `.toFixed(4)`), `DIP_S` deliberately stays in the engine because the animation registry (`build_animation_registry.py:76`) and `test_page_performs.py:653` read it off the engine text. GOLDEN FIRST: `dip-boundary` (the pure black boundary frame with the cut inside it) + `@proof-ramp` (14.88 s, dipA 0.4894: the fully drawn line page at just under half its light), from the inline code before the lift. `transitions.test.mjs` 12/12; `sync_kinetics: in sync (39 module(s): 15 kinetics, 24 species)`; sha256 67 frames none moved (`render_baseline --check` PASS 67); kinetics_sync + golden_frames + chart_transitions + transitions_e47 + catalog_drift 235 passed; Japan motion gate `3 FAIL / 0 WARN / 16 PASS / 1 JUDGE / 8 INFO` (6 dips, unchanged); card `exit:dip` `lives.form: module` (`dipAlpha`), a `kinetics:transitions` card added because the catalogue refuses a module no card names; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T23)** - `kinetics/transitions.mjs` (the straddle clock + the dip); the on-the-cut transitions stay inline by decision; 67 goldens identical; 39 modules | |
| **R26-101** | **Promote `page_enter:spiral` to a module (P55 T11 queue)** - engine symbol lpSpiral (+ lpVortex, lpParticles) (39 lines); rank 195 - 5 uses, wired (Tokyo test bed, bridge short); golden first: a `spiral-return` golden mid-unwind; queue: with the page vortex RETRACT (implicit card `page_exit:retract`) - one module, two directions. Follow the P55 T7 pattern: golden committed from the current inline code, lift every literal into a frozen dials object with no value changed, pure math exported and node-tested, `sync_kinetics.py --check`, every golden byte-identical, the card's `lives.form` -> module. PROMOTED 2026-09-14 (P57 T22, implementation lane). THE GROUPING DECISION, recorded in the module header: both directions - the page's spiral IN and the vortex RETRACT (`page_exit:retract`) - are ONE module, `species/spiral.mjs` (166 lines), because on disk they are not two things: `lpSpiral` is one function, one map (`lpVortex`) and one clock pair (`spiralClocks`), and leaving vs returning is only which clock wins the `Math.max` on `uc`/`uf`; both cards point at it. GOLDEN FIRST: `spiral-return` (mid-unwind: four series lines curled as spiral arms, the core tighter than the rim, every glyph and label strewn along its own arc, the charcoal whole behind) + `@proof-retract` 13.5 (the same map the other way) + `@proof-fade` 14.5 (the charcoal gone to the half-faded scribble field, one colour still draining), from the inline code before the lift. Frozen `LP_RETRACT` with NO value changed (COLOURS, CHARCOAL, IN, TURNS, LAG, ALPHA, RECT_FADE + the lifted literals R_FALL, CORE_BASE, CORE_GAIN, CORE_FALL, SHRINK, IN_FIELD, IN_COLOUR_AT, IN_COLOUR_S, LINE_W, LINE_RATE, LINE_FALL, LINE_MIN) - a 26,809-comparison equivalence run against the engine's old expressions: 0 differences; `LP_STATE_MAX`/`LP_MOUNT_RISE` stay in the engine (not the vortex's). No registry: the ledger slot calls `lpSpiral(st, scene, t, pg)` by name (verdict/record precedent). `spiral.test.mjs` 16/16; `sync_kinetics: in sync (38 module(s): 14 kinetics, 24 species)`; sha256 62/62 identical, `render_baseline --check` PASS 65 (3 new); kinetics_sync + golden_frames + catalog_drift 139 passed; cards `page_enter:spiral` + `page_exit:retract` `lives.form: module`; `effects_catalog_check` 0 failures. | **PROMOTED 2026-09-14 (P57 T22)** - `species/spiral.mjs` holds BOTH directions; golden `spiral-return`; 62 goldens identical; 38 modules | |
| **TR-15 BACKLOG** | **The Whisper gate normalizes numerals before it diffs** - `verify_take_whisper.py` reads "a hundred and twenty-two billion dollars" as `$122 billion` and scores the script's spelled-out numbers as replacements and "dollars" as a deletion: the approved tariff take FAILs at 12.7% WER with no word wrong. Spell out the heard numerals (or normalize both sides) before the alignment; the gate's 5% then means something on a numbers-dense short | found 2026-09-09 gating `retime_take.py`'s output - the re-timed take scored the same defects as the original | a normalizer in the gate + a test on the tariff take |
| **TR-14 BACKLOG** | **The ink bloom world change** - [INTAKE-INK-BLOOM-2026-09-08.md](INTAKE-INK-BLOOM-2026-09-08.md): the motion menu's radial reveal (29:1764) with an organic seeded edge, tendrils and droplets, the edge from doc 44 (dark rim, halo) not a gradient; an exit name `bloom:<x>,<y>`, opt-in, goldens byte-identical | the operator's harvest; E48's second accent for a TOPIC change - the second-lever seam is the one candidate | a species/exit + an intake |
| **R26-4 TOP** | **The chart on the hook (E44)** — the shorts shape becomes hook 0:00–0:03 → the first ledger page rolls out under sentence two → mechanism on the page by 0:10 → the host/archetype after or over it; update `docs/content-video-engine/51-THE-SHORTS-FORMAT.md` §51.2 and the script skill's `references/SHORTS-SHAPE.md`; the opening gate S02 (mechanism by 0:10) accepts a ledger page as the mechanism | `tokyo-tea-break/ANALYTICS-2026-09-06.md`: drop-off 0:11 inside a clip scene, first page at 0:17 (n = 8, a sample). The operator's read: the chart is the one thing the stick-figure channels cannot match | CLOSED 2026-09-12 (P52 T12, 2e6740d) - doc 51 s51.2 carries THE PAGE and THE HOST lines and the E44 paragraph; the script-writer skill's SHORTS-SHAPE.md the same two lines; gate_opening_structure.py S02 reads the first ledger page off the build's scene timeline (`--scenes`, `load_pages`, found beside `--timeline`): a [post-key] sentence past 0:10 PASSes when the page lands by 0:10 ("ON THE PAGE (E44)", both clocks printed); undeclared still FAILs; a page at 0:17 rescues nothing. Both shipped shorts land their first page under 2 s (1.99 / 1.82) and their verdicts do not move |
| **R26-5 TOP** | **Transient cues at a cut, gain and window** — the `press 3` cue at 8.99 s (gain 0.16, ~14.5 dB under the voice) is the last sound before the 0:11 drop; test 0.08 `[DERIVED: from the 2026-09-05 metering, halved]` and gate: no transient cue inside 0:05–0:12 unless a page lands with it (`sound/SOUND-PLAN.json`, `gate_motion` sound rows) | E44 §2a; operator 2026-09-06: "the camera flash sound maybe shouldn't be as aggressive" | CLOSED 2026-09-12 (P52 T13, d16155e) - `sound/SOUND-PLAN.json` carries `transient_gain` 0.08 with its `[DERIVED: from the 2026-09-05 metering, halved]` note and `build_short.PRESS_GAIN` reads 0.08 (the V2 cut carries no press cue, so no output changes; no re-render); M29 in `gate_motion_density.py` FAILs a transient cue inside 0:05-0:12 with no page landing within 1.5 s (beds exempt). Read on the approved Tokyo build: FAIL - the card throw's landing cue at 9.51 s, 2.02 s after the chart landed at 7.49 s - the shape E44 named, one cue later; it stands as the finding on the test bed. Open for the operator: whether a DOCK's own landing licenses its cue |
| **R26-6 TOP** | **A returning character mounts, never cuts on** — s02→s03 in Tokyo is a `cut` exit onto the host who entered s01 smoothly; the mount rule (doc 29 s9.15: a dissolve on a word) applies to every re-entry of an introduced character; gate it in `gate_motion` (a `cut` exit into a scene whose first species is a character already seen → FAIL) | E44 §2b; operator 2026-09-06: "the character cuts on to the scene instead of smoothly entering like he did in scene 1" | CLOSED 2026-09-12 (P52 T13, d16155e) - M30 in `gate_motion_density.py`: a scene whose entry is a `cut` and whose first species is a character an earlier scene already showed FAILs (a returning character MOUNTS, doc 29 s9.15); a character is DECLARED (`world.character` / `world.cast`, a `character` species row, an evidence entry of species character), never guessed from an asset's name. Dark on Tokyo (no scene declares its cast); the next short's shot table declares the host and the row measures it |
| **R26-0 TOP** | **The short's viewer sees the screens** — `viewer_windows.py` folds the SCREENS file's chart titles and the figures they show into each window's text (a `[screen]` line per named screen), so the blind read sees what the viewer sees; V01 then counts a chart-carried beat as perceived when the reader names the screen's figure. `content/video_engine/scripts/viewer_windows.py`, `viewer_score.py`, prompt v1 | **E43** (2026-09-06): the Tokyo short's fresh read flagged `[promise]`/`[rehook]` at 0:30–0:45 on a line whose numbers the charts carry - a text-only reader cannot see them, and conjoined beats are the shorts doctrine (E41 compress, don't remove). Also fixed today (7b54636): the runner/scorer stem bug that had the gate runner folding in a stale report | CLOSED 2026-09-12 (P52 T14, 45f17c2) - `viewer_windows.py` folds a `[screen]` line per screen into each window from the build's SCENE timeline (`--screens`, found beside `--timeline`) - not the `-SCREENS.md` file, which is the strength-screen / DECLARED-tag enumeration and carries no chart: the page's title, sub and drawn figures, the docks that land, the words a species writes; never a tag, judge block or species name. `viewer_score.py` V01 counts a chart-carried beat when the reader names the screen's figure (digits, or two content tokens of a label; a one-word country never). Prompt v2. Tokyo's stored reports re-scored: V01 21/24 -> 23/24, exactly E43's two beats flip. A fresh live read under v2 is the operator's dispatch |
| **R26-1 TOP** | **Field-coloured halo on direct data labels** — the label sits on its datum with a 7 px stroke in the page colour behind the glyphs (`paint-order: stroke fill`), so text punches through grid and bars with no box and no legend; `briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md:145-147` (Ginns 2006 split-attention, d = 0.72) | the operator's 2026-09-06 read of the chart examples: "we lost our labelling/charting best practices" (E28 chart reads at a glance). Only in the brief — not in doc 29, not in `GATES-REGISTRY.md`, not in any chart code | P50 T8 blend -> T11's finish (Bravos shots 104-105: the terminal TAG + the value bar - the label on its own field colour is the tag the recast hands to the bar); the halo as such not built |
| **R26-2 BACKLOG** | **Harmonisation tokens for a composited figure** — light wrap 4–12 px of blurred ground into the sprite edge; substrate grain `C = C_vector · [1 + κ(T_washi − 0.5)]`, κ 0.12–0.16; charcoal `#25313C` stroke with a 0.6–0.8 px feather; `48-THE-FIGURE-AND-THE-GROUND.md:161-165` \| 48.8 | orphaned in our own doctrine: no row, nothing in `kinetics/`. It is the fix for 48.7 "composited figures look pasted"; becomes TOP the day the host or a stick figure lands on a ledger page | a compositing pass in the sprite mount (two filters + one multiply); measured against a Wealth Logic composite first (E38) |
| **R26-3 → P50 T14 BUILT 2026-09-11** (the clothoid fitter `kinetics/clothoid.mjs`, doc 42 §42.4; the race A/B still to run) | **Euler spirals for generated geometry** — curvature-continuous paths instead of cubic Béziers for anything we generate; `42-DRAWING-KINETICS.md` \| 42.4, `FINDING-the-animation-math-and-what-it-changes.md` \| 5 | tracked, never built. Test against the race (operator 2026-09-06: "the race needs to be smoother, it feels a bit choppy"): if the choppiness is curvature jumps at segment joins, this is the fix; if it is timing, it is not — measure before adopting | ANSWERED 2026-09-12 (P52 T17, c14caeb) - the A/B on the race: TIMING, not curvature. `measure_motion_energy.py --race --window` reads geometry on the arclength-resampled path and timing on the speed's second difference; both arms (A the engine, B the race path through `clothoidFit`, same clock) on 433 frames: every mark at 2.5-4.8 % of its mean speed at every period (the per-segment `smoothstep` in `valueAt` has zero derivative at every knot; RACE_PERIOD puts one every 1.2 s); the zero-curvature control mark stalls identically; arm B moves none of it and is worse on bending, fairness and the join curvature STEP, and bows the mid-period value off the data. NOT adopted for the race; the fitter stays the flow arrows' and the arcs'. The lever is the clock (a C1 interpolation across periods) - a later slice. Gate 5: `tokyo-tea-break/build-short-t17/proof/race-strip.png` + 12 paired frames |
| **R26-102** | **The agentic-first production studio - READINESS REVIEWED, awaiting its plan (Astra or Fable)** (operator, 2026-09-13: *"seems like the next natural step is to build out the actual agentic-first human-production studio interface system with timeline, proof player, LLM-in-session awareness, etc"* and *"i think we should ultimately have asrta or fable draw up the real plans for this, you should just do the initial readiness review"*) - the review: `docs/content-video-engine/STUDIO-READINESS-REVIEW-2026-09-13.md` (what exists, prerequisites, the front-end fork, the edit-grammar gap, the Claude Code / Codex session link, the test baseline, constraints, risks, open questions). The operator's v1 choices: the in-session agent is a Claude Code OR Codex session; v1 cuts an existing build; full canvas editing. Prerequisites before building: R26-83 commit batch (operator's word), P51 HG2/HG3 closed IN the first studio session (operator ruling 2026-09-13), a ruling on P30, the test baseline, the plan's home across lanes. No plan is drafted by the Claude lane. | The MCP shortlist behind it is on disk: `docs/research/runs/video_engine_tooling_and_mcps/findings_mcps_and_tools.md` (financial-data, local STT, ComfyUI/vision and SVG-motion servers), synthesised in `docs/research/tech/VIDEO_ENGINE_TOOLING_AND_MCP_BLUEPRINT.md` - `fred-mcp-server`, `sec-edgar-mcp`, local `whisper-mcp`, `comfyui-mcp-server`; the studio plan picks from it (R26-110, 2026-09-14). | REVIEWED 2026-09-14: the readiness review is dated; GPT and Claude plan and animate together, Fable and Astra plan together (E99 s13) | REVIEWED 2026-09-14: the editor is not yet a human editing surface - the timeline comes first (E99 s17) |
| **R26-103** | **RECIPE CARDS - the unit of authoring (E96; the pipeline grill Q1: A + C)** (LANDED 2026-09-13, P56 T1-T5 + T9: schema `effect_recipes.v1`, 15 proven + 24 candidate recipe files, the generator/drift gate/resolver/CLI/gallery read them, `recipe_walk.py`, `authoring/recipes.py`, `--propose` recipes; `effects_card.py "badge ladder"` prints the recipe; the drift gate re-walks every proof) - `content/video_engine/effects/recipes/*.json`: one recipe = ordered card ids + offsets + the sentence act + `proof: {project, build, t}`; status `proven` (an instant in an approved cut) / `candidate`; the P55 generator writes them into `docs/EFFECTS-CATALOG.md` and `effects_card.py` resolves a recipe by name; the drift gate refuses a `proven` recipe without an instant and a member that is not a card; `authoring/recipes.py` presets. Seeds: the 15 measured in `docs/research/runs/grill_pipeline-value/recipes_r1.md` (the badge ladder 2.05/1.30 as its dials; `emphasize == datum index`); candidates: the 67 unused cards in their s4 slots. The comparator plan per beat picks a recipe; the critic audits the recipe at its instant against its proof. |
| **R26-104** | **THE ONE-SHOT FLOOR v2 (E96; grill Q2: a)** (LANDED 2026-09-13, P56 T6-T7: `gate_one_shot_floor.py` rows M35-M42, the four on-disk cuts as `PREDATES_E96`, the reference measured at run time, wired into `self_watch.py` with the recipe audit sheet; calibration Japan PASS / Tokyo v2 FAIL M37+M38 / Steel FAIL M38 / the thin cut FAIL M37-M39; OPEN: HG2 - the operator's first read of a cut under the floor, incl. the coverage reading (Japan measures 0.76 under "a fire overlapping a beat credits it")) - a gate script the self-watch bar runs before a watch is offered: distinct chart forms >= 3, chart-to-chart transforms >= 1, docks on >= 1/3 of beats, proven-recipe coverage >= 60% of beats, narrative : chart >= 1 : 1, and the parity table against the best approved short (Japan) - events/min reported, never a floor. The measures and their extraction from a compiled timeline are in `recipes_r1.md` s1 (`walk.py`, `cooc.py`). A cut under a floor is not offered; JUDGE stays for sequence and taste. |
| **R26-105** | **THE RELATION LAYER - the rig for evidence and the camera (E97; grill Q3: both defaults)** - compiler grammar `rel: {pin | aim | group | path | derive | camera | weight}` (`parent` as sugar), defaults `inherit: [translate]` and uniform parent scale, a label pins never parents, Z authored, closed-form only; a compile-time resolver folds every relation whose target is authored into per-scene poses (the `extend_camera_cards` trick), runtime only for the three measured target kinds (datum, caption span, press phrase); replaces `resolveTarget`'s switch, `camArrivalState`, `extend_camera_cards`; `non-scaling-stroke` on any path under a scaling group. Proofs, as recipes with goldens byte-identical: (1) a card pinned to a bar's tip with the camera following it; (2) the metaphor prop - poof arrival (~0.5 s), wire draw-on left-then-right (~0.8 s), pin, `derive` (the wire's kink under the ball's contact point), the ball's solo scale (~1.5 s) with the elephant keeping its size. Research: `docs/research/runs/grill_pipeline-value/findings_rig.md`, `BRAVOS-RIG-VERIFIED.md`. The figure rig stays R26-61. |
| **R26-106** | **THE TRUE VECTOR BRUSH (grill Q4: a, then b, then c)** - `kinetics/stroke.mjs` already computes the pen's width `prof.w`; nothing draws it (`drawOn` offsets a dash on a constant-width stroke). Build: a filled OUTLINE regrown from the path prefix each frame (perfect-freehand's outline polygon, or ~30 lines of our own normal offset) fed by `prof.w`, behind a kinetics flag with the dash-offset as the flag-off branch (goldens byte-identical); five brushes - marker (shipped), pen/nib, highlighter (the sanctioned ink-over-ink), charcoal/pencil (seeded jitter copies + a generated grain plate), dry brush (seeded gaps from `prof.v`); grain never from a filter chain (the soak ruling). Order: (a) the hand-drawn species (callout, squiggle, underline), (b) the chart's own series and axes (a substitution: `d` on a fill where `drawOn` set a dash), (c) whole-image draw-reveals - generate (Flow/Comfy) -> lineart (controlnet_aux) -> `medial_axis(return_distance=True)` (centreline + measured width) -> prune/order/fit -> the hand contract of doc 29 s8.10; potrace/vtracer only for fills. Research: `findings_brush.md`. |
| **R26-107** | **RESEARCH INGESTION GATE + parked-row triggers (the operator: "a process that immediately ingests the gemini research and adds to backlog or exploration")** - (BUILT 2026-09-14: `build_research_ledger.py` -> `docs/RESEARCH-LEDGER.{jsonl,md}`, the tenth docs layer (`build_docs_layers.py`, in sync); status per run `landed` (a backlog row or plan slice cites it) / `referenced` (a doc only) / `orphan`; the parked-row trigger table (R26-107, R26-108, D8, D9); 16 tests. Measured: 19 runs - 9 landed, 9 referenced, 1 orphan (`motion_graphics_from_still_images`). Parent ruling: a run cited only by the blueprint that ORDERED it is not ingested, so the grill's six are all un-landed -> R26-110) measured 2026-09-13: of 16 runs under `docs/research/runs/`, 2 have a backlog row and 6 are cited by nothing under their run path (`motion_graphics_from_still_images` 09-03, `wealth-logic-cuts` 09-06, `weight_mass` 09-07 [folded via its blueprint path], `treasury_yield_spike_september_2026` 09-11, `video_engine_tooling_and_mcps` 09-11, `vidiq_retention_zones` 09-12). Build: a docs-layer check that lists every run directory with no citing BACKLOG row / plan slice / doc (an orphan list, like the animation registry's), run by `build_docs_layers.py`; a `trigger:` field on parked rows (R26-61's is the model) that names what re-surfaces them, and a review of parked rows when a reference (Bravos) exercises the parked thing. Triage the six orphans into rows or explicit retirements. |
| **R26-108** | **CATALOGUE GAPS the recipe mining found** (PART LANDED 2026-09-13, P56 T8: cards `plate_option:ken`, `plate_option:world`, `plate_option:clip`, `dock_option:badge` (all implicit, live); `exit:wipe_right` needs no card - it is an option of `exit:wipe` and a recipe member names it as `{card: exit:wipe, option: wipe_right}`, Steel's 32 counted; the two laws are the recipes' dials. OPEN HALF - the long form speaks no species vocabulary: measured 75 of 75 Steel scenes carry `species: []`, 0 ledger pages, 75/75 bare plates, so M38 on a long form counts only the dock/world/exit/badge vocabulary and no species- or page-based recipe can be proven there; `trigger:` the next long-form build, or the compiler emitting a `species` array for the long form's shot table - a compiler plan, not a catalogue one) - `exit:wipe_right` (32 Steel and Paper uses; folded as an option under `exit:wipe`) and the Ken Burns pair (every Steel scene) have no card id, so a recipe cannot name them: give each a card (or make the gate accept an option token as a use) and add the Steel and Paper vocabulary (its timeline carries no `species` array) so the long form and the shorts stop speaking disjoint vocabularies; lift the two measured laws into their recipes' dials (badge ladder dock_enter + 2.05 s then + 1.30 s; `emphasize == the spotlight's / punch's datum index`). |
| **R26-109** | **"The Myth of Historical Normal" - the review's open requests (a rewrite order, the next letter; never a patch of the recorded take)** - (DRAFTED 2026-09-13 night: `content/video_engine/projects/systems-and-blowups/myth-of-historical-normal/REWRITE-ORDER-B.md`, status DRAFT awaiting the operator; proposes ERP -0.80% [verify] from the payload's own 10Y 4.83 (-0.95% retired, no pair reproduces it), the 480 s table replaced by the measured 565.4 s clock - the uncovered narration is 85.4 s not the review's ~39 s, six spans each with a proven recipe proposed, and zeta 0.82 / w0 14.5 kept (no mass reconciles damping 18 / stiffness 130; `kinetics/spring.mjs` reads neither). OPEN for the operator: the ring token - the gates FAIL either way (`weight` left the VO 2026-09-12; `percent` trips G27); the earnings-yield leg has no P/E payload) from bridge reply `f4541f7f6808` (REQUEST CHANGES, read 2026-09-13): (5) the ERP contradicts itself three ways (`VISUAL-CHOREOGRAPHY.md:36` still says -0.97%); (6) ~39 s of narration has no scene - the gate now measures 565.4 s against a 480 s table and `edit/EVIDENCE-COVERAGE.md:5` still asserts 480.0 s; (7) the spring spec contradicts itself (zeta 0.82 vs damping 18 / stiffness 130). Doctrine points kept from the same review: an evidence order's CNBC/Treasury attributions must match their URLs; a brief never overclaims a gate ("0 WARN") and its commands must run as written. The seven other requests were already handled (raw CSV payloads with sha256 cites, the ring number, the hook). | The payload is a run on disk: `docs/research/runs/treasury_yield_spike_september_2026/raw_payloads/` (DGS2/DGS10/DGS30, GFDEGDQ188S, A091RC1Q027SBEA) with `findings_treasury_yields.md` - `DGS10.csv` line 16878 reads `2026-09-09,4.83`, the figure REWRITE-ORDER-B derives its ERP from (`REWRITE-ORDER-B.md:95` already cites the folder) (R26-110, 2026-09-14). | REVIEWED 2026-09-14: approved (E99 s12) |
| **R26-110** | **Triage the six un-landed research runs into rows or explicit retirements (R26-107's first harvest)** - `docs/RESEARCH-LEDGER.md` lists them: `motion_graphics_from_still_images` (09-03, orphan; the Remotion still-motion blueprint - superseded by the scene-evidence engine's own motion? retire with the reason or fold into R26-105/106), `wealth-logic-cuts` (09-06; the reference cut measurements that set M13's thresholds - cite from doc 47 or the gate's docstring), `weight_mass` (09-07; folded via its blueprint into P47's weight dials - cite from the animation registry), `treasury_yield_spike_september_2026` (09-11; the bridge short's evidence - cite from R26-88's pass 2 or retire), `video_engine_tooling_and_mcps` (09-11; the readiness review's tooling input - cite from R26-102 or retire), `vidiq_retention_zones` (09-12; cited only by the blueprint that ordered it - fold into doc 46 s46.6 / the retention clock or retire). Each becomes a citing row, a doc cite, or a one-line retirement in the ledger's md; the gate stays red on orphans until then. | **DONE 2026-09-14 (P57 T2)** - `build_research_ledger.py`: 20 runs (0 orphan, 4 referenced, 16 landed) |
| **R26-111** | **Regenerate the `mechanism-town-v1` world plate (the operator's ruling 2026-09-14)** - the plate (catalogue row 13, sha `e82ef039...`) exists in no checkout; the row and the manifest entry carry `file_state: regenerate` (never render-eligible, not required on disk) until it is regenerated from its manifest request (the 16:9 interactive economic town) and `file_state: present` restored with a fresh sha256 in both `asset-catalog.v1.json` and `generation-manifest.v1.json` - before any cut resolves a world plate to it. The three struck SOURCE intermediates (`mechanism-index-basket-v2`, `mechanism-balance-ledger-v1`, `mechanism-economic-elevator-v1` sources; `source_state: struck`, their cutouts verify) stay struck and are not regenerated. The finance catalogue now validates under `finance_asset_catalog_styled.v1` (45 assets, 19 in the manifest). |
| **R26-112** | **The still-image motion-graphics research, retired by ruling** - `docs/research/runs/motion_graphics_from_still_images/` (09-03, the ledger's only orphan; `research_plan.md` + findings on 2.5D parallax/depth, code-driven Remotion, generative I2V, pipeline architecture) was synthesised into `docs/research/tech/MOTION_GRAPHICS_FROM_STILL_IMAGES_BLUEPRINT.md`; the one artifact it left in the engine is `content/video_engine/workflows/2_5d_parallax_inpaint.json` (Depth Anything v2 + DepthFlow camera displacement) | retired one day after it was run: **E32** (2026-09-04, `docs/portable/OPERATOR-RULINGS.md:1089-1098`) - *"motion means animation -> I need generative video... Only the third link is wrong, and it is what sent the engine to Flow, Wan, LTX and Depthflow to solve a problem it had already solved"* - and **E49** (2026-09-06) put Ken Burns and parallax back to being camera moves, not the stillness cure (R26-12). R26-7 holds the standing position: the engine's own capabilities carry the motion, B-roll is the exception. The blueprint stays as reference for the day a dock needs I2V | **RETIRED 2026-09-14 (P57 T2)** - reference only |
| **R26-113** | **The weight/density/mass research, already landed in the dials** - `docs/research/runs/weight_mass/` (09-07; `research_plan.md`, `CHECKPOINTS.md`, findings Q1-Q6: hit frame count, cast-shadow depth, receiver surface, density vs mass, sound impact cue, stop-motion specifics) -> `docs/research/motion/WEIGHT_DENSITY_MASS_RESEARCH_BLUEPRINT.md` | `content/video_engine/scripts/kinetics/stopaction.mjs:32-33` cites that blueprint by name for the squash/restitution dials (*"source on file: Williams pp. 93-94, 263; Lasseter 1987 s2.2; Whitaker & Halas 1981 p. 42 - WEIGHT_DENSITY_MASS_RESEARCH_BLUEPRINT.md Q1"*), which is what the order asked for - the `[DERIVED]`-from-a-vendor-demo numbers replaced by sourced ones (`docs/runbooks/WORK-ORDER-GEMINI-WEIGHT-MASS-2026-09-07.md`). Nothing open | **DONE 2026-09-14 (P57 T2)** - landed in `stopaction.mjs` |
| **R26-114** | **The vidIQ retention-zone read, and the one claim it leaves open** - `docs/research/runs/vidiq_retention_zones/findings_retention.md` (09-12; transcript anchors plus the reference channel's own analytics: AVD 4:46 = 35.5% APV; 0:00-0:30 100% -> ~66%, 0:30-2:45 -> ~38%, flat ~38% to 8:30, then -> 20%) -> `docs/research/retention/CHART_APEX_RETENTION_ARCHITECTURE_RESEARCH_BLUEPRINT.md` | zones 1-3 are already ours (E21 captions are the motion; E24 the opening minute; doc 46 s46.6 the picture change 3 frames before onset; the loop cycle our G36 ceiling already gates), so the run is ingested - **except** the blueprint's headline proposal, the *"Apex Chart Read"* (frame 0 on the live chart at its apex), which reads against **E24's "never open on the full chart"**. One operator ruling decides it; nothing is built until then | open 2026-09-14: a ruling, then one slice | REVIEWED 2026-09-14: correction: opening on a full chart was never refused (E99 s10) |
| **R26-115** | **`sync_kinetics` orders the regions itself - the import graph is the order, never a hand-kept list** (the operator, 2026-09-14, on the rule at `sync_kinetics.py:19` *"a module may import another module only when that module's region sits EARLIER in the engine"*: *"sounds like a major weakness in the program, and it makes it sound like we're not a first class creative engine still"*). It is a BUNDLER limit, not an engine one: the player is one self-contained file with no imports, so every module is pasted in as a region and a pasted `const` is visible only to the pastes after it - which made the region order a list a human keeps, and made a lane stop and ask before composing the melt and the figure into the compare (P57 T12b: `melt` sat at 10514 and `compare` at 896, both relocated by hand). THE FIX: the modules already declare their imports, so `--write` computes a topological order from that graph (a cycle is an error naming the two modules), keeps the ONE real constraint (`/* SPACE: page */` painters after the page registry, `:163-169`), and writes the regions in that order; `--check` fails when the engine's order is not the graph's. PROOF: a reorder moves no pixel - every golden byte-identical by sha256 - and `test_kinetics_sync.py` gains a test that an out-of-order engine is refused and a cyclic import is named. Then the relocation rule at `:19` and its error at `:225-227` are retired as prose | the next engine slice after T12b/T12c (the same file); one lane, one golden table | a bundler slice, no pixel |
| **R26-116** | **The melt-then-re-write STACK as a recipe of its own** (the operator, 2026-09-14: *"the method i described may become one of our effect-combination stacks"*) - the quoted figure's ink MELTS (E88's melt, the ink never the board) and the hand RE-WRITES at the same datum (E50's write): built for the compare verb (R26-70 T12b, `form: melt`) but general - any figure, label or title can leave that way and come back as something else. A candidate recipe naming the two members (`exit`-less: `page_species:figure` -> the melt of its ink -> `page_species:figure` re-written) with the compare golden as its first proof; proven when an approved cut carries it (E96: a beat is a recipe) | after T12b lands (the recipe file is in its write set) | a recipe file + the catalogue |
| **R26-117** | **The melt's THIRD ending: the ball MORPHS into the next chart** (the operator, 2026-09-14: *"melt it into a ball, then we either throw it off the page, splatter it back on to the canvas and build the chart/graph from that, or morph it from the ball into the chart"*). On the record the page melt (E88, `species/melt.mjs`) already sags, balls up (u 0.30-0.55, the outline morphed to a circle of `BALL_R` by morph_a) and ends by `throw` or `splash:chart` / `splash:plate` (the next world grows through the stains); the third ending is not built, and its pieces are: `page_enter:morph` already deforms a prop outline into the chart's shape, so the ball is the prop it starts from. Build `melt:morph` - the ball's ring handed to the next page's morph enter as its prop outline, one clock, no cut between - with a golden at the hand-over and the same three-ending grammar the compare verb gets (R26-70 T12c: `then: morph | splash | throw`) | after T12c proves the ball -> outline hand-over on a figure; the page-level version is the same math on a bigger ring | one engine slice, one golden | REVIEWED 2026-09-14: built and judged to the whole-chart standard: the ball becomes the next FULL chart (E99 s1) | **CARD CREATED 2026-09-15 (P61): `r26-117-ball-into-the-next-chart` on the queue as owed - the build is P61 T3, held behind HG1 (the whole-chart standard it must meet); it also delivers the melt's proof B for R26-76** | **RULED 2026-09-16 (E99 s53): "this is good, i think there's a rushed snap at the end transforming the chart to a ball, but the rest is good" - melt:morph ships; the compile phase re-timed into a visible fusion (P61 T3c); CLOSED as built** | **RULED 2026-09-16 (E99 s58): "Yes, this is good." - the closing ships; the default window stays 1.6 s; CLOSED** |
| **R26-118** | **The melt's ball has MASS and shows it - it lands, rolls and settles before its ending** (the operator, 2026-09-14, E88 s6: *"we need to make sure our ball has real density, and we should probably roll it around or manipulate it a bit for good measure to show that it has real mass & gravity, just like the hyperframes example did with their stop animation"*). On the record the ball forms with ONE weight cue - `impactSquash` on the MASS material at the instant it compiles (`melt.mjs:297-301`, `SQUASH` 0.30) - and `melt.mjs` imports two of stopaction's exports (`throwXf`, `impactSquash`) while the P47 module built from the weight/mass research (`weight_mass`, R26-113: hit frame count, cast-shadow depth, the receiver surface, density vs mass) also owns `contactShadow` (`SHADOW_FAR`/`SHADOW_NEAR`, `SHADOW_H_PX`), `groundDip` (`DIP_PX` per material), `groundShake` (the HyperFrames headline-slam three-frame answer), `rebound` (`e`), gravity `G_PX_S2` and `LAG_FRAMES`; `docs_find "roll"` has no rolling ball. BUILD, in `species/melt.mjs` between the ball phase and the ending, a WEIGHT phase (a named share of u): the ball LANDS on the board (the dip under it, the contact shadow tightening from FAR to NEAR, the one-frame squash - all stopaction's, on 2s), ROLLS a short authored way (no slip: the turn angle = distance / radius; a named friction decelerates it on the stepped clock; the squash tensor along the travel; the shadow rides under it; the ink's own texture must turn or the roll reads as a slide - the ball keeps a MARK from its ink so the rotation is visible), takes one NUDGE (a small second impulse - 'manipulate it a bit') and SETTLES (the material's spring, `SETTLE_S`) before `throw` / `splash` / `morph`. Every number a dial in `MELT`; the page melt and the figure's (R26-70) share it by construction. Golden `melt-ball-roll` at the roll's mid-turn + a proof at the settle; the existing melt goldens move ONLY if the operator rules the weight phase into the default length (else it is opt-in, `melt:weight`, and every golden stays identical) - the slice states which and why WIDENED the same day (E88 s7: *"a more living ball, that is wriggling to contain itself, and has real, metallic mass and density"*): the ball is a DROP - its boundary a ring r(theta, t) = R (1 + sum over l of a_l cos(l theta + phi_l) e^(-t / tau_l) cos(omega_l t)) of Rayleigh surface modes l = 2, 3, 4 (omega_l^2 proportional to l (l - 1)(l + 2) times the material's tension over its density and R^3, tau_l from its viscosity - the numbers are DERIVED dials, cited to Rayleigh 1879 / Lamb, and tuned by eye), excited by the compile and re-excited by every landing and nudge, never let go still (E49: a floor amplitude that keeps it wriggling to contain itself), the ring's AREA renormalised every frame (incompressible), the outline drawn as a closed cubic (morph_a's reconstruction) under the gooey threshold the melt already owns; METAL by default (`MASS.metal`) with a specular highlight - a lit spot offset toward the stage light that slides on the surface with the roll and the modes; `docs_find`: 0 hits for surface tension / Rayleigh / specular / gooey - new math, ours. The roll's mark (above) and the highlight together carry the rotation. **BUILT 2026-09-14** - opt-in `melt:weight[:<material>]` (metal default; ink / paper / liquid; an unknown material refused by name): a 1.15 s land / roll / nudge / settle phase in `species/melt.mjs` on `kinetics/drop.mjs`'s living Rayleigh surface (modes 2-4, area renormalised, a floor amplitude per E49, mercury's sigma/rho/nu from the quarantined living_drop run tagged PLAUSIBLE, galinstan not dialled - contested); the roll is `stopaction.rollXf` (no slip, constant friction, stops where the math says - node-pinned); a knot of the ball's own ink at 0.62 r carries the turn, the highlight stays on the light; golden `melt-ball-roll` + `@proof-land` + `@proof-settle`; every other golden byte-identical; 41 modules in sync; node 528/528; `test_transitions_e47` pins MELT_W_S. The parent's read of the frames: heavy, the roll reads, the mark turns - but the surface reads GLOSSY (a gel ball) more than METALLIC: the next dial is a dark Fresnel rim + a reflection band; the operator judges the frame | after T12c (the same file); the reference frames: the HyperFrames stop-motion example the P47 dials cite | built, awaiting the operator's eye on 'metallic' (frames in tests/golden/frames/melt-ball-roll*) | REVIEWED 2026-09-14: reads glossy - more shadows: a dark Fresnel rim, a metallic band, at least one point of deep shadow (E99 s3) | **BUILT 2026-09-15 (P61 T5): twenty named dials - the DARK Fresnel rim (the ruling's sign, opposite the blueprint's bright grazing rim), the metallic band, one point of deep shadow, the tighter contact shadow - in `kinetics/drop.mjs`'s material profiles and `species/melt.mjs`'s paint; the silhouette ring 120.7 -> 64.6, the contact shadow 26.9 -> 10.5; behind `melt:weight`, the default byte-identical; HG5 open on `r26-118-metallic-ball` (crop + two clips); the body's orange and the straight band named as the honest limit** | **RULED 2026-09-15 (E99 s42): "This doesn't work, the ball is too blurred so the prior work is actually better ... the darkness feels right, but the blur is wrong, and i think the rim is wrong" - P61 T5b: the prior surface back, the darkness kept, the rim and band off, plus the ball melting to slate grey and to the reference colour for the operator's eye** | **RULED 2026-09-16 (E99 s49): "I like the reference" - the weight ball's default body becomes the reference black (P61 T5c); the ink BLEND (orange + green swirling to olive, the dark olive as shading) is R26-146; the FULL-PAGE melt (everything melts over a plate or the cream, then the throw-to-form) is R26-147** |
| **R26-119** | **RESEARCH: the living metallic drop - Rayleigh modes, damping, the highlight, the stop-motion craft of a heavy ball** (E88 s5-s7; order `docs/runbooks/WORK-ORDER-GEMINI-LIVING-DROP-2026-09-14.md`, run `docs/research/runs/living_drop/` when it lands) - the law's shape is held (omega_l^2 = l(l-1)(l+2) sigma / (rho R^3); Lamb's damping); the NUMBERS (sigma, rho, nu for a liquid metal vs ink), what a real drop does after an impact, the highlight's rule and size, and how animators sell a heavy ball at 12 fps are `[DERIVED]` until sourced. Feeds R26-118's dials | **SENT 2026-09-14** (the operator: 'research approved') - bridge packet `de496f8521b3`, lane gemini, profile video-researcher, shape report-landed; lands through R26-107's gate; **LANDED 2026-09-14, gate read: PLAUSIBLE** (`docs/research/runs/living_drop/` five findings + `docs/research/motion/LIVING_METALLIC_DROP_RESEARCH_BLUEPRINT.md`; zero fetched pages on disk so nothing CONFIRMED; provenance audit 0/0). Usable: tau_2 = R^2/(5 nu); 3-6 visible cycles / 80-220 ms settle; wobble 2-3 drawings on 2s; highlight seat r = R sin(theta_L/2). NOT usable: galinstan sigma 0.535 (contested), the ink row (no source), the 0.15-0.20 R highlight size (misattributed - DERIVED). One invented path (`docs/effects/`). Gate table `docs/research/runs/p58-2-5d/research-gate-2026-09-14.md`. QUARANTINED until the operator approves | a Gemini run | REVIEWED 2026-09-14: RELEASED - the usable findings may be built on; the rest stay excluded (E99 s24) |
| **R26-120** | **RESEARCH: the morph that handles holes and topology - implicit / SDF blends vs ring correspondence** (E76 s5; order `docs/runbooks/WORK-ORDER-GEMINI-IMPLICIT-MORPH-2026-09-14.md`, run `docs/research/runs/implicit_morph/`) - the compare's glyph morph (R26-70 T12c) and the ball -> chart morph (R26-117) pair rings by a hand-written rule; the gooey threshold is already an implicit surface, so the question is whether an SDF blend (Turk & O'Brien 1999 and after) is the right law for ink and what it costs at 12 fps in a canvas; plus the hand's stroke order for a re-draw (skeletonisation). Decides whether `form: morph` moves to a field blend before it ships | **SENT 2026-09-14** (the operator: 'research approved') - bridge packet `abf0f57339d3`, lane gemini, profile video-researcher, shape report-landed; lands through R26-107's gate; **LANDED 2026-09-14, gate read: UNSOURCED-editorial; the '10.1 % of budget' cost REJECTED** (`docs/research/runs/implicit_morph/` + `docs/research/motion/IMPLICIT_MORPH_RESEARCH_BLUEPRINT.md`; its own reply.md lists doi.org as unreachable yet prints Turk & O'Brien quotes as CONFIRMED; ten rows stamp CONFIRMED on DERIVED; 'SVG gooey = 0.00 ms' false on its face; two HIGH design-proposal sections). Keep as hypotheses only: smin is a union not an interpolator; DEGEN_R's r->0 tangent singularity - test on goldens before any morph_a change. QUARANTINED until the operator approves | a Gemini run | REVIEWED 2026-09-14: RELEASED - the usable findings may be built on; the rest stay excluded (E99 s24) |
| **R26-121** | **RESEARCH: the relation layer - what 2D rigs and constraint systems get right, harvested** (the operator on Bravos' rigging, 2026-09-13; R26-105 the unbuilt `rel` layer; order `docs/runbooks/WORK-ORDER-GEMINI-RIG-LAYER-2026-09-14.md`, run `docs/research/runs/rig_layer/`) - Rive state machines + constraints, Spine/DragonBones constraint evaluation, After Effects expression idioms + the 2D camera rig, Theatre.js, and the minimal 2D solver math (pin, aim, follow-path, two-bone IK, weight/lag) as pure functions - harvested, never swapped in (`steal-now-harvest-not-replace`). Feeds R26-105's plan | **SENT 2026-09-14** (the operator: 'research approved') - bridge packet `cee8b03cbd8f`, lane gemini, profile video-researcher, shape report-landed; lands through R26-107's gate; **LANDED 2026-09-14, gate read: PLAUSIBLE, the best-formed of the three** (`docs/research/runs/rig_layer/` five findings + CHECKPOINTS + `docs/research/motion/RIG_CONSTRAINTS_RELATION_LAYER_RESEARCH_BLUEPRINT.md`; zero fetched pages so nothing CONFIRMED). Usable now: Spine's constraint overwrite rule; Theatre.js has no declarative derive; two-bone IK theta_2 = sigma acos((d^2-l1^2-l2^2)/(2 l1 l2)) with the reach clamp. One HIGH design-proposal section (s3.2's camera-rig graph) to ignore. Feeds R26-105. QUARANTINED until the operator approves | a Gemini run | REVIEWED 2026-09-14: RELEASED - the usable findings may be built on; the rest stay excluded (E99 s24) |
| **R26-122** | **THE 2.5D STAGE (E98, the operator 2026-09-14: *"most of bravos work is 2.5d, and that's how we started out as well ... get back to prompting every plate and probably drawing charts/graphs as 2.5d"*)** - a PRP, not a slice: (1) every plate prompted as the four-layer depth separation the Bravos analysis specifies (`REPORT.md:28-31`) - separate generations or a depth-split of one (the retired `workflows/2_5d_parallax_inpaint.json` Depth Anything + DepthFlow path is on disk for the split), each layer with its alpha and a depth, indexed in the plate library (`plate_library` gains `layers[] {path, depth, role}`); (2) the camera (E59, `kinetics/camera.mjs`) reads a depth per layer so its push/pan/follow yields parallax by construction - never authored for its own sake (E49 stands); (3) chart forms as objects in that space - the ledger page as a card at a depth, an extruded-bar and a tilted-plane line variant behind a page option, the flat page the default (E50-E53 unchanged); (4) the docks, the ball, the slide and the rig (E97/R26-105) move THROUGH the depth. Recall: `kinetics/homography.mjs` (the embed surface is already a plane in space), docs 42/43 from the 2.5D monograph, `docs_find "depth map"` 0 hits. Goldens for each; no approved cut rebuilt (E45); the first 2.5D cut is a new one. Draft as P58 after P57's HG rulings **P58 RUNNING 2026-09-14** - T1 the probe built (`comfy_depth_split.py` + `probe_2_5d.py`, 18 frames, three routes) and HG1 ruled (E98 s6: a layer is generated, never cut out of a stacked generation; the split is the fallback for an existing flat plate); T2 dispatched **2026-09-14 evening: T1-T5 COMPLETE and committed** (a4e44ba probe + HG1, f89c2ba the library, 417cf2d the camera over layers, 07bedbf the page at a depth, a12b37c the two chart forms); T6 (docks / ball / slide through depth; the rig sub-step DROPPED - R26-105 unbuilt) in flight on the engine lock; T7 brief ready (`build-p58-2-5d`, three changes, the approved builds read-only); T8 last. Human gates open: HG3 (R26-131), HG2 after T7 | the operator's word to draft P58 | P58 running (T1-T5 done; T6 in flight; HG3 open) | BUILT 2026-09-14 - P58 T1-T8 landed, AWAITING HG2 + HG3 (R26-131): T1 the probe + E98 s6 `a4e44ba`; T2 the library's layers[] `f89c2ba`; T3 the camera over layers `417cf2d` (golden `camera-layers`); T4 the page at a depth `07bedbf` (`page-depth`); T5 the extruded bar + tilted line `a12b37c` (`form-extruded-bar`, `form-tilted-line`); T6 the docks + the melt's ball at a depth `451b31d` (`dock-depth`, `melt-depth`) and the slide `9d6768b` (`slide-depth`); the kit's page-cut sound fix `39564ec`; T7 the first 2.5D cut `0107120` - attempt 1 Tokyo failed the parent's read, attempt 2 Japan built (`japan-tariff-trick/build-p58-2-5d/`, motion 1 FAIL vs the approved 3, sound identical) and the depth does not read in its 0.45 s arrival; T8 the record (CAPABILITIES rows, the plate-prompting line in BUILD-PIPELINE / PIPELINE / HEADLESS_CLAIM_RESUME, the ten layers). The finding: a layered plate shows depth only while a camera move holds it on screen, so the next 2.5D cut is authored depth-first. Open beside it: R26-132 part 2 (a depth card in frame), R26-133 (a plate's drift idle paints nothing). Rig sub-step dropped (R26-105 not landed) | REVIEWED 2026-09-14: HG3 stays open until the forms are shown in motion; a slanted page only with a dock in front or as the lead into the next plate; the extruded bar's leave breaks down more; slide depth does not earn its keep on a slide (E99 s4, s5) | REVIEWED 2026-09-14: HG2 stays open on the card's rewrite; depth-first is the operator's lean (E99 s15) | REVIEWED 2026-09-14: HG3's recommendations stand; no scribble clearing to cream, the charcoal mount over the scribble build, the tilted line for a near-identical pair (E99 s18) | REVIEWED 2026-09-14: HG2 RULED (depth-first; the slant as a transition or behind a foreground; the plane kept as the door's material) - with HG3 ruled at E99 s18, P58's human gates are closed (E99 s26) |
| **R26-123** | **OPERATOR: the caption's default setting (P57 HG2, E90 s3)** - `cap_life: pop | stagger | blend` shipped opt-in (R26-77); the parent's read of the band sheet: the BLEND reads at every instant, the stagger alone loses the word to the voice, the pop is crisp but flat. Judge by EYE (`content/video_engine/projects/systems-and-blowups/tokyo-tea-break/self-watch-p57-t14/contact-sheet.png`, `contact-sheet-band.png`, the nine frames at 25.23 / 25.33 / 25.43 s) and by EAR (`http://127.0.0.1:8761/player-{pop,stagger,blend}.html` - a PRIVATE copy of build-short.v2 served from the session's scratch dir; if the port is dead, the copy is at `scratchpad/p57/t14/tokyo-private/` and re-serves in one command). Rule which becomes the default (or none yet) and whether the blend's dials move (`POP_LEAD` 1.22, `POP_S` 0.20) | the operator | one ruling; then one compiler default | REVIEWED 2026-09-14: the caption default is the blend (E99 s6) |
| **R26-124** | **OPERATOR: two form rulings from P57 HG2** - (1) the SLIDE's seam: at mid-slide both boards abut on the centre line with the deckle notch at the seam (`tests/golden/frames/slide-mid.png`) - boards, or a seam to close? (2) the race's RANK-SWAP lane: at the crossing the passing row's label and value step INTO its own bar with a halo (`race-swap.png`) - does it read as two rows? Also the race's two path settings side by side (`race-path-eased.png` / `race-path-clothoid.png`): which is the default for a race page | the operator | rulings; each one dial or a golden | REVIEWED 2026-09-14: the slide is good; the race reads as separate rules and was fine even with the clash (E99 s8) |
| **R26-125** | **OPERATOR: P57 HG1 - the measured defects' frames, one sitting** - the sheet `docs/research/runs/p57-burndown/HG-HANDOFF.md` names every frame: R26-66 the empty stage, R26-67 the ring's flag, R26-68 the span's shade (P53's frames), R26-71 the figure off its own line (`build-review/self-watch/p57-t10/before-t31p78.png` vs `after-t31p78.png`), R26-70 the compare morph (`compare-morph@proof-sag` / `@proof-ball` / `@proof-050` / the base; the counter as `compare-count@proof-mid`; the text melt as `compare-streak@proof-melt`). Note: at a figure's scale the ball is a small disc by construction - R26-118 gives it mass and a living surface | the operator | one watch | REVIEWED 2026-09-14: the span's grey reads darker; the empty-stage hand-off and the R26-71 proof must be shown so a person can read them (E99 s7) | REVIEWED 2026-09-14: R26-71 and R26-67 accepted; the hand-off clips read as a jump with no transformation - the whole sequence is owed (E99 s19) |
| **R26-126** | **OPERATOR: the push word** - main is ahead of origin by the P57 batches (`4376de6` T1-T5, `527617f` T10-T12, `2a82cd4` T13-T16, `cbc0ef2` T17-T23, + batch 4 the compare morph / rulings / rows / orders / P58 draft, + the batch after it); nothing is pushed without the word (the 09-14 push word was one-time) | the operator | 'push' | PUSHED 2026-09-14: on the operator's word in the session - *"you can push when ready"* (after marking push on the review page) - `git push origin main` `5ccc484..3163de3`, main level with origin |
| **R26-127** | **Session-local state that dies with the session (2026-09-14) - restart on resume** - (1) the bridge daemon (`bridge_daemon.py --loop --poll-sec 60`) was DEAD since 09-07 (stale lock pid 52120) and was restarted from the P57 session; it carries the three research replies (R26-119/120/121) and dies with the session - `python content/video_engine/scripts/bridge_daemon.py --once --dry-run --json` then `--loop` on resume; (2) the :8761 caption player (R26-123) is served from the session scratch; (3) the T10 frames live under `build-review/self-watch/p57-t10/` (untracked, kept) and the build-review engine copy + `layout-probe.json` are MODIFIED in the tree by design (patched to read the frames), never committed. A runbook line for 'the daemon is a service, not a session' is owed: the daemon belongs in the scheduler (P46's open scheduler entry) | the parent, on resume | a scheduler entry |
| **R26-128** | **The living-drop research lane stalled on our tooling (2026-09-14)** - the first reply to R26-119 read *"I am waiting for `build_docs_layers.py --write` to finish"* - the research profile ran OUR layer regen (it is told to use docs_find, which is read-only) and blocked on a lock; a follow-up (`bridge_reply.py`, followups/01.md) sent it back to the order. The profile needs a line: docs_find only, never a --write; and the daemon's tier 0 should refuse a report-landed reply with no `## Verdict up front` as a non-answer rather than escalating it as a reply | the next bridge slice (R26-88 pass 2) | a profile line + a tier-0 rule |
| **R26-129** | **The research ledger counts the bridge TRANSPORT as a run, so a live daemon makes the layer drift** (2026-09-14: `build_docs_layers.py --write` then `--check` read `research-ledger is stale` every time while `bridge_daemon.py --loop` ran - `docs/research/runs/bridge/` is the packet queue, and its file count (99 -> 140 in a day) is the ledger's `files` column for the `bridge` run). The bridge is transport, not research: `build_research_ledger.py` should skip `bridge/` (or any run dir whose name a small denylist carries) so the ledger's truth is the research runs; the P46 plan citations that land it today move to the ledger's own note. One slice, one test (a run dir named `bridge` is not a run); then the daemon can stay up while the layers regenerate | the next docs slice | small |
| **R26-130** | **The plate indexer had a silent hole: an approved plate no scanner reproduced** (found by P58 T2, 2026-09-14) - `plate-p-viewers-desk` (operator-approved, render-eligible, provenance `omni-video/stills/APPROVALS.json`) was in the shipped `PLATE-LIBRARY.json` but no scanner in `build_plate_library.py` found it, so every rebuild dropped it and nothing said so. T2 carried it forward through `tokyo-tea-break/assets/plates/plates.json` (an entry may name a plate elsewhere in the project) and the rebuild diff is now checked (`gone set()`). Still owed: a rebuild that LOSES a record the shipped index had must refuse by name (the diff is printed, not gated), and a sweep for any other approved plate whose path no scanner covers | open | a gate on the indexer's diff |
| **R26-131** | **THE OPERATOR'S OPEN JUDGEMENTS after P58 T1-T5 (2026-09-14)** - one sheet each, frames on disk, nothing rebuilt: (1) **HG3 the two chart forms** - `docs/research/runs/p58-2-5d/HG3-SHEET.md`: the prism (`tests/golden/frames/form-extruded-bar*.png` vs `thread-baseline.png`; parent: take), the tilted line (`form-tilted-line*.png` vs `ledger-page-mid-build.png`; parent: layered-plate-only), `throw=depth` keep/rename/retire (parent: keep + the pair refusal); the ruling goes into OPERATOR-RULINGS as an E98 amendment by the parent, a refused form deleted with its failure written (RECALL-RECEIPT s5). (2) **The ball's metal** (R26-118, `melt-ball-roll*.png`): heavy and the roll reads; the parent reads the surface GLOSSY not metallic - a dark Fresnel rim + a reflection band is the next dial if the operator agrees. (3) **Route (a) at GPT Image 2.5, the real comparison** (E98 s6, HG1 sheet `docs/research/runs/p58-2-5d/HG1-SHEET.md`): the headless lane's output is stamped gpt-image 2.0 with no transparency (the operator suspects 2.5 behind the stamp; P59/P60 settles it) - if the operator hand-generates one plate's four layers (our prompts in `build-p58-probe/prompted/<plate>/manifest.json`) into the same folder, `probe_2_5d.py --frames` puts them on the sheet unchanged. (4) **HG2 - READY 2026-09-14**: the Japan 2.5D cut `japan-tariff-trick/build-p58-2-5d/` at http://127.0.0.1:8764/player.html (the Tokyo attempt failed the parent's read and is not served); sheet `docs/research/runs/p58-2-5d/HG2-SHEET.md` - four rulings: the layered plate (depth-first next cut / option only / refuse; the depth does not read in a retrofit), the page at depth + tilt, snap vs camera for the receipt arrival (recipe coverage 0.76 -> 0.64), and R26-133 the plate drift idle. (5) **The push** (R26-126): main is 12 commits ahead of origin, none pushed | open - the operator's eye | rulings | UPDATED 2026-09-14 (late): the push count above is stale - main is 22 commits ahead of origin (`git rev-list --left-right --count main...origin/main` = 22 0; origin last moved at the authorized push 5ccc484). Every open judgement here, with this pass's since (the evidence door watched, HG2 sheet sent, the plate idle to be seen), now lives in the review queue (`docs/content-video-engine/REVIEW-QUEUE.md`, interactive page served by `serve_review_queue.py`); the review queue carries the operator's answers back to this row | REVIEWED 2026-09-14: item 4 (HG2): the card lacked the moment and the question - it is rewritten; the operator leans depth-first, a slanted page only as a transition or behind a foreground (E99 s15) | REVIEWED 2026-09-14: item 4, HG2 RULED: depth-first; the slant only as a transition or behind a foreground; the page plane paid for the door (E99 s26) |
| **R26-132** | **Two holes P58 T6 left in the depth it built** (found by the parent reading T6's goldens, 2026-09-14) - (1) **the melt's depth has no compiler refusal**: `melt_depth()` (`build_scene_timeline_f.py:1597`) has no caller, and the engine's `camDepthSwap` returns nothing for a world whose camera already went onto its own planes (a page authoring `;depth=`), so `melt:...:depth=<k>` on such a page silently paints the flat melt - refuse the pair by name in the compiler, as `throw=depth` + `depth=` is refused. (2) **a dock at a depth overshoots the move that aimed at it**: `tests/golden/frames/dock-depth.png` (the hold) - the card at k 1.15 under a focus_zoom aimed at its flat box runs off the right and bottom edges, and M25 (`gate_motion_density.py:69`) reads cards over ink, the caption strip and type size, not the card in frame, so a cut would pass it; owed: an in-frame row for a settled depth card at every landing (M24's sibling, `_in_frame_faults` :835), and the question whether a focus_zoom aimed at a depth card aims at its projected box | open | a refusal + an in-frame row | (1) CLOSED 2026-09-14 by P58 T6c - `melt_depth_page_error` (`build_scene_timeline_f.py:1614`, called in `_melt_boundary` :1796) refuses a melt at a depth on a page that already stands at depth=, and the slide's depth is refused beside the same page or a layered plate; (2) the depth card in frame: open |
| **R26-133** | **A plate's `drift` idle paints nothing** (found by the parent during P58 T7, 2026-09-14) - the engine reads a plate world's idle in ONE place and keeps only its scale: `scene-evidence-engine.mjs:12733` `zi = idleXf(idleOf("plate", scene.world.idle), ...).scale`. `idleXf` (`:1456`) returns `drift` as `{scale: 1, dx, dy}`, so a plate authored `;idle=drift` holds perfectly still - E49's named idle is not delivered. The APPROVED Japan short carries it on five plates (`japan-tariff-trick/build-short/japan-short.timeline.json`: 5 `"idle": "drift"`, from `build_short.py` rows `plate-podium;idle=drift`, `plate-gates`, `plate-ship`, `plate-vault`, `plate-check`); the class default `breath` (`:5995`) is a scale and does paint. Owed: (1) whether M18 (the frozen-frames row, `measure_frozen_frames.py`) read those plates as still, and why it passed; (2) the fix - the plate's `dx`/`dy` into the world's rest term (and, on a layered plate, per plane) - changes how approved plates MOVE on any future render, so it is the operator's eye, not a silent patch; a golden per idle kind on a plate | open - the operator's eye before the fix | an idle that paints + a gate that reads it | THE OPERATOR, 2026-09-14: *"Plate idle should probably paint, but would have to see what it looks like"* - so the next step is a look, not a fix: a side-by-side of a drift plate painted vs held, for the operator's review pass (no approved cut changes until it is seen); the operator is holding scope until the review player pass | **E99 s38 (2026-09-15): the operator could not judge the painted-vs-held proof - "I can't make a decision on it because it's not realy a visible shift" (2.3 px at eight frames per pixel) - so the card is OWED again: held beside painted at an amplitude and hold that READ, or the finding that no amplitude reads as life inside a plate's real hold; the decision stays the operator's, on a proof a viewer can see** | **RULED 2026-09-16 (E99 s55): "we need both the drift painted as an option and the alive ... maybe a slightly smaller drift (maybe 30 px?) AND the alive water ... 40 px ... too much motion for a long form, but ... might work really well for shorts" - P61 T14: the amplitude dial + the alive plate as a plate-life route, composable; CLOSED as a ruling** **T14 BUILT 2026-09-16: `plate_idle_drift_px` per scene (30 / 40; the 2 px constant the floor) and the ALIVE PLATE as a layered plate's background wall, the planes / camera / drift composing over it; HG-drift-2 on the queue.** **E99 s63 (2026-09-16): "the processing we had done on the lamp previously had removed the green, now it's back ... 30 px drift might still be too much, maybe 20 px" - the alive plate must carry the PROCESSED planes; 20 px tried beside 30; T14b.** **E99 s64 (2026-09-16): "20 px is the right drift but the lamp green still shows ... let me see 20px with parallax and 20px with ken burns" - 20 px stands; the far plane's fill under the lamp; two clips; T14c.** **E99 s65 (2026-09-16): "the parallax + drift can read as random motion, whereas ken burns+ drift gives us directionally controlled motion" - CLOSED: Ken Burns + 20 px on the alive flat plate is the long-form plate life.** |
| **R26-134** | **THE EVIDENCE DOOR (E98 s7)** (the operator, 2026-09-14, on the HG2 frames) - a card lands flat and fills the frame like a plate, then swings open on a hinge, and the world mounted underneath it is what it reveals: the card's tilt is a MOTION, not the pose the P58 T7 page jumped to at 35.72 s. Built as a transition INTO a scene (E47), `door[:<hinge>][:<s>]`, through the one projective path (`kinetics/homography.mjs` + the page plane), its math in `kinetics/transitions.mjs`, a golden `door-open`; first instance Japan's tariff bill opening onto the Treasury vault (`japan-tariff-trick/build_short.py:285` -> `:289`, the receipt page on its approved snap, the vault entering by door instead of dip) in a private build. Recall: docs_find 0 hits for "swing" / "evidence door" / "book flip"; doc 29 Part 6's 3D book-flip never built (`TRANSITIONS-REVIEW-2026-09-06.md:82`) | in build - brief scratchpad `p58/DOOR-brief.md`, dispatched after the whole-tree run | a transition + a golden + the Japan instance | BUILT 2026-09-14 - `door[:<hinge>][:<s>]` (`DOOR.S` 0.9 s, the edge-on end angle, five refusals by name), golden `door-open`, 95 pre-existing goldens unchanged, 241 pytest + 544 node pass; the Japan instance `japan-tariff-trick/build-p58-door/` (row 6 on its approved snap, row 7 entering by `door:right`) - motion 1 FAIL vs the approved copy's 3, the floor unchanged, the sound cues identical; the parent read the frames: the flat bill swings open on its right edge onto the vault and goes edge-on - AWAITING the operator's watch | WATCHED 2026-09-14 - the operator: *"the door effect works well. sounds like the empty-stage gate should be measured differently"* - the door stays a draft card until a cut authored with it is approved; the measurement follow-up is in build (the empty-stage measure counts a picture plate as a world on stage; the door's 2.2 s 'empty' reading at 45.42 s was the measurer, not the door) |
| **R26-135** | **CAPABILITIES to the record standard, so agents other than Claude can one-shot** (the operator, 2026-09-14: *"i thought we had built our index to be searchable/readable by the line for research, why isn't capability to the same standard? ... we need to improve this ASAP so that other agents can produce besides Claude"*) - measured: `CAPABILITIES.md` is 241 KB, 14 headings, 183 rows averaging 1,305 bytes (longest 9,153); the section index carries a record per row but a hit lands on a paragraph, and `docs_find.py`'s default budget surfaced no capabilities row for "suck" or "ledger page". (1) A capabilities layer: one record per capability (name, section, line, what, where, state, proof, cards, rulings, backlog), a compact one-line-per-capability page an agent reads at the start, `docs_find` searching it first - IN BUILD (a lane, 2026-09-14). (2) The one-shot runbook (the ordered loop: package -> script gates -> species by sentence -> recipes per act -> build -> probe / motion / floor gates -> self-watch -> read the frames, plus the rules the casebook shows agents break). (3) Routing: the video-engine entry, AGENT_START_HERE and every role definition name `docs_find.py`, the capabilities page, `effects_card.py` and `authoring/recipes.py` (today only one role definition names `docs_find`, none the recipes or the floor). (4) A calibration: a fresh non-Claude agent one-shots a known script and the floor gate + the thin-one-shot casebook case judge it | (1) in build; (2)-(4) open | a layer + a runbook + routing + a measured pass | (1)-(3) BUILT 2026-09-14: (1) `build_capabilities_index.py` -> `docs/CAPABILITIES-INDEX.jsonl` + `.md` (183 records, one per row; 22 malformed rows flagged and repaired), registered after `docs-index`, `docs_find.py` searches it FIRST, and `docs_find.py --capabilities [--state LIVE] [--section <text>]` prints one line per capability; (2) `docs/runbooks/ONE-SHOT.md` (the nine-step loop with verified commands, the rules agents break, the known gaps); (3) routing in `AGENTS.md`, `CLAUDE.md`, `docs/AGENT_START_HERE.md` and all eight roles on both sides (`.claude/agents/*.md`, `.codex/agents/*.toml`) - `GEMINI.md`, `docs/AGENTS-VIDEO-ENGINE.md` and `SKILL_ROUTER.md` wait (the other session has them modified); (4) the calibration run: open |
| **R26-136** | **The one-shot loop's missing tools and unrecorded rules** (found by an explorer's recall for `docs/runbooks/ONE-SHOT.md`, 2026-09-14; fact sheet in the session scratchpad `p58/ONE-SHOT-recall.md`) - (1) **no tool writes `<build>/BEAT-PLAN.jsonl`**, the file M41 FAILs without (`gate_one_shot_floor.py:445` reads it; `t0`, `comparator.compared_to`, a recipe or `why_none`), so step 4 of the loop is hand-authored; (2) **`authoring/recipes.py` has no command line** (a library: `load` :70, `preset` :135, `for_act` :148) - the only `--propose` is `lint_species_choice.py`'s, which drafts species rows, not recipes: a `--propose recipes` per act is owed; (3) **M13 (the cut in an acoustic gap) has no row in `docs/GATES-REGISTRY.md`** - it is enforced in `authoring/words.py` `cut_before`, defined in E41 #3 (`OPERATOR-RULINGS.md:1298`); (4) **four rules agents break live only in memories, with no ruling**: a fix never lowers motion, a served review build is never rebuilt, never pipe a gated step into tail, recall before propose - each needs the operator's recorded words written as a ruling; (5) **search misses** for natural terms - "beat plan", "recipes propose", "compile_timeline", "self_watch", "serve_player no-store", "judge the frame", "gate fit", "blind viewer", "retime take", "motion density gate" each return 0 hits (the capabilities layer, R26-135 (1), closes part of this; the rest want index aliases) | open | a beat-plan writer + a recipes CLI + a registry row + four rulings + aliases | REVIEWED 2026-09-14: three rules confirmed (a fix never lowers motion; never pipe a gated step into tail; recall before propose); the frozen review link is an agent practice, not a ruling (E99 s11) **RULED 2026-09-16 (E99 s66): (1) and (2) are NOT gaps - "a tool can't realistically write the beat plan or the script, those always have to come from intelligence"; the beat plan is authored by the agent doing the one-shot. (3) the registry row and (4) the four rulings and (5) the aliases stay open.** |
| **R26-137** | **The approved Japan tariff short is RE-RENDERED with the P61 mechanisms the operator approves** (E99 s34, the operator 2026-09-15: *"Japan will be re-rendered eventually, but it's not a concern right now."*) - the 09-09 render stands (E45); when the operator schedules it, the re-render carries the 24 fps render clock (E99 s36), `melt:gather` / `melt:weight` where its melts sit, and whichever field entries and endings the gates rule - and it must answer **M44** (a plate under 6 s), which today reads FAIL on the approved cut (`gate_motion_density` on `build-short`: 4 FAIL / 16 PASS since M44 landed; the cut untouched, E45), so the re-render is the first cut that has to clear it. Not a P61 acceptance; no P61 slice authors a P61 flag into its rows | scheduled by the operator - "eventually, not now" | a build + a render + the watch |
| **R26-138** | **The vortex map is written twice** (P61 T6, 2026-09-15) - `meltGatherAt` in `species/melt.mjs` carries `lpVortex`'s five terms because `sync_kinetics` refuses an import from a later region (R26-115's module-order rule); `test_melt_gather.py` pins the copy to `LP_RETRACT`'s numbers and `lpVortex`'s output so a drift fails. The fix: a shared `kinetics/vortex.mjs` with the spiral's region moved ahead of the melt's - a slice of its own, every golden byte-identical | after HG6 | one module + a region move + the sync |
| **R26-139** | **The gather turns the axis hairlines rigidly** (P61 T6) - the axes' `<line>` children are not sampled, so under `melt:gather` they turn as sticks while the series curl (doc 29 s9.31: 'redrawn point by point'); converting them to sampled polylines finishes the swirl | with R26-138 | small |
| **R26-140** | **A WARM player's pixels are not byte-stable** (found by P61 T2, 2026-09-15; pre-existing) - three captures of the shipped `data-to-bars` golden from one warm headless browser differed by 13 bytes; `test_golden_frames.py` holds because every surface renders in a fresh page, and a render flake on `page-depth@proof-leave` in one batch run (P61 T5) was identical on three isolated re-renders. Owed: the cause (GPU raster timing and a font's late swap are the candidates) and a `render_baseline.py --repeat` probe that reports drift by surface | open | a probe + a finding |
| **R26-141** | **`effects_catalog_check.py` does not validate the card schema** (P61 T4b, 2026-09-15) - T4b's cards passed the drift gate with a `does` over 240 chars and a `lives.note` over 200, and only `build_docs_layers.py --write` (six minutes later, through `build_effects_catalog.py`) refused them; a lane's validate line runs the drift gate, not the layers. Fix: the drift gate loads `configs/effect_card.schema.json` and refuses a card the schema refuses, naming the field and the cap | open | ~20 lines + a test on a broken copy |
| **R26-142** | **The agenda page's palette lives in the painter, not the template; the page form is proven at 16:9 only** (P61 T8) - the five classes `agplate` / `agmed` / `agtitle` / `agfig` / `agicon` are styled as presentation attributes because `scene-evidence-player.template.html` was outside T8's write set; the house convention puts `PAGE_INK` / `PAGE_EDGE` / `PAGE_GOLD` / `PAGE_CHALK` in the template's species CSS beside `.agnum` / `.agrow` - one line per dial, goldens byte-identical. And an `agenda-page-9x16` golden: the layout is a pure function of the box, so it should hold on a short, but nothing has measured it | after HG8 | small |
| **R26-143** | **A 6 px corner detail at the remake's two hand-over instants** (P61 T2) - the page's bar `<rect>` has rx 6 and the ring that hands over to it is a polyline, so at the hand-over the corner squares off for a frame; below the reading's threshold, recorded not fixed. Also untested: `compare` authored beside a `remake` on one page (neither refused nor proven) | with HG1's ruling | small |
| **R26-144** | **The bridge daemon's paths-written check looks under `sent/` after the packet has moved to `replied/`** (found 2026-09-15 on the Gemini lane's 'Money Physics outreach fact check': FINDINGS.md was written beside the packet, the packet moved to `replied/`, the form check looked for `sent/<id>/FINDINGS.md`, queued a repair that replied with the same path, and the loop re-queued - two replies 'waiting' that nothing can close). Fix: resolve a PATHS WRITTEN entry against the packet's CURRENT folder (sent / replied / done) before refusing, and a repair never re-asks for a path the daemon itself moved; the three `escalated.json` flags on the 09-14 timeouts (runs that landed through the gate) are stale and the inbox hook should not count them | open - P46 tooling | ~20 lines + a test on a moved packet |
| **R26-145** | **Two browser suites interfere in one pytest process** (2026-09-15): `test_chart_transitions.py` run in the same session as `test_effects_gallery.py` fails six browser tests with Playwright's 'Sync API inside the asyncio loop' and a NetworkError; alone it passes 79/79. One of the two leaves an event loop running (the gallery's capture or the served-player fixture). Owed: the fixture that leaks the loop, named and closed, so the two run together | open | a fixture fix + the pair run in CI order |
| **R26-146** | **The ball's inks BLEND: `body=blend`** (the operator, 2026-09-16, E99 s49: *"If we actually let the ink colors merge, then it's orange and green and gets merged to olive, so we could use a blend of the colors swirling with eachother, then the dark olive becomes the shading"*) - the melting page's series inks MIXED the way the soak mixes (Kubelka-Munk, `species/soak.mjs`'s mixing law - cite it), SWIRLING inside the ball on the living surface (`kinetics/drop.mjs`'s modes carry the swirl), the darkest mix as the shading in place of the pit; a golden at the settle + a clip beside the reference ball before it is asked; a page with one series blends with the board's ink | a candidate - the operator's eye on the clip | one melt.mjs option + a golden |
| **R26-147** | **The FULL-PAGE melt: `melt:all`** (the operator, 2026-09-16, E99 s49: *"we could melt everything and thats when either the slate or black metal reference would really make sense -- plus beneath the full melt would likely be a world plate or a cream, so then we can either do the throw-to-form plate"*) - E88's chart-only melt (the board stays) remains the default; `melt:all` melts the BOARD with its ink over what lies beneath (the next scene's world plate, or the cream ground), the ball taking the slate or reference body (no chart colour left to keep) and then the ending that fits - the throw-to-form onto the plate (the prop-becomes-the-chart path, `page_enter:morph`) or the splash. DESIGN ROW FIRST: what is beneath at each melt in the approved shorts, what the deckle does when the board itself melts, and whether the K-M soak runs in reverse; the operator's word before a slice | design first, then the operator's word | a design sheet, then one engine slice |
| **R26-148** | **A morph page with two DIFFERENT series colours is untested** (P61 T3, 2026-09-16) - the morph's fill is `MORPH.FILL_A` of the INCOMING series' stroke and the handed ball's paint is the OUTGOING chart's ink; on the goldens both pages are one file, so the hand-off is seamless; two pages with different first-series colours would carry a hue change as well as an alpha one across the hand-over. Owed: a golden pair with different inks and a rule (the ball's ink cross-fades to the incoming ink over M_FADE, or the fill takes the ball's ink) | before a short authors melt:morph across two coloured pages | one golden + one dial |
| **R26-149** | **`buildMorph` targets series 0 only, with no refusal** (P47 T3's law, named by P61 T3) - a page whose FIRST series is not the one the sentence is about morphs into the wrong area. Owed: a `series` word on `world.morph` (default 0) and a refusal by name when the named series has no area under a line | with the first multi-series morph page | small |
| **R26-150** | **`polyStrip` is x-monotone: a deep C-shaped silhouette loses its bay** (P61 T3) - the strip carries the bay's shadow column by column; blobs, balls, plates and heads are fine, a horseshoe is not, and nothing refuses one. Owed: a monotonicity check on the traced poly that refuses (or splits) a silhouette whose vertical extent doubles back, naming the x range | before a planted source that is not a blob | a check + a test |
| **R26-151** | **`build_golden_sources.py` drifts on two unrelated surfaces when run** (found by P61 T5b, 2026-09-16) - regenerating the sources rewrites `tiers-two` and `treemap-cross` though nothing about them changed (a non-deterministic builder input: a dict order, a timestamp or a random seed not pinned); the lane restored them by hand. Owed: the source of the drift named and pinned so the builder is idempotent; `test_golden_frames` gains a check that a second build of the sources is byte-identical | open | a probe + a pin |
| **R26-152** | **A `field=plates` morph page is reachable by the grammar but has no golden** (P61 T3b, 2026-09-16) - the morph page's ground now arrives by its field entry (E99 s52); the soak is the default and proven, the two-plate cross-fade under a morph runs off the same `b` (`lpPlateRecede` / `st.fieldPlate`) and should work, and nothing proves it. Owed: a `morph-planted-plates` golden + a browser read of the cross-fade under the morph | before a short authors a morph page with `;field=plates` | one golden |
| **R26-153** | **`MORPH.GROUND` is a SHARE of the morph's seconds, so a very short `morph=<s>` gets a very short soak** (P61 T3b) - floored at 0.05 s, no refusal names it; a 0.3 s morph would soak its board in 0.15 s, which is the snap E99 s52 refused by another door. Owed: a floor in seconds (the soak's own minimum) or a refusal by name when the ground's time falls under it | open | small |
| **R26-154** | **The melt's pitch length has no compiler twin** (P61 T6b, 2026-09-16) - `MELT.T_S` (the pick-up + flight, 0.77 s) lives only in `species/melt.mjs`; `build_scene_timeline_f.py` carries `MELT_S` / `MELT_W_S` / `MELT_G_S` but not `MELT_T_S` (nor `MELT_M_S` for the morph ending), and `test_transitions_e47` pins the pairs it has. Owed: the two twins beside the others and the pin, one line each | open | small |
| **R26-155** | **`melt:weight` before a splash rolls AWAY from the landing** (P61 T6b) - `meltRollDir` aims at the ink box's centre, so a weight-then-splash ball winds up backwards before the pitch; left deliberately (the longest forward chord reads as a wind-up) and named on the HG6-2 card; if the operator wants the roll toward the landing it is one sign in `meltRollDir` when a pitch follows | the operator's read of the clip | one line |
| **R26-156** | **The alive plate as a plate-life route** (E99 s55, the operator 2026-09-16) - the ambient lane's composite (the still with one life region generated by SAM 2 + Wan 2.1 VACE; LTX-Video 2B produced no motion in five runs and leaves the backend claim) enters the plate library as a CLIP plate a row can name, with the drift dial, Ken Burns and the parallax layers composing over it; CAPABILITIES:142-143 amended; a golden pair (the 30 px drift; the same plate alive) | P61 T14 | one engine dial + the plate-library entry + two goldens |
| **R26-157** | **The off-screen throw: the ball picked OFF the screen and thrown back in faster** (E99 s56, the operator 2026-09-16: *"pick the ball off of the screen so it's not in camera view, then throw it with increased velocity"*) - a splash variant: the pick-up carries the ball out of the frame, a beat of nothing, then it re-enters on a faster, flatter arc and splats; the chord and speed named, the shadow arriving with it | after T7c | one melt.mjs option + goldens |
| **R26-158** | **The docs layers were source, not build output - the dual bottleneck (P63, 2026-09-16).** The operator: *"anything we do makes it stale underneath, so everybody has to wait on BOTH the engine lock AND the doc layer to stabilize. we have created a dual bottleneck production system."* Measured: the whole `--check` 91 s on EVERY `git commit` (the user-level hook), 68 s of it the animation registry, 60 of 60 commits rewriting 15 MB of generated layers, 6 of 11 layers stale from one lane's uncommitted engine edit. **Closed by P63 T1-T4: `docs_layers.py` (inputs, digest, ensure), the readers ensure on read, the registry 64 s -> 2.4 s byte-identical, the 24 outputs gitignored and untracked, the hook's gate removed; `--check` 0.7-1.0 s.** | closed | P63 |
| **R26-159** | **The gates registry's 15 s** (`build_gates_registry.py --check`; `evals/LAYERS-TIMING-2026-09-16.md`) - off the commit path since P63, still the slowest layer on an incremental `--ensure` (105 s on one measured stamp). The same per-file precompute as R26-158's registry fix. | open | P63 T5 **CLOSED by P64 T2 (2026-09-16): a per-file token set built once, the corpus built once; --check 14.7 s -> 0.8 s, byte-identical (31ebf09).** |
| **R26-160** | **`build_animation_registry.py` is 932 lines against the 800-line cap its own docstring cites** (838 at HEAD before P63 T3) - `FileScan` + `word_tokens` are the natural split into a sibling module. | open | P63 T5 |
| **R26-161** | **The `LayerError` handling repeats in five readers** (docs_find, authoring.effects, the one-shot floor gate, the gallery, the provenance audit; P63 T2) - a `docs_layers.ensure_or_warn()` fold. And the player server's catalogue route pays 418 ms a GET for the digest (1 ms before): a per-server TTL if the editor's open feels it. | open | P63 T5 **The GET clause CLOSED by P64 T1 (2026-09-16): the route is status-only, 420 ms -> 2 ms. The LayerError fold stays open.** |
| **R26-162** | **`test_the_committed_tree_passes_check` runs the real ensure over the live checkout** and so sees another lane's half-done state (it met T14's `dials.note` on the plate-idle card) - the one place the old ping-pong survives, in a test. Either the test ensures into a scratch copy of the tree, or it is marked as the integration check it is and runs only in the record slices. | open | P63 T5 |
| **R26-163** | **M17's morph invariants against a PLANTED source** (P61 T3d, the real beat): three of the four invariants (centroid, axis, area) are unreachable by construction for any planted source - they were written for the fixture's own shape - and one is real: `min det -0.182`, a strip inversion of the ARAP mesh on a tall-thin source (the tie, ~2:1). Nothing visible at the probed frames. Gate the three off for `world.morph.poly` sources and fix the strip on tall-thin polys (`polyStrip` x-monotone, R26-153). | open | P61 T3d |
| **R26-164** | **A layered plate's idle drift walks each plane at its own depth - under a free idle the eye reads no direction** (E99 s65: "the parallax + drift can read as random motion"). The drift on a layered plate follows the camera's direction (the planes' walk projected onto the move) or is off; a bare drift stays the flat plate's. `paintPlanes` in the engine; `plate_idle_drift_px`. | open | P61 close |
| **R26-165** | **`world.ken_burns` has no window by grammar** - its clock is the scene's own 0 -> 1 (P61 T14c: B's zoom matched A's 1.0 -> 1.14 but not its timing). A `ken_burns(scale, deg, t0, t1)` window so a lean can start on a word, like the authored camera key. `build_scene_timeline_f.py`. | open | P61 close |
| **R26-166** | **`comfy_depth_split.py` is 944 lines against the 800 guideline** (P61 T14b's matte pass + T14c's hole fill): `--process` / `--fill-holes` and their measures split into a sibling `depth_split_clean.py`. Beside it: `split()` inpaints the whole disocclusion as ONE region and `lab_match`es it to ONE ring - the cause of the lamp's second green; a per-hole match (each hole to its own ring) at split time would make `--fill-holes` unnecessary. | open | P61 close |
| **R26-167** | **M11 ("a chart enters by 10 s") fails every windowed test-bed beat** (T3d, T14, T14b, T14c - the beat's content, never the composition). The gate takes a `--beat` / windowed mode that scores the window's own claim, or the test beds carry a chart. `gate_motion_density.py`. | open | P61 close |
| **R26-168** | **The one-shot floor's recipe row (M38, 0.60) is unreachable on a cut that obeys the newer gates** (one-shot #3, E99 s66, 2026-09-16: 0.03 with every beat planned). `card-becomes-the-chart` is proven on a 3-5 s plate and M44 asks six; `emphasized-bar-lit` on a light 7.55 s after a build and M11 wants a built page lit at once; the badge ladder's rails are illegible at 9:16. Re-prove the recipes on six-second plates and built pages, or give M38 a "new mechanism" reading. `gate_one_shot_floor.py`, `docs/EFFECTS-CATALOG.jsonl`. | open | one-shot #3 |
| **R26-169** | **The verdict wall's boxes are landscape; a portrait chart card is clipped to its header on the wall** (one-shot #3, `species/verdict.mjs`). A wall on a short needs a landscape rendition of each portrait card, or a portrait box. | open | one-shot #3 |
| **R26-170** | **A bars page with NEGATIVE values puts the category labels and the value tags at the same foot** (the weak-prints page, M28 12 pairs; `ymin` does not move them). The story builder needs the labels above the zero line when the bars point down (E28). `ledger_page.py`, the engine's bars painter. | open | one-shot #3 |
| **R26-171** | **A badge rail on any card at 9:16 sits at 13 stage px (M25)** - Tokyo's B3 generalises: no rails on the portrait stage; a card's page carries its own badges. The compiler could refuse `badges` on a 9:16 dock, or scale the rail to the stage. | open | one-shot #3 |
| **R26-172** | **`chart_to park` shrinks the page's pills under 11 px at any scale below ~0.85 (M25)** - parking is not a way to make room for a card on 9:16; the page's own empty room is (E65). A park could keep its pills at the stage size. | open | one-shot #3 |
| **R26-173** | **The stackbox is not scrub-safe across a seek that skips `clear_at` + 1.4 s**: the burst's cards stay as ghosts on later frames in the probe (`probe.py --sheet` at 65.5-70.5 s after a seek from 63.5); a continuous render is clean. `species/verdict.mjs` REMOVE_AFTER. | open | one-shot #3 |
| **R26-174** | **The host's two renderings share one cut** - the stick HollowStickMike on the stills, the coloured Mike on the two-fingers clip (Japan did the same). One host per cut, or a two-fingers clip on HollowStickMike. | open | one-shot #3 |
| **R26-175** | **The three one-shots' comparison (E99 s66)**: the Fable parent's is built and on the queue (`one-shot-3-fable-memory-calendar`); Opus's and Astra's follow on the same loop and are compared on M35-M42, the motion gate and the parent's read. | open | E99 s66 |
| — | **Object handling: re-parenting with cached offsets** (a hand takes a prop without a pop) — `sources/reference_analyses/complete_research_evidence_bundle/09_…grounding.md:349-361` | not in doc 48; rig-dependent, so it rides on **D3** (poses first keeps it out) | — → **E42: stays out** (poses first) |

## P45 triage — 2026-09-05 (the corpus reconciled with what is built)

The judgment pass over the generated registries: [TRIAGE-2026-09-05.md](TRIAGE-2026-09-05.md) —
54 decisions (5 TOP, 31 BACKLOG, 6 EXPLORE, 12 RETIRE), plan
[P45](../../.claude/PRPs/plans/P45-CORPUS-RECONCILIATION.plan.md). **Every row below carries its
triage id and nothing was added in the write-up**; the rulings cited as R1–R8 (operator) and D1–D8
(the parent's judgments on the architect's eight) are at `P45-CORPUS-RECONCILIATION.plan.md:195-207`.
Two rows are held for open operator decisions — see **pending D3 / D6** at the end.

### TOP — the next list (each becomes its own order, plan:67)

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| **P45 O1 → P47 T1 BUILT 2026-09-06** | **The cadence kinetics module** — frame cadence chosen by translation speed (on-1s / on-2s / on-3s), `briefs/ANSWERS-RESEARCH-BRIEF-animation-craft.md:185-193` \| The On-1s/On-2s/On-3s Decision Rule | **R2 + D7.** Orphaned in the registry (`ANIMATION-REGISTRY.md:582`) while two stepped clocks already ship hand-tuned with no rule behind them: `SOAK_STEP.FPS 8` (`ANIMATION-REGISTRY.md:84`) and `SP.LIFE_FPS 10` (`:231`), and the operator asked for more step motion this week (`BACKLOG.md:218`). **The source contradicts itself — 250 px/s at `briefs:187`, 100 px/s at `:545` — so test BOTH against those two clocks, report the crossover we actually see, and adopt neither number on the page** | one order |
| **P45 T4 + P45 T3** | **FK/IK boundary + zero-slip anchoring for the stick lane** — contact resolves IK, free gesture resolves FK, handover blended on the min-jerk quintic (`48-THE-FIGURE-AND-THE-GROUND.md:16,26` \| 48.1); a grounded sprite anchors `50% 100%` and binds to floor velocity (`48…:16` \| 48.7, G-j) | **R4**, the stick lane in flight (E39, `OPERATOR-RULINGS.md:1210-1230`). Tracked in the registry at `ANIMATION-REGISTRY.md:453-456` (FK/IK — the row reads *retired* only because `BACKLOG.md:402` is X10 **closed by doc 48**, a graduation, not a withdrawal) and `:440-446` (zero-slip, 8 records). **G-j and G-k check behaviour nothing builds** (`BACKLOG.md:304-305`) and can only ever INFO until something binds the ground | one order |
| **P45 X1** | **The motion-authoring order, restored into doc 29** — character/prop action → localized environmental action → information reveal → camera action, `15-LIVING-SCENE-COMMUNICATION-LANGUAGE.md:139-149` \| 5. Motion discipline and `16-EDITORIAL-MOTION-SYSTEM.md:48-60` \| 3. Motion ownership | **R1.** Present in both compressed docs and absent from 29: `rg "authored in this order" docs/` returns only those two files and the index copies (`DOCS-INDEX.jsonl:890,905`). Restore the prose **now**, gate only after one episode is measured against it; the "a camera move alone is not a visual event" clause rides with it as EXPLORE (D4 → P45 X2) | one order (lands with T4's lift) |
| **P45 M13** | **The M13 cut-gap gate in the edit pass** — a cut lands in an acoustic gap ≥ 0.30 s, at 0.8 of the gap, mid-word share ≤ 25 % (`47-FINDINGS-TO-CHECKS.md:202`; 46 §46.3) | **R8.** Not a gate: absent from `GATES-REGISTRY.md` (128 records / 116 ids across the five script-side checkers, `:19` — a scope the registry header now states, D8). What exists is the measurement `content/video_engine/scripts/measure_cut_gaps.py` and one per-project constant, `tokyo-tea-break/build_short.py:33` `CUT_AT = 0.8`. The thresholds were settled from the reference 2026-09-04 (E38, `OPERATOR-RULINGS.md:1196-1208`) and E41 §3 writes M13 into the caption/cut rule (`:1283-1284`) | one order |

### BACKLOG — real work, owned by a plan or a row, not now (29 of 31; two held below)

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| **P45 O6** | **C3 nested coordinate spaces** — `M_world = M_viewport × M_camera × M_ledger × M_chart`, `briefs/…animation-craft.md:431` | A6 already owns it (`BACKLOG.md:289`, expose the chart's coordinate mapper); 43 §43.2's anchor work is its general form | — |
| **P45 O8 → P47 T3 BUILT 2026-09-07** | **43.5 the morph, two methods** — cotangent-weight Laplacian; vertex (A) vs triangle ARAP (B), `43-SCENE-GRAPH-AND-TRANSFORM.md:91` | the same item as P45 T1 under a different name — the heading only reads orphaned because the name differs; dedupe into P38 T4 (`BACKLOG.md:247`) | — |
| **P45 O10** | **48.8 harmonisation in our tokens** — Koschmieder contrast drift to the cream, 4–12 px light wrap, washi grain κ≈0.12–0.16, `48…:163` | P42's content, and what gives G-i/G-j a path to green (`BACKLOG.md:235`, `:316`); orphaned at `ANIMATION-REGISTRY.md:530` | — |
| **P45 S3** | **Skinning and deformation** — BBW, DQS, the LBS volume collapse, path morphing, the zero-area proof (`…/09_…grounding.md:192,200,208` and three more) | the live items are P38 T4 (ARAP) and T7 (DQS), `BACKLOG.md:247`, `:249`; the other 19 records are their bibliography | — |
| **P45 S6** | **Paper and ink physics** — anisotropic Darcy wicking in washi (Chu & Tai 2005), elastic paper, Koschmieder harmonisation (`…/ACADEMIC_LITERATURE…md:146,272`) | 44.3 wicking is a JUDGE row bounded by E22 (`47-FINDINGS-TO-CHECKS.md:131-133`) and deprioritised (`BACKLOG.md:529-530`); K-M was ruled off the soak after six rounds (`:218`); harmonisation rides with P45 O10 | — |
| **P45 S7** | **Geometry and projection** — the 2.5D stack: transform pipeline, anchor point, Z-stacking, ground projection, homography, disocclusion, contact shadow (24 records) | P42 (grounding) and 43 §43.3 (the Z-stack) own the buildable parts, `BACKLOG.md:235`, `:321`; the ground-plane homography (`…/09_…grounding.md:415`) is the acceptance test G-i/G-j lack | — |
| **P45 T1 → P47 T3 BUILT 2026-09-07** | **ARAP / polar decomposition** — `J=RS`, `det(J(t))>0` at every t, `47-FINDINGS-TO-CHECKS.md:55`; 40 records at `ANIMATION-REGISTRY.md:373-412` | P38 T4 owns it; no object page in the short (`BACKLOG.md:222`), and its flag is declared and unread (P45 F1) | — |
| **P45 T2** | **Euler spiral / clothoid** — `dκ/ds = const`, minimise `E_MVS=∫(dκ/ds)²ds`, `42-DRAWING-KINETICS.md:112` \| 42.4 | D1 says build, do not gate; the quality threshold is X7 and has no basis yet (`BACKLOG.md:401`) | — |
| **P45 T6** | **48.5 reach and grasp / prop attachment** — `v_max=1.875·v̄` at τ=0.50, grip aperture peaks at τ≈0.68; attach by cached offset matrix | P38 T8 (`BACKLOG.md:250`); its flag `prop_attach` is declared and unread (P45 F3) | — |
| **P45 T8** | **48.7 grounding, the eye-line half** — a composited figure's eyes sit on the plate's horizon; the shadow softens with distance (G-i) | P42; the zero-slip half is TOP (P45 T3), the harmonisation half is P45 O10; the gate has no owner today (`BACKLOG.md:303`) | — |
| **P45 T9** | **Multi-plane geometry / LDIs / inpainting** — `H=K₂(R+tnᵀ/d)K₁⁻¹`, SAM 2 → LaMa → Z-stack cards, `45-PARALLAX-AND-PLATE-MOTION.md` \| 45.5 | **R5**: only if it buys capability beyond parallax — "our other local generation stuff kind of failed on us, except for parallax, which isn't much better than Ken Burns"; B6's ban currently leaves those plates still (`BACKLOG.md:356-360`) | — |
| **P45 T10** | **The Deegan dark rim (coffee-ring)** — a 1–2 px darker edge density at the ink boundary, `44-INK-AND-SURFACE.md:27` \| 44.2 | **D2**: this week's ink verdict was "actually paint it with ink" (Backlog 9, `BACKLOG.md:219`), not another filter term; the coffee-ring edge was deprioritised 2026-09-04 (`:529-530`) and R1 is a retrieval finding, not a production stumble | — |
| **P45 T11** | **2.6 pseudo-3D head turns** — cylindrical projection of feature points, sprite swap over 2–3 frames at peak angular velocity | deferred with a trigger — the first shot that turns a head (`BACKLOG.md:518`, D8 row) | — |
| **P45 T12** | **3.7 two-handed closed kinematic chains** — both wrists lock to a prop's sockets, the prop is the master | the registry's *retired* is a keyword misread (the retire rule fired on the word **closed** in the section title); `BACKLOG.md:519` says deferred with a trigger — the first two-handed prop | — |
| **P45 T14** | **43.4 dirty flags** — mark a subtree dirty instead of recomputing `M_world` every frame | build, low priority; triggered when node counts grow (`BACKLOG.md:517`) | — |
| **P45 C1** | **The opening gate's JUDGE rows** — terminal stress (J03), the context-dump ban (J04), the phonetic anchor (J08) | already owned by name and already emitted; the verdict kind is correct — no gap (`CHECK-RESPONSIBILITIES.md:126-128`) | — |
| **P45 C2** | **The classical nodes outside the gate** — the grand payoff, the chiastic centre, recontextualization, the final triad, best evidence late, the pattern unit | §3c owns all six by name (`CHECK-RESPONSIBILITIES.md:130-145`); the craft map now carries them with `gates: judge`, which is the enumeration they lacked | — |
| **P45 C3** | **The roster rows the audit does not count** — the callback ledger, the duty roster (`CHECK-RESPONSIBILITIES.md:147-150`) | owned by name; the audit counts what it can and §3d says the rest is the agent's | — |
| **P45 C4** | **Antecedent integrity** — every sentence opening on a connective or bare pronoun resolves to its predecessor (`patterns/STRENGTH-LOOP.md:84`) | `enumerate_strength_screens.py:19` already enumerates the candidates; only the verdict is human (X1, `CHECK-RESPONSIBILITIES.md:155-156`) | — |
| **P45 C5** | **The L0 reader's five** — cashed concreteness (S4), the ear test (S6), the delete test (S8), the never-list (S10) | owned by name (`CHECK-RESPONSIBILITIES.md:158-161`); the log is the deliverable | — |
| **P45 C6** | **The thesis lens** — the script argues one of the standing theses (`patterns/STRENGTH-LOOP.md:96`) | owned, T1–T12 named in §3h (`CHECK-RESPONSIBILITIES.md:180`) | — |
| **P45 C7** | **The fixpoint loop** — a round that fires no gate but made an edit runs again (`patterns/STRENGTH-LOOP.md:108`) | owned by the runtime sequence, §4 (`CHECK-RESPONSIBILITIES.md:196`) | — |
| **P45 C8** | **Fourteen judge-only devices with no §3 owner row** — the answer format, peak-end, the dopamine ladder, the assignment, the never-repeat rule, the camera test, the cadence wave, the aside voice, the named-subject rule, the referent test, instrument continuity, mechanisms not prescriptions, the rewrite budget, the enumeration mandate (`CRAFT-MAP.md:35,48,49,53,57,63,66,67,97,103,108,110,111,114`) | by CHECK-RESPONSIBILITIES R4 an unreached device must be named; **D1**: one additive block in §3, not fourteen items — the craft map is generated and a second roster would drift | — |
| **P45 F1 → P47 T3 CLOSED 2026-09-07 (the `morph` page enter reads it)** | **`arap_morph` is a declared flag no site reads** — `samples/scene-evidence-player.template.html:446` | folds into P38 T4 (P45 T1); the flag is a promise the code does not keep (R3, `BACKLOG.md:415`) | — |
| **P45 F2** | **`dqs_skinning` is a declared flag no site reads** — `…template.html:447` | folds into P38 T7 (P45 T5, held for D3) | — |
| **P45 F3** | **`prop_attach` is a declared flag no site reads** — `…template.html:448` | folds into P38 T8 (P45 T6) | — |
| **P45 F4** | **The flag-roster test refuses a declared flag nothing reads** — `content/video_engine/tests/test_kinetics_flags.py` | cheap and mechanical, but it gates nothing we shipped wrong — the three flags above are already known (asked for by R3, `BACKLOG.md:415`) | — |
| **P45 W1** | **Four shipped gate scripts sit outside the registry scan** — `gate_comfy_config.py`, `gate_grounding.py`, `gate_vertical_safe_box.py`, `judge_muted_caption.py` | **D8**: the scope sentence landed in the registry header with this pass (`docs/GATES-REGISTRY.md`, generated from `build_gates_registry.py`); **extending the scan** waits for the next comfy/grounding gate change — a wrong scope claim is how M13 could have been read as present | — |
| **P45 W2** | **The research bundle is counted twice** — three scanned files exist byte-identically under `complete_research_evidence_bundle/` (`ANIMATION-REGISTRY.md:606-612`) | dedupe by content hash in `build_animation_registry.py` so the orphan count means what it says (~half of the 128 research-side records are duplicate paths) | — |

### EXPLORE — the first step is a measurement or a read, not a build (6)

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| **P45 S2** | **Figure kinematics / biomechanics** — 2-bone analytical trig IK, the FK-vs-IK boundary, ankle/hip strategies, COM/BOS, respiration (`…/09_…grounding.md:149,182,120,92,252`, 19 records) | the buildable half is already TOP (P45 T3/T4); these are its source derivations — the law of cosines is the whole solver for a two-bone arm, so read them when the rig exists, do not plan them separately | — |
| **P45 S5** | **Secondary motion and placement dynamics** — appendages as driven damped oscillators, `ÿ+2ζω₀ẏ+ω₀²y = −ẍ_base`, phase lag φ (`…/09_…grounding.md:277` \| 2.8, `:383` \| 3.6) | **this is the buried motion-cohesion research plan T8 was sent to find**: `:283-285` names follow-through, overlapping action and drag, and doc 48 §48.6 lifted only the phase lag out of it | — |
| **P45 T7** | **48.6 mass before the object moves** — `φ = arctan(2ζ(ω_d/ω₀)/(1−(ω_d/ω₀)²))` on the `M_p` we already adopted | this is what replaced the 0.22 (`BACKLOG.md:516`); plan T8 measures whether the phase lag alone accounts for what we see | — |
| **P45 T13** | **The 0.22 secondary-motion budget** — `E_secondary = ∫ v_sec² dt ≤ 0.22·E_primary`, lag 2–4 frames, settle ≤ 1.5× primary, `briefs/…animation-craft.md:302-307` | **R3**: measure per motion piece, per scene and per screen (plan T8) and print 0.22 beside each measured ratio; it is a **starting reference** carrying `[DERIVED]` (R6), never a cited finding — already called an invented number at `47-FINDINGS-TO-CHECKS.md:146` | — |
| **P45 X2** | **"A camera move alone is not a visual event"** — the sub-clause of the authoring order, `15-…:148-149`, `16-…:57` | **D4**: restoring the prose is free (it rides with P45 X1); flipping `SPECIES_EVENTS` — `gate_motion_density.py:108-111` credits `punch` / `push` / `focus_zoom` — re-verdicts every build we have judged, so measure one episode both ways first | — |
| **P45 X3** | **The 2–6 s plate-hold ceiling** — `16-EDITORIAL-MOTION-SYSTEM.md:42` \| 2. Timing authority | **D5**: the number survives *inside the first minute*, where M10 already applies it (`GATES-REGISTRY.md:121`), and nowhere else — the shipped gate holds `PLATE_HOLD_MAX_S = 20.0` (`gate_motion_density.py:64`); whether 6 s generalises to the whole runtime is a measurement, not a merge | — |

### RETIRE — struck from the triage list only; no doc line is deleted (R7) (12)

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| ~~**P45 O2**~~ | ~~**Dynamic nib pooling** — `w(s)=w₀(v/v_mean)^-0.25` plus a 75° corner-dwell rule, `briefs/…animation-craft.md:295-298`~~ | width/ink coupling ships with our own dials (`kinetics/stroke.mjs:16,39`, `LAMBDA_W 0.12`, `ALPHA_W 1`); the −0.25 exponent is a competing derived number | — |
| ~~**P45 O3**~~ | ~~**The five overlapping synergies** — a synthesis blurb, `briefs/…animation-craft.md:120`~~ | prose, not a formula; the registry matched a `$`-bearing line inside it — there is nothing to build | — |
| ~~**P45 O4**~~ | ~~**Analytic mass-spring-damper** — `mẍ+cẋ+kx=0`, ω₀, ζ, the underdamped step response, `briefs/…animation-craft.md:222-224`~~ | shipped: `kinetics/spring.mjs:6-8`, all three regimes (P43 T5, `47-FINDINGS-TO-CHECKS.md:199`) | — |
| ~~**P45 O5**~~ | ~~**B5 rhythm as a distribution** — shot lengths as a lognormal, `briefs/…animation-craft.md:396`~~ | the tail item it implies is deprioritised by ruling (`BACKLOG.md:526-528`, `47-FINDINGS-TO-CHECKS.md:150`) | — |
| ~~**P45 O7**~~ | ~~**C6 what Rive / Lottie / Flash got right** — `State(f) = SceneGraph(t = f/fps)`, `briefs/…animation-craft.md:456`~~ | this is what P39 proved and locked: determinism + golden frames (`BACKLOG.md:229`) | — |
| ~~**P45 S1**~~ | ~~**Stroke kinematics** — arc-length reparameterisation, the κ₀ regulariser, the physiological mandates (20 records)~~ | implemented verbatim in `kinetics/stroke.mjs:5-7,16,38-39` (P43 T2) — a **citation** orphan, not a capability gap: the module cites our numbered doc, never the research file | — |
| ~~**P45 S4**~~ | ~~**The damping regimes** — the closed-form second-order step response, underdamped / critical / overdamped (14 records)~~ | shipped as `kinetics/spring.mjs:6-8` with the seek test (P43 T5); keeping it open would re-open a closed item | — |
| ~~**P45 S8**~~ | ~~**LTX mechanics under the hood** — the 3D VAE compression, `num_frames = 8n+1`, latent mask pinning~~ | the frame law is a shipped gate: `gate_comfy_config.py:81-82` (G-m, `47-FINDINGS-TO-CHECKS.md:191`) | — |
| ~~**P45 S9**~~ | ~~**Word-gap boundary snapping at Δt ≥ 0.45 s** — `…/DRAWING_ENGINE…RESEARCH.md:400-403`~~ | superseded by our own measurement on the reference — 0.30 s, cut at 0.8 of the gap (46 §46.3, X3 closed at `BACKLOG.md:392`); keeping 0.45 would reinstate the error E38 corrected. Keep the method, drop the constant | — |
| ~~**P45 T15**~~ | ~~**The doc-47 section headings** ("1. DESIGNED OUT", "4. DEMOTED") captured as laws — `ANIMATION-REGISTRY.md:351`, `:361`~~ | registry artifact: the captured text is the squash row (shipped, P43 T4) and the A1-timing-chart row (superseded by `M_p`, `47:147`) — both already decided | — |
| ~~**P45 X4**~~ | ~~**No facts, dates, labels or quotations inside generated pixels** — `15-…:135-137` \| Fact surfaces~~ | a DUPLICATE in T4's terms: already live as E33 (`OPERATOR-RULINGS.md:1102`), partly in `29-EVIDENCE-MOTION-STANDARDS.md:1070`, and enforced for parallax by G-b — strike in doc 15 with a pointer, never delete | — |
| ~~**P45 W3**~~ | ~~**89 dial records carry no status** — `ANIMATION-REGISTRY.md:6`, `:138-243`~~ | correct as designed — a dial is a value, not a claim; noted only so the header's 191/96/7/145 is not read as covering dials | — |

### D3 / D6 — ruled 2026-09-06 (E42): poses first; tag all thirteen

| id | item — what and where | why / evidence | cost |
|---|---|---|---|
| **P45 O9** | **48.2 balance — the gesturing figure's hips** — `XCOM = r_COM + v_COM/ω₀`, `ω₀=√(g/l)` (Hof 2005): static COM is not the balance condition, `48-THE-FIGURE-AND-THE-GROUND.md:39` | **D3**: R4 named FK/IK and zero-slip, not balance. A stick figure that *gestures* needs hips. Held: does the lane animate a rig, or swap approved poses? (parent's recommendation: poses first per E40 — that keeps this in BACKLOG) | — → **E42: BACKLOG** (poses first; rig only what the first shot cannot fake) |
| **P45 T5** | **48.3 the rig / DQS / LBS collapse** — joints blend on SE(2) geodesics; LBS returns the zero matrix at 180°, w=0.5 (`ANIMATION-REGISTRY.md:366-367`; P38 T7, `BACKLOG.md:249`) | **D3**, same decision: a stick figure with bending elbows needs DQS; a pose swap does not. TOP only if the lane rigs joints | — → **E42: BACKLOG** (same ruling; P38 T7 keeps it) |
| — | **D6 — where `[DERIVED]` stops** — the three live FAIL thresholds computed from our own material: G-c `intensity ≤ 0.18` (`45-PARALLAX-AND-PLATE-MOTION.md:53`), G-o's 12 px legibility floor and its ≥ 59 px consequence (`50-THE-PHONE-IS-THE-SCREEN.md:36,41-48`) — TRIAGE §3, no §1 row | the tagging is plan T5's write set, not a backlog row; **nothing is tagged in those sections until D6 lands** (parent's recommendation: tag all thirteen — a threshold computed from our own template is exactly the class E38 warned about) | — → **E42: tag all thirteen** — P45 T5 does the tagging |

## Routed from the demoted list (47 §4)

Demoted from *doctrine* — not dropped. Each lands as a build item, an exploration, or a closure.

| # | item | where it goes |
|---|---|---|
| D1 | **Euler-spiral generator** for procedural curves (arrows, balance arms, connectors, axes) | **Build, do not gate.** Quality threshold is X7. → P45 T2 |
| D2 | **Per-material `ζ` / `ω₀` values** — paper, metal, fabric, liquid, ink | **Exploration X10.** T2 gives the model; these are the numbers that make it usable, and they are ours to derive. |
| ~~D3~~ | ~~Secondary-motion ratio~~ | **RESOLVED 2026-09-04** by 48 §48.6 — the analytic phase lag `φ = arctan(2ζ(ω_d/ω₀)/(1−(ω_d/ω₀)²))` on the `M_p` we already adopted. No invented ratio needed. |
| D4 | **Dirty-flag propagation** in the scene graph | **Build, low priority.** Triggered when node counts grow past hand-tuning — props and rigs will do it. → P45 T14 |
| D8 | **Pseudo-3D head turns** — cylindrical projection, the 35–45° three-quarters gate, sprite swap over 2–3 frames at peak angular velocity (09 §2.6) | **Deferred with a trigger:** the first shot that turns a head. → P45 T11 |
| D9 | **Two-handed closed kinematic chains** — the prop is the master, both wrists lock to its sockets (09 §3.7) | **Deferred with a trigger:** the first two-handed prop. → P45 T12 |
| D5 | **BBW / 2D dual-quaternion skinning** | **Trigger corrected 2026-09-04** (48 §48.3): not a bending prop — **rigging a figure**. Elbows and knees need it immediately, because linear blend skinning returns the zero matrix at 180°. Now a Tier-1 designed-out item in 47 §1. |
| D6 | **A1 timing chart** | **Closed.** Superseded by `M_p = exp(−πζ/√(1−ζ²))` — the model replaces the table. |
| D7 | **Grow the shot-length tail** | Deprioritised by ruling — see below. |

## Deprioritised by ruling

- **Grow the shot-length tail.** Our median and CPM match the reference; only the spread
  is tighter (IQR 3.4 s vs 6.9 s). Operator 2026-09-04: a 26 s hold is a luxury for an
  audience that already stayed. Recorded in 46 §46.1, not actioned.
- **Coffee-ring edge and anisotropic wicking** — JUDGE rows bounded by E22's refusal list.
  Below the motion work, and worth nothing on a page that still draws like a plotter.

## Withdrawn

- **G-f "the actor must be a rig, not a generation."** Withdrawn by the operator
  2026-09-04. It reasoned from *Wealth Logic* compositing its host to a rule that
  generation fails — which the operator's own shipped work contradicts — and it was the
  wrong *kind* of rule for the animation layer: asset provenance is a channel and style
  decision, not animation handling. The cutout rig remains a technique in 43 §43.6. What
  it exposed is X10.
- **N1 "M10 measures the wrong thing."** Wrong. `gate_motion_density.py:277` measures gaps
  between *visual events*, not shot boundaries, and shot length has its own separate
  ceiling (`PLATE_HOLD_MAX_S = 20.0`, two-dock escape). I inferred a defect from a FAIL
  message without reading the implementation — the error I spent the day cataloguing in
  others. See 47 §0.

## Carried debt

- **`f10b` plate rehome** — 55 indexed plates still resolve into the codex worktree. P62's review (2026-09-16): `build_plate_library.py:9` hard-codes that worktree's path (`CODEX`) and its pilot block at `:545-:549` reads it, so a rewrite of the 55 paths is undone by the next rebuild - the builder is what moves; and two wave-06 manifests differ between the checkouts (`wave-06-depth-layer-manifest.v1.json`, `wave-06-review-manifest.v1.json`), so a rehome must carry f10b's copies beside main's and prove the rebuilt library differs only in path prefixes (E10: approval state comes from the manifest).
- **Steel and Paper's timeline uses absolute worktree paths.** (188 `f10b` references across 71 files repo-wide, most of them tracked proof manifests - history cites where a thing came from; not rewritten by P62.)
- **Flow driver has no README**; a fresh checkout needs `npm install` in `tools/google-flow-driver`.
- **Drive connector needs reconnecting.**
- **Tokyo `plate-01-*`** — all three rolls predate the character-binding fix.
- **The Steel and Paper re-script** (`REWRITE-ORDER-G`) — now with G-g available to it,
  which is the single finding that most changes a script.
