# WORKTREE REGISTER - every checkout on this machine, who is writing what in it, right now

E99 s47 (2026-09-15): parallel work is the requirement, and the cost is paid with AWARENESS - every agent reads this
file at start and updates its row at every commit. The operator, 2026-09-16, closing P62's first draft: *"isn't a
better way to just check git before pushing to main and always preferring non-destructive methods?"* - so this page
is a hand table plus a checklist, and `git` carries every other fact. It exists because two lanes appended rulings
blind on 2026-09-16 and collided on E99 s49 (the audio lane renumbered to s54), and because worktrees existed that no
board named. `git worktree list` is the census; `content/video_engine/tests/test_worktree_register.py` fails when a
worktree has no row here or a ruling number appears twice - it runs in the checklist below, before main.

Rules while a lane is live:
- A shared file (`OPERATOR-RULINGS.md`, `CAPABILITIES.md`, `BACKLOG.md`, the queue) is committed by ONE lane at a
  time, and the commit message names any text another lane wrote that rides along.
- Ruling numbers: claim the NEXT free `E99 s<n>` here BEFORE writing it; a script that appends a ruling reads the
  last number on disk at run time, never from memory.
- The engine lock (`scene-evidence-engine.mjs`, `kinetics/**`, `species/**`, `build_scene_timeline_f.py`, the player
  template) has one holder PER WORKTREE; name the slice.
- A second agent works in its own worktree off main - `git worktree add <path> -b <lane>/<slug> main` - a worktree,
  never a clone: it shares the objects, the LFS store and the commit hooks (which resolve from the main checkout, so
  a hook change merges to main first). Its row lands here before its first commit; sprawl is accepted while the row
  names it (s47).
- The docs layers are BUILD OUTPUT (P63 / P64): gitignored, rebuilt by the write, never committed and never claimed;
  each worktree carries its own `docs/.layers/`.

## Before anything reaches main - the checklist (only git, nothing destructive)

1. In the lane's worktree: `git fetch`, then `git merge main` - a merge, never a rebase of a shared branch.
2. Run, unpiped, from that worktree: `python -m pytest content/video_engine/tests/test_worktree_register.py content/video_engine/tests/test_golden_frames.py -q` (a duplicate ruling number or an unregistered worktree fails here, before main).
3. A conflict on `OPERATOR-RULINGS.md` or `review-answers.jsonl`: keep both sides. A duplicate ruling number: the INCOMING lane takes the next free number and fixes its citations in the same commit (`rg "E99 s<n>"`).
4. If the engine moved on either side: the goldens re-pinned ONCE, in one commit, the sha table in the message.
5. From main: `git merge --ff-only <branch>`; then update the lane's row in `docs/WORKTREE-REGISTER.md` (last merge).
6. Never `--force`, never amend after a push, never delete a branch or a worktree without the operator's word, never `git add -A`; deletions in an index-only commit. Push only on the operator's fresh word in chat.

### Shared video registries: proof, then non-destructive push check

- When a capability becomes built, changes state, or retires, update its row in
  `docs/content-video-engine/CAPABILITIES.md` in the same integration commit and
  cite code plus a test or review receipt. A passing diagnostic is not finished
  art or operator approval. When a reusable effect is proven, update its source
  card under `content/video_engine/effects/cards/`; when a combination is proven,
  update its recipe under `content/video_engine/effects/recipes/` with the played
  instant. Do not promote a card/recipe solely from a test or agent summary.
  Regenerate the local indexes with `build_capabilities_index.py --write` and
  `build_effects_catalog.py --write`, then check both; generated docs layers
  remain gitignored build output, not cross-lane edits.
- Before an authorized push containing any of those three shared paths, run
  `git fetch`, then
  `python content/video_engine/scripts/check_shared_video_registries.py --base origin/main --head HEAD`.
  It reads content diffs and divergent shared
  commits across **every** worktree, without restoring, copying, rebasing, or
  cleaning another lane's files. A line-ending-only `status` flag is not a
  content edit. An in-progress shared edit or overlapping unmerged commit holds
  the push; coordinate with its owner.
- Send Claude the emitted candidate fingerprint, candidate commit and Claude
  branch heads through the bridge as exact `REGISTRY-FINGERPRINT:`,
  `REGISTRY-CANDIDATE:` and one `REGISTRY-CLAUDE-HEAD:` line per Claude branch.
  Its review reply must include `POSITION: done`, `REGISTRY-ACK: <fingerprint>`
  and repeat the candidate/head lines. Re-run the check with
  `--ack-file <bridge-reply.md>` immediately before push. Both generated-registry checks
  must pass. If the candidate, base, or a Claude branch head moved, the old
  fingerprint is invalid: obtain a fresh review. Never use this check as push
  authorization; the operator's current word remains required.

## The rows

| path | branch | lane / owner | purpose | live slice | write set (beyond the slice's own) | engine lock | rulings claimed | last merge to main |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `C:/Users/Snipe/.codex/worktrees/backlog-hygiene/Outreach Program` | `codex/backlog-hygiene` | Codex / implementation_luna; parent review and integration | First-pass video-engine backlog hygiene using Registry board discipline as read-only style reference; mechanically split historical ledger | Canonical active queue and evidence-backed archive complete; 72 close-marked legacy rows remain a separate revalidation queue; September 2026 history split and line-map verified | `docs/content-video-engine/BACKLOG.md`; `docs/content-video-engine/BACKLOG-HISTORY-2026-09.md`; `docs/content-video-engine/backlog/archive/2026-09-completed.md`; `docs/content-video-engine/BACKLOG-DISCIPLINE.md`; this register row; no engine or episode writes | none | none | `5d8c945` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/worktrees/rig-deformation-stress/Outreach Program` | `codex/rig-deformation-stress` | Codex / implementation_luna; parent review and integration | SHARED-MODEL-ENGINES T5a.4 generic-rig deformation stress comparison | Complete diagnostic; local distortion tails measured on saved mild poses, no fighter art or T5b claim | New `content/video_engine/src/modeling/blender/deformation_stress.py`, `content/video_engine/scripts/model_deformation_stress.py`, `content/video_engine/tests/test_model_deformation_stress.py`; ignored `content/video_engine/review/model-engines/benchmark-v1/3d/deformation-stress/`; this register row; existing source blend/authoring/player read-only | none (no engine lock) | none | `177cc7f` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/worktrees/model-deep-flexion/Outreach Program` | `codex/model-deep-flexion` | Codex / implementation_luna; parent review and integration | SHARED-MODEL-ENGINES T5a.5: deep elbow/knee flexion and matched LBS/DQS skinning comparison | Local main integration complete as negative review-only diagnostic; no fighter-art or T5b approval | New `content/video_engine/src/modeling/blender/deep_flexion.py`, `content/video_engine/scripts/model_deep_flexion.py`, `content/video_engine/tests/test_model_deep_flexion.py`; ignored `content/video_engine/review/model-engines/benchmark-v1/3d/deep-flexion/`; source blend, prior probe and player read-only | none (no engine lock) | `ce7fc6a`, `d325e1f` | merged to local main at `d325e1f`; not pushed |
| `C:/Users/Snipe/.codex/worktrees/model-exchange-layers/Outreach Program` | `codex/model-exchange-layers` | Codex / implementation_luna; parent review and integration | SHARED-MODEL-ENGINES T7a.5: opt-in equal-depth source-grid projection group for fighter visible-contribution layers | Parent and independent review complete: 29-frame grouped projection bridge verified; fixed source-view only, no hidden-surface reconstruction, independent parallax, fighter-art/T7b/HG2 approval | `content/video_engine/src/modeling/layered.py`, `content/video_engine/src/modeling/exchange_layers.py`, `content/video_engine/scripts/model_exchange_layers.py`, `content/video_engine/tests/test_model_layered.py`, `content/video_engine/tests/test_model_exchange_layers.py`; ignored `content/video_engine/review/model-engines/benchmark-v1/2_5d/fighter-planes-t7a4/`; this register row; pinned source scene/receipts and player read-only | none (no engine lock) | `8296c44`, `b854c25`, `2cf9f57`, `7a407d3` | merged to local main at `e352307`; not pushed |
| `C:/Users/Snipe/.codex/worktrees/model-garment-stress/Outreach Program` | `codex/model-garment-stress` | Codex / implementation_luna; parent review and integration | Bounded offline diagnosis of the blue shorts shard on the saved v1.1 Rigify family | T5a.8 source-to-screen map localizes the long fin to a Solidify rim/transition near source face 40; two in-memory vertex caps remove the long fin visually but each flips the same two local triangles, so no production correction is safe; source blend unchanged; no fighter-art or T5b claim | Ignored `content/video_engine/review/model-engines/benchmark-v1/3d/garment-stress/` evidence only; source blend and existing modeling code read-only; no production edits | none (no engine lock) | none | no commit; parent review/integration only |
| `C:/Users/Snipe/.codex/worktrees/model-head-likeness/Outreach Program` | `codex/model-head-likeness` | Codex / implementation_luna; parent visual review and integration | SHARED-MODEL-ENGINES T4b: reference-fitted editable Sharaf head and bounded image-wrap proof | Front projected identity reads at phone size; clay geometry and turned projection fail finished art. Review packet hash-copied to main; no fighter asset or HG2 claim | Ignored `content/video_engine/review/model-engines/benchmark-v1/art/fighters/sharaf-sculpt-proof/` and this row only; existing native assets, prior proofs, shared engine/player read-only | none (no engine lock) | none | review-only; no production merge or push |
| `C:/Users/Snipe/.codex/worktrees/model-sprite-pivot/Outreach Program` | `codex/model-sprite-pivot` | Codex / implementation_luna; parent visual review and integration | SHARED-MODEL-ENGINES T7a.6: opt-in rigid 2.5D head-sprite pivot on the shared contact clock | Complete review-only mechanics proof; parent saw recognizable fixed-view head pivot with visible neck seam, not fighter art/episode/T7b/HG2; focused 46 passed, 1 skipped; register/goldens 163 passed; main model/layer/motion/contract suite 106 passed, 2 skipped | `content/video_engine/src/modeling/layered.py`; new `content/video_engine/tests/test_model_sprite_pivot.py`; ignored `content/video_engine/review/model-engines/benchmark-v1/2_5d/split-head/layered-proof/`; existing art, player and Blender source read-only | none (no engine lock) | `1af5bac` | merged to local main at `1af5bac`; no push |
| `C:/Users/Snipe/.codex/worktrees/model-cutout-masks/Outreach Program` | `codex/model-cutout-masks` | Codex parent implementation and visual review | SHARED-MODEL-ENGINES T7a.7: opt-in per-layer alpha polygon masks on one hash-pinned character raster, for editable 2.5D cutouts without regenerating identity pixels | In progress; review-only Sharaf white-shorts concept; no fighter art, source-timed action, T7b or HG2 claim | `content/video_engine/src/modeling/layered.py`; new `content/video_engine/tests/test_model_cutout_masks.py`; ignored `content/video_engine/review/model-engines/benchmark-v1/2_5d/fullbody-cutout/`; PRP/capability row after verification; player, native fighter source and official footage read-only | none (no engine lock) | none | pending; no push |
| `C:/Users/Snipe/.codex/wt/rc` | `codex/model-scene-compiler` | Codex / implementation_luna, execution_sol escalation; parent review and integration | SHARED-MODEL-ENGINES T6a: review-only shared-scene compiler and render-pass proof | Complete diagnostic; saved/reopened prop/environment scene and three beauty/depth/ID-mask frames; no fighter art/T5b/T6/HG2/HG3 claim | New `content/video_engine/src/modeling/blender/scene.py`, `render.py`; `content/video_engine/tests/fixtures/modeling/blender/generalization-scene.model_scene.v1.json`; `content/video_engine/tests/test_model_blender.py`; ignored `B/3d/generalization-compiler/`; existing sources/contracts/player read-only | none (no engine lock) | none | `fc90f41` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/wt/lb` | `codex/model-layer-bridge` | Codex / implementation_luna, execution_sol escalation; parent contract and integration | SHARED-MODEL-ENGINES T7a.2: saved-Blender-to-2.5D raster bridge diagnostic | Complete diagnostic; three fixed-view Blender poses, native geometry masks and metric depth feed the existing layered player; generic fighter only, no T7b/art/HG2 claim | New `content/video_engine/src/modeling/bake_layers.py`; `content/video_engine/tests/test_model_layer_bake_bridge.py`; new `content/video_engine/tests/fixtures/modeling/layered/fighters/bridge-diagnostic/`; ignored `B/2_5d/fighters/bridge-diagnostic/`; existing saved Blender source, authored-layer engine, motion contract, player and T6 modules read-only | none (no engine lock) | none | `29a4c04` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/worktrees/astra/Outreach Program` | `codex/Astra` | Codex / Astra parent | SHARED-MODEL-ENGINES: reusable Blender-authored 2.5D/3D assets and fighter benchmark; E99 s90 chart-key claim RELEASED to Fable by operator 2026-09-22 | HG1 approved; diagnostic T4a/T7a, bounded raster input and garment shell are on local main. Sharaf primitive-head probe `71c138e` failed internal likeness review and stays unmerged; HG2 art unapproved | Model-engine PRP, native preset/asset files, authored-layer backend and fixtures, fighter art and scoped model-engine files only | none (no engine lock) | s89-s91 merged; next free s92 | T1/T2/T3/T5a merged at `6ef1ff1`; raster/garment checkpoint at `6f69bde`; no push |
| `C:/Users/Snipe/.codex/worktrees/model-head-targets/Outreach Program` | `codex/model-head-targets` | Codex parent / Sol escalation | SHARED-MODEL-ENGINES T4b: local MPFB facial-target quality proof, isolated from rejected primitive-head experiment | Diagnostic proof merged; four real MPFB face shape keys on existing rig, but no likeness or HG2 art claim | New target-probe fixture/tests and ignored review evidence only; shared authoring/player files read-only until a reviewed proof and plan decision | none (no engine lock) | none | `9f5b82b` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/wt/mg` | `codex/model-generalization` | Codex / Luna T4c, Sol xhigh escalation after three bounded failures | SHARED-MODEL-ENGINES T4c: editable hinged prop and layered environment diagnostic fixtures | Complete and merged to local main; socket/hinge, floor/layer and model contract proven as diagnostic blockouts; no humanoid fields or art approval | `content/video_engine/assets/modeling/native/generalization/`; `content/video_engine/tests/fixtures/modeling/generalization/`; `content/video_engine/tests/test_model_generalization.py`; ignored review evidence only | none (no engine lock) | none | `64e8f7a` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/wt/hl` | `codex/model-head-look` | Codex / Luna T4b first-look, Sol xhigh escalation after five routing failures | SHARED-MODEL-ENGINES T4b: reference-constrained Sharaf head-and-shoulders source proofs | MPFB and official CC0 head-source proofs internally rejected. Local TripoSR preflight blocked before generation by missing Windows C++20 compiler; review-only evidence preserved, directed art-source decision pending, no fighter-art claim | `content/video_engine/assets/modeling/native/fighters/` only after passing internal visual proof; `content/video_engine/tests/fixtures/modeling/head-look/`; `content/video_engine/tests/test_model_head_look.py`; ignored `B/art/fighters/head-look/`, `B/art/fighters/cc0-head-proof/` and `B/art/fighters/local-triposr-probe/` evidence; no shared player/engine writes | none (no engine lock) | none | register claim `b095d25` merged via `9bf65c8`; no model source merged, no push |
| `C:/Users/Snipe/.codex/wt/ia` | `codex/model-mesh-audit` | Codex / implementation_luna, execution_sol escalation; parent integration | SHARED-MODEL-ENGINES T4d: source-neutral Blender geometry/rig audit for native and generated model candidates | Complete diagnostic; records source hashes and mesh facts, not visual-quality or art approval | `content/video_engine/src/modeling/blender/mesh_audit.py`; `content/video_engine/scripts/model_mesh_audit.py`; `content/video_engine/tests/test_model_mesh_audit.py`; isolated audit fixtures and ignored review receipts only | none (no engine lock) | none | `cbea189` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/wt/fc` | `codex/model-contact-probe` | Codex / implementation_luna, execution_sol review fixes; parent integration | SHARED-MODEL-ENGINES T5a.1: native Rigify planted-foot diagnostic on the existing v1.1 fighter family | Complete diagnostic; 294 weighted sole vertices, 0.101 mm compensated slip under a 5 mm budget; no T5b, likeness or HG2 claim | `content/video_engine/src/modeling/blender/contact.py`; `content/video_engine/scripts/model_foot_contact_probe.py`; `content/video_engine/tests/test_model_foot_contact_probe.py`; ignored `B/3d/foot-contact-probe/` only; native source and player read-only | none (no engine lock) | none | `795931f` on 2026-09-23; no push |
| `C:/Users/Snipe/.codex/wt/pr` | `codex/model-planar-rig` | Codex / implementation_luna, execution_sol escalation; parent contract and integration | SHARED-MODEL-ENGINES T7a.1: optional shared-clock 2.5D planar contact diagnostic | Complete diagnostic; planted target through frame 40 and FK release at 41; proxy art unskinned, no T7b/fighter art/HG2 claim | `content/video_engine/src/modeling/planar_rig.py`; opt-in `content/video_engine/src/modeling/layered.py`; `content/video_engine/scripts/model_planar_rig_probe.py`; new `content/video_engine/tests/fixtures/modeling/layered/rig/`; `content/video_engine/tests/test_model_planar_rig.py`; ignored `B/2_5d/planar-rig/`; shared motion, player and existing fixture read-only | none (no engine lock) | none | `8ec82d9` on 2026-09-23; no push |
| `C:/Users/Snipe/Downloads/Outreach Program` | `main` | P62 parent (Claude/Fable) + its Opus lanes - this register's author | doctrine, gates, evidence, review; P61 / P63 / P64 complete | P65 THE RECIPE LAB + P66 THE SHAPE COMPILER + P67 THE VERIFIED RECEIPT AND THE CRITIC running IN PARALLEL as delegated slices on main (the operator, 2026-09-16: "yes, run them in parallel"); forty-seven waves committed 2026-09-17/18 (9380ea7 .. 6fca0c4: the door plan, s73 + the survivorship audit, s74, the sixth pass with its review fixes, the change-report fix, the transform-first chooser with the calendar base, the ninth pass - an authored entry stands; the calendar base v7 carded then RULED by s77 - THE COMPILER IS STOPPED, P66 retired; s75/s76/s77 written; the figure regression of 09-14 restored and the bars-page label row thinned (R26-191); TOKYO TEA BREAK v2 built by hand on the approved cut's structure (`REBUILD-TREATMENT-V2.md`, `build_short_v2.py`), read on frames beside the approved cut, the voice through chain G, the operator: "tokyo tea looks good" / "render tokyo tea @ 1440p 24 fps" - RENDERED from `build-v2-frozen-c/` (1440x2560, 24 fps, 88.83 s, -15.1 LUFS; the card `tokyo-tea-break-v2-the-rebuild` on the queue); v2 approved to upload (s78), then the operator's second look with VidIQ's read (s79 - the life mechanics, the axes open, the clip docked; s80 - the slot rule, the punch rule): TOKYO v3 / v3b built and RENDERED (`build-v3-frozen-a` / `-b`, 1440x2560 / 24 fps; in Downloads as tokyo-tea-break-v3-1440p.mp4 and -v3b-); the punches measured out until the caption yields under a camera key (R26-201); next: the operator's pick of the file to upload); E99 s69 / s70 / s71 written (the gates guide; recipes are timings that compose; the vocabulary is the record's; a light is never the move; the throw is signature); the parent read every rendition on its sheet beside the approved cut before the cards returned: on the queue - the re-proved set (six recipes carrying their mechanism; two named as not on this bed), the first real batch (twelve plate-carries-a-card candidates, all read), the generated base v7 with its critic (ten of eleven instants the same as the approved cut, the recast BUILDING after the engine's keyed-false line); one-shot #3's card still on the queue; next: the Opus one-shot on the compiler's base, then Astra's | the queue (`review-queue.v1.json` + `REVIEW-QUEUE.md`), the plans, `CAPABILITIES.md` / `BACKLOG.md` rows it owns | FREE - no holder since P61 T14 (2026-09-16) | E99 s49-s53, s55-s65 (committed); s66 (claimed 2026-09-16, written); s67 (claimed 2026-09-16, written); s68 (claimed 2026-09-16, written); s69 (claimed 2026-09-17, written); s70 (claimed 2026-09-17, written); s71 (claimed 2026-09-17, written); s72 (claimed 2026-09-17, written); s73 (claimed 2026-09-17, written); s74 (claimed 2026-09-17, written); s75 (claimed 2026-09-17, written); s76 (claimed 2026-09-17, written); s77 (claimed 2026-09-17, written); s78 (claimed 2026-09-17, written); s79 (claimed 2026-09-18, written); s80 (claimed 2026-09-18, written); next free: **s89** (RENUMBERED AT MERGE 2026-09-22: s85 and s86 are ASTRA's - MP-FED-LIQUIDITY approved 09-18, SCRIPT-REVIEW-CLEARANCE 09-20 - written in the main checkout's copy of the rulings; s87 and s88 are the Fable lane's, written as s85/s86 in its own copy - the stamp and the stamp's size. From now on a lane writes `E99 s??` and the number is assigned at merge) (s84 claimed 2026-09-18 by the P62 parent - the queue answers on HG3 and P65 HG2) (s83 claimed 2026-09-18 by the P62 parent - the interior drift) (s82 claimed 2026-09-18 by the P62 parent - the operator's read of the H unit) (s81 claimed 2026-09-18 by the P62 parent - the long form returns, P68 approved) | is main |
| `C:/Users/Snipe/Downloads/Outreach Program` | `main` | audio / voice lane (Claude) | chain G tone chain, `tempo_edit`, the ten-test restoration of `test_authoring_kit.py` | its own | `authoring/audio.py`, `audio_synth.py`, `master_vo_tone.py`, `audio_spectrum.py`, `tempo_edit.py`, the audio tests, `scripts/hooks/test_deletions.py` + `.git/hooks/commit-msg`, doc 37, `.env.example`; **not `gate_opening_structure.py`** (its short-mode tag fix landed as cd53b6d on the operator's word). NOTE from the P62 parent: under a pathspec-limited commit `test_deletions.py` audits the STAGED diff, not the commit's - that fix is yours | none | E99 s48, s54 (committed) | is main |
| `C:/Users/Snipe/Downloads/Outreach Program` | `main` | package lane (Gemini / Flow, via the bridge) | scripts B / C, thumbnails, the outreach package (`ai-slower-useful-faster/`, `normal-for-which-bridge/package-2026-09-15/`, the `MP-NORMAL-OUTREACH` plan) | its own | its project folders only | none | none | is main |
| `C:/Users/Snipe/.codex/worktrees/f10b/Outreach Program` | `codex/stickly-woodblock-variant` | codex (Astra), August | DORMANT - unmerged: 1 commit (808eb14, 2026-08-23), 1169 behind main; 534 dirty files; 55 plate-library rows resolve into it (`build_plate_library.py:9`, B2) | none | none | none | none | never |
| `C:/Users/Snipe/.codex/worktrees/p29-remotion-console/Outreach Program` | `codex/p31-semantic-evidence-and-word-timed-captions` | codex (Astra), August | DORMANT - unmerged: 0, 1165 behind main | none | none | none | none | merged (0 ahead) |
| `C:/Users/Snipe/Downloads/Outreach Program/.claude/worktrees/content-generation-system-52f077` | `claude/spark-animations-marketing-cc6738` | Claude, August | DORMANT - unmerged: 0, 1173 behind main; 8,893 untracked / 20,620 ignored files never inventoried | none | none | none | none | merged (0 ahead) |
| `C:/Users/Snipe/Downloads/Outreach Program/.claude/worktrees/sweet-villani-1c3a16` | `claude/content-generation-system-52f077` | Claude - the P61 / P62 parent's session cwd | DORMANT as a checkout - every edit and commit of the parent lands in main; unmerged: 2 commits on the branch (the CLAUDE.md / AGENTS.md stabilisation, superseded on main), 703 behind; flattened into main 2026-09-08; removable only from a session not sitting in it, on the operator's word | none | none | none | none | 2026-09-08 (flattened) |
| `C:/Users/Snipe/Downloads/Outreach Program/.claude/worktrees/fable-p68` | `claude/fable-p68` | THE FABLE LANE - the Claude/Fable parent + its Opus lanes (created 2026-09-22 from `main` at bd2a000, after the main checkout was found switched to `codex/Astra` with 65 dirty files from three lanes) | P68 Steel and Paper H: the long form's engine doors, built and read here, then sent to main complete and documented | P68: R26-236 (c7e6065), R26-235 (db6938a), R26-245 + R26-20 (this wave); next R26-247, R26-230, T6 the body **P69 APPROVED and RUNNING (2026-09-22, the operator: `/prp-implement P69`, "resolve all non-human blockers and i'll review human gates at the end"; the claim at `ff08213` predates the approval)** - T0-T7 + T14 done and MERGED to main 2026-09-22 (rulings E99 s92-s95 and backlog R26-249..256 numbered at this merge; next free **s112** / **R26-296** after the 2026-09-23 evening merge (T26e + rows 15-18)); rows 7-17 built (T15-T25; row 15's PROP 1 waits on the page measure), MERGED to main 2026-09-23 with lane B (P62: lane B -> main 19144bb, main -> lane A 970a60d, then main fast-forwarded to lane A; the P69 children R26-276..R26-289 filed after the backlog split); the body rows continue here from row 18 (T26); lane B (`claude/p69-s90`, the engine lane: T6b, T8-T10b, T12) is created from main now. R26-247 (a stamp is a landing to the gate, cues and camera) + the chip's `form: "stamp"` by the real stamp; T6 the body of Steel and Paper H; and **E99 s90** (the full-stage page: B's phone type scale, badges as the key), taken back from Astra on the operator's word. | gitignored build inputs COPIED IN read-only from the main checkout (vo-h-scratch, evidence/objects, host, assets/props/cutouts, assets/icons + icons/cutouts, steel-and-paper/sound, review/claims); plate ids resolve by ABSOLUTE path into the main checkout. NEVER writes the main checkout | none | E99 s87 (committed with R26-20); E99 s88 (claimed 2026-09-22) | not yet - unpushed; merges to main on the operator's word |
| `C:/Users/Snipe/Downloads/Outreach Program/.claude/worktrees/p69-s90` | `claude/p69-s90` | P69 LANE B - the engine lane (created 2026-09-22 from main at 8cb542d) | the engine slices of P69 in sequence, so lane A's body rows never render on an engine mid-edit: T6b (the prop's cross-hatch shadow, E99 s92) + the finer hatch, T6c (the 196 px bar cap, s96), T8-T10 (the `longform` profile, s97), T10b, T10c, T12, T36-T43 (the Bravos verbs) | T6b, T6c, T8-T10, T10b 91de567, T10c b42d6dc + 11ed037, the key-rail fixes 5ddc035, T6d ff0ad89, review-4 fixes 1931431, T26a + T26b af869b7 - MERGED to main 2026-09-23 at 19144bb (REVIEW-P69-LANE-B-MERGE-4: merge after MJ1, closed as a named HG3 departure); next: T8b panels, T11-T13, T36-T44 | the engine (`scene-evidence-engine.mjs`, `kinetics/*`, `species/*`), `ledger_page.py`, `build_scene_timeline_f.py` validation, their tests and goldens, `page-boxes.v1.json`; NEVER the plan, BACKLOG, rulings or register (lane A owns those, so the lanes never collide) | HOLDS the engine lock for P69 | none (numbers assigned at merge) | not yet |

## MP-FED-LIQUIDITY execution — 2026-09-18

### Alignment supersession — 2026-09-22

Order `docs/research/runs/bridge/queue/e6e94125cdb2/brief.md` lives in the trunk's local bridge folder. Astra source work is now in the dedicated worktree row above; the main checkout is trunk-only. All older Astra engine leases below are historical and closed. The controlling engine lock remains NONE on main. New ruling/backlog numbers are assigned at merge, not claimed independently in lane copies. Follow-up order `6a3c72d239a2` supersedes the wait/rebase instruction: merge main into Astra once, reconcile and verify, then await Fable review; no move of main or push. Required ignored inputs are copied non-destructively from trunk for verification only; original assets remain there.

Current lease amendment: T14 renderer and T15 compiler leases released. T14 passed parent107 focused/158 golden-worktree checks; T15 passed parent123 tests after independent review and finite-span/stack-comparator fixes. No active shared engine writer; no golden repin. This supersedes historical T14/T15 leases below.

Actual checkout branch is `main` at `C:/Users/Snipe/Downloads/Outreach Program`; `codex/Astra` lives only at `C:/Users/Snipe/.codex/worktrees/astra/Outreach Program`. The following lease account is historical; the table's 20ae64c6dfa9 row is current. Astra parent owns the approved MP-FED-LIQUIDITY episode slice and its queue/plan records. Engine lease: Astra parent, MP-FED-LIQUIDITY T14 only (`build_scene_timeline_f.py` and new `test_fed_axis_handoff.py`); T11/T12 code reviewed with 76 focused tests PASS, three existing skips; T13 reviewed with 51 tests PASS including browser probes. Fixture integration pending representative build. Subsequent engine slices are sequential. Parent verified production files clean before T11 and prior engine row FREE; the existing fixture's hash-only dirty change is preserved. Baseline: 158 worktree/golden tests and 123 page/chart tests PASS. E99 s85 written for the operator's explicit `$prp-implement mp-fed-liquidity` authorization; E99 s86 written for `$prp-implement` SCRIPT-REVIEW-CLEARANCE; next free s87. No commits or merges authorized.

### Astra current engine lease — Fed T17

Supersedes the T14-only lease above: parent owns integration; delegated implementation is limited to `ledger_page.py`, `samples/scene-evidence-engine.mjs` and a new focused readability test. T14/T15 writers are finished. T17 adds opt-in full-stage landscape line geometry and readable axes through matched SVG/Python layout; no caption policy, gate, portrait or legacy chart redesign. Operator explicitly authorized necessary engine/framework enhancement during the script clarity review. No commit/push authority is added.

### Astra sequential integration — Fed opening review and silent-bed mux

T17 implementation is finished; parent review and episode integration remain parent-owned. The disjoint T18 opening-review worker owns `gate_opening_structure.py`, its focused test, and new `opening_review.py`/test only. Parent retains runner/recorder wiring and documentation. Parent separately owns the verified no-cue filter-chain fix in `render_episode.py` and new `test_render_audio_mix.py`: preserve existing cue gains/envelopes and loudness policy, prove both zero-cue and cue mixes through actual ffmpeg. No other engine writes, global audio policy changes, commits or pushes are authorized by this entry.

### Astra T20 module/compiler split

2026-09-19: T19 workers have returned and released their leases. `fed_flow_extension` owns only `scripts/species/flow.mjs` and its new Node extension test/receipt. Parent owns compiler validation, Python extension tests and final engine sync; no concurrent engine writer. Existing declarations retain their behavior. No source-value changes, asset promotion, gate relaxation, commit or push.

## Hand-off 2026-09-18 - the stable point for Astra's worktree (the parent's usage expiring)

### Astra sequential engine lease — Fed T19 compiler contract

2026-09-19: prior T17/T18 production writers are finished. Parent owns T19 architecture and integration under the operator's explicit evidence-stage and engine-enhancement instructions. Sole delegated engine writer is `fed_surface_compiler`, limited to `build_scene_timeline_f.py` and new `test_page_surface_handoff.py`; renderer/template/gates remain read-only until this slice returns. Existing dirty engine edits preserved. Scope is source-bound live dense-line page handoff from a registered cream surface, not chart-card embedding; B1 default refusal and width floor unchanged. No asset promotion, commit or push authorized.

T19 sequential handoff: compiler worker returned and released its files; parent independently ran the named90-test suite PASS and inspected binding/parser/geometry code. Sole next engine writer is `fed_surface_player`, limited to `samples/scene-evidence-engine.mjs`, `samples/scene-evidence-player.template.html`, new `test_page_surface_player.py` and an episode-local review diagnostic builder/output. Compiler and shared gates are read-only for that writer. Follow `.context/fed-hall-renderer-layout.md` including parent constraints: aspect-matched source host, cream/dark ink scoped to surface pages, actual destination profile, and no pixel/label duplication. No real asset eligibility change or production render is authorized by this lease.

T19 checkpoint: `fed_surface_player` returned no edits and no live tool handles; that broad lease is released. Next bounded writer `fed_surface_geometry` owns only new `content/video_engine/scripts/kinetics/page_surface.mjs` and new `content/video_engine/tests/test_page_surface_geometry.py`, plus its receipt. Underscore filename matches sync_kinetics region/import grammar. Pure geometry helpers reuse existing homography; engine/template remain unchanged until the later painting slice. Parent independently owns episode source-presentation integration; source figures/raw evidence stay unchanged.

T19 painting lease: parent reviewed geometry helper and independently ran10tests PASS. `fed_surface_paint` may now edit only `docs/content-video-engine/samples/scene-evidence-engine.mjs`, `docs/content-video-engine/samples/scene-evidence-player.template.html`, new `content/video_engine/tests/test_page_surface_player.py`, and `.context/fed-surface-paint-receipt.md`. Integrate the helper region using existing sync tooling; geometry module/test remain with geometry worker until receipt. No compiler, gate, source, asset approval or narration edits. Synthetic browser proof only; actual media review follows parent integration.

T19 parent takeover: painting worker interrupted and returned authoritative checkpoint: only page_surface kinetics region registered/populated; no live tool/session handles. Worker lease released. Parent now owns renderer/template integration and focused player test, preserving pre-existing dirty changes. No concurrent engine writer.

T19 state continuation lease: parent hands sole engine write access to `surface_renderer_review` for existing chart-state/perform integration only; worker also owns new `test_page_surface_states.py` and `.context/fed-surface-states-receipt.md`. Parent owns separate safe-margin regression/planning, ledger and plan; no simultaneous engine edits. Existing synthetic MP4 capture worker has no engine write access. Lease ends on reviewed handback.

T19 state handback: worker interrupted for checkpoint and returned engine-only edits, no tests, no live handles. Lease released; parent reviewed changes, authored `test_page_surface_states.py`, fixed reveal-wrapper opacity ownership and derived source/subtitle sizing, and resumed sole engine integration ownership. Initial five surface/state browser tests PASS15.63s; broader verification and real fixture remeasurement follow.

**Branch from `main` at the sha the last wave printed (26c406a or later - `git log --oneline -1`); every wave of the night is committed, nothing pushed (70+ ahead; push only on the operator's word).** The working tree of the main checkout may carry ONE lane's uncommitted edits (R26-224, the fifteen failing tests - `content/video_engine/tests/R26-224-NOTE.md` when it lands); a new worktree branches from HEAD and does not carry them.

**What is done:** P68 T1 (Script H PASS on its own 174-wpm take), T2 (the scratch clock), T3/T3b (the audit; R26-205..217), T5 (the 1:30 unit, then the 30 s BED on nine engine doors - frozen copy g on :8782 is the card), T5b (the doors: R26-218/219/221/223/228/205/226/232/233/234/220/201; R26-225's write half). Rulings s81-s83 (next free s84).

**What Astra starts on, in order (one lane, one scope, one file - E99 s82 (a)):**
1. R26-235 - a full-stage page served the measured fixture (`ledger_page.py:1547`; `measure_page_boxes.py --write` grows a `full_stage` key) - blocks every 16:9 card and camera.
2. R26-230 - the full-stage page's top and bottom bands as numbers beside `CAPTION_ANCHOR` (the caption SIZE at 16:9 is the operator's open question A on the HG3 card - build the bands, leave the size as built until answered).
3. R26-222 (M28 on a y-only rescale's tick hand-over), R26-231 (a page build checked against the row's span) - small, compiler.
4. Then T6 THE BODY of Steel and Paper H the bed's way: `build_episode_h.py` extended row by row from `REBUILD-TREATMENT-H.md` (its "Amended" foot section overrides the rows above it), the frames read beside `build-f/` at the same sentence, a frozen copy per card, the critic ONCE at the card, the take `vo-h-scratch/` (174 wpm) until the voice is chosen at HG3. The operator's open answers gate the body's captions (A), the end tags' form (B), the voice, the plate balance - build what does not depend on them first (the pages, the props as stamps on DIRECT matches only, the melt/throw beats), and name the four on every card.
5. The P65 cards are ruled as answered/closed pending (the operator's words on the queue); P65 T8's re-proof rows absorb HG2's reads before any recipe card returns.

**The rules that bit tonight, so they do not bite again:** a row's `exit` is the move INTO it; `melt:throw` into a plate is refused (use `melt:splash:plate`); a page is BORN on `;domain=`; `;build=lines[:s]` draws whole lines in turn (a series held at 0 takes no turn); the interior word walk is gone (s83) - the lead point, halo and bloom are the life; the caption on a 16:9 page row is in the anchored strip; `follow: true` on the reveal rescale; `plate_idle_paints` on with 20 px; the critic runs once, at the card; the 30 s bed before the unit, the unit before the body; every collision fixed before a card.
